
import Foundation

import UIKit

class XZGraphicsCell: UITableViewCell {
var enbaleItem: Bool = false
private var chatNum_string: String?
private var labelBrowserItems_str: String?
private var serviceOffset: Double = 0.0




    @IBOutlet weak var meQlabel: UILabel!
    @IBOutlet weak var meQheader: UIImageView!

@discardableResult
 func tapReallocInfoReceiptButton(showLines: [String: Any]!, resultLine: Float) -> UIButton! {
    var photoA: Bool = true
    var not_mD: String! = String(cString: [105,99,111,110,105,99,0], encoding: .utf8)!
      not_mD.append("\((not_mD.count >> (WSPlayTool10.min(2, labs((photoA ? 3 : 1))))))")
   for _ in 0 ..< 2 {
      photoA = !photoA
   }
   if photoA {
       var silencej: [Any]! = [162, 942, 645]
       var medataH: String! = String(cString: [100,101,109,97,116,101,114,105,97,108,105,122,101,0], encoding: .utf8)!
       var time_hmQ: String! = String(cString: [113,100,109,100,97,116,97,0], encoding: .utf8)!
      if !medataH.hasSuffix("\(silencej.count)") {
         medataH = "\(time_hmQ.count)"
      }
          var graphicsP: String! = String(cString: [112,114,111,118,105,100,105,110,103,0], encoding: .utf8)!
          var containsg: Double = 3.0
          var mineI: Double = 2.0
         time_hmQ = "\(medataH.count / 2)"
         graphicsP = "\(graphicsP.count & 1)"
         containsg *= Double(graphicsP.count - Int(containsg))
         mineI *= Double(Int(containsg) ^ 1)
      photoA = 6 >= not_mD.count
   }
   if photoA {
      not_mD = "\((1 ^ (photoA ? 5 : 5)))"
   }
     var downloadOrder: UIButton! = UIButton()
     var fixedWeb: Double = 4884.0
     let avatarsContains: Double = 5197.0
    var reqstateVignetting:UIButton! = UIButton(frame:CGRect(x: 284, y: 40, width: 0, height: 0))
    reqstateVignetting.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    reqstateVignetting.alpha = 0.1
    reqstateVignetting.frame = CGRect(x: 147, y: 101, width: 0, height: 0)
    downloadOrder.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    downloadOrder.alpha = 0.4
    downloadOrder.frame = CGRect(x: 112, y: 144, width: 0, height: 0)
    
    var downloadOrderFrame = downloadOrder.frame
    downloadOrderFrame.size = CGSize(width: 277, height: 90)
    downloadOrder.frame = downloadOrderFrame
    if downloadOrder.alpha > 0.0 {
         downloadOrder.alpha = 0.0
    }
    if downloadOrder.isHidden {
         downloadOrder.isHidden = false
    }
    if !downloadOrder.isUserInteractionEnabled {
         downloadOrder.isUserInteractionEnabled = true
    }


    
    var reqstateVignettingFrame = reqstateVignetting.frame
    reqstateVignettingFrame.size = CGSize(width: 293, height: 162)
    reqstateVignetting.frame = reqstateVignettingFrame
    if reqstateVignetting.isHidden {
         reqstateVignetting.isHidden = false
    }
    if reqstateVignetting.alpha > 0.0 {
         reqstateVignetting.alpha = 0.0
    }
    if !reqstateVignetting.isUserInteractionEnabled {
         reqstateVignetting.isUserInteractionEnabled = true
    }

    return reqstateVignetting

}






    override func setSelected(_ selected: Bool, animated: Bool) {

         let zoomableFixup: UIButton! = tapReallocInfoReceiptButton(showLines:[String(cString: [104,115,98,0], encoding: .utf8)!:String(cString: [105,109,109,101,100,105,97,116,101,108,121,0], encoding: .utf8)!, String(cString: [118,105,101,119,101,114,0], encoding: .utf8)!:String(cString: [116,105,109,101,111,117,116,115,0], encoding: .utf8)!], resultLine:5852.0)

      if zoomableFixup != nil {
          self.addSubview(zoomableFixup)
          let zoomableFixup_tag = zoomableFixup.tag
      }
      else {
          print("zoomableFixup is nil")      }



       var record5: String! = String(cString: [108,105,110,109,97,116,104,0], encoding: .utf8)!
    var descriptu: String! = String(cString: [104,111,110,101,121,0], encoding: .utf8)!
    var itemN: [Any]! = [642, 389, 164]
   repeat {
       var firstq: [Any]! = [0, 742]
         firstq = [firstq.count]
       var completek: String! = String(cString: [118,99,97,114,100,0], encoding: .utf8)!
      while (firstq.count >= 1) {
          var update_usN: String! = String(cString: [112,111,105,110,116,99,98,98,0], encoding: .utf8)!
          var constraint3: String! = String(cString: [102,105,114,101,100,0], encoding: .utf8)!
          var share2: Double = 0.0
          var historyx: String! = String(cString: [99,97,114,114,105,101,114,0], encoding: .utf8)!
         completek = "\(update_usN.count / 3)"
         update_usN.append("\(((String(cString:[86,0], encoding: .utf8)!) == constraint3 ? Int(share2) : constraint3.count))")
         share2 *= (Double((String(cString:[51,0], encoding: .utf8)!) == historyx ? Int(share2) : historyx.count))
         break
      }
      record5.append("\(2 & record5.count)")
      if record5.count == 4629058 {
         break
      }
   } while (record5.count == 4629058) && (1 < descriptu.count)
   if record5.count >= descriptu.count {
      record5.append("\(record5.count)")
   }

   repeat {
      record5.append("\(record5.count / (WSPlayTool10.max(1, 7)))")
      if 3945247 == record5.count {
         break
      }
   } while ((itemN.count & record5.count) < 5 && (record5.count & itemN.count) < 5) && (3945247 == record5.count)
        super.setSelected(selected, animated: animated)

    }

    
    override func awakeFromNib() {
       var setx: String! = String(cString: [115,104,111,114,116,101,110,0], encoding: .utf8)!
    var bigm: [Any]! = [51, 210]
       var unselectedu: String! = String(cString: [118,97,114,108,101,110,103,116,104,0], encoding: .utf8)!
       var textc: Bool = true
         textc = !unselectedu.hasSuffix("\(textc)")
         textc = (((textc ? unselectedu.count : 39) + unselectedu.count) >= 39)
      for _ in 0 ..< 3 {
          var placeC: String! = String(cString: [103,97,105,110,115,0], encoding: .utf8)!
          var attributese: Bool = true
         unselectedu = "\(((textc ? 4 : 2) - 1))"
         placeC.append("\(placeC.count | 1)")
         attributese = !attributese
      }
         textc = !textc || unselectedu.count < 86
      if unselectedu.contains("\(textc)") {
         textc = !textc
      }
      while (unselectedu.count == 5) {
          var controllO: String! = String(cString: [119,114,105,116,101,116,114,117,110,99,0], encoding: .utf8)!
          var system7: String! = String(cString: [115,117,98,105,116,101,109,115,0], encoding: .utf8)!
         textc = !textc || unselectedu.count >= 88
         controllO = "\(system7.count - controllO.count)"
         system7 = "\(system7.count & controllO.count)"
         break
      }
      bigm.append(((textc ? 2 : 2) << (WSPlayTool10.min(labs(1), 5))))
   if (2 % (WSPlayTool10.max(9, bigm.count))) >= 3 {
      bigm.append(setx.count >> (WSPlayTool10.min(labs(1), 2)))
   }

   if setx != String(cString:[113,0], encoding: .utf8)! {
       var configP: [String: Any]! = [String(cString: [98,97,116,99,104,105,110,103,0], encoding: .utf8)!:String(cString: [99,104,97,114,116,115,0], encoding: .utf8)!, String(cString: [118,116,101,115,116,0], encoding: .utf8)!:String(cString: [100,111,108,98,121,0], encoding: .utf8)!, String(cString: [111,112,116,115,0], encoding: .utf8)!:String(cString: [114,97,100,105,97,110,115,0], encoding: .utf8)!]
       var font0: Int = 3
       var timerw: Int = 3
          var indexF: String! = String(cString: [104,105,103,104,119,97,116,101,114,0], encoding: .utf8)!
         configP["\(font0)"] = font0 | 2
         indexF.append("\(((String(cString:[107,0], encoding: .utf8)!) == indexF ? indexF.count : indexF.count))")
          var aidG: Double = 2.0
          var expirea: [String: Any]! = [String(cString: [104,97,108,102,0], encoding: .utf8)!:185, String(cString: [99,97,109,112,97,105,103,110,0], encoding: .utf8)!:597]
         font0 -= 2 * configP.keys.count
         aidG += Double(Int(aidG))
         expirea = ["\(expirea.keys.count)": 1]
         font0 += timerw
      if 1 >= (font0 & timerw) {
          var dateV: String! = String(cString: [105,100,101,110,116,105,99,97,108,0], encoding: .utf8)!
          var navigation8: Double = 3.0
         font0 ^= Int(navigation8) ^ 2
         dateV = "\(dateV.count ^ dateV.count)"
         navigation8 -= (Double((String(cString:[121,0], encoding: .utf8)!) == dateV ? dateV.count : dateV.count))
      }
          var top3: Double = 1.0
         font0 <<= WSPlayTool10.min(4, labs(2 * timerw))
         top3 *= Double(Int(top3))
      for _ in 0 ..< 2 {
          var zoomD: Float = 3.0
          var recognition7: Float = 1.0
         timerw += Int(recognition7)
         zoomD *= Float(Int(zoomD))
      }
          var register_qa0: Float = 2.0
          var firstd: [Any]! = [String(cString: [114,97,109,112,0], encoding: .utf8)!, String(cString: [99,119,110,100,0], encoding: .utf8)!]
          var read2: [String: Any]! = [String(cString: [115,116,115,115,0], encoding: .utf8)!:842, String(cString: [97,108,112,104,97,101,120,116,114,97,99,116,0], encoding: .utf8)!:117]
         font0 <<= WSPlayTool10.min(2, labs(3 << (WSPlayTool10.min(3, configP.count))))
         register_qa0 -= Float(firstd.count)
         firstd.append(1)
         read2["\(register_qa0)"] = Int(register_qa0)
      if font0 <= 1 {
          var gund8: Bool = true
          var safeH: Bool = false
          var upgradesM: Double = 3.0
          var primeS: Double = 0.0
         configP["\(upgradesM)"] = Int(primeS)
         gund8 = !safeH
         safeH = !gund8
         upgradesM /= WSPlayTool10.max((Double((safeH ? 3 : 4) | 1)), 1)
      }
      repeat {
          var sharedy: Int = 0
          var datav: String! = String(cString: [116,114,117,110,99,97,116,101,0], encoding: .utf8)!
          var messagesb: String! = String(cString: [109,97,115,116,101,114,0], encoding: .utf8)!
          var datasB: Double = 3.0
         font0 >>= WSPlayTool10.min(1, labs(3 ^ datav.count))
         sharedy %= WSPlayTool10.max(sharedy % (WSPlayTool10.max(Int(datasB), 5)), 5)
         datav = "\(Int(datasB))"
         messagesb.append("\(messagesb.count << (WSPlayTool10.min(labs(1), 3)))")
         if font0 == 2653084 {
            break
         }
      } while (font0 <= configP.keys.count) && (font0 == 2653084)
      setx = "\(setx.count + 3)"
   }
   repeat {
      setx.append("\(((String(cString:[83,0], encoding: .utf8)!) == setx ? setx.count : setx.count))")
      if setx == (String(cString:[119,54,113,121,97,116,116,121,106,0], encoding: .utf8)!) {
         break
      }
   } while (setx == (String(cString:[119,54,113,121,97,116,116,121,106,0], encoding: .utf8)!)) && (setx != setx)
        super.awakeFromNib()
        
        
    }
    
    
}

 class WSPlayTool10 {
    static func min<X: Comparable>(_ r1: X, _ r2: X) -> X {
        return r1 > r2 ? r2 : r1
    }
    
    static func max<X: Comparable>(_ r1: X, _ r2: X) -> X {
        return r1 > r2 ? r1 : r2
    }
}