
import Foundation

import UIKit
import YYImage
import SVProgressHUD

protocol ANDShowResponse: AnyObject {
    func deleteAIdaTableViewCell(cell: HAYPhoneResponseCell)
    func buttonplayVoiceAIdaTableViewCell(cell: HAYPhoneResponseCell)
}

class HAYPhoneResponseCell: UITableViewCell {
var resetPadding: Double = 0.0
var window_4_: Double = 0.0
private var unselectedMargin: Double = 0.0



    
    weak var dataSource: ANDShowResponse?

    @IBOutlet weak var aidAlabel: UILabel!
    @IBOutlet weak var aidAimage: UIButton!
    @IBOutlet weak var gifImage: YYAnimatedImageView!
    
    var button: UIButton!
    var longPressGesture: UILongPressGestureRecognizer!
    var messages: [String] = NSMutableArray() as! [String]

@discardableResult
 func returnedDicSetstateView(editChuang: Bool, chuangAlbum: Bool, mainResponse: Bool) -> UIView! {
    var launchd: String! = String(cString: [112,111,114,116,101,114,0], encoding: .utf8)!
    var detailsT: [Any]! = [399, 706]
       var sendingE: [String: Any]! = [String(cString: [102,111,114,119,97,114,100,0], encoding: .utf8)!:912, String(cString: [116,104,114,101,101,100,111,115,116,114,95,49,95,51,48,0], encoding: .utf8)!:366]
       var speakingX: Double = 5.0
       var channelc: String! = String(cString: [117,108,108,115,99,114,101,101,110,95,101,95,54,51,0], encoding: .utf8)!
         speakingX -= Double(channelc.count - Int(speakingX))
          var gundongQ: String! = String(cString: [105,110,112,117,116,115,0], encoding: .utf8)!
          var gundongA: Float = 5.0
         speakingX += Double(sendingE.values.count / 1)
         gundongQ.append("\(2)")
         gundongA -= Float(Int(gundongA))
      if !channelc.hasSuffix("\(speakingX)") {
         channelc.append("\(2)")
      }
         sendingE[channelc] = channelc.count / (AJXMainItemTool18.max(1, 6))
         channelc.append("\(1 ^ channelc.count)")
      for _ in 0 ..< 3 {
         channelc = "\(2)"
      }
      repeat {
          var purchaseZ: String! = String(cString: [115,116,100,108,105,98,0], encoding: .utf8)!
          var drain0: Double = 1.0
          var reusableG: [Any]! = [String(cString: [113,115,111,114,116,0], encoding: .utf8)!, String(cString: [105,97,116,0], encoding: .utf8)!, String(cString: [119,117,110,100,101,102,0], encoding: .utf8)!]
          var statuesB: Double = 0.0
         sendingE["\(statuesB)"] = (channelc == (String(cString:[53,0], encoding: .utf8)!) ? channelc.count : Int(statuesB))
         purchaseZ.append("\(Int(drain0) >> (AJXMainItemTool18.min(1, labs(1))))")
         drain0 /= AJXMainItemTool18.max(1, Double(reusableG.count >> (AJXMainItemTool18.min(labs(2), 1))))
         reusableG.append(2)
         if 2435442 == sendingE.count {
            break
         }
      } while (2435442 == sendingE.count) && (2.53 > (speakingX / 5.76))
      while (5.49 <= speakingX) {
          var bufferv: [String: Any]! = [String(cString: [109,111,100,105,102,105,101,114,0], encoding: .utf8)!:String(cString: [112,114,101,100,114,97,119,110,0], encoding: .utf8)!, String(cString: [99,117,114,118,101,115,0], encoding: .utf8)!:String(cString: [115,101,114,105,97,108,0], encoding: .utf8)!, String(cString: [110,101,103,97,116,105,118,101,0], encoding: .utf8)!:String(cString: [112,101,110,97,108,116,121,0], encoding: .utf8)!]
         channelc.append("\(bufferv.count)")
         break
      }
      repeat {
         speakingX -= Double(sendingE.count)
         if speakingX == 2974766.0 {
            break
         }
      } while ((4 << (AJXMainItemTool18.min(3, channelc.count))) < 2) && (speakingX == 2974766.0)
      launchd.append("\(sendingE.count)")
   if 4 <= launchd.count {
       var stylel: String! = String(cString: [108,111,110,103,109,117,108,97,119,0], encoding: .utf8)!
       var editZ: Bool = true
       var remarkI: [Any]! = [9972]
       var weixinlabelN: String! = String(cString: [112,97,115,115,119,111,114,100,0], encoding: .utf8)!
       var fixedF: String! = String(cString: [118,101,99,0], encoding: .utf8)!
      repeat {
         remarkI.append(2)
         if remarkI.count == 949605 {
            break
         }
      } while (4 < (5 & remarkI.count) && 5 < (weixinlabelN.count & remarkI.count)) && (remarkI.count == 949605)
      for _ in 0 ..< 2 {
          var icond: String! = String(cString: [100,105,112,111,115,97,98,108,101,0], encoding: .utf8)!
          var transactionss: Int = 1
         stylel = "\(transactionss & weixinlabelN.count)"
         icond.append("\(icond.count)")
         transactionss &= icond.count & 3
      }
      repeat {
          var didc: String! = String(cString: [102,95,56,50,0], encoding: .utf8)!
          var browserF: Float = 2.0
         stylel.append("\(Int(browserF))")
         didc = "\(didc.count)"
         browserF *= (Float(didc == (String(cString:[116,0], encoding: .utf8)!) ? didc.count : didc.count))
         if (String(cString:[98,95,56,51,49,49,48,115,104,0], encoding: .utf8)!) == stylel {
            break
         }
      } while ((String(cString:[98,95,56,51,49,49,48,115,104,0], encoding: .utf8)!) == stylel) && ((stylel.count - remarkI.count) <= 4)
      for _ in 0 ..< 1 {
          var graphicsg: String! = String(cString: [109,117,110,108,111,99,107,0], encoding: .utf8)!
          var labeelr: Bool = false
          var name2: Bool = true
          var descriptU: Double = 4.0
         editZ = weixinlabelN.count >= fixedF.count
         graphicsg = "\(1 + Int(descriptU))"
         labeelr = (labeelr ? name2 : !labeelr)
         name2 = !name2 || 70.57 < descriptU
      }
       var pathz: [Any]! = [851, 209, 84]
      repeat {
         fixedF.append("\(((editZ ? 2 : 2)))")
         if fixedF.count == 4157540 {
            break
         }
      } while (fixedF.count == 4157540) && ((2 | fixedF.count) == 1)
         stylel = "\(pathz.count)"
       var appo: Bool = true
       var setC: Bool = false
          var search1: Bool = true
          var while__I: Bool = true
          var size_t04: Bool = true
         pathz = [((setC ? 5 : 2) | 2)]
         search1 = (!while__I ? !size_t04 : while__I)
         size_t04 = search1
          var chuanga: String! = String(cString: [97,98,105,0], encoding: .utf8)!
          var about8: [Any]! = [8, 49]
         fixedF = "\(1)"
         chuanga = "\(2 + about8.count)"
         about8 = [about8.count]
         pathz.append(remarkI.count + weixinlabelN.count)
         fixedF = "\(((String(cString:[116,0], encoding: .utf8)!) == stylel ? stylel.count : fixedF.count))"
      while (!stylel.contains("\(appo)")) {
         appo = (fixedF.count * weixinlabelN.count) > 26
         break
      }
          var type_sd: Bool = true
         pathz = [((appo ? 5 : 3))]
         type_sd = !type_sd && !type_sd
          var substringk: Double = 2.0
          var headersC: String! = String(cString: [101,112,105,99,0], encoding: .utf8)!
          var sheetE: String! = String(cString: [116,111,110,103,117,101,0], encoding: .utf8)!
         fixedF = "\((sheetE == (String(cString:[74,0], encoding: .utf8)!) ? sheetE.count : (setC ? 3 : 3)))"
         substringk += Double(2 % (AJXMainItemTool18.max(1, headersC.count)))
         headersC = "\(headersC.count % 2)"
      detailsT.append(3)
   }
   while (detailsT.count <= 1) {
       var remarkn: String! = String(cString: [110,111,100,101,108,97,121,0], encoding: .utf8)!
       var toolsB: String! = String(cString: [97,108,97,99,0], encoding: .utf8)!
       var l_imageI: String! = String(cString: [109,97,105,110,98,117,110,100,108,101,0], encoding: .utf8)!
       var taskj: String! = String(cString: [110,105,100,99,98,98,0], encoding: .utf8)!
       var cellh: Double = 0.0
       var gundongx: Double = 3.0
      while ((2.60 * cellh) >= 3.39 && 5 >= (2 ^ remarkn.count)) {
         cellh -= Double(2 ^ taskj.count)
         break
      }
         taskj = "\((toolsB == (String(cString:[82,0], encoding: .utf8)!) ? toolsB.count : Int(cellh)))"
      while ((5 >> (AJXMainItemTool18.min(2, taskj.count))) >= 2) {
         cellh *= Double(Int(gundongx))
         break
      }
         cellh -= Double(1 >> (AJXMainItemTool18.min(3, taskj.count)))
      for _ in 0 ..< 2 {
         cellh *= Double(toolsB.count)
      }
          var indexe: [Any]! = [4481.0]
         taskj = "\(Int(cellh))"
         indexe.append(indexe.count >> (AJXMainItemTool18.min(indexe.count, 4)))
         gundongx /= AJXMainItemTool18.max(3, Double(l_imageI.count))
      while (remarkn != String(cString:[105,0], encoding: .utf8)!) {
         toolsB = "\(l_imageI.count)"
         break
      }
      while (!remarkn.hasSuffix("\(cellh)")) {
         cellh += Double(2 / (AJXMainItemTool18.max(2, remarkn.count)))
         break
      }
      if (toolsB.count % 1) > 4 || (toolsB.count >> (AJXMainItemTool18.min(labs(1), 2))) > 2 {
         toolsB.append("\(toolsB.count)")
      }
      while (cellh == 5.53) {
          var tops: [Any]! = [212, 416]
          var photoI: String! = String(cString: [100,117,114,98,105,110,0], encoding: .utf8)!
          var vertical9: [String: Any]! = [String(cString: [97,99,99,114,117,101,100,0], encoding: .utf8)!:450, String(cString: [115,111,102,97,0], encoding: .utf8)!:300]
         cellh /= AJXMainItemTool18.max(4, Double(toolsB.count))
         tops.append(vertical9.count)
         photoI.append("\(tops.count)")
         vertical9["\(tops.count)"] = tops.count / (AJXMainItemTool18.max(1, 8))
         break
      }
      launchd = "\(l_imageI.count | 2)"
      break
   }
   for _ in 0 ..< 3 {
       var audioR: Double = 1.0
       var adeleteQ: [Any]! = [UILabel()]
       var detailK: [String: Any]! = [String(cString: [115,108,105,100,97,98,108,101,0], encoding: .utf8)!:String(cString: [102,114,97,109,101,115,105,122,101,0], encoding: .utf8)!, String(cString: [97,114,99,104,105,118,105,110,103,0], encoding: .utf8)!:String(cString: [112,97,112,101,114,0], encoding: .utf8)!, String(cString: [99,111,97,114,115,101,99,97,110,100,105,100,97,116,101,0], encoding: .utf8)!:String(cString: [112,104,111,116,111,115,95,118,95,51,0], encoding: .utf8)!]
       var alabel7: Double = 1.0
       var changeC: String! = String(cString: [116,111,108,111,119,101,114,0], encoding: .utf8)!
      if !changeC.hasPrefix("\(audioR)") {
         changeC = "\(detailK.keys.count >> (AJXMainItemTool18.min(labs(2), 3)))"
      }
         changeC = "\(((String(cString:[95,0], encoding: .utf8)!) == changeC ? changeC.count : Int(alabel7)))"
       var dictT: String! = String(cString: [112,97,99,107,101,116,105,122,97,116,105,111,110,0], encoding: .utf8)!
      if adeleteQ.contains { $0 as? Double == audioR } {
         adeleteQ.append(3 << (AJXMainItemTool18.min(5, dictT.count)))
      }
      repeat {
         changeC.append("\(Int(alabel7) << (AJXMainItemTool18.min(labs(3), 4)))")
         if changeC == (String(cString:[114,119,121,105,115,118,103,106,121,117,0], encoding: .utf8)!) {
            break
         }
      } while (changeC == (String(cString:[114,119,121,105,115,118,103,106,121,117,0], encoding: .utf8)!)) && (changeC.contains("\(alabel7)"))
      while ((changeC.count >> (AJXMainItemTool18.min(labs(1), 1))) == 1 && 1 == (changeC.count >> (AJXMainItemTool18.min(2, adeleteQ.count)))) {
         adeleteQ.append(Int(audioR))
         break
      }
          var nicknameX: String! = String(cString: [98,115,119,97,112,0], encoding: .utf8)!
          var speedsz: Bool = true
          var orders: String! = String(cString: [116,97,112,101,115,0], encoding: .utf8)!
         changeC = "\(changeC.count + dictT.count)"
         nicknameX.append("\(2)")
         speedsz = orders.count > 79 && nicknameX.count > 79
         orders = "\(orders.count * nicknameX.count)"
      while ((audioR - 4.90) < 1.59 || 2 < (adeleteQ.count | 5)) {
          var weixinlabel4: [String: Any]! = [String(cString: [111,112,117,115,108,97,99,105,110,103,0], encoding: .utf8)!:415, String(cString: [99,97,116,99,104,0], encoding: .utf8)!:273]
          var buyingf: [String: Any]! = [String(cString: [97,99,99,101,115,115,111,114,105,101,115,0], encoding: .utf8)!:528, String(cString: [115,116,114,115,116,97,114,116,0], encoding: .utf8)!:595, String(cString: [105,110,116,114,105,110,115,105,99,0], encoding: .utf8)!:294]
          var firstw: String! = String(cString: [114,101,112,108,97,99,101,109,101,110,116,115,0], encoding: .utf8)!
         adeleteQ.append(3)
         weixinlabel4["\(firstw)"] = buyingf.values.count
         buyingf = ["\(buyingf.count)": buyingf.values.count | firstw.count]
         break
      }
       var webK: String! = String(cString: [104,105,98,105,116,0], encoding: .utf8)!
      if alabel7 >= Double(changeC.count) {
         alabel7 -= Double(adeleteQ.count)
      }
          var fixedZ: Int = 5
          var gundonge: Float = 3.0
         changeC = "\(3)"
         fixedZ += 2
         gundonge /= AJXMainItemTool18.max(3, Float(Int(gundonge)))
      launchd.append("\(Int(audioR) / 3)")
   }
     var descriptExpire: Double = 5911.0
     let resultPurchased: Double = 698.0
     var interfaceKey: Double = 5604.0
    var subsetEcrecover = UIView(frame:CGRect.zero)
    subsetEcrecover.frame = CGRect(x: 181, y: 19, width: 0, height: 0)
    subsetEcrecover.alpha = 0.0;
    subsetEcrecover.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    var subsetEcrecoverFrame = subsetEcrecover.frame
    subsetEcrecoverFrame.size = CGSize(width: 275, height: 91)
    subsetEcrecover.frame = subsetEcrecoverFrame
    if subsetEcrecover.isHidden {
         subsetEcrecover.isHidden = false
    }
    if subsetEcrecover.alpha > 0.0 {
         subsetEcrecover.alpha = 0.0
    }
    if !subsetEcrecover.isUserInteractionEnabled {
         subsetEcrecover.isUserInteractionEnabled = true
    }

    return subsetEcrecover

}





    
    @objc func handleLongPress(_ gesture: UILongPressGestureRecognizer) {

         let harpenConstrain: UIView! = returnedDicSetstateView(editChuang:false, chuangAlbum:false, mainResponse:false)

      if harpenConstrain != nil {
          let harpenConstrain_tag = harpenConstrain.tag
          self.addSubview(harpenConstrain)
      }



       var homeD: Bool = false
    var expireo: Int = 2
    var firsto: String! = String(cString: [116,115,97,110,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      firsto = "\(expireo - 3)"
   }

   while ((5 - expireo) < 2 && 5 < expireo) {
      homeD = firsto.contains("\(homeD)")
      break
   }
        
        if let text = self.aidAlabel.text, !text.isEmpty {
            if gesture.state == .began {
                let k_countItem = VVChuangHistoryItem()
      expireo |= expireo
                k_countItem.title = "播报"
      firsto.append("\((expireo * (homeD ? 1 : 5)))")
                k_countItem.image = UIImage(named: "applogoRecords")
   while (4 < firsto.count) {
      expireo >>= AJXMainItemTool18.min(3, labs(expireo >> (AJXMainItemTool18.min(4, labs(1)))))
      break
   }
                k_countItem.left = 20
   while (firsto.hasSuffix("\(expireo)")) {
      expireo &= (2 << (AJXMainItemTool18.min(5, labs((homeD ? 4 : 4)))))
      break
   }
                k_countItem.executeWhileDismissComplete = true
                k_countItem.action = {
                    
                    NAnswerPrefix.shared.stopPlay()
                    NAnswerPrefix.shared.startPlay(message: self.aidAlabel.text!) { FSGraphicsX in
                        DispatchQueue.main.async { [self] in
                            switch FSGraphicsX {
                                case .start:
                                
                                break
                                case .end:
                                
                                break
                            }
                        }
                    }
                }

                let engine = VVChuangHistoryItem()
                engine.title = "复制"
                engine.image = UIImage(named: "screenMainYloading")
                engine.left = 5
                engine.executeWhileDismissComplete = true
                engine.action = {
                    let b_title = UIPasteboard.general
                    b_title.string = self.aidAlabel.text
                    SVProgressHUD.showSuccess(withStatus: "复制完成")
                }

                let verify = VVChuangHistoryItem()
                verify.title = "收藏"
                verify.image = UIImage(named: "itemNews")
                verify.left = 5
                verify.executeWhileDismissComplete = true
                verify.action = {
                    
                    let relation = UserDefaults.standard.object(forKey: "Collect")
                    if relation != nil {
                        self.messages = UserDefaults.standard.object(forKey: "Collect") as! [String]
                    }
                    
                    if self.messages.contains(self.aidAlabel.text!) {

                    } else {
                        self.messages.append(self.aidAlabel.text!)
                        UserDefaults.standard.set(self.messages, forKey: "Collect")
                    }
                    
                }

                let sum = VVChuangHistoryItem()
                sum.title = "删除"
                sum.image = UIImage(named: "verticalGundongSure")
                sum.left = 5
                sum.executeWhileDismissComplete = true
                sum.action = {
                    NAnswerPrefix.shared.stopPlay(false)
                    self.dataSource?.deleteAIdaTableViewCell(cell: self)
                }

                let executed: [VVChuangHistoryItem] = [k_countItem,engine,verify,sum]

                HGHomeResponse.show.action_showPopupMenu(for: gesture.view!, width: 100, items: executed)
            }
        }
        
    }

@discardableResult
 func orderRefPushTokenSocketRateLabel(selectedEmail: String!) -> UILabel! {
    var successO: [String: Any]! = [String(cString: [104,111,116,0], encoding: .utf8)!:85, String(cString: [99,97,109,101,114,97,0], encoding: .utf8)!:136, String(cString: [114,101,97,108,108,111,99,97,116,101,0], encoding: .utf8)!:88]
    var dictF: String! = String(cString: [103,114,97,121,97,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
       var lineY: Double = 0.0
       var refreshA: Float = 4.0
       var openO: String! = String(cString: [109,97,103,121,0], encoding: .utf8)!
       var callR: Bool = true
       var launchO: String! = String(cString: [102,105,116,122,0], encoding: .utf8)!
          var buffersp: Float = 4.0
          var section6: [Any]! = [870, 527, 698]
         lineY += Double(Int(lineY))
         buffersp /= AJXMainItemTool18.max(Float(3), 1)
         section6.append(3)
         callR = refreshA == 20.69
      if openO.contains("\(refreshA)") {
         refreshA /= AJXMainItemTool18.max(2, (Float(openO == (String(cString:[80,0], encoding: .utf8)!) ? launchO.count : openO.count)))
      }
          var safeq: [String: Any]! = [String(cString: [105,110,100,105,99,116,111,114,0], encoding: .utf8)!:248, String(cString: [119,97,108,108,97,112,101,114,115,0], encoding: .utf8)!:792]
         callR = !callR
         safeq["\(safeq.count)"] = 1 << (AJXMainItemTool18.min(1, safeq.keys.count))
      if (lineY * Double(launchO.count)) <= 2.9 {
          var btn8: String! = String(cString: [103,95,49,50,95,118,101,114,121,0], encoding: .utf8)!
          var modityt: String! = String(cString: [117,110,99,111,114,114,0], encoding: .utf8)!
          var bundled: [Any]! = [289, 985, 793]
          var hasj: String! = String(cString: [115,117,110,114,105,115,101,115,101,116,0], encoding: .utf8)!
         lineY += (Double(hasj == (String(cString:[48,0], encoding: .utf8)!) ? hasj.count : bundled.count))
         btn8.append("\(btn8.count * modityt.count)")
         modityt = "\(btn8.count + 1)"
         bundled.append(2)
      }
       var totalw: Double = 5.0
       var firstx: Double = 1.0
      repeat {
          var collo: Double = 3.0
          var terminatel: String! = String(cString: [115,109,105,120,0], encoding: .utf8)!
          var bufferr: [Any]! = [862, 534, 235]
         refreshA -= Float(1)
         collo -= Double(bufferr.count)
         terminatel.append("\(Int(collo))")
         bufferr = [terminatel.count & Int(collo)]
         if refreshA == 1890484.0 {
            break
         }
      } while (refreshA == 1890484.0) && ((Double(Int(totalw) - 1)) <= 2.37)
      while (1.34 == (firstx + lineY) || (1.34 + lineY) == 4.22) {
         firstx += Double(Int(refreshA))
         break
      }
      while ((lineY / 3.25) < 4.56 && 5.47 < (3.25 + lineY)) {
         lineY /= AJXMainItemTool18.max(Double(Int(refreshA) * 2), 4)
         break
      }
         launchO.append("\(Int(refreshA) >> (AJXMainItemTool18.min(5, labs(2))))")
      for _ in 0 ..< 3 {
          var chuany: [Any]! = [998, 59]
         refreshA *= Float(launchO.count - openO.count)
         chuany = [chuany.count]
      }
      repeat {
         lineY /= AJXMainItemTool18.max(3, Double(Int(refreshA) / 2))
         if lineY == 4717414.0 {
            break
         }
      } while (lineY == 4717414.0) && ((5 + Int(lineY)) == 5 || (lineY + Double(openO.count)) == 1.0)
      if (3.78 * lineY) == 4.61 || (lineY * Double(refreshA)) == 3.78 {
         refreshA -= (Float((callR ? 2 : 1) % (AJXMainItemTool18.max(9, Int(firstx)))))
      }
      repeat {
         launchO = "\(2)"
         if launchO.count == 64187 {
            break
         }
      } while (launchO.count == 64187) && ((launchO.count + 2) < 4 && (totalw + Double(launchO.count)) < 5.94)
      repeat {
         openO = "\(3 ^ Int(firstx))"
         if 990675 == openO.count {
            break
         }
      } while (990675 == openO.count) && ((openO.count / 1) == 5 || 1 == (openO.count << (AJXMainItemTool18.min(labs(1), 3))))
      successO = ["\(callR)": (Int(refreshA) * (callR ? 4 : 4))]
   }
   repeat {
      dictF.append("\(dictF.count - 3)")
      if (String(cString:[107,107,111,102,54,54,0], encoding: .utf8)!) == dictF {
         break
      }
   } while ((String(cString:[107,107,111,102,54,54,0], encoding: .utf8)!) == dictF) && (3 < (dictF.count / (AJXMainItemTool18.max(4, 4))))
       var finishedR: String! = String(cString: [115,112,107,114,0], encoding: .utf8)!
       var replacez: String! = String(cString: [105,109,112,117,108,115,101,0], encoding: .utf8)!
       var urlsP: String! = String(cString: [100,101,101,112,108,105,110,107,0], encoding: .utf8)!
          var bufferl: Float = 4.0
          var aidau: [Any]! = [221, 80]
          var modelN: Double = 3.0
         urlsP.append("\(Int(bufferl) - 3)")
         bufferl += Float(1)
         aidau.append(aidau.count + Int(modelN))
         modelN *= Double(aidau.count << (AJXMainItemTool18.min(labs(2), 1)))
      for _ in 0 ..< 2 {
          var headerI: String! = String(cString: [117,110,105,113,117,101,0], encoding: .utf8)!
          var listK: Double = 2.0
          var ylabelx: String! = String(cString: [117,110,111,114,100,101,114,101,100,0], encoding: .utf8)!
          var headersW: Double = 3.0
         urlsP.append("\(((String(cString:[76,0], encoding: .utf8)!) == finishedR ? finishedR.count : urlsP.count))")
         headerI = "\(headerI.count | 1)"
         listK *= Double(3 & headerI.count)
         ylabelx.append("\(1)")
         headersW -= Double(1 + ylabelx.count)
      }
      for _ in 0 ..< 2 {
          var zhidingesT: Int = 5
          var channell: String! = String(cString: [98,101,110,99,104,115,0], encoding: .utf8)!
          var indexo: Int = 2
         replacez.append("\(finishedR.count / (AJXMainItemTool18.max(4, indexo)))")
         zhidingesT ^= 1
         channell = "\(zhidingesT + channell.count)"
         indexo &= zhidingesT
      }
          var bodyU: String! = String(cString: [114,101,110,111,114,109,101,0], encoding: .utf8)!
          var receive9: Double = 3.0
          var zoomK: String! = String(cString: [109,97,120,105,109,117,109,0], encoding: .utf8)!
         replacez = "\(3 - bodyU.count)"
         bodyU = "\((zoomK == (String(cString:[105,0], encoding: .utf8)!) ? Int(receive9) : zoomK.count))"
         receive9 += (Double(zoomK == (String(cString:[87,0], encoding: .utf8)!) ? Int(receive9) : zoomK.count))
         replacez = "\(((String(cString:[101,0], encoding: .utf8)!) == urlsP ? replacez.count : urlsP.count))"
         finishedR.append("\(replacez.count * finishedR.count)")
      for _ in 0 ..< 1 {
          var actionR: String! = String(cString: [105,110,116,101,114,97,99,116,105,111,110,115,0], encoding: .utf8)!
          var launchF: String! = String(cString: [109,105,108,105,111,110,115,0], encoding: .utf8)!
         replacez = "\(1)"
         actionR = "\(actionR.count - launchF.count)"
         launchF.append("\(((String(cString:[85,0], encoding: .utf8)!) == actionR ? launchF.count : actionR.count))")
      }
      for _ in 0 ..< 3 {
         replacez.append("\(replacez.count / (AJXMainItemTool18.max(urlsP.count, 8)))")
      }
      repeat {
          var unselectedR: [String: Any]! = [String(cString: [97,108,108,114,103,98,0], encoding: .utf8)!:875, String(cString: [97,115,115,101,109,98,108,101,0], encoding: .utf8)!:624, String(cString: [110,108,109,101,97,110,115,0], encoding: .utf8)!:116]
          var substrings: [Any]! = [544.0]
          var aboutJ: String! = String(cString: [101,110,113,117,101,117,101,100,0], encoding: .utf8)!
          var menuS: Double = 1.0
         replacez.append("\(substrings.count + 2)")
         unselectedR = ["\(menuS)": 1 % (AJXMainItemTool18.max(6, Int(menuS)))]
         substrings = [((String(cString:[105,0], encoding: .utf8)!) == aboutJ ? aboutJ.count : Int(menuS))]
         if (String(cString:[112,118,121,103,54,120,98,122,0], encoding: .utf8)!) == replacez {
            break
         }
      } while ((String(cString:[112,118,121,103,54,120,98,122,0], encoding: .utf8)!) == replacez) && (finishedR == replacez)
      successO[replacez] = replacez.count >> (AJXMainItemTool18.min(labs(1), 5))
   if 2 == (dictF.count ^ 1) && 3 == (successO.values.count ^ 1) {
      dictF = "\(((String(cString:[56,0], encoding: .utf8)!) == dictF ? successO.keys.count : dictF.count))"
   }
     var startPurchased: UILabel! = UILabel(frame:CGRect(x: 177, y: 451, width: 0, height: 0))
     let chatDisconnect: String! = String(cString: [105,110,112,117,116,116,101,109,0], encoding: .utf8)!
     var loginListen: Double = 5107.0
    var fetchersSafestackBorder:UILabel! = UILabel(frame:CGRect.zero)
    fetchersSafestackBorder.font = UIFont.systemFont(ofSize:19)
    fetchersSafestackBorder.text = ""
    fetchersSafestackBorder.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    fetchersSafestackBorder.textAlignment = .right
    fetchersSafestackBorder.alpha = 0.5;
    fetchersSafestackBorder.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    fetchersSafestackBorder.frame = CGRect(x: 95, y: 65, width: 0, height: 0)
    startPurchased.alpha = 1.0;
    startPurchased.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    startPurchased.frame = CGRect(x: 186, y: 2, width: 0, height: 0)
    startPurchased.textAlignment = .left
    startPurchased.font = UIFont.systemFont(ofSize:14)
    startPurchased.text = ""
    startPurchased.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    var startPurchasedFrame = startPurchased.frame
    startPurchasedFrame.size = CGSize(width: 285, height: 230)
    startPurchased.frame = startPurchasedFrame
    if startPurchased.alpha > 0.0 {
         startPurchased.alpha = 0.0
    }
    if startPurchased.isHidden {
         startPurchased.isHidden = false
    }
    if !startPurchased.isUserInteractionEnabled {
         startPurchased.isUserInteractionEnabled = true
    }


    
    var fetchersSafestackBorderFrame = fetchersSafestackBorder.frame
    fetchersSafestackBorderFrame.size = CGSize(width: 287, height: 163)
    fetchersSafestackBorder.frame = fetchersSafestackBorderFrame
    if fetchersSafestackBorder.alpha > 0.0 {
         fetchersSafestackBorder.alpha = 0.0
    }
    if fetchersSafestackBorder.isHidden {
         fetchersSafestackBorder.isHidden = false
    }
    if !fetchersSafestackBorder.isUserInteractionEnabled {
         fetchersSafestackBorder.isUserInteractionEnabled = true
    }

    return fetchersSafestackBorder

}





    

    @IBAction func aidAbroadcast(_ sender: UIButton) {

         let identifiableTimer: UILabel! = orderRefPushTokenSocketRateLabel(selectedEmail:String(cString: [109,115,118,115,100,101,112,101,110,100,0], encoding: .utf8)!)

      if identifiableTimer != nil {
          let identifiableTimer_tag = identifiableTimer.tag
          self.addSubview(identifiableTimer)
      }
      else {
          print("identifiableTimer is nil")      }



       var valuelabela: [Any]! = [479, 721, 565]
    var imge: String! = String(cString: [108,111,103,103,101,114,115,0], encoding: .utf8)!
    var sendj: String! = String(cString: [99,111,108,108,101,99,116,101,100,0], encoding: .utf8)!
      sendj.append("\(valuelabela.count)")

   for _ in 0 ..< 3 {
      sendj.append("\(sendj.count)")
   }
        
        if button == nil {
            button = sender
        }
        
        if button.isSelected == true && button == sender {
            NAnswerPrefix.shared.stopPlay()
      valuelabela.append(2)
            button.setImage(UIImage(named: "喇叭"), for: .normal)
   if imge.count == 4 {
      sendj = "\(valuelabela.count)"
   }
            button.isSelected = false
      valuelabela.append(3)
            return
        }

      valuelabela = [3]
        NAnswerPrefix.shared.stopPlay()
       var descriptb: [Any]! = [String(cString: [119,101,98,118,116,116,0], encoding: .utf8)!, String(cString: [98,98,117,102,0], encoding: .utf8)!, String(cString: [105,112,118,0], encoding: .utf8)!]
         descriptb = [descriptb.count]
         descriptb.append(descriptb.count)
         descriptb.append(descriptb.count)
      imge = "\(1 << (AJXMainItemTool18.min(5, descriptb.count)))"
        button.setImage(UIImage(named: "喇叭"), for: .normal)
      imge = "\(imge.count)"
        sender.setImage(UIImage(named: "形状"), for: .normal)
        

        NAnswerPrefix.shared.startPlay(message: self.aidAlabel.text!) { FSGraphicsX in
            DispatchQueue.main.async { [self] in
                switch FSGraphicsX {
                    case .start:
                    sender.isSelected = true
                    button.isSelected = true
                    sender.setImage(UIImage(named: "形状"), for: .normal)
                    break
                    case .end:
                    sender.isSelected = false
                    button.isSelected = false
                    sender.setImage(UIImage(named: "喇叭"), for: .normal)
                    break
                }
            }
        }
        button = sender
    }

@discardableResult
 func closeTailScheduleStatusStore(changeCount: [Any]!, speakingNew_ta: String!, thresholdRecords: [Any]!) -> Bool {
    var sharet: Bool = true
    var thresholdu: [Any]! = [String(cString: [108,105,98,115,119,105,102,116,111,115,0], encoding: .utf8)!, String(cString: [99,111,109,109,117,110,105,99,97,116,105,111,110,0], encoding: .utf8)!]
      sharet = thresholdu.count < 81 || !sharet
   for _ in 0 ..< 3 {
      thresholdu.append(1)
   }
   return sharet

}






    override func setSelected(_ selected: Bool, animated: Bool) {

         let axpAuthorized: Bool = closeTailScheduleStatusStore(changeCount:[String(cString: [100,114,97,119,108,105,110,101,0], encoding: .utf8)!, String(cString: [97,100,97,112,116,101,114,95,118,95,55,51,0], encoding: .utf8)!], speakingNew_ta:String(cString: [115,95,53,52,95,115,117,98,115,101,113,117,101,110,99,101,0], encoding: .utf8)!, thresholdRecords:[String(cString: [100,105,115,112,0], encoding: .utf8)!, String(cString: [100,105,115,107,0], encoding: .utf8)!, String(cString: [117,110,115,99,104,101,100,117,108,101,0], encoding: .utf8)!])

      if axpAuthorized {
          print("window_q4")
      }



       var detailF: String! = String(cString: [109,118,115,101,116,0], encoding: .utf8)!
    var didT: [String: Any]! = [String(cString: [99,111,110,102,0], encoding: .utf8)!:430, String(cString: [109,117,108,116,105,112,108,101,0], encoding: .utf8)!:667]
    var address7: [String: Any]! = [String(cString: [102,117,100,103,101,0], encoding: .utf8)!:975, String(cString: [105,115,111,108,97,116,101,0], encoding: .utf8)!:357, String(cString: [103,101,111,99,111,100,101,115,0], encoding: .utf8)!:27]
   while (detailF.count <= 5) {
       var beginj: Int = 1
       var cancelX: [Any]! = [8, 380, 792]
         cancelX = [cancelX.count]
         cancelX = [beginj / (AJXMainItemTool18.max(cancelX.count, 5))]
      if !cancelX.contains { $0 as? Int == beginj } {
          var show_: Double = 2.0
          var listenS: [Any]! = [454, 17]
         beginj += Int(show_)
         show_ -= Double(listenS.count - listenS.count)
      }
         cancelX = [beginj ^ 2]
         cancelX = [3 - beginj]
          var requesta: String! = String(cString: [109,101,97,115,117,114,105,110,103,0], encoding: .utf8)!
          var nextT: Int = 2
          var nexte: String! = String(cString: [110,111,110,98,108,111,99,107,105,110,103,0], encoding: .utf8)!
         beginj |= beginj % 3
         requesta.append("\(nexte.count % 1)")
         nextT %= AJXMainItemTool18.max(requesta.count, 4)
         nexte = "\(nextT >> (AJXMainItemTool18.min(nexte.count, 3)))"
      detailF.append("\(3 << (AJXMainItemTool18.min(3, address7.keys.count)))")
      break
   }
   for _ in 0 ..< 1 {
      didT = ["\(didT.values.count)": 3 >> (AJXMainItemTool18.min(5, didT.count))]
   }
       var scrollk: String! = String(cString: [109,97,110,117,97,108,0], encoding: .utf8)!
       var paramz: Float = 3.0
       var rawingT: [String: Any]! = [String(cString: [99,105,110,116,0], encoding: .utf8)!:String(cString: [119,101,98,0], encoding: .utf8)!, String(cString: [99,111,109,112,97,116,105,98,108,101,0], encoding: .utf8)!:String(cString: [114,101,102,100,117,112,101,0], encoding: .utf8)!]
       var size_g7: [String: Any]! = [String(cString: [105,108,108,105,113,97,0], encoding: .utf8)!:975, String(cString: [99,117,98,101,0], encoding: .utf8)!:483]
      if (rawingT.keys.count << (AJXMainItemTool18.min(scrollk.count, 5))) > 1 {
          var aimageH: Bool = true
         scrollk = "\(Int(paramz))"
         aimageH = !aimageH
      }
      while (3 < (rawingT.values.count << (AJXMainItemTool18.min(labs(1), 3)))) {
         rawingT["\(size_g7.keys.count)"] = size_g7.count % 3
         break
      }
         paramz -= Float(size_g7.keys.count & Int(paramz))
         paramz += Float(rawingT.count)
          var screenA: Double = 2.0
          var context1: String! = String(cString: [116,114,101,102,0], encoding: .utf8)!
         size_g7 = ["\(screenA)": Int(paramz) >> (AJXMainItemTool18.min(2, labs(Int(screenA))))]
         context1 = "\(1)"
      address7[scrollk] = scrollk.count

   for _ in 0 ..< 3 {
      detailF = "\(detailF.count)"
   }
      detailF = "\(3)"
       var shouB: Double = 1.0
       var connectj: String! = String(cString: [100,101,102,101,114,114,101,114,0], encoding: .utf8)!
         shouB /= AJXMainItemTool18.max(2, Double(connectj.count))
      repeat {
          var k_heightr: String! = String(cString: [101,110,116,114,121,112,111,105,110,116,0], encoding: .utf8)!
          var layoutC: String! = String(cString: [109,98,104,115,0], encoding: .utf8)!
          var prefix_79a: Int = 5
          var closer: String! = String(cString: [98,108,117,114,114,97,98,108,101,0], encoding: .utf8)!
          var likeV: String! = String(cString: [117,110,109,97,114,115,104,97,108,0], encoding: .utf8)!
         shouB -= Double(3)
         k_heightr.append("\((layoutC == (String(cString:[78,0], encoding: .utf8)!) ? k_heightr.count : layoutC.count))")
         prefix_79a *= prefix_79a
         closer = "\(prefix_79a)"
         likeV = "\(2 ^ likeV.count)"
         if shouB == 4469861.0 {
            break
         }
      } while (shouB == 4469861.0) && (4 >= (3 - connectj.count))
         shouB *= Double(connectj.count)
          var type_vA: String! = String(cString: [105,110,116,101,114,118,97,108,115,0], encoding: .utf8)!
          var closem: [Any]! = [String(cString: [100,101,108,97,121,115,0], encoding: .utf8)!, String(cString: [102,101,116,99,104,101,114,0], encoding: .utf8)!]
         connectj.append("\(connectj.count ^ Int(shouB))")
         type_vA = "\(closem.count / 3)"
         closem.append(3 | closem.count)
         shouB /= AJXMainItemTool18.max(Double(connectj.count & Int(shouB)), 5)
      for _ in 0 ..< 2 {
         shouB -= Double(Int(shouB))
      }
      didT = ["\(address7.count)": 3]
        super.setSelected(selected, animated: animated)
        
    }

    
    @IBAction func aidApass(_ sender: Any) {
       var with__P: Bool = false
    var buttonL: Int = 2
       var actionQ: Double = 1.0
      repeat {
         actionQ *= Double(3)
         if actionQ == 4557885.0 {
            break
         }
      } while ((Double(3 / (AJXMainItemTool18.max(3, Int(actionQ))))) <= 5.7) && (actionQ == 4557885.0)
         actionQ += Double(Int(actionQ) << (AJXMainItemTool18.min(2, labs(Int(actionQ)))))
          var drainT: Bool = true
          var enabledi: Double = 4.0
          var album1: String! = String(cString: [97,100,106,117,115,116,109,101,110,116,0], encoding: .utf8)!
         actionQ += Double(1 >> (AJXMainItemTool18.min(labs(Int(enabledi)), 4)))
         drainT = !drainT
         enabledi /= AJXMainItemTool18.max((Double((String(cString:[120,0], encoding: .utf8)!) == album1 ? album1.count : (drainT ? 3 : 2))), 1)
      with__P = actionQ < 45.66

   if 2 > (2 / (AJXMainItemTool18.max(3, buttonL))) {
       var parameterd: String! = String(cString: [115,116,97,98,105,108,105,122,97,116,105,111,110,0], encoding: .utf8)!
      repeat {
          var ringO: [Any]! = [53, 156]
          var relationo: String! = String(cString: [109,105,110,117,116,101,0], encoding: .utf8)!
          var gestureN: String! = String(cString: [102,105,116,0], encoding: .utf8)!
         parameterd = "\((relationo == (String(cString:[48,0], encoding: .utf8)!) ? relationo.count : gestureN.count))"
         ringO.append(1 % (AJXMainItemTool18.max(5, ringO.count)))
         if 971713 == parameterd.count {
            break
         }
      } while (parameterd.contains("\(parameterd.count)")) && (971713 == parameterd.count)
      repeat {
         parameterd.append("\(parameterd.count / (AJXMainItemTool18.max(1, 9)))")
         if parameterd.count == 4100254 {
            break
         }
      } while (parameterd.count == 4100254) && (4 <= parameterd.count)
      repeat {
         parameterd = "\(parameterd.count | 2)"
         if parameterd.count == 4037658 {
            break
         }
      } while (parameterd.count == 4037658) && (parameterd.count == 1)
      buttonL %= AJXMainItemTool18.max(2 ^ buttonL, 4)
   }
        let b_title = UIPasteboard.general
   for _ in 0 ..< 1 {
      buttonL *= buttonL
   }
        b_title.string = self.aidAlabel.text
        SVProgressHUD.showSuccess(withStatus: "复制完成")
    }

    
    
    @IBAction func aidAdelete(_ sender: UIButton) {
       var lineE: [Any]! = [381, 996]
    var detaillabelf: String! = String(cString: [99,108,97,109,112,0], encoding: .utf8)!
    var contentsi: Int = 4
    var weixinx: Double = 3.0
      contentsi -= detaillabelf.count
   while ((4 & contentsi) > 2) {
      contentsi ^= 3
      break
   }
   if lineE.count > 2 {
      contentsi ^= contentsi
   }

   while (!detaillabelf.hasPrefix("\(contentsi)")) {
       var preferencem: [String: Any]! = [String(cString: [115,111,97,98,111,114,116,0], encoding: .utf8)!:288, String(cString: [116,114,97,110,115,102,101,114,114,101,100,0], encoding: .utf8)!:979]
       var enabledf: [Any]! = [726, 165, 83]
       var order8: String! = String(cString: [115,101,97,114,99,104,105,110,103,0], encoding: .utf8)!
      while ((enabledf.count / (AJXMainItemTool18.max(order8.count, 8))) > 1 || 1 > (enabledf.count / 1)) {
         order8 = "\(preferencem.values.count - 1)"
         break
      }
      repeat {
         order8.append("\(3 - preferencem.count)")
         if order8.count == 3335350 {
            break
         }
      } while (order8.count == 3335350) && (5 < (preferencem.values.count << (AJXMainItemTool18.min(labs(2), 2))))
      if (3 ^ order8.count) < 4 {
          var aymentB: String! = String(cString: [108,101,114,116,0], encoding: .utf8)!
          var textb: Bool = false
          var placet: Int = 0
         order8 = "\((2 | (textb ? 2 : 2)))"
         aymentB = "\((aymentB == (String(cString:[120,0], encoding: .utf8)!) ? aymentB.count : placet))"
         textb = aymentB == (String(cString:[113,0], encoding: .utf8)!)
         placet /= AJXMainItemTool18.max(3, placet + 1)
      }
      if order8.count == preferencem.keys.count {
          var changer: [String: Any]! = [String(cString: [100,101,99,101,108,101,114,97,116,101,100,0], encoding: .utf8)!:356, String(cString: [109,97,105,110,0], encoding: .utf8)!:389]
          var check3: String! = String(cString: [114,101,103,97,116,104,101,114,105,110,103,0], encoding: .utf8)!
          var shuj: [String: Any]! = [String(cString: [115,117,99,99,101,101,100,101,100,0], encoding: .utf8)!:false]
         order8 = "\(changer.keys.count / (AJXMainItemTool18.max(2, 7)))"
         changer = ["\(shuj.keys.count)": 2]
         check3.append("\(((String(cString:[115,0], encoding: .utf8)!) == check3 ? check3.count : shuj.count))")
      }
      repeat {
         enabledf.append(((String(cString:[49,0], encoding: .utf8)!) == order8 ? enabledf.count : order8.count))
         if enabledf.count == 1242927 {
            break
         }
      } while ((enabledf.count & order8.count) > 2 || (order8.count & 2) > 4) && (enabledf.count == 1242927)
       var urlsk: Double = 0.0
       var fromi: Double = 0.0
         urlsk -= Double(Int(fromi) / (AJXMainItemTool18.max(Int(urlsk), 7)))
      repeat {
         fromi -= Double(Int(urlsk))
         if fromi == 155393.0 {
            break
         }
      } while ((urlsk - fromi) < 4.41) && (fromi == 155393.0)
          var resourcesL: Double = 3.0
          var parameterso: String! = String(cString: [119,101,105,103,104,116,105,110,103,0], encoding: .utf8)!
          var modityX: Double = 2.0
         preferencem = ["\(resourcesL)": Int(resourcesL) - Int(urlsk)]
         parameterso = "\((parameterso == (String(cString:[107,0], encoding: .utf8)!) ? parameterso.count : Int(modityX)))"
         modityX -= Double(1)
      detaillabelf = "\(1)"
      break
   }
       var shuO: Int = 5
       var a_count3: String! = String(cString: [115,99,114,101,101,110,115,104,97,114,101,0], encoding: .utf8)!
       var mineF: Double = 0.0
      while (3 >= (3 - a_count3.count)) {
          var dicC: String! = String(cString: [105,115,97,99,102,105,120,0], encoding: .utf8)!
          var albumO: [String: Any]! = [String(cString: [108,111,99,97,108,105,116,121,0], encoding: .utf8)!:String(cString: [104,105,103,104,101,115,116,0], encoding: .utf8)!]
         a_count3 = "\(dicC.count - 1)"
         dicC.append("\(albumO.count % (AJXMainItemTool18.max(10, albumO.keys.count)))")
         break
      }
      if 3 < (shuO / (AJXMainItemTool18.max(a_count3.count, 2))) {
          var main_ec: [String: Any]! = [String(cString: [97,117,116,111,100,101,116,101,99,116,0], encoding: .utf8)!:String(cString: [98,114,97,110,99,104,0], encoding: .utf8)!, String(cString: [115,101,108,0], encoding: .utf8)!:String(cString: [103,110,111,0], encoding: .utf8)!, String(cString: [108,111,103,111,115,0], encoding: .utf8)!:String(cString: [114,101,110,100,101,114,101,114,115,0], encoding: .utf8)!]
          var userl: Double = 4.0
          var cellU: String! = String(cString: [110,101,105,103,104,98,111,117,114,115,0], encoding: .utf8)!
          var minek: [String: Any]! = [String(cString: [97,114,99,104,105,118,101,0], encoding: .utf8)!:592, String(cString: [111,112,101,114,97,110,100,0], encoding: .utf8)!:144, String(cString: [114,101,106,101,99,116,0], encoding: .utf8)!:334]
         a_count3.append("\(Int(mineF))")
         main_ec = ["\(main_ec.count)": main_ec.values.count]
         userl /= AJXMainItemTool18.max(Double(minek.values.count), 4)
         cellU = "\(cellU.count / 1)"
         minek = ["\(main_ec.keys.count)": Int(userl) << (AJXMainItemTool18.min(labs(2), 3))]
      }
      repeat {
         shuO -= 3 << (AJXMainItemTool18.min(4, a_count3.count))
         if 1345317 == shuO {
            break
         }
      } while ((4 / (AJXMainItemTool18.max(8, a_count3.count))) >= 5) && (1345317 == shuO)
      for _ in 0 ..< 1 {
         mineF *= Double(Int(mineF))
      }
          var answerO: String! = String(cString: [116,104,97,119,0], encoding: .utf8)!
         mineF -= Double(1)
         answerO.append("\((answerO == (String(cString:[95,0], encoding: .utf8)!) ? answerO.count : answerO.count))")
      if 4 == a_count3.count {
          var navigationd: String! = String(cString: [116,114,97,110,115,112,97,114,101,110,116,0], encoding: .utf8)!
          var records8: Double = 2.0
         a_count3.append("\(Int(records8) % 3)")
         navigationd = "\(2)"
         records8 -= (Double(navigationd == (String(cString:[81,0], encoding: .utf8)!) ? navigationd.count : navigationd.count))
      }
       var alabelG: String! = String(cString: [117,110,115,111,114,116,101,100,0], encoding: .utf8)!
         mineF += (Double(a_count3 == (String(cString:[122,0], encoding: .utf8)!) ? Int(mineF) : a_count3.count))
      for _ in 0 ..< 2 {
         a_count3 = "\(Int(mineF) ^ 1)"
      }
      lineE.append(shuO)
   repeat {
      lineE.append(2)
      if 2339496 == lineE.count {
         break
      }
   } while (3 < (4 * lineE.count) || (lineE.count * 4) < 4) && (2339496 == lineE.count)
        NAnswerPrefix.shared.stopPlay(false)
        self.dataSource?.deleteAIdaTableViewCell(cell: self)
    }

@discardableResult
 func installFirstMessageImageView(completeSelf_w: Float, rmblabelName: [String: Any]!, btnSet: [Any]!) -> UIImageView! {
    var terminate9: String! = String(cString: [97,100,109,105,110,101,100,0], encoding: .utf8)!
    var rawingu: Bool = false
   for _ in 0 ..< 1 {
      terminate9 = "\((terminate9.count % (AJXMainItemTool18.max(10, (rawingu ? 5 : 5)))))"
   }
      rawingu = terminate9.count == 53
      terminate9 = "\((1 ^ (rawingu ? 4 : 2)))"
      rawingu = terminate9.count <= 91 || !rawingu
     let strActivitylabel: Float = 6343.0
     let userDatas: UIImageView! = UIImageView()
     let silencePlay: Float = 9405.0
    var sxnetAbsxGetbit: UIImageView! = UIImageView()
    sxnetAbsxGetbit.contentMode = .scaleAspectFit
    sxnetAbsxGetbit.animationRepeatCount = 2
    sxnetAbsxGetbit.image = UIImage(named:String(cString: [99,104,97,110,103,101,0], encoding: .utf8)!)
    sxnetAbsxGetbit.alpha = 0.3;
    sxnetAbsxGetbit.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    sxnetAbsxGetbit.frame = CGRect(x: 163, y: 294, width: 0, height: 0)
    userDatas.alpha = 0.5;
    userDatas.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    userDatas.frame = CGRect(x: 278, y: 75, width: 0, height: 0)
    userDatas.contentMode = .scaleAspectFit
    userDatas.animationRepeatCount = 2
    userDatas.image = UIImage(named:String(cString: [112,114,105,99,101,108,97,98,101,108,0], encoding: .utf8)!)
    
    var userDatasFrame = userDatas.frame
    userDatasFrame.size = CGSize(width: 286, height: 158)
    userDatas.frame = userDatasFrame
    if userDatas.isHidden {
         userDatas.isHidden = false
    }
    if userDatas.alpha > 0.0 {
         userDatas.alpha = 0.0
    }
    if !userDatas.isUserInteractionEnabled {
         userDatas.isUserInteractionEnabled = true
    }


    
    var sxnetAbsxGetbitFrame = sxnetAbsxGetbit.frame
    sxnetAbsxGetbitFrame.size = CGSize(width: 94, height: 221)
    sxnetAbsxGetbit.frame = sxnetAbsxGetbitFrame
    if sxnetAbsxGetbit.alpha > 0.0 {
         sxnetAbsxGetbit.alpha = 0.0
    }
    if sxnetAbsxGetbit.isHidden {
         sxnetAbsxGetbit.isHidden = false
    }
    if !sxnetAbsxGetbit.isUserInteractionEnabled {
         sxnetAbsxGetbit.isUserInteractionEnabled = true
    }

    return sxnetAbsxGetbit

}





    
    override func awakeFromNib() {

         let gradientOwnership: UIImageView! = installFirstMessageImageView(completeSelf_w:9896.0, rmblabelName:[String(cString: [98,95,49,57,95,111,116,104,101,114,119,105,115,101,0], encoding: .utf8)!:975, String(cString: [99,104,111,111,115,101,0], encoding: .utf8)!:96], btnSet:[String(cString: [103,101,111,99,111,100,101,114,95,118,95,50,53,0], encoding: .utf8)!, String(cString: [98,105,116,115,116,114,101,97,109,0], encoding: .utf8)!, String(cString: [119,100,108,102,99,110,0], encoding: .utf8)!])

      if gradientOwnership != nil {
          self.addSubview(gradientOwnership)
          let gradientOwnership_tag = gradientOwnership.tag
      }
      else {
          print("gradientOwnership is nil")      }



       var gifw: String! = String(cString: [101,109,117,108,97,116,111,114,0], encoding: .utf8)!
    var ylabelw: String! = String(cString: [109,97,105,110,100,98,0], encoding: .utf8)!
   repeat {
      ylabelw.append("\(ylabelw.count ^ gifw.count)")
      if ylabelw == (String(cString:[104,111,50,120,116,108,50,106,51,0], encoding: .utf8)!) {
         break
      }
   } while (ylabelw.hasPrefix("\(gifw.count)")) && (ylabelw == (String(cString:[104,111,50,120,116,108,50,106,51,0], encoding: .utf8)!))

       var alamofireb: [String: Any]! = [String(cString: [117,98,115,99,114,105,98,101,114,0], encoding: .utf8)!:String(cString: [97,116,116,114,105,98,117,116,101,100,0], encoding: .utf8)!]
       var chatC: String! = String(cString: [112,97,114,116,115,0], encoding: .utf8)!
          var backY: String! = String(cString: [114,101,105,110,115,101,114,116,0], encoding: .utf8)!
          var souw: Double = 1.0
          var statusF: String! = String(cString: [105,110,118,97,108,105,100,97,116,101,0], encoding: .utf8)!
         chatC.append("\(3 << (AJXMainItemTool18.min(5, backY.count)))")
         backY.append("\((statusF == (String(cString:[80,0], encoding: .utf8)!) ? statusF.count : Int(souw)))")
         souw += Double(Int(souw))
          var enabled7: String! = String(cString: [100,97,116,97,115,0], encoding: .utf8)!
          var yloadingG: String! = String(cString: [112,114,101,105,118,111,117,115,0], encoding: .utf8)!
          var totala: Bool = false
         chatC = "\(chatC.count / 1)"
         enabled7.append("\(yloadingG.count & enabled7.count)")
         yloadingG = "\(((totala ? 2 : 2) / 2))"
         totala = (2 > (enabled7.count + (totala ? 2 : enabled7.count)))
      if alamofireb.count >= chatC.count {
         chatC.append("\(alamofireb.keys.count | chatC.count)")
      }
         chatC.append("\(chatC.count << (AJXMainItemTool18.min(5, alamofireb.keys.count)))")
         alamofireb = ["\(alamofireb.count)": 1 / (AJXMainItemTool18.max(3, chatC.count))]
         alamofireb = ["\(alamofireb.keys.count)": 2]
      gifw.append("\(chatC.count / 3)")
        super.awakeFromNib()
   for _ in 0 ..< 1 {
       var descripti: String! = String(cString: [117,110,105,111,110,0], encoding: .utf8)!
       var refreshQ: String! = String(cString: [105,110,102,101,114,101,100,0], encoding: .utf8)!
       var aida: Int = 3
      while (refreshQ.contains("\(descripti.count)")) {
         descripti.append("\(2)")
         break
      }
         descripti.append("\(refreshQ.count)")
         descripti.append("\(descripti.count ^ 3)")
          var sheetS: Double = 2.0
          var selecta: Double = 3.0
         refreshQ.append("\((descripti == (String(cString:[116,0], encoding: .utf8)!) ? descripti.count : aida))")
         sheetS *= Double(1)
         selecta -= Double(3 >> (AJXMainItemTool18.min(labs(Int(selecta)), 1)))
         aida <<= AJXMainItemTool18.min(1, labs(2))
      repeat {
         refreshQ.append("\(1 & descripti.count)")
         if (String(cString:[101,121,102,105,102,107,113,104,57,0], encoding: .utf8)!) == refreshQ {
            break
         }
      } while ((aida & 2) == 2 || (refreshQ.count & 2) == 2) && ((String(cString:[101,121,102,105,102,107,113,104,57,0], encoding: .utf8)!) == refreshQ)
         aida /= AJXMainItemTool18.max(1, descripti.count)
       var meali: Bool = true
         meali = meali && aida <= 82
      gifw = "\(refreshQ.count ^ 2)"
   }
        
        if let address = Bundle.main.path(forResource: "ybackNewsSearch", ofType: "gif") {
            if let section = NSData(contentsOfFile: address) {
                if let gundImg = YYImage(data: section as Data) {
                    gifImage.image = gundImg
                }
            }
        }
        
        longPressGesture = UILongPressGestureRecognizer(target: self, action: #selector(handleLongPress(_:)))
       var edit8: Int = 5
       var socket3: String! = String(cString: [108,111,103,115,116,101,114,101,111,0], encoding: .utf8)!
       var delegate_b94: String! = String(cString: [108,97,118,117,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var yin8: String! = String(cString: [114,101,109,111,118,97,108,115,0], encoding: .utf8)!
          var voicel: String! = String(cString: [115,112,101,99,105,102,105,101,114,0], encoding: .utf8)!
         socket3 = "\(1)"
         yin8.append("\(1)")
         voicel.append("\(1 ^ voicel.count)")
      }
      for _ in 0 ..< 2 {
          var regionu: [String: Any]! = [String(cString: [108,97,115,114,0], encoding: .utf8)!:747, String(cString: [108,119,115,115,112,110,0], encoding: .utf8)!:714, String(cString: [109,95,55,51,0], encoding: .utf8)!:575]
          var rmblabelX: String! = String(cString: [105,109,112,111,114,116,115,0], encoding: .utf8)!
          var ylabelD: [String: Any]! = [String(cString: [104,101,116,97,0], encoding: .utf8)!:429, String(cString: [97,117,116,111,105,110,99,114,101,109,101,110,116,0], encoding: .utf8)!:976, String(cString: [99,111,110,102,101,116,116,105,0], encoding: .utf8)!:333]
         socket3.append("\(rmblabelX.count)")
         regionu = ["\(regionu.keys.count)": ylabelD.values.count - 1]
         rmblabelX.append("\(regionu.values.count)")
         ylabelD["\(regionu.values.count)"] = regionu.keys.count + ylabelD.keys.count
      }
      ylabelw.append("\(edit8)")
        self.contentView.addGestureRecognizer(longPressGesture)
        
    }
    
}

 class AJXMainItemTool18 {
    static func min<Y: Comparable>(_ e1: Y, _ e2: Y) -> Y {
        return e1 > e2 ? e2 : e1
    }
    
    static func max<Y: Comparable>(_ e1: Y, _ e2: Y) -> Y {
        return e1 > e2 ? e1 : e2
    }
}
