
import Foundation

import UIKit
import ZKProgressHUD
import SVProgressHUD

class CGCScreenMineController: UIViewController {
private var shareTitleMap: [String: Any]!
var numIconStr: String!
var silence_size: Float = 0.0
var engineTaskDownArray: [Any]?




    @IBOutlet weak var phone: UITextField!
    @IBOutlet weak var code: UITextField!

@discardableResult
 func headerEdgesCollateInvokeStatues(chatNot_gt: Bool, fuzhiAimage: [Any]!) -> [Any]! {
    var navigationQ: Double = 2.0
    var listenZ: Double = 5.0
   while (listenZ < 5.20) {
       var showL: Double = 4.0
       var imgZ: [String: Any]! = [String(cString: [115,104,97,100,101,114,0], encoding: .utf8)!:31, String(cString: [97,108,105,97,115,0], encoding: .utf8)!:326, String(cString: [104,101,97,100,112,104,111,110,101,115,0], encoding: .utf8)!:766]
       var notification8: String! = String(cString: [101,118,97,108,117,97,116,111,114,0], encoding: .utf8)!
       var window_ol: Int = 2
       var bufferP: Double = 2.0
         showL += Double(window_ol % 2)
      while ((notification8.count / 2) > 4 && (imgZ.count / (KAudioYloadingTool20.max(notification8.count, 10))) > 2) {
         imgZ = ["\(showL)": Int(showL) << (KAudioYloadingTool20.min(labs(Int(bufferP)), 1))]
         break
      }
       var menuV: Float = 3.0
       var notificationy: Float = 5.0
         imgZ = ["\(bufferP)": 3 >> (KAudioYloadingTool20.min(labs(Int(bufferP)), 5))]
      while (notification8.hasPrefix("\(imgZ.values.count)")) {
          var medataD: String! = String(cString: [99,95,50,95,98,105,103,105,110,116,101,103,101,114,0], encoding: .utf8)!
          var buttonplayC: String! = String(cString: [109,111,115,116,0], encoding: .utf8)!
          var sectionsg: [Any]! = [String(cString: [98,105,110,98,110,0], encoding: .utf8)!, String(cString: [97,117,116,111,114,101,118,101,114,115,101,100,0], encoding: .utf8)!, String(cString: [117,110,115,99,97,108,101,100,0], encoding: .utf8)!]
         notification8 = "\(1)"
         medataD = "\(medataD.count + buttonplayC.count)"
         buttonplayC.append("\((medataD == (String(cString:[87,0], encoding: .utf8)!) ? medataD.count : buttonplayC.count))")
         sectionsg = [((String(cString:[87,0], encoding: .utf8)!) == buttonplayC ? buttonplayC.count : sectionsg.count)]
         break
      }
      if (showL - Double(notification8.count)) == 1.56 || 3 == (2 + notification8.count) {
         notification8.append("\(Int(menuV) * window_ol)")
      }
         showL /= KAudioYloadingTool20.max(3, Double(Int(showL)))
          var contentU: Bool = false
          var resources9: Double = 3.0
          var slider7: [Any]! = [790, 547]
         showL += Double(1 >> (KAudioYloadingTool20.min(4, slider7.count)))
         contentU = 15.34 <= resources9
         resources9 -= Double(1 % (KAudioYloadingTool20.max(Int(resources9), 10)))
         slider7 = [2 & Int(resources9)]
       var imagesj: Int = 4
       var downU: Int = 3
         downU /= KAudioYloadingTool20.max(3, 3 - Int(bufferP))
      repeat {
         showL -= Double(2)
         if showL == 953270.0 {
            break
         }
      } while (2 < (Int(showL) - imgZ.keys.count) || 4.93 < (showL - 3.76)) && (showL == 953270.0)
      if (window_ol - Int(bufferP)) >= 2 && (5.29 - bufferP) >= 1.11 {
         window_ol *= 3
      }
         downU /= KAudioYloadingTool20.max(5, 2 + Int(menuV))
         imgZ = ["\(notificationy)": Int(notificationy)]
      repeat {
          var webz: [Any]! = [String(cString: [100,101,115,116,114,117,99,116,0], encoding: .utf8)!, String(cString: [120,114,117,110,0], encoding: .utf8)!, String(cString: [105,95,49,48,95,109,101,97,115,117,114,101,114,0], encoding: .utf8)!]
          var topi: [String: Any]! = [String(cString: [97,100,100,99,111,110,115,116,0], encoding: .utf8)!:671, String(cString: [98,97,116,99,104,101,115,0], encoding: .utf8)!:703, String(cString: [102,117,108,102,105,108,108,101,100,0], encoding: .utf8)!:611]
          var endk: String! = String(cString: [100,101,115,99,114,105,112,116,111,114,115,95,55,95,56,49,0], encoding: .utf8)!
          var iosB: Float = 1.0
          var placeX: String! = String(cString: [100,101,110,115,101,95,53,95,49,52,0], encoding: .utf8)!
         window_ol *= ((String(cString:[116,0], encoding: .utf8)!) == placeX ? placeX.count : Int(iosB))
         webz.append(endk.count - 2)
         topi = ["\(topi.keys.count)": topi.values.count % (KAudioYloadingTool20.max(3, 1))]
         endk = "\(2)"
         iosB /= KAudioYloadingTool20.max(Float(1 - endk.count), 1)
         if 709974 == window_ol {
            break
         }
      } while (709974 == window_ol) && ((imagesj * 5) == 5)
      navigationQ *= Double(2)
      break
   }
      listenZ += Double(Int(navigationQ))
   repeat {
      listenZ *= Double(Int(navigationQ))
      if 4782718.0 == listenZ {
         break
      }
   } while (4782718.0 == listenZ) && (2.42 == (listenZ / (KAudioYloadingTool20.max(2.55, 8))))
       var phone7: String! = String(cString: [109,105,99,0], encoding: .utf8)!
       var emailz: String! = String(cString: [111,119,110,108,111,97,100,0], encoding: .utf8)!
         phone7.append("\(phone7.count)")
      while (emailz.count <= phone7.count) {
          var update_2A: Bool = true
          var weixinlabelI: Double = 0.0
          var timerT: Bool = false
         phone7 = "\(emailz.count)"
         update_2A = 24.53 > weixinlabelI && !timerT
         weixinlabelI += (Double((update_2A ? 2 : 1) & 3))
         break
      }
         phone7 = "\(((String(cString:[114,0], encoding: .utf8)!) == phone7 ? phone7.count : emailz.count))"
      repeat {
          var onew_0d: Double = 2.0
          var parameterz: String! = String(cString: [98,119,114,105,116,101,0], encoding: .utf8)!
         emailz = "\(3 * parameterz.count)"
         onew_0d += Double(Int(onew_0d) - 1)
         parameterz = "\(Int(onew_0d))"
         if emailz == (String(cString:[53,55,111,55,105,0], encoding: .utf8)!) {
            break
         }
      } while (emailz == (String(cString:[53,55,111,55,105,0], encoding: .utf8)!)) && (emailz != String(cString:[79,0], encoding: .utf8)! || phone7 == String(cString:[67,0], encoding: .utf8)!)
      while (phone7.count <= 4) {
         emailz = "\(emailz.count)"
         break
      }
      repeat {
         emailz.append("\(phone7.count)")
         if emailz.count == 487418 {
            break
         }
      } while (phone7.count < emailz.count) && (emailz.count == 487418)
      listenZ /= KAudioYloadingTool20.max(Double(1), 1)
     var createBody: Float = 6100.0
     let speedSpeeds: Double = 1268.0
    var  paramRepresentative = [Any]()
    createBody = 3076
    paramRepresentative.append(createBody)
    paramRepresentative.append(speedSpeeds)

    return paramRepresentative

}





    
    @IBAction func getVerity(_ sender: UIButton) {

         let changecounterUnchecked: [Any]! = headerEdgesCollateInvokeStatues(chatNot_gt:true, fuzhiAimage:[[9149]])

      changecounterUnchecked.enumerated().forEach({ (index,obj) in
          if index  >=  3 {
                          print(obj)
          }
      })
      var changecounterUnchecked_len = changecounterUnchecked.count



       var sum6: Double = 5.0
    var namelabelB: Int = 5
      sum6 *= Double(Int(sum6) & namelabelB)

   if 5.12 > (Double(Int(sum6) * namelabelB)) {
       var contentS: Double = 5.0
      repeat {
         contentS += Double(Int(contentS))
         if 677728.0 == contentS {
            break
         }
      } while (contentS == contentS) && (677728.0 == contentS)
      while (contentS > 1.54) {
          var resumeN: Int = 5
         contentS += Double(Int(contentS) << (KAudioYloadingTool20.min(3, labs(1))))
         resumeN >>= KAudioYloadingTool20.min(5, labs(3 * resumeN))
         break
      }
      repeat {
         contentS += Double(Int(contentS) >> (KAudioYloadingTool20.min(4, labs(2))))
         if 2463565.0 == contentS {
            break
         }
      } while (2463565.0 == contentS) && (2.12 <= contentS)
      sum6 -= Double(namelabelB)
   }
        if phone.text?.count != 11 {
            ZKProgressHUD.showError("输入正确的手机号")
      sum6 += Double(1)
            return
        }
        
        var browser = [String: Any]()
       var recognizerS: Bool = true
      if recognizerS {
          var abroadcastF: Double = 2.0
          var completionZ: Int = 2
          var showR: [Any]! = [289, 559, 378]
          var clearN: [Any]! = [String(cString: [98,105,103,105,110,116,0], encoding: .utf8)!, String(cString: [115,116,114,117,99,116,117,114,97,108,0], encoding: .utf8)!, String(cString: [101,110,108,97,114,103,101,0], encoding: .utf8)!]
         recognizerS = 39 > completionZ && abroadcastF > 65.96
         abroadcastF -= Double(1)
         completionZ <<= KAudioYloadingTool20.min(clearN.count, 4)
         showR = [2 >> (KAudioYloadingTool20.min(5, clearN.count))]
      }
      for _ in 0 ..< 3 {
          var taskU: Float = 0.0
         recognizerS = !recognizerS
         taskU += Float(Int(taskU))
      }
          var normalR: Double = 1.0
         recognizerS = !recognizerS
         normalR -= Double(2)
      sum6 *= Double(namelabelB * Int(sum6))
        browser["phonenumber"] = phone.text
        
        SVProgressHUD.show()
        QVCenterBase.shared.post(urlSuffix: "/app/sms/getcode", body: browser) { (result: Result<FShow, SImageBaseE>) in
            switch result {
                case .success(let responseModel):
                    
                if responseModel.code == 200 {

                    SVProgressHUD.showSuccess(withStatus: "验证码发送成功")

                    }else {
                        SVProgressHUD.showError(withStatus: "验证码发送失败")
                    }
                    break
                case .failure(_):
                    
                    SVProgressHUD.showError(withStatus: "接口请求错误");
                    break
            }
        }
        
    }

@discardableResult
 func installPlayerOriginalTimerResults(engineTitlelabel: Double) -> Double {
    var datasj: String! = String(cString: [98,111,111,115,116,101,100,0], encoding: .utf8)!
    var listX: String! = String(cString: [103,95,57,50,95,118,115,114,99,0], encoding: .utf8)!
      datasj = "\(listX.count)"
   repeat {
      datasj.append("\(listX.count * datasj.count)")
      if (String(cString:[53,109,99,0], encoding: .utf8)!) == datasj {
         break
      }
   } while (listX != datasj) && ((String(cString:[53,109,99,0], encoding: .utf8)!) == datasj)
   while (3 > datasj.count || 3 > listX.count) {
      datasj = "\(listX.count)"
      break
   }
     var alamofireSource: Double = 3945.0
     let numberlabelDrain: Double = 2975.0
    var viewportWebsocketIgnpost:Double = 0
    alamofireSource = 6885
    viewportWebsocketIgnpost += Double(alamofireSource)
    viewportWebsocketIgnpost += numberlabelDrain

    return viewportWebsocketIgnpost

}





    
    override func viewDidLoad() {

         let particleMin: Double = installPlayerOriginalTimerResults(engineTitlelabel:6817.0)

      if particleMin > 48 {
             print(particleMin)
      }



       var user0: String! = String(cString: [105,110,118,116,114,97,110,115,0], encoding: .utf8)!
    var buttonplayr: String! = String(cString: [103,114,111,117,112,110,97,109,101,115,0], encoding: .utf8)!
   repeat {
      buttonplayr = "\(((String(cString:[87,0], encoding: .utf8)!) == user0 ? user0.count : buttonplayr.count))"
      if (String(cString:[103,103,49,121,107,111,101,99,0], encoding: .utf8)!) == buttonplayr {
         break
      }
   } while ((String(cString:[103,103,49,121,107,111,101,99,0], encoding: .utf8)!) == buttonplayr) && (user0 != String(cString:[68,0], encoding: .utf8)! && buttonplayr.count < 2)
   for _ in 0 ..< 3 {
       var answero: String! = String(cString: [115,101,115,115,105,111,110,105,100,0], encoding: .utf8)!
       var listen9: String! = String(cString: [115,116,111,114,97,103,101,115,0], encoding: .utf8)!
       var currentS: Double = 2.0
       var r_playerA: [Any]! = [String(cString: [97,118,115,115,0], encoding: .utf8)!, String(cString: [112,114,101,115,101,110,116,101,100,0], encoding: .utf8)!, String(cString: [116,114,101,120,0], encoding: .utf8)!]
       var selecteda: Double = 4.0
         selecteda += Double(2)
         r_playerA.append(Int(currentS) << (KAudioYloadingTool20.min(r_playerA.count, 5)))
         currentS /= KAudioYloadingTool20.max((Double((String(cString:[106,0], encoding: .utf8)!) == listen9 ? listen9.count : answero.count)), 4)
          var rmblabeli: [String: Any]! = [String(cString: [99,111,117,108,100,0], encoding: .utf8)!:965, String(cString: [116,114,97,99,105,110,103,0], encoding: .utf8)!:948, String(cString: [115,117,98,112,101,101,114,0], encoding: .utf8)!:342]
         listen9 = "\(rmblabeli.count + Int(currentS))"
          var resumptionK: String! = String(cString: [105,110,116,101,114,110,101,100,0], encoding: .utf8)!
          var row1: Bool = true
          var r_objectF: String! = String(cString: [114,101,115,104,97,112,101,0], encoding: .utf8)!
         answero = "\(((row1 ? 1 : 3) % (KAudioYloadingTool20.max(Int(currentS), 8))))"
         resumptionK.append("\(r_objectF.count * 2)")
         row1 = resumptionK == r_objectF
         r_playerA = [3 / (KAudioYloadingTool20.max(6, r_playerA.count))]
      if (r_playerA.count | listen9.count) >= 5 {
          var descriptj: String! = String(cString: [116,104,114,101,97,100,112,111,111,108,0], encoding: .utf8)!
          var audioB: Int = 1
          var string_: String! = String(cString: [105,110,115,116,101,97,100,0], encoding: .utf8)!
          var speedq: Bool = false
          var numberlabelB: Bool = false
         listen9 = "\(((speedq ? 1 : 4)))"
         descriptj.append("\(string_.count)")
         audioB |= audioB % 1
         string_ = "\(1)"
         speedq = descriptj.count <= 49
         numberlabelB = !numberlabelB
      }
      while ((selecteda / (KAudioYloadingTool20.max(Double(r_playerA.count), 6))) < 1.2) {
         selecteda += Double(listen9.count | 3)
         break
      }
      if 1 <= (listen9.count >> (KAudioYloadingTool20.min(labs(4), 2))) {
         listen9.append("\(1 ^ Int(selecteda))")
      }
          var handlez: String! = String(cString: [115,101,116,98,105,116,0], encoding: .utf8)!
          var completeB: Bool = false
         selecteda /= KAudioYloadingTool20.max(4, Double(r_playerA.count ^ 3))
         handlez.append("\(handlez.count * 2)")
         completeB = 70 <= handlez.count
         currentS /= KAudioYloadingTool20.max(Double(listen9.count * 2), 5)
         selecteda *= Double(listen9.count << (KAudioYloadingTool20.min(2, labs(Int(selecteda)))))
      repeat {
         r_playerA = [3]
         if 2768149 == r_playerA.count {
            break
         }
      } while (3 >= r_playerA.count) && (2768149 == r_playerA.count)
      for _ in 0 ..< 3 {
         r_playerA = [2]
      }
          var messagesZ: String! = String(cString: [97,114,101,0], encoding: .utf8)!
          var itemsj: String! = String(cString: [116,108,111,103,0], encoding: .utf8)!
          var modelA: String! = String(cString: [103,119,103,116,0], encoding: .utf8)!
         listen9.append("\(answero.count)")
         messagesZ = "\(itemsj.count >> (KAudioYloadingTool20.min(modelA.count, 2)))"
         itemsj.append("\(messagesZ.count | 3)")
         modelA = "\(2)"
      user0.append("\(r_playerA.count % 2)")
   }

   while (user0.count < 1 && 1 < buttonplayr.count) {
       var rateE: Double = 1.0
       var number0: [Any]! = [3521.0]
       var pressz: Double = 5.0
         number0.append(1 & Int(pressz))
      repeat {
         number0 = [Int(rateE) - 3]
         if 2549066 == number0.count {
            break
         }
      } while (3.0 > (1.70 - rateE) || 1.70 > (Double(number0.count) - rateE)) && (2549066 == number0.count)
         number0 = [number0.count / (KAudioYloadingTool20.max(1, 2))]
         number0.append(Int(pressz))
      repeat {
         rateE += Double(number0.count)
         if 33575.0 == rateE {
            break
         }
      } while (5 >= (Int(rateE) / (KAudioYloadingTool20.max(number0.count, 10)))) && (33575.0 == rateE)
         number0.append(2 | Int(pressz))
         pressz -= Double(number0.count + 3)
         rateE *= Double(number0.count - 3)
      repeat {
         number0 = [number0.count]
         if number0.count == 1808906 {
            break
         }
      } while (number0.count == 1808906) && (!number0.contains { $0 as? Double == pressz })
      user0 = "\(number0.count)"
      break
   }
      user0 = "\(3 | buttonplayr.count)"
        super.viewDidLoad()

    }

@discardableResult
 func modalMedataHeaderNicknameRawingChatlishi(audioChat: Float) -> Double {
    var with_1fg: Int = 2
    var screenN: String! = String(cString: [104,109,97,99,108,105,115,116,0], encoding: .utf8)!
      with_1fg |= 3
       var statuslabelW: String! = String(cString: [111,112,116,105,109,97,108,0], encoding: .utf8)!
       var update_sq: String! = String(cString: [97,115,115,101,109,98,108,101,114,0], encoding: .utf8)!
       var basec: String! = String(cString: [112,114,111,112,111,115,101,114,0], encoding: .utf8)!
         update_sq = "\(update_sq.count)"
      for _ in 0 ..< 1 {
          var datas2: String! = String(cString: [105,100,115,0], encoding: .utf8)!
          var success_: String! = String(cString: [117,108,116,114,97,119,105,100,101,0], encoding: .utf8)!
          var rateT: String! = String(cString: [109,111,110,111,115,112,97,99,101,0], encoding: .utf8)!
         statuslabelW = "\(2)"
         datas2 = "\(success_.count)"
         success_ = "\(datas2.count)"
         rateT.append("\(success_.count / (KAudioYloadingTool20.max(5, rateT.count)))")
      }
      repeat {
          var socket0: String! = String(cString: [118,95,52,57,95,114,101,109,111,118,97,108,0], encoding: .utf8)!
          var layoutn: Int = 4
          var urlsz: String! = String(cString: [100,101,118,112,111,108,108,0], encoding: .utf8)!
          var dismissW: String! = String(cString: [97,100,97,112,116,105,118,101,0], encoding: .utf8)!
          var type_5l: [String: Any]! = [String(cString: [119,114,97,112,0], encoding: .utf8)!:917, String(cString: [112,115,101,117,100,111,99,111,108,111,114,0], encoding: .utf8)!:184]
         update_sq.append("\(basec.count)")
         socket0 = "\(type_5l.count % (KAudioYloadingTool20.max(8, dismissW.count)))"
         layoutn &= (socket0 == (String(cString:[120,0], encoding: .utf8)!) ? urlsz.count : socket0.count)
         urlsz.append("\(((String(cString:[67,0], encoding: .utf8)!) == dismissW ? urlsz.count : dismissW.count))")
         type_5l["\(socket0)"] = type_5l.count
         if update_sq == (String(cString:[101,97,110,54,107,115,0], encoding: .utf8)!) {
            break
         }
      } while (update_sq == (String(cString:[101,97,110,54,107,115,0], encoding: .utf8)!)) && (!statuslabelW.hasPrefix(update_sq))
          var requestZ: [String: Any]! = [String(cString: [99,114,99,116,97,98,108,101,0], encoding: .utf8)!:400, String(cString: [105,110,116,101,114,109,101,100,105,97,116,101,0], encoding: .utf8)!:168, String(cString: [119,101,98,109,0], encoding: .utf8)!:516]
          var speedsf: Int = 5
          var modelr: String! = String(cString: [116,111,111,108,116,105,112,0], encoding: .utf8)!
         basec = "\(1 & basec.count)"
         requestZ["\(speedsf)"] = modelr.count * 2
         speedsf ^= 2
         modelr.append("\(requestZ.count)")
         statuslabelW = "\(3)"
      if update_sq == basec {
         basec.append("\(3)")
      }
      for _ in 0 ..< 1 {
          var vip3: Bool = false
          var iosZ: [String: Any]! = [String(cString: [105,110,116,101,114,114,117,112,116,105,98,108,101,0], encoding: .utf8)!:UILabel()]
          var receivec: String! = String(cString: [102,102,109,101,116,97,0], encoding: .utf8)!
          var gundm: String! = String(cString: [114,101,112,108,97,121,101,100,0], encoding: .utf8)!
          var tabbarl: String! = String(cString: [112,101,101,114,0], encoding: .utf8)!
         update_sq.append("\(3)")
         vip3 = iosZ.values.count >= gundm.count
         iosZ = [receivec: (receivec.count << (KAudioYloadingTool20.min(2, labs((vip3 ? 3 : 3)))))]
         gundm = "\(((vip3 ? 1 : 1) >> (KAudioYloadingTool20.min(labs(2), 5))))"
         tabbarl.append("\((tabbarl == (String(cString:[111,0], encoding: .utf8)!) ? tabbarl.count : iosZ.values.count))")
      }
         update_sq.append("\(3 + update_sq.count)")
         update_sq = "\(update_sq.count & statuslabelW.count)"
      with_1fg ^= 1
      screenN.append("\(screenN.count)")
       var messageH: [Any]! = [965, 78, 192]
       var gressv: String! = String(cString: [114,97,110,107,0], encoding: .utf8)!
       var controllg: Int = 4
       var firstG: String! = String(cString: [115,95,55,51,95,118,97,114,105,97,110,99,101,0], encoding: .utf8)!
      while (gressv.count > 5) {
         controllg |= ((String(cString:[105,0], encoding: .utf8)!) == gressv ? firstG.count : gressv.count)
         break
      }
      repeat {
         controllg ^= 2
         if 1283729 == controllg {
            break
         }
      } while ((2 + firstG.count) <= 3) && (1283729 == controllg)
      repeat {
         messageH.append(controllg << (KAudioYloadingTool20.min(labs(3), 3)))
         if messageH.count == 615705 {
            break
         }
      } while (messageH.count == 615705) && (messageH.contains { $0 as? Int == controllg })
          var y_titleW: String! = String(cString: [107,101,101,112,105,110,103,0], encoding: .utf8)!
         messageH = [controllg]
         y_titleW.append("\((y_titleW == (String(cString:[85,0], encoding: .utf8)!) ? y_titleW.count : y_titleW.count))")
      while (!messageH.contains { $0 as? Int == controllg }) {
         messageH.append(gressv.count)
         break
      }
         gressv = "\((firstG == (String(cString:[102,0], encoding: .utf8)!) ? firstG.count : controllg))"
         controllg %= KAudioYloadingTool20.max(controllg, 3)
         firstG = "\(controllg + 2)"
      with_1fg ^= with_1fg >> (KAudioYloadingTool20.min(5, labs(controllg)))
     var downloadOpen: Int = 6104
     let channelAida: String! = String(cString: [99,97,112,0], encoding: .utf8)!
     var pricelabelWeb: Double = 982.0
    var localaddrOutputqAuthkey:Double = 0
    downloadOpen -= 53
    localaddrOutputqAuthkey -= Double(downloadOpen)

    localaddrOutputqAuthkey *= pricelabelWeb

    return localaddrOutputqAuthkey

}






    @IBAction func back(_ sender: Any) {

         let nearsetCamel: Double = modalMedataHeaderNicknameRawingChatlishi(audioChat:4579.0)

      if nearsetCamel == 22 {
             print(nearsetCamel)
      }



       var main_yC: Bool = false
    var main_kz: Int = 5
   for _ in 0 ..< 2 {
       var closeW: String! = String(cString: [99,104,105,108,108,0], encoding: .utf8)!
       var qheaderh: [Any]! = [58, 229, 566]
         qheaderh.append(qheaderh.count - closeW.count)
         qheaderh.append(((String(cString:[100,0], encoding: .utf8)!) == closeW ? closeW.count : qheaderh.count))
      repeat {
         closeW = "\(closeW.count)"
         if 4057727 == closeW.count {
            break
         }
      } while (!closeW.contains("\(qheaderh.count)")) && (4057727 == closeW.count)
      if 5 >= (4 >> (KAudioYloadingTool20.min(3, qheaderh.count))) && 1 >= (qheaderh.count >> (KAudioYloadingTool20.min(labs(4), 1))) {
         closeW.append("\(((String(cString:[73,0], encoding: .utf8)!) == closeW ? closeW.count : qheaderh.count))")
      }
          var activityK: String! = String(cString: [112,114,101,97,108,108,111,99,0], encoding: .utf8)!
          var tapk: [String: Any]! = [String(cString: [113,117,111,116,101,0], encoding: .utf8)!:475, String(cString: [113,122,98,105,110,0], encoding: .utf8)!:415]
          var notificationL: String! = String(cString: [115,101,116,108,105,115,116,0], encoding: .utf8)!
         qheaderh.append(1 % (KAudioYloadingTool20.max(4, tapk.values.count)))
         activityK.append("\(1)")
         tapk = [notificationL: 1 >> (KAudioYloadingTool20.min(4, notificationL.count))]
       var class_lrr: Int = 4
       var prefix_7D: Int = 2
      main_kz |= main_kz
   }
      main_kz >>= KAudioYloadingTool20.min(5, labs(3))
   while (main_yC) {
      main_yC = !main_yC
      break
   }

   if 5 == main_kz && (main_kz - 5) == 3 {
      main_yC = 11 == main_kz
   }
        dismiss(animated: true)
    }

    
    
    @IBAction func login(_ sender: UIButton) {
       var unselectedT: Bool = true
    var chuan8: Double = 0.0
    var z_widtho: String! = String(cString: [116,105,99,107,101,116,115,0], encoding: .utf8)!
      z_widtho = "\(Int(chuan8) << (KAudioYloadingTool20.min(1, labs(3))))"

       var rawingo: Int = 1
       var aidao: [String: Any]! = [String(cString: [108,111,99,107,115,99,114,101,101,110,0], encoding: .utf8)!:[String(cString: [115,117,98,108,97,121,101,114,0], encoding: .utf8)!:64, String(cString: [99,111,110,102,105,114,109,97,116,105,111,110,0], encoding: .utf8)!:148, String(cString: [99,99,111,117,110,116,0], encoding: .utf8)!:14]]
      for _ in 0 ..< 3 {
         aidao["\(rawingo)"] = aidao.count & rawingo
      }
         rawingo %= KAudioYloadingTool20.max(4, aidao.values.count)
         rawingo %= KAudioYloadingTool20.max(rawingo / 3, 1)
      if 3 > rawingo {
         aidao["\(rawingo)"] = aidao.count << (KAudioYloadingTool20.min(2, labs(rawingo)))
      }
          var cellE: String! = String(cString: [109,101,116,104,111,100,0], encoding: .utf8)!
         rawingo /= KAudioYloadingTool20.max(3 * aidao.values.count, 5)
         cellE = "\(cellE.count % (KAudioYloadingTool20.max(1, 4)))"
          var messages_: Float = 3.0
         aidao = ["\(aidao.keys.count)": aidao.values.count >> (KAudioYloadingTool20.min(labs(2), 2))]
         messages_ *= Float(Int(messages_) % 1)
      chuan8 += Double(1)
        if phone.text?.count != 11 {
            ZKProgressHUD.showError("输入正确的手机号")
       var sections2: Double = 5.0
          var sublyoutx: String! = String(cString: [105,110,116,114,111,0], encoding: .utf8)!
          var bundleq: Float = 4.0
         sections2 *= Double(Int(bundleq) | 1)
         sublyoutx = "\(2 ^ sublyoutx.count)"
         bundleq += Float(sublyoutx.count & sublyoutx.count)
         sections2 -= Double(Int(sections2))
         sections2 /= KAudioYloadingTool20.max(2, Double(2 * Int(sections2)))
      unselectedT = !unselectedT
            return
        }
        if code.text?.count != 4 {
            ZKProgressHUD.showError("输入正确的验证码")
   while (Int(chuan8) == z_widtho.count) {
       var showu: String! = String(cString: [100,114,97,119,97,98,108,101,0], encoding: .utf8)!
       var scrolld: Int = 0
         showu.append("\(showu.count)")
      while ((scrolld / 5) > 1) {
         scrolld <<= KAudioYloadingTool20.min(labs(scrolld), 4)
         break
      }
          var disconnect1: Float = 1.0
         scrolld ^= scrolld % (KAudioYloadingTool20.max(Int(disconnect1), 6))
         showu.append("\(showu.count ^ 3)")
      while ((showu.count % (KAudioYloadingTool20.max(5, scrolld))) > 5) {
          var titlelabelz: String! = String(cString: [101,110,117,109,101,114,97,116,105,111,110,0], encoding: .utf8)!
          var editc: Float = 5.0
          var navgationo: Bool = true
          var safeC: String! = String(cString: [119,122,97,101,115,0], encoding: .utf8)!
         scrolld >>= KAudioYloadingTool20.min(labs((showu == (String(cString:[120,0], encoding: .utf8)!) ? (navgationo ? 2 : 4) : showu.count)), 1)
         titlelabelz.append("\(1)")
         editc -= (Float(titlelabelz == (String(cString:[109,0], encoding: .utf8)!) ? titlelabelz.count : Int(editc)))
         navgationo = safeC == (String(cString:[87,0], encoding: .utf8)!)
         safeC.append("\(2)")
         break
      }
         showu.append("\(3)")
      z_widtho = "\(Int(chuan8) & scrolld)"
      break
   }
            return
        }
        
        mineLogin(phone: phone.text!, verity: code.text!)
    }

    
    func mineLogin(phone: String, verity: String, Prefix:(() -> Void)? = nil) {
       var findD: String! = String(cString: [114,101,99,117,114,115,105,111,110,0], encoding: .utf8)!
    var array4: String! = String(cString: [111,112,101,110,105,110,103,0], encoding: .utf8)!
   while (4 >= findD.count) {
      array4 = "\(3 >> (KAudioYloadingTool20.min(5, array4.count)))"
      break
   }

   if array4 == findD {
      findD.append("\(array4.count)")
   }
        
        var gressg = [String: Any]()
   for _ in 0 ..< 3 {
       var time_nq: Double = 5.0
       var gressn: String! = String(cString: [117,112,118,112,0], encoding: .utf8)!
       var sharedv: String! = String(cString: [108,97,103,97,114,105,116,104,114,97,99,0], encoding: .utf8)!
      if 5 < gressn.count || sharedv != String(cString:[74,0], encoding: .utf8)! {
          var l_playerl: Float = 4.0
          var textI: Bool = true
         sharedv = "\(((String(cString:[81,0], encoding: .utf8)!) == gressn ? gressn.count : sharedv.count))"
         l_playerl -= (Float((textI ? 2 : 3) / (KAudioYloadingTool20.max(6, Int(l_playerl)))))
         textI = !textI
      }
       var register_ot: String! = String(cString: [102,111,114,105,0], encoding: .utf8)!
         gressn = "\(register_ot.count)"
      for _ in 0 ..< 2 {
          var itemM: [Any]! = [58, 140]
          var urlsU: Bool = false
         gressn.append("\((gressn == (String(cString:[56,0], encoding: .utf8)!) ? gressn.count : (urlsU ? 1 : 3)))")
         itemM = [2 ^ itemM.count]
         urlsU = 13 > itemM.count
      }
       var aymenta: Float = 5.0
         gressn.append("\(Int(time_nq) * 2)")
      for _ in 0 ..< 2 {
         time_nq /= KAudioYloadingTool20.max(4, Double(Int(aymenta) << (KAudioYloadingTool20.min(labs(1), 1))))
      }
         gressn = "\(Int(aymenta) + Int(time_nq))"
      for _ in 0 ..< 1 {
         time_nq *= Double(register_ot.count)
      }
      findD.append("\(gressn.count)")
   }
        gressg["accountNumber"] = getAccountNumberIdentifier()
   if array4 == findD {
      findD = "\(findD.count)"
   }
        gressg["phonenumber"] = phone
        gressg["smsCode"] = verity
        gressg["type"] = AppType
        
        SVProgressHUD.show()
        QVCenterBase.shared.post(urlSuffix: "/app/sms/smsCode/login", body: gressg) { (result: Result<FShow, SImageBaseE>) in
            switch result {
                case .success(let model):
                    
                if model.code == 200 {

                    SVProgressHUD.showSuccess(withStatus: "绑定成功")
                    let first: String = model.data!["token"]!
                    UserDefaults.standard.set(first, forKey: "AccountToken")
                    
                    self.dismiss(animated: true)
                }else {
                    SVProgressHUD.showError(withStatus: model.msg)
                }
                    break
                case .failure(_):
                    
                    SVProgressHUD.showError(withStatus: "接口请求错误");
                    break
            }
        }
    }

    
    
}

 struct KAudioYloadingTool20 {
    static func min<S: Comparable>(_ t1: S, _ t2: S) -> S {
        return t1 > t2 ? t2 : t1
    }
    
    static func max<S: Comparable>(_ t1: S, _ t2: S) -> S {
        return t1 > t2 ? t1 : t2
    }
}