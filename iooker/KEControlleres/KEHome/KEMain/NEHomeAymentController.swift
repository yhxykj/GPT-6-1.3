
import Foundation

import UIKit

class NEHomeAymentController: UIViewController {
private var u_width: Double = 0.0
private var ringbufferIndex: Int = 0
private var holderlabel_max: Double = 0.0
var resumptionListen_arr: [Any]?


 var recordingModel: SRegisterObject!

    
    var scrollView: UIScrollView!
    @IBOutlet weak var line: UIImageView!
    @IBOutlet weak var titleView: UIView!
    @IBOutlet weak var lineLeftLayout: NSLayoutConstraint!
    
    @IBOutlet weak var chatBtn: UIButton!
    @IBOutlet weak var voiceBtn: UIButton!
    @IBOutlet weak var AIhBtn: UIButton!

@discardableResult
 func nextHuihuaFeedback(timelabelEdit: Float, pasteboardMessage: [String: Any]!) -> Bool {
    var lengthr: Bool = false
    var end6: String! = String(cString: [108,115,104,105,102,116,0], encoding: .utf8)!
       var listt: [Any]! = [411, 616]
       var userdataH: Double = 3.0
       var graphicsw: String! = String(cString: [108,105,103,104,116,101,110,105,110,103,0], encoding: .utf8)!
         listt.append(1)
          var meald: Double = 3.0
          var popupC: String! = String(cString: [115,98,117,118,0], encoding: .utf8)!
         graphicsw = "\(Int(userdataH) >> (MSAPhoneTool17.min(labs(Int(meald)), 5)))"
         meald /= MSAPhoneTool17.max(1, (Double(popupC == (String(cString:[70,0], encoding: .utf8)!) ? popupC.count : popupC.count)))
      while (4 < (5 >> (MSAPhoneTool17.min(4, listt.count))) && (5.46 * userdataH) < 2.41) {
          var openg: Bool = true
          var apps: Int = 2
          var navgationO: [Any]! = [968, 209]
          var kefuK: [String: Any]! = [String(cString: [111,112,97,99,105,116,121,0], encoding: .utf8)!:true]
         listt.append(navgationO.count)
         openg = openg && kefuK.values.count == 49
         apps /= MSAPhoneTool17.max(apps & kefuK.count, 3)
         navgationO = [kefuK.keys.count * 3]
         break
      }
      if 4.95 >= (5.18 / (MSAPhoneTool17.max(6, userdataH))) && (userdataH / (MSAPhoneTool17.max(5.18, 5))) >= 3.66 {
          var with_2L: [String: Any]! = [String(cString: [114,118,118,108,99,0], encoding: .utf8)!:990, String(cString: [116,114,105,110,103,0], encoding: .utf8)!:196]
          var messgaer: String! = String(cString: [116,105,99,107,0], encoding: .utf8)!
          var weixin8: [String: Any]! = [String(cString: [114,101,99,101,110,116,0], encoding: .utf8)!:String(cString: [118,105,98,114,97,116,101,0], encoding: .utf8)!, String(cString: [97,108,101,114,116,0], encoding: .utf8)!:String(cString: [115,101,110,100,118,0], encoding: .utf8)!, String(cString: [114,101,103,101,120,112,0], encoding: .utf8)!:String(cString: [99,111,110,100,0], encoding: .utf8)!]
         userdataH /= MSAPhoneTool17.max(Double(Int(userdataH)), 1)
         with_2L["\(weixin8.keys.count)"] = 2
         messgaer.append("\(weixin8.values.count)")
      }
         listt = [Int(userdataH) * 3]
         userdataH /= MSAPhoneTool17.max(1, Double(listt.count % (MSAPhoneTool17.max(9, Int(userdataH)))))
      if (userdataH / 1.1) > 3.9 {
          var tablelist2: String! = String(cString: [112,114,105,118,0], encoding: .utf8)!
          var edit0: String! = String(cString: [98,110,98,105,110,112,97,100,0], encoding: .utf8)!
          var webK: [Any]! = [612, 333]
         userdataH *= Double(tablelist2.count ^ listt.count)
         tablelist2 = "\(edit0.count)"
         edit0.append("\(webK.count % (MSAPhoneTool17.max(3, 9)))")
         webK = [edit0.count * webK.count]
      }
         userdataH += Double(1 | listt.count)
      for _ in 0 ..< 1 {
          var prefix_l_: Bool = true
          var rmb1: String! = String(cString: [102,97,110,116,111,109,0], encoding: .utf8)!
         userdataH += Double(3 << (MSAPhoneTool17.min(4, rmb1.count)))
      }
      end6.append("\(2)")
   if lengthr {
      end6.append("\(end6.count / (MSAPhoneTool17.max(3, 8)))")
   }
   repeat {
      end6.append("\(1)")
      if end6.count == 3830749 {
         break
      }
   } while (end6.count == 3830749) && (5 == end6.count && !lengthr)
   for _ in 0 ..< 3 {
      lengthr = !lengthr || end6.count <= 73
   }
   return lengthr

}






    override func viewDidLoad() {

         let cblkSearched: Bool = nextHuihuaFeedback(timelabelEdit:6054.0, pasteboardMessage:[String(cString: [108,97,117,110,99,104,0], encoding: .utf8)!:865, String(cString: [97,110,100,108,101,0], encoding: .utf8)!:209])

      if cblkSearched {
          print("ok")
      }



       var dismissg: Double = 4.0
    var bangZ: [String: Any]! = [String(cString: [102,105,108,101,110,97,109,101,0], encoding: .utf8)!:832, String(cString: [114,101,99,111,100,101,0], encoding: .utf8)!:601]
      bangZ = ["\(bangZ.values.count)": bangZ.values.count & Int(dismissg)]

   repeat {
      dismissg *= Double(bangZ.values.count << (MSAPhoneTool17.min(4, labs(Int(dismissg)))))
      if dismissg == 2008972.0 {
         break
      }
   } while ((Int(dismissg) / (MSAPhoneTool17.max(2, bangZ.keys.count))) >= 5 && 4.79 >= (dismissg / (MSAPhoneTool17.max(4.57, 6)))) && (dismissg == 2008972.0)
recordingModel = SRegisterObject()
      dismissg += Double(bangZ.keys.count)

        super.viewDidLoad()
        NotificationCenter.default.addObserver(self, selector: #selector(updateScrollView), name: NSNotification.Name("updateScrollViewNotificationName"), object: nil)
        
        self.scrollView = UIScrollView()
        self.scrollView.backgroundColor = .clear
        self.view.addSubview(self.scrollView)
        self.scrollView.isScrollEnabled = false
        self.scrollView.snp.makeConstraints { make in
            make.left.right.bottom.equalTo(0)
            make.top.equalTo(44)
        }
        
        let window = UIApplication.shared.keyWindow
        let substring = window?.safeAreaInsets.top ?? 0.0
        print("Top safe area: \(substring)")

        let sublyoutController = KASocketController()
        self.scrollView.addSubview(sublyoutController.view)
        self.addChild(sublyoutController)
        sublyoutController.view.snp.makeConstraints { make in
            make.left.top.equalTo(0)
            make.top.equalTo(0)
            make.height.equalTo(UIScreen.main.bounds.size.height-substring-88)
            make.width.equalTo(UIScreen.main.bounds.size.width)
        }

        let n_heightController = NPopupRecordsController()
        self.scrollView.addSubview(n_heightController.view)
        self.addChild(n_heightController)
        n_heightController.view.snp.makeConstraints { make in
            make.top.equalTo(0)
            make.left.equalTo(UIScreen.main.bounds.size.width)
            make.width.equalTo(UIScreen.main.bounds.size.width)
            make.bottom.equalTo(UIScreen.main.bounds.size.height-substring-88)
        }
        
        let findController = LHistoryController()
        self.scrollView.addSubview(findController.view)
        self.addChild(findController)
        findController.view.snp.makeConstraints { make in
            make.top.equalTo(0)
            make.left.equalTo(UIScreen.main.bounds.size.width*2)
            make.width.equalTo(UIScreen.main.bounds.size.width)
            make.height.equalTo(UIScreen.main.bounds.size.height-substring-88)
        }
        
    }

    
    
    override func viewDidAppear(_ animated: Bool) {
       var minea: String! = String(cString: [112,101,101,108,0], encoding: .utf8)!
    var sumb: Double = 1.0
    var coll2: [Any]! = [150, 797, 647]
   while (sumb > Double(minea.count)) {
       var playb: [String: Any]! = [String(cString: [97,100,100,105,116,105,111,110,115,0], encoding: .utf8)!:159, String(cString: [103,101,110,101,114,97,116,101,0], encoding: .utf8)!:756, String(cString: [113,109,98,108,0], encoding: .utf8)!:729]
       var dismissk: String! = String(cString: [119,114,105,116,101,97,108,105,103,110,0], encoding: .utf8)!
       var statuslabelx: String! = String(cString: [112,114,111,118,105,100,101,114,115,0], encoding: .utf8)!
      while (dismissk.count >= playb.keys.count) {
          var gundongo: [String: Any]! = [String(cString: [112,105,112,0], encoding: .utf8)!:String(cString: [115,101,99,117,114,101,100,0], encoding: .utf8)!, String(cString: [108,111,99,97,116,101,100,0], encoding: .utf8)!:String(cString: [97,114,114,97,121,115,105,122,101,0], encoding: .utf8)!, String(cString: [109,111,118,105,101,0], encoding: .utf8)!:String(cString: [103,114,97,112,104,99,121,99,108,101,115,0], encoding: .utf8)!]
         dismissk = "\((dismissk == (String(cString:[115,0], encoding: .utf8)!) ? dismissk.count : gundongo.count))"
         break
      }
         dismissk.append("\((dismissk == (String(cString:[82,0], encoding: .utf8)!) ? playb.values.count : dismissk.count))")
      sumb /= MSAPhoneTool17.max(5, Double(coll2.count >> (MSAPhoneTool17.min(3, labs(Int(sumb))))))
      break
   }
   if sumb == 1.32 {
       var audiou: String! = String(cString: [116,98,108,101,110,100,0], encoding: .utf8)!
       var briefn: [Any]! = [750, 459, 567]
       var fromo: Double = 1.0
       var ylabelf: [String: Any]! = [String(cString: [114,115,116,114,105,112,0], encoding: .utf8)!:[494, 413, 385]]
      if 3 < (briefn.count & 4) || (briefn.count + 4) < 4 {
         briefn = [2 % (MSAPhoneTool17.max(1, briefn.count))]
      }
          var j_width5: String! = String(cString: [98,114,111,119,110,0], encoding: .utf8)!
          var restoreC: [String: Any]! = [String(cString: [101,103,117,108,97,114,0], encoding: .utf8)!:216, String(cString: [110,101,97,114,108,121,0], encoding: .utf8)!:634]
          var timerl: Float = 0.0
         ylabelf["\(timerl)"] = ((String(cString:[110,0], encoding: .utf8)!) == audiou ? audiou.count : Int(timerl))
         j_width5 = "\(j_width5.count + restoreC.count)"
         restoreC[j_width5] = j_width5.count >> (MSAPhoneTool17.min(4, restoreC.count))
      if briefn.count < audiou.count {
         audiou = "\(Int(fromo))"
      }
      if audiou.hasSuffix("\(fromo)") {
         audiou = "\(briefn.count | Int(fromo))"
      }
      for _ in 0 ..< 3 {
         fromo += Double(audiou.count)
      }
      repeat {
          var mealC: Float = 3.0
          var editL: [Any]! = [228, 718, 204]
          var shu_: [String: Any]! = [String(cString: [119,114,111,116,101,0], encoding: .utf8)!:247, String(cString: [100,101,102,105,110,105,116,105,111,110,115,0], encoding: .utf8)!:244]
          var promptv: [String: Any]! = [String(cString: [104,97,110,100,108,101,0], encoding: .utf8)!:668, String(cString: [114,111,116,97,116,105,110,103,0], encoding: .utf8)!:147, String(cString: [109,111,100,105,102,121,0], encoding: .utf8)!:777]
          var picker5: [Any]! = [502, 805]
         briefn.append(Int(fromo) - 2)
         mealC -= Float(2)
         editL.append(Int(mealC))
         shu_["\(promptv.values.count)"] = 1
         promptv["\(editL.count)"] = editL.count
         picker5.append(promptv.count % (MSAPhoneTool17.max(2, 9)))
         if briefn.count == 3113123 {
            break
         }
      } while (briefn.count == 3113123) && (ylabelf["\(briefn.count)"] == nil)
         audiou = "\(1)"
       var enabledv: Double = 5.0
       var purchaseK: Double = 0.0
      for _ in 0 ..< 2 {
         briefn = [Int(enabledv)]
      }
      if ylabelf.values.contains { $0 as? Int == briefn.count } {
         briefn = [Int(enabledv) & audiou.count]
      }
         audiou.append("\(2 + ylabelf.values.count)")
         enabledv += Double(ylabelf.values.count)
      sumb *= Double(ylabelf.values.count)
   }
   while ((1 ^ minea.count) < 4 && 1 < (minea.count ^ 1)) {
      minea = "\(3 - coll2.count)"
      break
   }

   while (2 < (minea.count + coll2.count) || 1 < (2 + minea.count)) {
       var imgP: Float = 0.0
       var tapy: String! = String(cString: [103,101,116,116,105,109,101,111,102,100,97,121,0], encoding: .utf8)!
       var size_hed: [Any]! = [570, 171]
       var silenceh: String! = String(cString: [102,114,97,109,101,115,101,116,116,101,114,0], encoding: .utf8)!
       var gesturea: [String: Any]! = [String(cString: [112,100,115,0], encoding: .utf8)!:370, String(cString: [109,97,103,110,105,116,117,100,101,115,0], encoding: .utf8)!:248, String(cString: [115,116,114,105,100,101,97,98,108,101,0], encoding: .utf8)!:910]
          var objC: [Any]! = [String(cString: [112,114,101,100,105,99,97,116,101,0], encoding: .utf8)!, String(cString: [118,101,114,116,105,99,97,108,0], encoding: .utf8)!]
          var indexG: [String: Any]! = [String(cString: [118,101,99,115,0], encoding: .utf8)!:874, String(cString: [116,114,97,110,115,99,111,100,101,0], encoding: .utf8)!:861]
         tapy = "\(Int(imgP) << (MSAPhoneTool17.min(gesturea.keys.count, 2)))"
         objC = [2 ^ objC.count]
         indexG["\(objC.count)"] = indexG.keys.count << (MSAPhoneTool17.min(objC.count, 4))
          var sepakx: String! = String(cString: [108,111,116,115,0], encoding: .utf8)!
          var navigatione: String! = String(cString: [116,111,107,101,110,105,122,101,114,0], encoding: .utf8)!
          var nextu: String! = String(cString: [100,99,109,112,0], encoding: .utf8)!
         size_hed.append(sepakx.count)
         sepakx.append("\(nextu.count / 1)")
         navigatione = "\(navigatione.count)"
         nextu.append("\(1)")
      if !silenceh.hasPrefix("\(imgP)") {
         silenceh.append("\(gesturea.values.count | tapy.count)")
      }
      while (1 < (silenceh.count % 4) || (silenceh.count % (MSAPhoneTool17.max(4, 8))) < 4) {
         gesturea["\(imgP)"] = gesturea.values.count + 1
         break
      }
      while (1 == (5 | size_hed.count) || 5 == (gesturea.values.count | size_hed.count)) {
         gesturea = [tapy: 2]
         break
      }
      repeat {
         tapy = "\(silenceh.count + size_hed.count)"
         if tapy.count == 929315 {
            break
         }
      } while (tapy.count >= 1) && (tapy.count == 929315)
      if (Int(imgP) + 4) >= 4 && 5 >= (silenceh.count % 4) {
         imgP /= MSAPhoneTool17.max(Float(size_hed.count), 1)
      }
      while (!silenceh.contains("\(gesturea.count)")) {
          var constraintV: Bool = true
          var dismissU: Double = 5.0
          var ios1: String! = String(cString: [105,109,97,103,101,115,0], encoding: .utf8)!
          var navgationB: Int = 0
          var aboutq: String! = String(cString: [99,111,110,102,105,103,117,114,97,116,105,111,110,0], encoding: .utf8)!
         gesturea["\(constraintV)"] = Int(dismissU) | 2
         constraintV = aboutq.hasSuffix("\(navgationB)")
         dismissU *= Double(navgationB << (MSAPhoneTool17.min(labs(1), 2)))
         ios1 = "\(((String(cString:[73,0], encoding: .utf8)!) == ios1 ? ios1.count : navgationB))"
         aboutq.append("\(2 << (MSAPhoneTool17.min(4, labs(navgationB))))")
         break
      }
      if (Int(imgP) - tapy.count) < 5 || (Float(tapy.count) - imgP) < 3.61 {
         tapy = "\(gesturea.values.count | 1)"
      }
      if 5 == (4 ^ silenceh.count) || (silenceh.count ^ 4) == 5 {
          var iconT: [Any]! = [String(cString: [115,112,101,99,105,102,105,101,114,115,0], encoding: .utf8)!, String(cString: [112,111,108,113,97,0], encoding: .utf8)!, String(cString: [99,102,116,115,116,0], encoding: .utf8)!]
         size_hed.append(silenceh.count << (MSAPhoneTool17.min(4, gesturea.count)))
         iconT = [iconT.count]
      }
       var configD: Double = 0.0
         imgP += Float(1)
      while ((tapy.count - Int(imgP)) == 3 && 3.86 == (2.2 - imgP)) {
          var playingc: Int = 2
          var searchI: String! = String(cString: [117,110,101,100,105,116,97,98,108,101,0], encoding: .utf8)!
         tapy.append("\(tapy.count | gesturea.count)")
         playingc *= playingc % (MSAPhoneTool17.max(6, searchI.count))
         searchI.append("\(playingc)")
         break
      }
          var emailq: [String: Any]! = [String(cString: [102,105,110,97,108,0], encoding: .utf8)!:String(cString: [108,115,98,102,117,108,108,0], encoding: .utf8)!, String(cString: [116,114,97,110,115,102,111,114,109,115,0], encoding: .utf8)!:String(cString: [99,114,97,115,104,0], encoding: .utf8)!]
         size_hed.append(2)
         emailq["\(emailq.keys.count)"] = emailq.count % (MSAPhoneTool17.max(2, emailq.keys.count))
         size_hed.append(tapy.count)
      coll2 = [3]
      break
   }
   repeat {
      coll2 = [3 >> (MSAPhoneTool17.min(1, coll2.count))]
      if coll2.count == 3641624 {
         break
      }
   } while (coll2.count == 3641624) && ((4 ^ coll2.count) == 4)
      minea = "\(minea.count & coll2.count)"
        super.viewDidAppear(animated)
        self.scrollView.contentSize = CGSize(width: UIScreen.main.bounds.size.width*3, height: 0)
    }


    @IBAction func itemClick(_ sender: UIButton) {
       var v_centerZ: [String: Any]! = [String(cString: [106,97,99,111,115,117,98,0], encoding: .utf8)!:500, String(cString: [105,110,116,102,114,0], encoding: .utf8)!:149, String(cString: [111,119,110,0], encoding: .utf8)!:88]
    var sortb: String! = String(cString: [108,97,109,101,0], encoding: .utf8)!
      sortb.append("\(v_centerZ.count | sortb.count)")

        for obj in self.titleView.subviews {
            if let button = obj as? UIButton {
                button.setTitleColor(UIColor(red: 229/255, green: 230/255, blue: 229/255, alpha: 1.0), for: .normal)
      sortb = "\(sortb.count)"
                if button == sender {
                    button.setTitleColor(UIColor(red: 121/255, green: 252/255, blue: 160/255, alpha: 1.0), for: .normal)
      sortb.append("\(sortb.count ^ v_centerZ.values.count)")
                    lineLeftLayout.constant = sender.center.x-15;
                }
            }
        }
        if sender.tag == 801 {
            self.scrollView.setContentOffset(CGPoint(x: 0, y: 0), animated: true)
        }
        if sender.tag == 802 {
            self.scrollView.setContentOffset(CGPoint(x: UIScreen.main.bounds.size.width, y: 0), animated: true)
        }
        if sender.tag == 803 {
            self.scrollView.setContentOffset(CGPoint(x: UIScreen.main.bounds.size.width*2, y: 0), animated: true)
        }
        
      v_centerZ = ["\(v_centerZ.keys.count)": sortb.count & v_centerZ.values.count]
    }

    
    @objc func updateScrollView() {
       var handlerA: String! = String(cString: [117,95,57,52,0], encoding: .utf8)!
    var paramj: Float = 4.0
   if (Int(paramj) / (MSAPhoneTool17.max(9, handlerA.count))) < 3 || 4.12 < (paramj / (MSAPhoneTool17.max(Float(handlerA.count), 9))) {
      paramj -= Float(3 + Int(paramj))
   }

      handlerA.append("\(Int(paramj))")
        self.scrollView.setContentOffset(CGPoint(x: UIScreen.main.bounds.size.width*2, y: 0), animated: true)
   repeat {
      handlerA = "\(1)"
      if handlerA.count == 3157708 {
         break
      }
   } while (handlerA.hasSuffix("\(paramj)")) && (handlerA.count == 3157708)
        self.chatBtn.setTitleColor(UIColor(red: 229/255, green: 230/255, blue: 229/255, alpha: 1.0), for: .normal)
   while (Int(paramj) < handlerA.count) {
      paramj += Float(Int(paramj))
      break
   }
        self.voiceBtn.setTitleColor(UIColor(red: 229/255, green: 230/255, blue: 229/255, alpha: 1.0), for: .normal)
        self.AIhBtn.setTitleColor(UIColor(red: 121/255, green: 252/255, blue: 160/255, alpha: 1.0), for: .normal)
        lineLeftLayout.constant = self.AIhBtn.center.x-15
    }
    
}

 class MSAPhoneTool17 {
    static func min<E: Comparable>(_ r1: E, _ r2: E) -> E {
        return r1 > r2 ? r2 : r1
    }
    
    static func max<E: Comparable>(_ r1: E, _ r2: E) -> E {
        return r1 > r2 ? r1 : r2
    }
}