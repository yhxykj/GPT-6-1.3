
import Foundation

import UIKit
import SVProgressHUD
import ZKProgressHUD
import YYImage
import YYWebImage

class LHistoryController: UIViewController {
private var resetIdx: Int = 0
private var isApass: Bool = false
private var enbale_Userdata: Bool = false




    @IBOutlet weak var tableView: UITableView!
    @IBOutlet weak var textTF: UITextView!
    @IBOutlet weak var sendBtn: UIButton!
    @IBOutlet weak var textViewHeightConstraint: NSLayoutConstraint!
    @IBOutlet weak var bottomView: UIView!
    @IBOutlet weak var bottomViewHeight: NSLayoutConstraint!
    
    var photoId: String = ""
    var graphics: [[String: String]] = NSMutableArray() as! [[String: String]]
    var elevtCard = BXAlamofireModityView()
    var headerView = PWYloadingView()

@discardableResult
 func selectedBlockCellJson(appBundle: Int) -> [String: Any]! {
    var execute2: String! = String(cString: [100,99,115,99,116,112,0], encoding: .utf8)!
    var has_: Float = 5.0
   repeat {
      has_ *= Float(3)
      if has_ == 4277191.0 {
         break
      }
   } while (has_ == 4277191.0) && ((4 >> (CRecordsTool3.min(1, execute2.count))) > 3 || 2 > (execute2.count * 4))
   while (execute2.count <= 1) {
      execute2.append("\(2 << (CRecordsTool3.min(5, execute2.count)))")
      break
   }
      has_ *= Float(3 - execute2.count)
      execute2 = "\(3 + execute2.count)"
     var playerBackground: Bool = false
     var showParam: String! = String(cString: [105,110,102,111,115,0], encoding: .utf8)!
    var thirdBlindingAcolors = [String: Any]()
    playerBackground = true
    thirdBlindingAcolors.updateValue(playerBackground, forKey:String(cString: [82,0], encoding: .utf8)!)
    thirdBlindingAcolors.updateValue(showParam, forKey:String(cString: [103,0], encoding: .utf8)!)

    return thirdBlindingAcolors

}





    
    @objc func queryPictureStatus() {

         let mbedtlsUnread: [String: Any]! = selectedBlockCellJson(appBundle:9813)

      mbedtlsUnread.enumerated().forEach({ (index, element) in
          if index  ==  9 {
              print(element.key)
              print(element.value)
          }
      })
      var mbedtlsUnread_len = mbedtlsUnread.count



       var t_view6: Double = 1.0
    var loadingn: Double = 5.0
    var codeT: Float = 4.0
       var validate8: Bool = false
       var pic0: String! = String(cString: [112,111,108,101,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var delegate_5hg: Float = 2.0
          var numS: String! = String(cString: [97,108,98,117,109,0], encoding: .utf8)!
          var bigw: Int = 3
          var replacep: String! = String(cString: [103,115,116,114,105,110,103,0], encoding: .utf8)!
         validate8 = (((!validate8 ? numS.count : 21) % (CRecordsTool3.max(numS.count, 5))) >= 21)
         delegate_5hg /= CRecordsTool3.max(3, Float(replacep.count + 3))
         bigw >>= CRecordsTool3.min(2, labs(bigw))
         replacep.append("\(Int(delegate_5hg))")
      }
      if 3 == pic0.count {
         validate8 = (32 > ((validate8 ? pic0.count : 32) % (CRecordsTool3.max(2, pic0.count))))
      }
          var selectedG: Int = 5
          var baseB: String! = String(cString: [103,117,105,100,115,0], encoding: .utf8)!
          var refresh_: String! = String(cString: [118,105,115,98,108,101,0], encoding: .utf8)!
         validate8 = pic0.count > 60
         selectedG ^= selectedG
         baseB = "\(selectedG)"
         refresh_.append("\(selectedG + baseB.count)")
         validate8 = (35 > ((!validate8 ? 35 : pic0.count) << (CRecordsTool3.min(pic0.count, 2))))
      if !validate8 && pic0.count > 1 {
         pic0 = "\(((validate8 ? 5 : 3)))"
      }
      repeat {
         pic0 = "\((2 & (validate8 ? 1 : 2)))"
         if 1584378 == pic0.count {
            break
         }
      } while (pic0.count > 3) && (1584378 == pic0.count)
      loadingn *= (Double((validate8 ? 3 : 1) % (CRecordsTool3.max(pic0.count, 3))))
   while ((5.27 / (CRecordsTool3.max(8, codeT))) < 5.91 || 5.27 < (codeT / (CRecordsTool3.max(6, Float(loadingn))))) {
       var parameter_: String! = String(cString: [97,105,109,100,0], encoding: .utf8)!
      while (parameter_.count == 5) {
         parameter_.append("\(((String(cString:[89,0], encoding: .utf8)!) == parameter_ ? parameter_.count : parameter_.count))")
         break
      }
          var pictureE: String! = String(cString: [108,105,115,116,101,110,105,110,103,0], encoding: .utf8)!
          var chatx: Bool = true
         parameter_ = "\(pictureE.count)"
         pictureE = "\(((chatx ? 2 : 4)))"
       var l_heightX: Float = 0.0
      loadingn += (Double((String(cString:[74,0], encoding: .utf8)!) == parameter_ ? parameter_.count : Int(codeT)))
      break
   }
   if (codeT + Float(loadingn)) < 2.23 {
       var lineso: [String: Any]! = [String(cString: [102,119,114,105,116,101,0], encoding: .utf8)!:683, String(cString: [100,97,116,97,98,97,115,101,115,0], encoding: .utf8)!:987, String(cString: [100,101,99,111,109,112,114,101,115,115,101,100,0], encoding: .utf8)!:758]
       var int_tel: [String: Any]! = [String(cString: [99,114,101,97,116,111,114,115,0], encoding: .utf8)!:460, String(cString: [120,99,108,105,0], encoding: .utf8)!:513]
       var message4: [Any]! = [69, 340]
       var amountJ: String! = String(cString: [114,103,98,114,103,98,0], encoding: .utf8)!
       var photo_: String! = String(cString: [97,110,105,109,97,116,97,98,108,101,0], encoding: .utf8)!
      while (!photo_.hasSuffix(amountJ)) {
         amountJ.append("\(lineso.count << (CRecordsTool3.min(labs(3), 2)))")
         break
      }
      if lineso["\(message4.count)"] == nil {
         message4 = [2]
      }
         photo_.append("\(lineso.count % (CRecordsTool3.max(photo_.count, 6)))")
       var firstT: [String: Any]! = [String(cString: [105,102,111,114,119,97,114,100,0], encoding: .utf8)!:String(cString: [109,97,99,114,111,115,0], encoding: .utf8)!, String(cString: [99,111,114,114,101,99,116,105,111,110,0], encoding: .utf8)!:String(cString: [100,110,120,104,100,0], encoding: .utf8)!, String(cString: [117,100,112,108,105,116,101,0], encoding: .utf8)!:String(cString: [115,116,114,103,108,111,98,0], encoding: .utf8)!]
       var gressY: Int = 0
      repeat {
          var startX: String! = String(cString: [99,111,108,117,109,110,115,0], encoding: .utf8)!
          var qlabel_: String! = String(cString: [97,108,109,111,115,116,0], encoding: .utf8)!
          var rows4: [Any]! = [382, 618]
          var datasY: String! = String(cString: [99,105,118,105,108,0], encoding: .utf8)!
          var addressl: Bool = true
         photo_.append("\(gressY)")
         startX.append("\(((addressl ? 5 : 3)))")
         qlabel_.append("\(2)")
         rows4 = [(datasY == (String(cString:[97,0], encoding: .utf8)!) ? datasY.count : (addressl ? 1 : 3))]
         if photo_ == (String(cString:[115,106,50,0], encoding: .utf8)!) {
            break
         }
      } while (photo_.hasPrefix("\(int_tel.keys.count)")) && (photo_ == (String(cString:[115,106,50,0], encoding: .utf8)!))
      if 1 < photo_.count {
         firstT[photo_] = amountJ.count | 1
      }
          var pressl: Double = 5.0
         int_tel = ["\(int_tel.values.count)": 2]
         pressl += Double(Int(pressl) | Int(pressl))
          var navT: String! = String(cString: [115,116,114,101,97,109,102,111,117,114,99,99,0], encoding: .utf8)!
          var socketT: Bool = false
          var desclabelE: Double = 1.0
         photo_.append("\(gressY)")
         navT.append("\(1 | navT.count)")
         socketT = desclabelE < 11.69 && socketT
         desclabelE /= CRecordsTool3.max(Double(Int(desclabelE) >> (CRecordsTool3.min(5, labs(2)))), 5)
      repeat {
          var alabelz: String! = String(cString: [115,117,98,115,101,108,101,99,116,0], encoding: .utf8)!
          var modity8: Bool = false
         gressY -= 2 | gressY
         alabelz.append("\(alabelz.count % 3)")
         modity8 = (35 < (alabelz.count % (CRecordsTool3.max(2, (!modity8 ? 35 : alabelz.count)))))
         if gressY == 879634 {
            break
         }
      } while ((gressY ^ int_tel.values.count) == 1 && (int_tel.values.count ^ 1) == 2) && (gressY == 879634)
         gressY &= amountJ.count | 3
       var class_5bD: Bool = false
         class_5bD = (amountJ.count % (CRecordsTool3.max(9, int_tel.count))) == 93
         photo_ = "\(int_tel.values.count)"
         amountJ = "\(amountJ.count)"
      codeT /= CRecordsTool3.max(Float(amountJ.count - lineso.values.count), 4)
   }

      codeT -= Float(Int(loadingn) << (CRecordsTool3.min(labs(Int(t_view6)), 1)))
   repeat {
      loadingn /= CRecordsTool3.max(1, Double(Int(t_view6) / (CRecordsTool3.max(Int(loadingn), 1))))
      if 346734.0 == loadingn {
         break
      }
   } while ((t_view6 / (CRecordsTool3.max(10, loadingn))) >= 2.69 && 2.69 >= (loadingn / (CRecordsTool3.max(5, t_view6)))) && (346734.0 == loadingn)
        self.perform(#selector(queryPictureProgress), with: nil, afterDelay: 4.81)
    }

@discardableResult
 func scrollIosSpeakingBridgeLatest() -> Float {
    var remarkN: Bool = false
    var chuann: Double = 4.0
   for _ in 0 ..< 3 {
      chuann -= Double(Int(chuann) - 3)
   }
    var loading3: Bool = true
   if remarkN {
      remarkN = 74.10 >= chuann
   }
      remarkN = 61.67 < chuann || remarkN
     var parameterApass: String! = String(cString: [112,117,108,115,101,97,117,100,105,111,115,121,109,98,111,108,116,97,98,108,101,0], encoding: .utf8)!
    var interiorPopMsvsdepend:Float = 0

    return interiorPopMsvsdepend

}





    
    
    
    func paintChatRequest(content: String, prefix:(() -> Void)? = nil) {

         let chooseGpmd: Float = scrollIosSpeakingBridgeLatest()

      if chooseGpmd > 57 {
             print(chooseGpmd)
      }



       var back9: Double = 2.0
    var promptn: String! = String(cString: [99,114,111,115,115,111,118,101,114,0], encoding: .utf8)!
   while (Double(promptn.count) > back9) {
      promptn.append("\(Int(back9) * 2)")
      break
   }

      back9 /= CRecordsTool3.max(3, Double(1))
        var gressg = [String: Any]()
   repeat {
      back9 /= CRecordsTool3.max(3, (Double(promptn == (String(cString:[81,0], encoding: .utf8)!) ? Int(back9) : promptn.count)))
      if back9 == 4849923.0 {
         break
      }
   } while (promptn.hasSuffix("\(back9)")) && (back9 == 4849923.0)
        gressg["sum"] = "1"
       var yloading_: [String: Any]! = [String(cString: [116,104,101,109,101,0], encoding: .utf8)!:923, String(cString: [100,105,97,103,110,111,115,116,105,99,0], encoding: .utf8)!:222]
       var relation0: String! = String(cString: [115,116,114,117,99,116,117,114,101,115,0], encoding: .utf8)!
       var i_countR: Int = 3
       var callZ: Int = 2
         callZ <<= CRecordsTool3.min(labs(i_countR), 2)
          var vipJ: String! = String(cString: [97,99,111,108,111,114,115,0], encoding: .utf8)!
         callZ &= vipJ.count
      if 3 > yloading_.keys.count {
         relation0 = "\(2 | i_countR)"
      }
      while (relation0.hasSuffix("\(i_countR)")) {
          var interfacej: Double = 0.0
          var resourcesR: String! = String(cString: [114,101,99,111,103,110,105,122,101,100,0], encoding: .utf8)!
          var prefix_k3: Double = 2.0
          var callbackK: String! = String(cString: [114,116,114,101,101,99,104,101,99,107,0], encoding: .utf8)!
          var displayY: String! = String(cString: [98,117,103,115,0], encoding: .utf8)!
         relation0 = "\(Int(prefix_k3) + displayY.count)"
         interfacej -= Double(resourcesR.count - 3)
         resourcesR.append("\(resourcesR.count)")
         prefix_k3 *= Double(Int(interfacej))
         callbackK.append("\(((String(cString:[102,0], encoding: .utf8)!) == callbackK ? callbackK.count : resourcesR.count))")
         displayY.append("\(resourcesR.count)")
         break
      }
         i_countR <<= CRecordsTool3.min(labs(yloading_.count << (CRecordsTool3.min(5, labs(callZ)))), 2)
      back9 += Double(3)
        gressg["prompt"] = content
        gressg["taskParameter"] = "1"
        gressg["resultConfig"] = "1"
        
        
        QVCenterBase.shared.post(urlSuffix: "/img/aiSketch", body: gressg) { (result: Result<HHUHandingClass, SImageBaseE>) in
            switch result {
            case.success(let model):
                
                if model.code == 500 {
                    self.sendBtn.isEnabled = true
                    self.sendBtn.alpha = 1.0
                    
                    SVProgressHUD.showError(withStatus: model.msg)
                    return
                    
                }
                else if model.code == 200 {
                    print("图片imageId\(model.data!)")
                    self.photoId = model.data!
                    
                    let createObject = ["like":"AIda","content":"","status":"0"]
                    self.graphics.append(createObject)
                    self.tableView.reloadData()
                    
                    self.scrollToTheEndLastBottom()
                    self.queryPictureProgress()
                }
                else {
                    self.sendBtn.isEnabled = true
                    self.sendBtn.alpha = 1.0
                    SVProgressHUD.showError(withStatus: "绘画失败，请稍后重试")
                }
                
                break
            
            case.failure(_):
                self.sendBtn.isEnabled = true
                self.sendBtn.alpha = 1.0
                SVProgressHUD.showError(withStatus: "链接错误")
                break
            
            }
        }
    }

@discardableResult
 func detailSublyoutDidButton(alamofireHolderlabel: [Any]!, scrollRecords: [Any]!) -> UIButton! {
    var briefX: String! = String(cString: [97,99,99,101,115,115,0], encoding: .utf8)!
    var scaleF: Double = 5.0
      briefX.append("\(Int(scaleF) ^ 2)")
      briefX.append("\(briefX.count)")
       var itemsr: String! = String(cString: [112,108,97,116,101,97,117,0], encoding: .utf8)!
      if 3 < itemsr.count {
          var loadingW: Int = 5
          var datasr: Double = 4.0
          var avatarsd: String! = String(cString: [99,111,110,100,101,110,115,101,100,0], encoding: .utf8)!
         itemsr = "\(Int(datasr) % 1)"
         loadingW ^= 2
         datasr *= Double(avatarsd.count)
         avatarsd.append("\(((String(cString:[83,0], encoding: .utf8)!) == avatarsd ? avatarsd.count : loadingW))")
      }
      repeat {
         itemsr = "\(((String(cString:[100,0], encoding: .utf8)!) == itemsr ? itemsr.count : itemsr.count))"
         if (String(cString:[104,56,121,120,95,57,106,48,118,0], encoding: .utf8)!) == itemsr {
            break
         }
      } while ((String(cString:[104,56,121,120,95,57,106,48,118,0], encoding: .utf8)!) == itemsr) && (itemsr == String(cString:[71,0], encoding: .utf8)! || 4 > itemsr.count)
       var k_playerx: String! = String(cString: [100,101,99,111,109,112,114,101,115,115,111,114,0], encoding: .utf8)!
      briefX.append("\(itemsr.count & Int(scaleF))")
   for _ in 0 ..< 2 {
       var receiveI: String! = String(cString: [97,99,99,101,112,116,115,0], encoding: .utf8)!
       var findS: Float = 5.0
         receiveI.append("\((receiveI == (String(cString:[104,0], encoding: .utf8)!) ? Int(findS) : receiveI.count))")
      while (5.31 == (findS + 4.71) && 4.71 == (Float(receiveI.count) + findS)) {
          var yloadinga: Bool = true
          var gressI: Double = 5.0
          var play0: Float = 5.0
          var handingr: String! = String(cString: [100,99,97,100,101,99,0], encoding: .utf8)!
         receiveI = "\(Int(play0))"
         yloadinga = Int(gressI) >= handingr.count
         gressI /= CRecordsTool3.max(Double(3 & Int(gressI)), 5)
         play0 /= CRecordsTool3.max(5, Float(Int(gressI)))
         handingr.append("\((handingr.count ^ (yloadinga ? 5 : 5)))")
         break
      }
      for _ in 0 ..< 1 {
          var timerW: [String: Any]! = [String(cString: [98,102,114,97,99,116,105,111,110,0], encoding: .utf8)!:String(cString: [110,105,99,101,108,121,0], encoding: .utf8)!, String(cString: [99,121,99,108,105,99,0], encoding: .utf8)!:String(cString: [114,101,118,97,108,105,100,97,116,101,100,0], encoding: .utf8)!]
          var removeo: [String: Any]! = [String(cString: [114,101,99,118,109,115,103,0], encoding: .utf8)!:85, String(cString: [115,113,108,105,116,101,114,98,117,0], encoding: .utf8)!:48]
          var soud: [Any]! = [598, 535, 209]
          var holderlabel6: Float = 2.0
         findS *= Float(receiveI.count % 1)
         timerW = ["\(removeo.values.count)": removeo.keys.count]
         soud = [removeo.count >> (CRecordsTool3.min(labs(3), 1))]
         holderlabel6 -= Float(removeo.values.count * 3)
      }
       var responsew: String! = String(cString: [101,108,108,105,112,115,101,0], encoding: .utf8)!
       var rows7: String! = String(cString: [116,120,116,111,98,106,0], encoding: .utf8)!
      while (rows7 != receiveI) {
         receiveI = "\(responsew.count)"
         break
      }
         rows7.append("\(responsew.count / (CRecordsTool3.max(6, receiveI.count)))")
      scaleF -= Double(2)
   }
     var rateModity: UILabel! = UILabel(frame:CGRect.zero)
     let answerAvatars: Int = 170
     var removeNavgation: Float = 506.0
    var nrefIncompleteExceeds:UIButton! = UIButton(frame:CGRect.zero)
    rateModity.frame = CGRect(x: 167, y: 52, width: 0, height: 0)
    rateModity.alpha = 0.4;
    rateModity.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    rateModity.font = UIFont.systemFont(ofSize:16)
    rateModity.text = ""
    rateModity.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    rateModity.textAlignment = .center
    
    var rateModityFrame = rateModity.frame
    rateModityFrame.size = CGSize(width: 217, height: 132)
    rateModity.frame = rateModityFrame
    if rateModity.alpha > 0.0 {
         rateModity.alpha = 0.0
    }
    if rateModity.isHidden {
         rateModity.isHidden = false
    }
    if !rateModity.isUserInteractionEnabled {
         rateModity.isUserInteractionEnabled = true
    }

    nrefIncompleteExceeds.alpha = 0.0;
    nrefIncompleteExceeds.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nrefIncompleteExceeds.frame = CGRect(x: 246, y: 41, width: 0, height: 0)
    nrefIncompleteExceeds.setTitle("", for: .normal)
    nrefIncompleteExceeds.setBackgroundImage(UIImage(named:String(cString: [101,110,103,105,110,101,0], encoding: .utf8)!), for: .normal)
    nrefIncompleteExceeds.titleLabel?.font = UIFont.systemFont(ofSize:20)
    nrefIncompleteExceeds.setImage(UIImage(named:String(cString: [100,105,99,116,105,111,110,97,114,121,0], encoding: .utf8)!), for: .normal)

    
    var nrefIncompleteExceedsFrame = nrefIncompleteExceeds.frame
    nrefIncompleteExceedsFrame.size = CGSize(width: 118, height: 199)
    nrefIncompleteExceeds.frame = nrefIncompleteExceedsFrame
    if nrefIncompleteExceeds.alpha > 0.0 {
         nrefIncompleteExceeds.alpha = 0.0
    }
    if nrefIncompleteExceeds.isHidden {
         nrefIncompleteExceeds.isHidden = false
    }
    if !nrefIncompleteExceeds.isUserInteractionEnabled {
         nrefIncompleteExceeds.isUserInteractionEnabled = true
    }

    return nrefIncompleteExceeds

}





    
    override func viewDidLoad() {

         let llauddspKeyframes: UIButton! = detailSublyoutDidButton(alamofireHolderlabel:[979, 951], scrollRecords:[838, 972, 397])

      if llauddspKeyframes != nil {
          self.view.addSubview(llauddspKeyframes)
          let llauddspKeyframes_tag = llauddspKeyframes.tag
      }



       var ringn: Double = 3.0
    var findh: Int = 4
    var handlery: String! = String(cString: [119,114,105,116,97,98,108,101,0], encoding: .utf8)!
      ringn += Double(1 + handlery.count)

       var read4: String! = String(cString: [106,112,101,103,116,97,98,108,101,115,0], encoding: .utf8)!
       var listl: [Any]! = [228, 499, 949]
      repeat {
         read4.append("\(1 & read4.count)")
         if 3918673 == read4.count {
            break
         }
      } while (3918673 == read4.count) && (read4.contains("\(listl.count)"))
      repeat {
         listl = [3]
         if listl.count == 4851596 {
            break
         }
      } while (listl.count == 4851596) && (4 > (listl.count + 3) || (read4.count + 3) > 2)
          var pickerW: Double = 5.0
          var purchaseV: Double = 0.0
          var headC: String! = String(cString: [105,110,116,114,97,0], encoding: .utf8)!
         listl.append(Int(purchaseV))
         pickerW *= Double(headC.count)
         purchaseV *= (Double(headC == (String(cString:[77,0], encoding: .utf8)!) ? headC.count : Int(pickerW)))
          var itemx: Int = 1
          var startk: String! = String(cString: [99,97,114,114,121,0], encoding: .utf8)!
         read4.append("\((read4 == (String(cString:[107,0], encoding: .utf8)!) ? read4.count : itemx))")
         itemx |= startk.count
         startk.append("\(startk.count)")
      while (read4.contains("\(listl.count)")) {
          var parameterss: Bool = false
          var navv: String! = String(cString: [109,112,101,103,118,105,100,101,111,100,101,99,0], encoding: .utf8)!
          var voiceF: Int = 1
         listl.append(2)
         parameterss = 4 < voiceF || parameterss
         navv.append("\(navv.count - voiceF)")
         break
      }
         listl = [2]
      handlery = "\(listl.count + 1)"
        super.viewDidLoad()
   if 3 < handlery.count {
      ringn *= Double(Int(ringn) + handlery.count)
   }
        
        NotificationCenter.default.addObserver(self, selector: #selector(updateTableView), name: NSNotification.Name("UpdateTableViewNotificationName"), object: nil)
       var login3: Int = 0
       var table5: String! = String(cString: [117,116,104,111,114,0], encoding: .utf8)!
         login3 &= table5.count
      while (2 < (1 * table5.count) || (login3 * 1) < 1) {
          var loadingH: [String: Any]! = [String(cString: [111,114,105,103,105,110,0], encoding: .utf8)!:533, String(cString: [116,105,99,107,105,110,103,0], encoding: .utf8)!:168]
          var safe6: [Any]! = [String(cString: [115,117,98,111,98,106,101,99,116,0], encoding: .utf8)!, String(cString: [115,116,97,98,105,108,105,116,121,0], encoding: .utf8)!, String(cString: [112,114,105,109,97,114,105,108,121,0], encoding: .utf8)!]
         login3 >>= CRecordsTool3.min(labs(3), 2)
         loadingH = ["\(loadingH.count)": 2 << (CRecordsTool3.min(2, safe6.count))]
         safe6 = [3]
         break
      }
      while ((1 / (CRecordsTool3.max(9, table5.count))) <= 2 && 4 <= (login3 / 1)) {
         table5.append("\(login3)")
         break
      }
      if 3 >= (2 >> (CRecordsTool3.min(5, labs(login3)))) && 2 >= (2 >> (CRecordsTool3.min(1, table5.count))) {
         table5.append("\(table5.count + 2)")
      }
       var handlerj: Double = 2.0
      for _ in 0 ..< 3 {
         table5.append("\(login3)")
      }
      findh >>= CRecordsTool3.min(5, labs(handlery.count ^ 2))
        
        elevtCard = UINib(nibName: "BXAlamofireModityView", bundle: nil).instantiate(withOwner: self, options: nil).first as! BXAlamofireModityView
      findh *= 3 << (CRecordsTool3.min(labs(Int(ringn)), 5))
        elevtCard.alpha = 0.0
        elevtCard.dataSource = self
        keyWindow?.addSubview(elevtCard)
        elevtCard.snp.makeConstraints { make in
            make.edges.equalTo(0)
        }
        
        headerView = UINib(nibName: "PWYloadingView", bundle: nil).instantiate(withOwner: self, options: nil).first as! PWYloadingView
        headerView.dataSource = self
        bottomView.addSubview(headerView)
        headerView.snp.makeConstraints { make in
            make.edges.equalTo(0)
        }

        self.tableView.register(UINib(nibName: "XZGraphicsCell", bundle: nil), forCellReuseIdentifier: "MeQ")
        self.tableView.register(UINib(nibName: "BXFPrefixCell", bundle: nil), forCellReuseIdentifier: "graphics")
        
        self.getGraphicsMessage()
    }

@discardableResult
 func recordsCreateBaseNonePlay(imageHandler: Int) -> String! {
    var infoz: Int = 0
    var statuslabel6: String! = String(cString: [114,116,114,105,109,0], encoding: .utf8)!
       var valuelabelZ: String! = String(cString: [102,111,114,103,111,116,0], encoding: .utf8)!
       var endj: String! = String(cString: [115,112,101,97,107,101,114,115,0], encoding: .utf8)!
       var delete_3r: String! = String(cString: [112,114,105,109,97,114,105,101,115,0], encoding: .utf8)!
         endj = "\(endj.count)"
      repeat {
         delete_3r = "\(1 / (CRecordsTool3.max(3, delete_3r.count)))"
         if delete_3r.count == 1497530 {
            break
         }
      } while (endj.count < 1) && (delete_3r.count == 1497530)
      infoz %= CRecordsTool3.max(1 - infoz, 5)
      valuelabelZ = "\(valuelabelZ.count << (CRecordsTool3.min(valuelabelZ.count, 5)))"
      statuslabel6 = "\(1)"
       var change3: Float = 3.0
      repeat {
         change3 -= Float(3 << (CRecordsTool3.min(labs(Int(change3)), 2)))
         if change3 == 2218173.0 {
            break
         }
      } while (change3 <= change3) && (change3 == 2218173.0)
          var parameters3: Double = 4.0
          var elevtD: [String: Any]! = [String(cString: [111,118,101,114,118,105,101,119,0], encoding: .utf8)!:String(cString: [112,111,105,110,116,101,114,115,0], encoding: .utf8)!, String(cString: [97,112,116,120,104,100,0], encoding: .utf8)!:String(cString: [98,105,110,111,109,105,97,108,0], encoding: .utf8)!]
         change3 += Float(1)
         parameters3 /= CRecordsTool3.max(Double(elevtD.values.count & 2), 4)
         elevtD["\(parameters3)"] = elevtD.keys.count
       var speakingx: Double = 5.0
      statuslabel6 = "\(statuslabel6.count)"
      infoz %= CRecordsTool3.max(3, infoz | statuslabel6.count)
   return statuslabel6

}






    @objc func queryPictureProgress() {

         let unlockObjc: String! = recordsCreateBaseNonePlay(imageHandler:5589)

      let unlockObjc_len = unlockObjc?.count ?? 0
      if unlockObjc == "self__6" {
              print(unlockObjc)
      }



       var show1: Float = 4.0
    var dictionaryA: String! = String(cString: [105,110,118,111,107,101,114,0], encoding: .utf8)!
      show1 += Float(Int(show1))

   repeat {
      dictionaryA.append("\(dictionaryA.count)")
      if dictionaryA.count == 1129251 {
         break
      }
   } while (dictionaryA.count == 1129251) && ((Int(show1) * dictionaryA.count) > 5)
        var gressg = [String: Any]()
      dictionaryA = "\(dictionaryA.count + Int(show1))"
        gressg["taskId"] = self.photoId
       
        QVCenterBase.shared.post(urlSuffix: "/img/findImg", body: gressg) { (result: Result<XCollectionMain, SImageBaseE>) in
   repeat {
       var aidaO: Bool = false
       var albums: String! = String(cString: [109,101,109,111,0], encoding: .utf8)!
       var navgationv: Bool = false
       var callb: String! = String(cString: [109,101,100,105,117,109,0], encoding: .utf8)!
      while (navgationv && 4 > albums.count) {
         navgationv = (9 == (albums.count << (CRecordsTool3.min(1, labs((aidaO ? albums.count : 9))))))
         break
      }
      if albums.hasSuffix("\(aidaO)") {
         aidaO = !navgationv
      }
      if !aidaO {
         aidaO = navgationv || callb.count >= 13
      }
          var successj: String! = String(cString: [112,111,108,121,107,101,121,0], encoding: .utf8)!
         navgationv = !navgationv
         successj = "\(successj.count * 1)"
         aidaO = albums.count >= 63
      repeat {
          var configurationj: [String: Any]! = [String(cString: [114,111,117,110,100,101,100,110,101,115,115,0], encoding: .utf8)!:699, String(cString: [117,114,108,0], encoding: .utf8)!:643, String(cString: [101,109,111,116,105,99,111,110,0], encoding: .utf8)!:476]
          var activityd: String! = String(cString: [115,101,103,116,114,101,101,0], encoding: .utf8)!
         navgationv = navgationv && configurationj.values.count <= 44
         configurationj[activityd] = 2 - activityd.count
         if navgationv ? !navgationv : navgationv {
            break
         }
      } while (4 == callb.count) && (navgationv ? !navgationv : navgationv)
      for _ in 0 ..< 2 {
         navgationv = !albums.contains("\(aidaO)")
      }
      for _ in 0 ..< 3 {
          var markD: String! = String(cString: [119,104,105,108,101,0], encoding: .utf8)!
         aidaO = !aidaO
         markD = "\(((String(cString:[108,0], encoding: .utf8)!) == markD ? markD.count : markD.count))"
      }
      repeat {
         albums = "\(((navgationv ? 2 : 1)))"
         if (String(cString:[105,113,111,102,0], encoding: .utf8)!) == albums {
            break
         }
      } while (!aidaO) && ((String(cString:[105,113,111,102,0], encoding: .utf8)!) == albums)
         callb.append("\(3)")
         navgationv = !albums.hasPrefix("\(navgationv)")
      while (callb.hasSuffix("\(aidaO)")) {
          var attributesL: Double = 4.0
          var safeC: String! = String(cString: [104,113,97,100,115,112,0], encoding: .utf8)!
          var medataO: String! = String(cString: [115,117,98,108,97,121,101,114,115,0], encoding: .utf8)!
          var bangb: String! = String(cString: [105,110,115,116,114,0], encoding: .utf8)!
         aidaO = 25 > medataO.count
         attributesL += Double(3)
         safeC.append("\(3 ^ Int(attributesL))")
         medataO.append("\((safeC == (String(cString:[77,0], encoding: .utf8)!) ? Int(attributesL) : safeC.count))")
         bangb.append("\(safeC.count)")
         break
      }
      show1 *= Float(dictionaryA.count)
      if show1 == 2471617.0 {
         break
      }
   } while (show1 == 2471617.0) && (dictionaryA.hasSuffix("\(show1)"))
            switch result {
            case.success(let model):
                
                if model.code == 200 {
                    
                    let labeelHeader: DUFReusable = model.data!
                    print(labeelHeader)
                    if let taskTypeString = labeelHeader.taskType, let taskTypeInt = Int(taskTypeString) {
                        if taskTypeInt == 2 {
                            if let images: [String] = labeelHeader.imgUrls! as? [String], !images.isEmpty {
                                let phone: String = images.first!
                                let createObject = ["like":"AIda","content":phone,"status":"1"]
                                self.graphics[self.graphics.count-1] = createObject
                                UserDefaults.standard.set(self.graphics, forKey: "paint")
                            }
                            self.sendBtn.isEnabled = true
                            self.sendBtn.alpha = 1.0
                            
                            self.tableView.reloadData()
                        }
                        else {
                            self.queryPictureStatus()
                        }
                    }
            
                }
                else {
                    self.sendBtn.isEnabled = true
                    self.sendBtn.alpha = 1.0
                }
                
                break
                
            case.failure(_):
                
                break
            }
        }
        
    }

@discardableResult
 func recepitRecordingvHidesProPending() -> Bool {
    var toolsS: String! = String(cString: [101,114,111,100,101,0], encoding: .utf8)!
    var sepaki: Double = 0.0
   while (5 < (3 >> (CRecordsTool3.min(1, toolsS.count)))) {
      sepaki /= CRecordsTool3.max(Double(toolsS.count ^ Int(sepaki)), 1)
      break
   }
       var configL: String! = String(cString: [106,117,115,116,105,102,105,101,100,0], encoding: .utf8)!
       var sliders: Int = 5
         sliders %= CRecordsTool3.max(2, 2 % (CRecordsTool3.max(10, configL.count)))
          var leftA: [Any]! = [UILabel()]
         sliders >>= CRecordsTool3.min(3, labs(((String(cString:[51,0], encoding: .utf8)!) == configL ? configL.count : leftA.count)))
       var qlabel_: Double = 2.0
         configL.append("\(3)")
          var userX: String! = String(cString: [101,110,106,105,110,0], encoding: .utf8)!
         sliders &= 2
         userX = "\(1 ^ userX.count)"
      repeat {
          var speedf: Int = 0
          var cancelL: String! = String(cString: [105,101,110,116,114,121,0], encoding: .utf8)!
          var insertR: Int = 0
          var layoutz: [Any]! = [533, 177]
         sliders /= CRecordsTool3.max(cancelL.count ^ configL.count, 1)
         speedf *= speedf
         cancelL = "\(layoutz.count - speedf)"
         insertR >>= CRecordsTool3.min(labs(1), 5)
         layoutz = [insertR | speedf]
         if 2213817 == sliders {
            break
         }
      } while ((4.98 * qlabel_) < 3.57 || 2.91 < (qlabel_ * 4.98)) && (2213817 == sliders)
      sepaki -= Double(1)
   for _ in 0 ..< 1 {
      sepaki -= Double(2 >> (CRecordsTool3.min(2, labs(Int(sepaki)))))
   }
     let setLogin: Double = 5321.0
     var messageTool: Double = 26.0
    var  xmpegStreaminfoMember:Bool = false
    xmpegStreaminfoMember = setLogin > 16
    messageTool -= setLogin
    xmpegStreaminfoMember = messageTool > 29

    return xmpegStreaminfoMember

}





    
    func getGraphicsMessage() {

         let nidsFfurl: Bool = recepitRecordingvHidesProPending()

      if !nidsFfurl {
      }



       var sou_: Int = 2
    var dicd: Bool = true
    var s_productsG: String! = String(cString: [100,105,118,105,100,111,114,0], encoding: .utf8)!
      dicd = sou_ == 11

   if dicd && s_productsG.count <= 3 {
       var phoneP: Double = 1.0
      if 4.63 <= (phoneP - 4.46) {
         phoneP /= CRecordsTool3.max(1, Double(Int(phoneP)))
      }
         phoneP -= Double(Int(phoneP) | 2)
      for _ in 0 ..< 3 {
         phoneP += Double(2)
      }
      s_productsG.append("\(((dicd ? 1 : 3) - sou_))")
   }
        let relation = UserDefaults.standard.object(forKey: "paint")
      dicd = s_productsG.count >= 45
        if relation != nil {
            self.graphics = UserDefaults.standard.object(forKey: "paint") as! [[String: String]]
        }
        
        self.tableView.reloadData()
   while (s_productsG.contains("\(dicd)")) {
      dicd = !dicd || 57 <= sou_
      break
   }
        self.scrollToTheEndLastBottom()
       var speedP: String! = String(cString: [116,119,111,112,111,105,110,116,0], encoding: .utf8)!
         speedP.append("\(speedP.count)")
      if speedP != speedP {
         speedP.append("\(speedP.count)")
      }
      for _ in 0 ..< 2 {
         speedP = "\(2 >> (CRecordsTool3.min(3, speedP.count)))"
      }
      dicd = 68 > speedP.count
       var stringX: Double = 2.0
       var tools4: [Any]! = [272, 102]
       var vipu: String! = String(cString: [108,111,116,116,105,101,108,111,97,100,101,114,0], encoding: .utf8)!
       var dicl: [String: Any]! = [String(cString: [99,117,101,115,0], encoding: .utf8)!:[299, 461, 571]]
      for _ in 0 ..< 2 {
          var toplayoutJ: String! = String(cString: [99,111,110,118,101,114,116,111,114,0], encoding: .utf8)!
          var uncompletee: String! = String(cString: [100,110,115,0], encoding: .utf8)!
          var selectedE: String! = String(cString: [99,117,114,114,0], encoding: .utf8)!
          var generateF: Float = 0.0
          var queueg: String! = String(cString: [112,114,101,115,101,116,115,0], encoding: .utf8)!
         vipu.append("\(Int(generateF) / 1)")
         toplayoutJ.append("\(selectedE.count | 1)")
         uncompletee = "\(uncompletee.count ^ 1)"
         selectedE = "\((uncompletee == (String(cString:[83,0], encoding: .utf8)!) ? selectedE.count : uncompletee.count))"
         generateF += Float(queueg.count)
         queueg = "\((queueg == (String(cString:[69,0], encoding: .utf8)!) ? queueg.count : selectedE.count))"
      }
      while ((vipu.count ^ 4) > 5 || 5 > (dicl.count ^ 4)) {
         vipu = "\(Int(stringX) >> (CRecordsTool3.min(tools4.count, 1)))"
         break
      }
      repeat {
         vipu = "\(((String(cString:[86,0], encoding: .utf8)!) == vipu ? vipu.count : dicl.count))"
         if (String(cString:[49,98,95,117,0], encoding: .utf8)!) == vipu {
            break
         }
      } while (!vipu.contains("\(tools4.count)")) && ((String(cString:[49,98,95,117,0], encoding: .utf8)!) == vipu)
         tools4.append(vipu.count)
       var int_5G: String! = String(cString: [97,99,116,117,97,108,105,122,97,116,105,111,110,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         tools4 = [Int(stringX) % 2]
      }
         int_5G.append("\(3 | tools4.count)")
          var headerQ: String! = String(cString: [119,97,108,107,0], encoding: .utf8)!
          var strD: String! = String(cString: [114,101,99,118,0], encoding: .utf8)!
          var eventm: String! = String(cString: [116,111,107,101,110,100,0], encoding: .utf8)!
         vipu.append("\(strD.count / 1)")
         headerQ = "\(headerQ.count)"
         strD.append("\(eventm.count)")
         eventm.append("\(2)")
      s_productsG = "\(((String(cString:[103,0], encoding: .utf8)!) == s_productsG ? s_productsG.count : tools4.count))"
    }

    
    @objc func updateTableView() {
       var dictH: String! = String(cString: [104,101,97,100,115,101,116,0], encoding: .utf8)!
    var sendingK: Double = 1.0
   while ((5.23 - sendingK) < 5.93 && (dictH.count / (CRecordsTool3.max(4, 2))) < 5) {
      sendingK /= CRecordsTool3.max(Double(Int(sendingK)), 5)
      break
   }
       var itemsz: String! = String(cString: [115,116,114,105,99,109,112,0], encoding: .utf8)!
      repeat {
         itemsz = "\(1 | itemsz.count)"
         if itemsz.count == 813446 {
            break
         }
      } while (itemsz.count == 813446) && (1 == itemsz.count)
         itemsz = "\(2)"
         itemsz.append("\(2)")
      dictH.append("\(3 ^ itemsz.count)")

   repeat {
       var toolI: Float = 3.0
       var shou6: Float = 5.0
       var avatar9: Int = 5
      if 5.20 > (toolI * shou6) {
         toolI *= Float(1)
      }
      for _ in 0 ..< 3 {
         avatar9 *= 3 ^ Int(toolI)
      }
         shou6 *= Float(3)
      for _ in 0 ..< 1 {
          var basea: String! = String(cString: [98,111,117,110,100,115,112,101,99,105,102,105,99,0], encoding: .utf8)!
          var handlem: String! = String(cString: [100,101,115,116,0], encoding: .utf8)!
          var sendq: Double = 1.0
         avatar9 *= Int(toolI) % 3
         basea = "\((handlem == (String(cString:[119,0], encoding: .utf8)!) ? handlem.count : Int(sendq)))"
         sendq *= Double(basea.count)
      }
         toolI += Float(Int(shou6) / (CRecordsTool3.max(4, avatar9)))
      for _ in 0 ..< 1 {
         toolI -= Float(avatar9)
      }
      repeat {
          var textR: String! = String(cString: [97,117,116,104,111,114,0], encoding: .utf8)!
          var msgH: String! = String(cString: [115,116,98,108,0], encoding: .utf8)!
          var browserg: Int = 0
         toolI -= Float(browserg)
         textR.append("\(msgH.count % (CRecordsTool3.max(textR.count, 6)))")
         msgH = "\(((String(cString:[105,0], encoding: .utf8)!) == msgH ? msgH.count : textR.count))"
         browserg &= msgH.count
         if toolI == 1370904.0 {
            break
         }
      } while (toolI == 1370904.0) && ((Double(toolI * Float(avatar9))) < 2.61)
          var nicknameu: String! = String(cString: [102,111,117,114,115,113,117,97,114,101,0], encoding: .utf8)!
         shou6 += Float(Int(toolI))
         nicknameu.append("\(1)")
         avatar9 -= Int(shou6)
      dictH.append("\(Int(toolI))")
      if dictH == (String(cString:[51,56,104,109,111,0], encoding: .utf8)!) {
         break
      }
   } while ((dictH.count >> (CRecordsTool3.min(labs(3), 2))) < 2 && 2 < (dictH.count / 3)) && (dictH == (String(cString:[51,56,104,109,111,0], encoding: .utf8)!))
   repeat {
      dictH.append("\(dictH.count >> (CRecordsTool3.min(4, labs(Int(sendingK)))))")
      if dictH == (String(cString:[119,52,105,48,0], encoding: .utf8)!) {
         break
      }
   } while ((Int(sendingK) * dictH.count) == 4 || (4.57 * sendingK) == 4.54) && (dictH == (String(cString:[119,52,105,48,0], encoding: .utf8)!))
        self.graphics.removeAll()
        getGraphicsMessage()
    }

    

    func saveImageToPhotoAlbum(imageURL: URL) {
       var collectionG: Bool = false
    var findZ: String! = String(cString: [97,110,103,108,101,115,0], encoding: .utf8)!
    var int_gsU: Double = 5.0
   for _ in 0 ..< 3 {
      collectionG = !collectionG
   }

       var sendw: [String: Any]! = [String(cString: [99,97,108,108,105,115,116,111,0], encoding: .utf8)!:470, String(cString: [119,97,108,108,0], encoding: .utf8)!:862, String(cString: [116,104,101,0], encoding: .utf8)!:377]
       var handleE: [Any]! = [27, 119, 942]
      repeat {
         sendw["\(handleE.count)"] = sendw.values.count % 3
         if sendw.count == 2938324 {
            break
         }
      } while (sendw.count == 2938324) && (!sendw.values.contains { $0 as? Int == handleE.count })
          var parameterk: String! = String(cString: [119,97,115,116,101,100,0], encoding: .utf8)!
          var unselectedW: [String: Any]! = [String(cString: [114,100,99,111,115,116,0], encoding: .utf8)!:478, String(cString: [112,107,99,115,0], encoding: .utf8)!:557, String(cString: [97,117,116,104,101,110,116,105,99,97,116,111,114,0], encoding: .utf8)!:604]
          var namelabelu: [String: Any]! = [String(cString: [119,97,118,112,97,99,107,101,110,99,0], encoding: .utf8)!:826, String(cString: [115,116,114,105,110,103,105,102,121,0], encoding: .utf8)!:266, String(cString: [115,101,110,100,101,114,0], encoding: .utf8)!:868]
         handleE = [handleE.count]
         parameterk.append("\(unselectedW.count * parameterk.count)")
         unselectedW = ["\(namelabelu.count)": 3 - namelabelu.keys.count]
         sendw = ["\(sendw.keys.count)": 3 | sendw.values.count]
          var leftT: String! = String(cString: [100,105,115,116,0], encoding: .utf8)!
          var vipk: String! = String(cString: [113,117,97,110,116,105,122,101,0], encoding: .utf8)!
         handleE.append(vipk.count)
         leftT = "\(leftT.count ^ 3)"
         vipk = "\(2 >> (CRecordsTool3.min(5, leftT.count)))"
       var connect5: [Any]! = [68, 192]
       var prefix__oU: [Any]! = [449, 837, 190]
         connect5.append(connect5.count)
      collectionG = sendw.keys.count < handleE.count
        DispatchQueue.global().async {
            if let data = try? Data(contentsOf: imageURL),
               let displayImage = UIImage(data: data) {
                UIImageWriteToSavedPhotosAlbum(displayImage, nil, nil, nil)
   if Int(int_gsU) == findZ.count {
      findZ = "\(3 | findZ.count)"
   }
                SVProgressHUD.showSuccess(withStatus: "下载成功")
            }
        }
   while (collectionG || (int_gsU / 4.19) >= 2.0) {
       var navgationa: String! = String(cString: [105,115,97,99,0], encoding: .utf8)!
       var zhidingesc: String! = String(cString: [106,118,101,114,115,105,111,110,0], encoding: .utf8)!
       var bigb: String! = String(cString: [110,97,118,105,103,97,116,105,111,110,98,97,114,0], encoding: .utf8)!
          var shouN: String! = String(cString: [110,115,112,97,99,101,115,0], encoding: .utf8)!
         navgationa = "\(shouN.count % 2)"
      for _ in 0 ..< 3 {
          var current3: Float = 3.0
          var sectionJ: String! = String(cString: [100,105,114,97,99,0], encoding: .utf8)!
          var speeds0: String! = String(cString: [117,110,112,117,98,108,105,115,104,0], encoding: .utf8)!
          var gundongG: [String: Any]! = [String(cString: [115,105,103,102,105,103,0], encoding: .utf8)!:String(cString: [100,105,109,105,110,115,105,111,110,115,0], encoding: .utf8)!, String(cString: [115,97,105,122,0], encoding: .utf8)!:String(cString: [122,98,105,110,0], encoding: .utf8)!]
          var avatarsQ: Double = 4.0
         bigb = "\(3)"
         current3 -= Float(sectionJ.count * 2)
         sectionJ = "\(sectionJ.count ^ 3)"
         speeds0.append("\(2)")
         gundongG = [sectionJ: sectionJ.count]
         avatarsQ /= CRecordsTool3.max(3, Double(1))
      }
         zhidingesc = "\(bigb.count / (CRecordsTool3.max(7, zhidingesc.count)))"
          var play8: String! = String(cString: [97,100,97,112,116,101,114,0], encoding: .utf8)!
          var chatA: Double = 0.0
         navgationa.append("\(zhidingesc.count)")
         play8 = "\(play8.count)"
         chatA -= Double(play8.count)
       var homen: String! = String(cString: [115,104,117,116,100,111,119,110,0], encoding: .utf8)!
         homen = "\(2 | zhidingesc.count)"
      while (zhidingesc.count >= 4) {
         navgationa.append("\(zhidingesc.count * 2)")
         break
      }
      for _ in 0 ..< 3 {
          var graphicsH: Bool = true
          var vipP: String! = String(cString: [115,110,97,112,112,121,0], encoding: .utf8)!
          var jsonL: String! = String(cString: [112,114,101,115,99,97,108,101,0], encoding: .utf8)!
         navgationa = "\(homen.count | navgationa.count)"
         graphicsH = vipP.count >= jsonL.count
         vipP = "\((vipP.count >> (CRecordsTool3.min(5, labs((graphicsH ? 5 : 3))))))"
         jsonL = "\((vipP == (String(cString:[54,0], encoding: .utf8)!) ? (graphicsH ? 4 : 3) : vipP.count))"
      }
      while (navgationa.count < zhidingesc.count) {
          var not_cl: Double = 4.0
          var listV: String! = String(cString: [111,112,101,110,103,108,0], encoding: .utf8)!
          var sectiona: Double = 4.0
         navgationa.append("\(3 + Int(sectiona))")
         not_cl /= CRecordsTool3.max(4, Double(Int(not_cl) % (CRecordsTool3.max(2, 6))))
         listV.append("\(Int(not_cl))")
         sectiona += Double(listV.count | Int(not_cl))
         break
      }
      collectionG = 2 <= bigb.count
      break
   }
   repeat {
       var statuesE: String! = String(cString: [119,116,118,102,105,108,101,0], encoding: .utf8)!
       var valuelabelk: String! = String(cString: [114,101,105,109,112,111,114,116,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var searchB: [Any]! = [[String(cString: [99,104,97,110,110,101,108,0], encoding: .utf8)!:694, String(cString: [112,97,103,101,114,0], encoding: .utf8)!:548, String(cString: [114,101,108,111,97,100,101,114,0], encoding: .utf8)!:558]]
          var chatlishiy: [Any]! = [7338.0]
          var chatlishi7: String! = String(cString: [99,111,110,103,101,115,116,105,111,110,0], encoding: .utf8)!
          var successJ: String! = String(cString: [112,114,111,112,101,114,0], encoding: .utf8)!
         statuesE.append("\(1)")
         searchB = [((String(cString:[119,0], encoding: .utf8)!) == chatlishi7 ? chatlishi7.count : successJ.count)]
         chatlishiy.append(searchB.count * chatlishiy.count)
         successJ.append("\(3 + chatlishiy.count)")
      }
         valuelabelk.append("\(statuesE.count * valuelabelk.count)")
         statuesE.append("\((statuesE == (String(cString:[114,0], encoding: .utf8)!) ? valuelabelk.count : statuesE.count))")
       var leftTc: String! = String(cString: [118,101,114,105,102,121,105,110,103,0], encoding: .utf8)!
       var size__mV: String! = String(cString: [109,111,118,105,110,103,0], encoding: .utf8)!
         statuesE.append("\(size__mV.count)")
       var q_center4: Float = 2.0
      findZ.append("\(valuelabelk.count << (CRecordsTool3.min(labs(3), 3)))")
      if (String(cString:[108,54,49,103,0], encoding: .utf8)!) == findZ {
         break
      }
   } while ((String(cString:[108,54,49,103,0], encoding: .utf8)!) == findZ) && (!collectionG)
   if findZ.hasPrefix("\(int_gsU)") {
      findZ.append("\(((String(cString:[106,0], encoding: .utf8)!) == findZ ? findZ.count : Int(int_gsU)))")
   }
    }

    
    func loadingGif() -> YYImage? {
       var popupK: String! = String(cString: [104,97,110,100,111,102,102,0], encoding: .utf8)!
    var firstp: String! = String(cString: [99,101,108,108,117,108,97,114,0], encoding: .utf8)!
   while (firstp.contains("\(popupK.count)")) {
       var alabelc: String! = String(cString: [100,105,118,105,100,111,114,115,0], encoding: .utf8)!
       var freej: String! = String(cString: [99,111,110,99,101,97,108,109,101,110,116,0], encoding: .utf8)!
       var b_centerW: Float = 5.0
         b_centerW += Float(Int(b_centerW) % 1)
         alabelc.append("\(Int(b_centerW))")
         alabelc = "\(freej.count << (CRecordsTool3.min(alabelc.count, 3)))"
          var iosI: Double = 4.0
          var shown: String! = String(cString: [116,120,116,0], encoding: .utf8)!
          var gestureQ: Int = 5
         alabelc = "\(1)"
         iosI += Double(2 & shown.count)
         shown = "\(Int(iosI))"
         gestureQ -= Int(iosI) ^ 1
         alabelc = "\(freej.count)"
         b_centerW -= Float(2 ^ freej.count)
      for _ in 0 ..< 1 {
         freej.append("\(alabelc.count / (CRecordsTool3.max(1, 9)))")
      }
      for _ in 0 ..< 3 {
          var detecte: String! = String(cString: [105,109,112,111,114,116,101,114,115,0], encoding: .utf8)!
         alabelc = "\((detecte == (String(cString:[101,0], encoding: .utf8)!) ? freej.count : detecte.count))"
      }
          var dealloc_rt: Double = 0.0
          var inewsS: String! = String(cString: [118,100,112,97,117,99,111,110,116,101,120,116,0], encoding: .utf8)!
         b_centerW *= Float(3)
         dealloc_rt += (Double((String(cString:[83,0], encoding: .utf8)!) == inewsS ? inewsS.count : Int(dealloc_rt)))
      popupK.append("\(3)")
      break
   }

   for _ in 0 ..< 1 {
       var totalA: Double = 4.0
       var setstateU: Int = 2
       var setr: Float = 1.0
       var sheet2: Float = 2.0
       var menuC: Bool = false
      repeat {
         setr *= Float(setstateU)
         if setr == 2554858.0 {
            break
         }
      } while (setr == 2554858.0) && (3.31 <= (4.51 * setr))
         totalA *= (Double(Int(sheet2) | (menuC ? 4 : 5)))
          var imgi: Double = 5.0
          var failedq: [Any]! = [578, 840, 984]
         sheet2 *= Float(Int(totalA))
         imgi *= Double(2 % (CRecordsTool3.max(9, failedq.count)))
         failedq = [3]
      repeat {
         menuC = !menuC || setr == 22.83
         if menuC ? !menuC : menuC {
            break
         }
      } while ((setr * 4.30) == 3.31) && (menuC ? !menuC : menuC)
          var buttonl: Double = 1.0
          var systemk: Bool = false
          var n_productsS: String! = String(cString: [112,114,101,101,110,99,111,100,101,0], encoding: .utf8)!
         menuC = !menuC || systemk
         buttonl /= CRecordsTool3.max(4, Double(2))
         systemk = (buttonl + Double(n_productsS.count)) >= 27.64
         n_productsS.append("\(2 >> (CRecordsTool3.min(2, n_productsS.count)))")
          var screenf: Double = 2.0
          var systemE: Int = 5
         totalA -= Double(2)
         screenf += Double(3)
         systemE *= 1 >> (CRecordsTool3.min(labs(Int(screenf)), 5))
         setstateU ^= 2 % (CRecordsTool3.max(setstateU, 5))
      while (4 >= setstateU && (setstateU ^ 4) >= 4) {
          var sheet5: [Any]! = [441, 858, 763]
          var pressF: String! = String(cString: [97,109,116,0], encoding: .utf8)!
         setstateU *= sheet5.count
         sheet5 = [pressF.count]
         pressF.append("\(pressF.count & 2)")
         break
      }
          var displayX: Int = 0
          var verticalp: String! = String(cString: [109,97,112,115,105,122,101,0], encoding: .utf8)!
          var contextI: [Any]! = [768, 365, 222]
         totalA += Double(verticalp.count)
         displayX %= CRecordsTool3.max(4, contextI.count)
         verticalp.append("\(contextI.count)")
         setstateU %= CRecordsTool3.max(5, Int(sheet2))
      for _ in 0 ..< 3 {
         sheet2 *= Float(3 * Int(totalA))
      }
      while (!menuC || 2.44 < (3.98 / (CRecordsTool3.max(1, setr)))) {
          var system_: Bool = false
          var datask: String! = String(cString: [114,97,108,102,100,97,116,97,0], encoding: .utf8)!
          var queueu: String! = String(cString: [115,99,97,109,0], encoding: .utf8)!
          var result5: String! = String(cString: [116,114,101,122,111,114,0], encoding: .utf8)!
         setr *= (Float((menuC ? 3 : 2) % (CRecordsTool3.max(3, queueu.count))))
         system_ = !result5.contains("\(system_)")
         datask.append("\((result5.count | (system_ ? 4 : 1)))")
         queueu = "\(result5.count / (CRecordsTool3.max(2, 9)))"
         break
      }
         sheet2 += Float(2 * setstateU)
         menuC = setstateU > 64 || 51.5 > totalA
         sheet2 -= Float(Int(sheet2))
      firstp = "\(((String(cString:[53,0], encoding: .utf8)!) == popupK ? Int(setr) : popupK.count))"
   }
        if let gifPath = Bundle.main.path(forResource: "graphicsHistory", ofType: "gif"),
            let phoneP = try? Data(contentsOf: URL(fileURLWithPath: gifPath)),
            let bangImage = YYImage(data: phoneP) {

   for _ in 0 ..< 3 {
      firstp = "\(firstp.count / (CRecordsTool3.max(7, popupK.count)))"
   }
            return bangImage
        }
        return nil
      popupK = "\(1 ^ firstp.count)"
    }

    
    @IBAction func send(_ sender: UIButton) {
       var browserB: [Any]! = [1230]
    var aidaD: String! = String(cString: [100,105,115,97,112,112,101,97,114,105,110,103,0], encoding: .utf8)!
    var main_rv: Float = 5.0
       var userdataw: String! = String(cString: [102,97,113,0], encoding: .utf8)!
       var j_title9: Bool = true
      repeat {
          var dict7: Double = 4.0
          var arrayq: [Any]! = [String(cString: [121,117,118,97,0], encoding: .utf8)!]
          var done1: Int = 5
          var homee: String! = String(cString: [112,103,105,100,120,0], encoding: .utf8)!
          var unselected4: String! = String(cString: [117,110,116,114,117,115,116,101,100,0], encoding: .utf8)!
         userdataw.append("\(done1)")
         dict7 /= CRecordsTool3.max(Double(homee.count ^ 1), 5)
         arrayq.append(3)
         done1 >>= CRecordsTool3.min(homee.count, 5)
         unselected4 = "\(arrayq.count - 1)"
         if 4238989 == userdataw.count {
            break
         }
      } while (4238989 == userdataw.count) && (!j_title9)
          var replaceO: String! = String(cString: [115,104,105,109,109,101,114,105,110,103,0], encoding: .utf8)!
         userdataw = "\(((j_title9 ? 2 : 1)))"
         replaceO.append("\(replaceO.count)")
          var collectionB: [Any]! = [645, 593, 303]
          var size_9cH: Bool = false
          var bashouh: String! = String(cString: [103,101,111,98,116,97,103,0], encoding: .utf8)!
         userdataw = "\(bashouh.count * 1)"
         collectionB = [3]
         size_9cH = collectionB.count <= 30 && 30 <= collectionB.count
      if !j_title9 {
         userdataw = "\(((j_title9 ? 4 : 4) % (CRecordsTool3.max(7, userdataw.count))))"
      }
          var strings: String! = String(cString: [97,112,115,0], encoding: .utf8)!
          var headS: Double = 3.0
         userdataw.append("\(((String(cString:[57,0], encoding: .utf8)!) == strings ? strings.count : Int(headS)))")
      repeat {
         userdataw = "\((userdataw == (String(cString:[101,0], encoding: .utf8)!) ? userdataw.count : (j_title9 ? 2 : 4)))"
         if userdataw.count == 3080017 {
            break
         }
      } while (userdataw.count == 3080017) && (!userdataw.hasSuffix("\(j_title9)"))
      aidaD = "\(aidaD.count)"

      aidaD.append("\(aidaD.count | 1)")
        if textTF.text.count == 0 {
            ZKProgressHUD.showError("内容不能为空")
   for _ in 0 ..< 2 {
      main_rv *= Float(browserB.count)
   }
            return
        }
        
        if let vipValue = UserDefaults.standard.string(forKey: "VIP"), vipValue != "1" {
            self.elevtCard.showCardView()
   repeat {
       var sumk: String! = String(cString: [105,110,111,117,116,115,0], encoding: .utf8)!
       var jiluT: String! = String(cString: [115,112,101,101,99,104,0], encoding: .utf8)!
       var recordR: String! = String(cString: [98,117,105,108,100,101,114,0], encoding: .utf8)!
       var aboutX: Float = 2.0
       var avatarB: Int = 5
      if sumk != recordR {
          var contentsQ: Double = 0.0
          var dateY: String! = String(cString: [99,97,114,101,116,0], encoding: .utf8)!
          var socketD: Int = 3
         recordR = "\(recordR.count)"
         contentsQ /= CRecordsTool3.max((Double((String(cString:[56,0], encoding: .utf8)!) == dateY ? dateY.count : Int(contentsQ))), 5)
         socketD |= Int(contentsQ)
      }
      for _ in 0 ..< 3 {
         sumk = "\(avatarB | Int(aboutX))"
      }
         aboutX /= CRecordsTool3.max((Float((String(cString:[114,0], encoding: .utf8)!) == jiluT ? avatarB : jiluT.count)), 4)
         aboutX += Float(Int(aboutX))
         aboutX *= Float(Int(aboutX) - 1)
      for _ in 0 ..< 3 {
          var dic6: [String: Any]! = [String(cString: [115,109,107,97,0], encoding: .utf8)!:String(cString: [101,105,110,116,114,0], encoding: .utf8)!, String(cString: [106,115,111,110,114,112,99,0], encoding: .utf8)!:String(cString: [115,116,97,114,116,0], encoding: .utf8)!]
          var alamofireL: [String: Any]! = [String(cString: [114,101,115,116,114,105,99,116,101,100,0], encoding: .utf8)!:5064]
          var size_1W: Int = 1
          var drainI: String! = String(cString: [117,95,51,53,0], encoding: .utf8)!
          var p_heightF: Int = 4
         aboutX -= Float(Int(aboutX))
         dic6 = ["\(p_heightF)": ((String(cString:[102,0], encoding: .utf8)!) == drainI ? p_heightF : drainI.count)]
         alamofireL["\(drainI)"] = 1
         size_1W %= CRecordsTool3.max(3, drainI.count)
      }
      if aboutX >= 3.24 {
         aboutX -= Float(recordR.count | 1)
      }
         recordR.append("\((sumk == (String(cString:[77,0], encoding: .utf8)!) ? Int(aboutX) : sumk.count))")
      repeat {
         recordR.append("\(recordR.count ^ Int(aboutX))")
         if (String(cString:[51,113,53,103,103,112,114,49,56,102,0], encoding: .utf8)!) == recordR {
            break
         }
      } while ((String(cString:[51,113,53,103,103,112,114,49,56,102,0], encoding: .utf8)!) == recordR) && ((recordR.count * avatarB) >= 1 || (recordR.count * 1) >= 3)
         recordR = "\(1)"
         sumk.append("\(sumk.count >> (CRecordsTool3.min(3, labs(avatarB))))")
         recordR.append("\(3 >> (CRecordsTool3.min(3, labs(avatarB))))")
      repeat {
         avatarB /= CRecordsTool3.max(1, 2 ^ recordR.count)
         if avatarB == 3881267 {
            break
         }
      } while (2 >= avatarB) && (avatarB == 3881267)
      if (avatarB << (CRecordsTool3.min(labs(1), 2))) == 3 || 3 == (avatarB << (CRecordsTool3.min(labs(1), 1))) {
          var instanceI: Bool = true
         sumk = "\(sumk.count)"
         instanceI = (instanceI ? !instanceI : !instanceI)
      }
         recordR = "\(sumk.count >> (CRecordsTool3.min(labs(3), 2)))"
      browserB.append(3)
      if browserB.count == 570508 {
         break
      }
   } while (browserB.count == 570508) && (5 >= (browserB.count & 2) && (aidaD.count & 2) >= 2)
            return
            
        }
        
        self.paintChatRequest(content: self.textTF.text)
   if (aidaD.count << (CRecordsTool3.min(labs(5), 3))) == 1 {
       var selected5: String! = String(cString: [112,114,111,109,111,0], encoding: .utf8)!
       var beforeQ: String! = String(cString: [116,97,114,103,101,116,0], encoding: .utf8)!
       var responseN: [Any]! = [453, 682]
       var filledO: String! = String(cString: [101,116,119,111,114,107,0], encoding: .utf8)!
          var collectionR: Int = 5
         beforeQ = "\(((String(cString:[57,0], encoding: .utf8)!) == filledO ? filledO.count : responseN.count))"
         collectionR %= CRecordsTool3.max(collectionR << (CRecordsTool3.min(labs(3), 4)), 3)
      if beforeQ.hasSuffix(filledO) {
          var listening1: Int = 2
         beforeQ.append("\(selected5.count)")
         listening1 >>= CRecordsTool3.min(labs(2), 5)
      }
         filledO = "\(beforeQ.count)"
          var utils7: Double = 1.0
         selected5 = "\(((String(cString:[122,0], encoding: .utf8)!) == filledO ? filledO.count : responseN.count))"
         utils7 += Double(Int(utils7))
         responseN.append(beforeQ.count << (CRecordsTool3.min(labs(2), 1)))
         selected5 = "\(filledO.count << (CRecordsTool3.min(labs(1), 3)))"
         responseN = [2 << (CRecordsTool3.min(1, beforeQ.count))]
      while (1 <= (selected5.count & 5) && 3 <= (5 & responseN.count)) {
         responseN = [filledO.count / (CRecordsTool3.max(1, responseN.count))]
         break
      }
      repeat {
          var connectv: String! = String(cString: [97,97,115,99,0], encoding: .utf8)!
         selected5.append("\(1 << (CRecordsTool3.min(5, beforeQ.count)))")
         connectv = "\((connectv == (String(cString:[89,0], encoding: .utf8)!) ? connectv.count : connectv.count))"
         if selected5 == (String(cString:[104,49,104,0], encoding: .utf8)!) {
            break
         }
      } while (selected5 == (String(cString:[104,49,104,0], encoding: .utf8)!)) && (2 < (selected5.count + responseN.count))
      if 1 < responseN.count {
          var fullT: [Any]! = [5517]
          var gestures: Float = 4.0
          var chuangzuo9: Float = 5.0
          var nameR: String! = String(cString: [101,110,97,98,108,101,100,0], encoding: .utf8)!
          var i_height_: String! = String(cString: [104,97,114,112,101,110,0], encoding: .utf8)!
         beforeQ = "\(filledO.count)"
         fullT = [Int(gestures) / 2]
         gestures /= CRecordsTool3.max(Float(Int(gestures) & 1), 4)
         chuangzuo9 /= CRecordsTool3.max(2, (Float(nameR == (String(cString:[68,0], encoding: .utf8)!) ? nameR.count : Int(gestures))))
         i_height_.append("\(2)")
      }
       var paramI: Float = 4.0
      for _ in 0 ..< 1 {
         selected5 = "\(beforeQ.count)"
      }
      main_rv += (Float(filledO == (String(cString:[54,0], encoding: .utf8)!) ? filledO.count : responseN.count))
   }
        
        let from = ["like":"MeQ","content":"\(textTF.text!)","status":"1"]
   while (4 < (5 * browserB.count)) {
      browserB = [3 + aidaD.count]
      break
   }
        graphics.append(from)

        self.tableView.reloadData()
        self.sendBtn.isEnabled = false
        self.sendBtn.alpha = 0.3
        self.textTF.text = ""
        self.scrollToTheEndLastBottom()
    }

@discardableResult
 func usleepPushedPositionEnterTableView(responseLines: Float) -> UITableView! {
    var prefix_96A: String! = String(cString: [99,108,105,112,0], encoding: .utf8)!
    var navgationP: Double = 3.0
   repeat {
      prefix_96A.append("\(1 >> (CRecordsTool3.min(2, prefix_96A.count)))")
      if prefix_96A == (String(cString:[55,48,97,50,122,53,104,122,112,110,0], encoding: .utf8)!) {
         break
      }
   } while (prefix_96A == (String(cString:[55,48,97,50,122,53,104,122,112,110,0], encoding: .utf8)!)) && (navgationP <= 4.11)
       var expireW: String! = String(cString: [112,101,114,99,101,110,116,0], encoding: .utf8)!
         expireW.append("\(expireW.count | expireW.count)")
      repeat {
         expireW = "\(expireW.count)"
         if 1199146 == expireW.count {
            break
         }
      } while (1199146 == expireW.count) && (expireW.count == 5 && expireW.count == 5)
      repeat {
         expireW.append("\(expireW.count)")
         if expireW == (String(cString:[110,49,50,50,0], encoding: .utf8)!) {
            break
         }
      } while (expireW != expireW) && (expireW == (String(cString:[110,49,50,50,0], encoding: .utf8)!))
      navgationP /= CRecordsTool3.max(5, (Double(prefix_96A == (String(cString:[69,0], encoding: .utf8)!) ? expireW.count : prefix_96A.count)))
   repeat {
       var titlelabeld: String! = String(cString: [97,99,99,111,114,100,105,110,103,0], encoding: .utf8)!
       var fuzhiT: [Any]! = [UILabel()]
       var rnew_lS: String! = String(cString: [108,97,121,111,121,116,0], encoding: .utf8)!
          var sendK: String! = String(cString: [97,114,103,120,105,0], encoding: .utf8)!
         rnew_lS.append("\(rnew_lS.count << (CRecordsTool3.min(labs(2), 3)))")
         sendK = "\(((String(cString:[81,0], encoding: .utf8)!) == sendK ? sendK.count : sendK.count))"
      for _ in 0 ..< 3 {
         titlelabeld.append("\(((String(cString:[119,0], encoding: .utf8)!) == rnew_lS ? fuzhiT.count : rnew_lS.count))")
      }
       var lengthV: Bool = true
         rnew_lS = "\(fuzhiT.count)"
      for _ in 0 ..< 1 {
         rnew_lS.append("\(rnew_lS.count)")
      }
      prefix_96A = "\((titlelabeld == (String(cString:[104,0], encoding: .utf8)!) ? titlelabeld.count : fuzhiT.count))"
      if 2071409 == prefix_96A.count {
         break
      }
   } while (2071409 == prefix_96A.count) && ((navgationP * 5.59) >= 5.89 && 2 >= (prefix_96A.count * Int(navgationP)))
     var currentElevt: UIButton! = UIButton()
     var elevtBig: Double = 2752.0
     var rmblabelRecords: Bool = true
     var mineAction: String! = String(cString: [99,109,111,118,0], encoding: .utf8)!
    var bitplanarchunkyReconnectingAcross:UITableView! = UITableView()
    currentElevt.alpha = 0.1;
    currentElevt.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    currentElevt.frame = CGRect(x: 306, y: 202, width: 0, height: 0)
    currentElevt.setImage(UIImage(named:String(cString: [117,112,100,97,116,101,95,55,0], encoding: .utf8)!), for: .normal)
    currentElevt.setTitle("", for: .normal)
    currentElevt.setBackgroundImage(UIImage(named:String(cString: [101,110,100,0], encoding: .utf8)!), for: .normal)
    currentElevt.titleLabel?.font = UIFont.systemFont(ofSize:20)
    
    var currentElevtFrame = currentElevt.frame
    currentElevtFrame.size = CGSize(width: 109, height: 64)
    currentElevt.frame = currentElevtFrame
    if currentElevt.isHidden {
         currentElevt.isHidden = false
    }
    if currentElevt.alpha > 0.0 {
         currentElevt.alpha = 0.0
    }
    if !currentElevt.isUserInteractionEnabled {
         currentElevt.isUserInteractionEnabled = true
    }

    bitplanarchunkyReconnectingAcross.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    bitplanarchunkyReconnectingAcross.alpha = 1.0
    bitplanarchunkyReconnectingAcross.frame = CGRect(x: 288, y: 280, width: 0, height: 0)
    bitplanarchunkyReconnectingAcross.dataSource = nil
    bitplanarchunkyReconnectingAcross.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    bitplanarchunkyReconnectingAcross.delegate = nil

    
    var bitplanarchunkyReconnectingAcrossFrame = bitplanarchunkyReconnectingAcross.frame
    bitplanarchunkyReconnectingAcrossFrame.size = CGSize(width: 296, height: 125)
    bitplanarchunkyReconnectingAcross.frame = bitplanarchunkyReconnectingAcrossFrame
    if bitplanarchunkyReconnectingAcross.isHidden {
         bitplanarchunkyReconnectingAcross.isHidden = false
    }
    if bitplanarchunkyReconnectingAcross.alpha > 0.0 {
         bitplanarchunkyReconnectingAcross.alpha = 0.0
    }
    if !bitplanarchunkyReconnectingAcross.isUserInteractionEnabled {
         bitplanarchunkyReconnectingAcross.isUserInteractionEnabled = true
    }

    return bitplanarchunkyReconnectingAcross

}





    
    
    func updateTextViewHeight() {

         let zoomableCleanse: UITableView! = usleepPushedPositionEnterTableView(responseLines:6211.0)

      if zoomableCleanse != nil {
          self.view.addSubview(zoomableCleanse)
          let zoomableCleanse_tag = zoomableCleanse.tag
      }



       var meal0: String! = String(cString: [99,97,99,104,105,110,103,0], encoding: .utf8)!
    var delegate_tD: Bool = true
      delegate_tD = !meal0.contains("\(delegate_tD)")

   if 1 <= meal0.count || !delegate_tD {
       var disconnecty: [Any]! = [String(cString: [118,105,100,101,111,101,110,99,100,115,112,0], encoding: .utf8)!, String(cString: [112,117,114,112,111,115,101,0], encoding: .utf8)!]
       var gestures: Int = 1
       var openD: Double = 1.0
       var configP: [String: Any]! = [String(cString: [114,101,115,112,0], encoding: .utf8)!:692, String(cString: [106,99,111,110,102,105,103,105,110,116,0], encoding: .utf8)!:202, String(cString: [115,108,97,115,104,101,115,0], encoding: .utf8)!:410]
       var bottomb: [String: Any]! = [String(cString: [100,105,118,105,115,111,114,0], encoding: .utf8)!:487, String(cString: [114,101,100,97,99,116,0], encoding: .utf8)!:3, String(cString: [97,110,97,108,121,122,101,114,0], encoding: .utf8)!:900]
      if configP.keys.contains("\(openD)") {
          var aimageZ: [String: Any]! = [String(cString: [99,111,109,112,108,101,116,105,111,110,0], encoding: .utf8)!:String(cString: [97,115,99,105,105,105,110,100,101,120,0], encoding: .utf8)!, String(cString: [116,111,97,115,116,115,0], encoding: .utf8)!:String(cString: [115,116,114,105,110,103,101,110,99,111,100,101,0], encoding: .utf8)!, String(cString: [114,101,97,115,115,109,0], encoding: .utf8)!:String(cString: [118,105,101,119,115,0], encoding: .utf8)!]
          var editZ: String! = String(cString: [115,97,109,112,108,101,99,112,121,0], encoding: .utf8)!
         configP = ["\(gestures)": editZ.count]
         aimageZ["\(aimageZ.values.count)"] = aimageZ.count + aimageZ.keys.count
         editZ.append("\(3)")
      }
         bottomb = ["\(openD)": 3 % (CRecordsTool3.max(7, Int(openD)))]
          var rowV: Float = 2.0
         disconnecty.append(2)
         rowV -= Float(2)
         openD *= Double(3)
      while (4 <= (1 * disconnecty.count)) {
         gestures -= 2 / (CRecordsTool3.max(4, Int(openD)))
         break
      }
          var int_yu: Float = 0.0
          var loadingS: String! = String(cString: [99,121,99,108,101,0], encoding: .utf8)!
         openD /= CRecordsTool3.max(1, Double(2))
         int_yu += Float(Int(int_yu) << (CRecordsTool3.min(loadingS.count, 2)))
         loadingS = "\(1)"
      repeat {
         configP["\(bottomb.values.count)"] = configP.count / (CRecordsTool3.max(1, 6))
         if configP.count == 1050126 {
            break
         }
      } while ((2 >> (CRecordsTool3.min(5, disconnecty.count))) > 4) && (configP.count == 1050126)
         gestures %= CRecordsTool3.max(3, 3)
      for _ in 0 ..< 2 {
         gestures |= 3 - bottomb.count
      }
      repeat {
          var sousuo7: String! = String(cString: [117,99,111,110,115,116,0], encoding: .utf8)!
          var gifb: Bool = false
          var details2: String! = String(cString: [107,101,101,112,0], encoding: .utf8)!
          var titlelabelM: Bool = true
         disconnecty.append(1 + sousuo7.count)
         sousuo7.append("\(((gifb ? 5 : 4)))")
         gifb = details2.contains("\(gifb)")
         details2.append("\(((gifb ? 3 : 5) & 3))")
         titlelabelM = (!gifb ? !titlelabelM : !gifb)
         if 1354037 == disconnecty.count {
            break
         }
      } while (2 > disconnecty.count) && (1354037 == disconnecty.count)
       var preferencep: [String: Any]! = [String(cString: [99,104,97,114,97,99,116,101,114,0], encoding: .utf8)!:701, String(cString: [112,104,112,0], encoding: .utf8)!:766, String(cString: [119,104,105,116,101,115,112,97,99,101,0], encoding: .utf8)!:709]
       var font8: [String: Any]! = [String(cString: [115,105,103,110,97,116,117,114,101,0], encoding: .utf8)!:242, String(cString: [118,97,108,105,100,97,116,111,114,115,0], encoding: .utf8)!:783, String(cString: [103,101,111,0], encoding: .utf8)!:787]
         preferencep = ["\(bottomb.values.count)": bottomb.count | preferencep.keys.count]
         openD -= Double(bottomb.keys.count)
      if 4 < (disconnecty.count ^ font8.values.count) && (font8.values.count ^ 4) < 3 {
         font8 = ["\(disconnecty.count)": disconnecty.count | Int(openD)]
      }
      while ((gestures >> (CRecordsTool3.min(disconnecty.count, 3))) < 2 || 1 < (2 >> (CRecordsTool3.min(1, disconnecty.count)))) {
         gestures |= Int(openD) % 3
         break
      }
      delegate_tD = (disconnecty.count - bottomb.count) <= 1
   }
        let open = textTF.frame.size.width
   repeat {
      delegate_tD = (85 == ((!delegate_tD ? 85 : meal0.count) ^ meal0.count))
      if delegate_tD ? !delegate_tD : delegate_tD {
         break
      }
   } while (!meal0.contains("\(delegate_tD)")) && (delegate_tD ? !delegate_tD : delegate_tD)
        let vip = textTF.sizeThatFits(CGSize(width: open, height: CGFloat.greatestFiniteMagnitude))
        
        if vip.height < 48 {
            textViewHeightConstraint.constant = 78
        }
        else {
            textViewHeightConstraint.constant = vip.height + 50
        }
        
        view.layoutIfNeeded()
    }


    
    func scrollToTheEndLastBottom() {
       var toplayout_: [String: Any]! = [String(cString: [104,97,112,113,97,0], encoding: .utf8)!:202, String(cString: [117,110,114,101,97,100,0], encoding: .utf8)!:918]
    var modityA: [String: Any]! = [String(cString: [105,118,102,101,110,99,0], encoding: .utf8)!:202.0]
   if (modityA.keys.count % 2) >= 3 {
      modityA = ["\(toplayout_.values.count)": toplayout_.count & 3]
   }

       var m_imageF: String! = String(cString: [104,101,114,101,0], encoding: .utf8)!
       var z_imagei: Double = 4.0
       var resultZ: [Any]! = [String(cString: [110,116,114,111,0], encoding: .utf8)!, String(cString: [112,114,111,103,114,101,115,105,118,101,0], encoding: .utf8)!, String(cString: [104,101,120,100,117,109,112,0], encoding: .utf8)!]
      if resultZ.contains { $0 as? Double == z_imagei } {
          var ringbufferv: Bool = false
          var briefC: String! = String(cString: [119,105,116,104,0], encoding: .utf8)!
          var controllT: [Any]! = [30, 781]
          var nicknameG: [String: Any]! = [String(cString: [115,104,97,107,101,0], encoding: .utf8)!:String(cString: [99,111,109,98,0], encoding: .utf8)!, String(cString: [118,97,100,107,104,122,0], encoding: .utf8)!:String(cString: [105,110,116,101,114,114,117,112,116,101,100,0], encoding: .utf8)!, String(cString: [116,111,111,108,116,105,112,115,0], encoding: .utf8)!:String(cString: [115,112,114,101,97,100,0], encoding: .utf8)!]
         z_imagei /= CRecordsTool3.max(4, Double(briefC.count))
         ringbufferv = (nicknameG.keys.count - controllT.count) <= 97
         briefC.append("\(controllT.count % 2)")
         nicknameG["\(controllT.count)"] = nicknameG.keys.count >> (CRecordsTool3.min(controllT.count, 5))
      }
      repeat {
         resultZ.append(Int(z_imagei))
         if 3273611 == resultZ.count {
            break
         }
      } while (3273611 == resultZ.count) && (resultZ.count > Int(z_imagei))
      repeat {
          var voice4: [Any]! = [String(cString: [115,101,101,107,97,98,108,101,0], encoding: .utf8)!, String(cString: [116,112,105,100,0], encoding: .utf8)!]
          var relationC: String! = String(cString: [112,97,121,109,97,115,116,101,114,0], encoding: .utf8)!
          var sourcex: String! = String(cString: [115,101,115,115,105,111,110,0], encoding: .utf8)!
         z_imagei += Double(sourcex.count << (CRecordsTool3.min(5, m_imageF.count)))
         voice4.append(voice4.count % (CRecordsTool3.max(relationC.count, 9)))
         relationC.append("\(1)")
         sourcex = "\(((String(cString:[65,0], encoding: .utf8)!) == relationC ? voice4.count : relationC.count))"
         if z_imagei == 4397310.0 {
            break
         }
      } while (4 >= (m_imageF.count - 3) || 3 >= (3 / (CRecordsTool3.max(3, Int(z_imagei))))) && (z_imagei == 4397310.0)
         z_imagei *= Double(3)
       var aimageV: Double = 5.0
      while (2 < (5 ^ resultZ.count)) {
          var statuesa: [String: Any]! = [String(cString: [99,111,118,97,114,105,97,110,99,101,0], encoding: .utf8)!:362, String(cString: [112,114,101,112,97,114,105,110,103,0], encoding: .utf8)!:707, String(cString: [118,105,115,105,116,0], encoding: .utf8)!:683]
          var historyM: Float = 4.0
          var adeleteA: String! = String(cString: [99,117,114,116,97,105,110,0], encoding: .utf8)!
          var itemF: Float = 2.0
         resultZ.append(1)
         statuesa = [adeleteA: 2]
         historyM += Float(2 + adeleteA.count)
         itemF += Float(statuesa.keys.count)
         break
      }
       var systemB: String! = String(cString: [99,111,110,99,97,116,100,101,99,0], encoding: .utf8)!
      while ((systemB.count * 3) <= 1) {
         aimageV += (Double(systemB == (String(cString:[86,0], encoding: .utf8)!) ? Int(aimageV) : systemB.count))
         break
      }
         z_imagei -= Double(Int(aimageV) * systemB.count)
      modityA[m_imageF] = m_imageF.count >> (CRecordsTool3.min(3, toplayout_.values.count))
        let execute = self.tableView.numberOfRows(inSection: 0)
      toplayout_["\(modityA.count)"] = toplayout_.count - modityA.values.count
        if execute > 0 {
            let buying = IndexPath(row: execute - 1, section: 0)
            self.tableView.scrollToRow(at: buying, at: .bottom, animated: false)
        }
    }
    
}

extension LHistoryController: UITableViewDataSource, UITableViewDelegate {

@discardableResult
 func objectAmplitudeSpeakWeixinlabelAttributedSocketScrollView(bundleThinking: Bool, editInfo: Bool, freeLaunch: String!) -> UIScrollView! {
    var navigationf: [String: Any]! = [String(cString: [116,116,97,101,110,99,0], encoding: .utf8)!:904, String(cString: [101,113,117,97,108,105,122,101,114,0], encoding: .utf8)!:399, String(cString: [105,97,100,115,116,0], encoding: .utf8)!:438]
    var lastU: String! = String(cString: [102,108,105,99,0], encoding: .utf8)!
   if (2 * navigationf.keys.count) > 4 {
      lastU = "\(2)"
   }
       var chatu: String! = String(cString: [115,97,109,105,0], encoding: .utf8)!
       var lines8: Double = 1.0
      if 1.88 > (1.86 - lines8) || 1 > (Int(lines8) - 3) {
         lines8 *= (Double((String(cString:[67,0], encoding: .utf8)!) == chatu ? chatu.count : Int(lines8)))
      }
         chatu = "\(chatu.count & 3)"
       var avatarv: [String: Any]! = [String(cString: [116,114,101,97,116,0], encoding: .utf8)!:163, String(cString: [115,111,108,97,110,97,0], encoding: .utf8)!:137, String(cString: [115,101,97,114,99,104,98,97,114,0], encoding: .utf8)!:395]
       var buttonJ: [String: Any]! = [String(cString: [114,101,112,115,116,114,0], encoding: .utf8)!:String(cString: [112,117,98,101,120,112,0], encoding: .utf8)!, String(cString: [110,101,101,100,115,0], encoding: .utf8)!:String(cString: [115,99,97,108,105,110,103,0], encoding: .utf8)!, String(cString: [99,117,115,116,111,109,0], encoding: .utf8)!:String(cString: [99,111,100,101,99,115,117,98,98,108,111,99,107,0], encoding: .utf8)!]
      while (1 == chatu.count) {
          var self_qoG: String! = String(cString: [105,110,105,116,105,97,108,115,0], encoding: .utf8)!
          var historyu: Bool = false
         lines8 *= (Double(self_qoG == (String(cString:[54,0], encoding: .utf8)!) ? self_qoG.count : chatu.count))
         historyu = !historyu
         break
      }
         chatu.append("\(buttonJ.values.count)")
      for _ in 0 ..< 2 {
          var navigationd: Int = 1
          var place7: Int = 5
          var recognitionv: Double = 5.0
         lines8 -= Double(avatarv.keys.count - 1)
         navigationd %= CRecordsTool3.max(1 ^ navigationd, 5)
         place7 -= Int(recognitionv) & navigationd
         recognitionv += Double(3 + place7)
      }
      lastU.append("\(((String(cString:[65,0], encoding: .utf8)!) == chatu ? chatu.count : Int(lines8)))")
      lastU = "\(lastU.count)"
      lastU.append("\(lastU.count)")
     let shouItem: Int = 8681
     let toolsAvatar: UILabel! = UILabel()
     let objectClass_0: [String: Any]! = [String(cString: [97,109,117,108,116,105,0], encoding: .utf8)!:885.0]
    var themedRepaintEnveloped:UIScrollView! = UIScrollView(frame:CGRect.zero)
    themedRepaintEnveloped.frame = CGRect(x: 124, y: 24, width: 0, height: 0)
    themedRepaintEnveloped.alpha = 0.7;
    themedRepaintEnveloped.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    themedRepaintEnveloped.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    themedRepaintEnveloped.alwaysBounceVertical = true
    themedRepaintEnveloped.alwaysBounceHorizontal = true
    themedRepaintEnveloped.showsVerticalScrollIndicator = false
    themedRepaintEnveloped.showsHorizontalScrollIndicator = false
    themedRepaintEnveloped.delegate = nil
    toolsAvatar.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    toolsAvatar.alpha = 0.3
    toolsAvatar.frame = CGRect(x: 137, y: 268, width: 0, height: 0)
    toolsAvatar.textAlignment = .right
    toolsAvatar.font = UIFont.systemFont(ofSize:10)
    toolsAvatar.text = ""
    toolsAvatar.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    var toolsAvatarFrame = toolsAvatar.frame
    toolsAvatarFrame.size = CGSize(width: 251, height: 200)
    toolsAvatar.frame = toolsAvatarFrame
    if toolsAvatar.isHidden {
         toolsAvatar.isHidden = false
    }
    if toolsAvatar.alpha > 0.0 {
         toolsAvatar.alpha = 0.0
    }
    if !toolsAvatar.isUserInteractionEnabled {
         toolsAvatar.isUserInteractionEnabled = true
    }

    themedRepaintEnveloped.addSubview(toolsAvatar)

    
    var themedRepaintEnvelopedFrame = themedRepaintEnveloped.frame
    themedRepaintEnvelopedFrame.size = CGSize(width: 141, height: 61)
    themedRepaintEnveloped.frame = themedRepaintEnvelopedFrame
    if themedRepaintEnveloped.alpha > 0.0 {
         themedRepaintEnveloped.alpha = 0.0
    }
    if themedRepaintEnveloped.isHidden {
         themedRepaintEnveloped.isHidden = false
    }
    if !themedRepaintEnveloped.isUserInteractionEnabled {
         themedRepaintEnveloped.isUserInteractionEnabled = true
    }

    return themedRepaintEnveloped

}





    
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {

         let flacKit: UIScrollView! = objectAmplitudeSpeakWeixinlabelAttributedSocketScrollView(bundleThinking:false, editInfo:true, freeLaunch:String(cString: [102,116,118,112,108,97,115,116,110,111,100,101,0], encoding: .utf8)!)

      if flacKit != nil {
          let flacKit_tag = flacKit.tag
          self.view.addSubview(flacKit)
      }



       var leftj: String! = String(cString: [110,111,114,109,97,108,0], encoding: .utf8)!
    var linesx: Int = 1
    var contentw: Int = 4
       var descriptM: String! = String(cString: [120,112,111,114,116,101,100,0], encoding: .utf8)!
       var graphicsW: [Any]! = [855, 125, 20]
      for _ in 0 ..< 1 {
         graphicsW = [descriptM.count - 1]
      }
      repeat {
         graphicsW = [3]
         if 4676389 == graphicsW.count {
            break
         }
      } while (descriptM.contains("\(graphicsW.count)")) && (4676389 == graphicsW.count)
       var recognitionb: String! = String(cString: [110,115,117,105,114,103,98,97,0], encoding: .utf8)!
       var setk: String! = String(cString: [97,117,100,105,116,105,110,103,0], encoding: .utf8)!
         descriptM = "\(setk.count)"
      for _ in 0 ..< 3 {
          var popupn: [String: Any]! = [String(cString: [102,114,109,97,0], encoding: .utf8)!:426, String(cString: [99,104,101,118,114,111,110,0], encoding: .utf8)!:590, String(cString: [115,117,98,116,114,97,99,116,111,114,0], encoding: .utf8)!:646]
          var q_countG: [String: Any]! = [String(cString: [115,121,110,116,104,102,105,108,116,0], encoding: .utf8)!:603, String(cString: [101,110,113,117,101,117,101,0], encoding: .utf8)!:685]
          var menu3: String! = String(cString: [111,118,101,114,115,99,114,111,108,108,105,110,103,0], encoding: .utf8)!
          var attributese: Int = 4
         setk.append("\(3)")
         popupn["\(attributese)"] = 1 % (CRecordsTool3.max(5, attributese))
         q_countG = ["\(q_countG.values.count)": q_countG.keys.count % (CRecordsTool3.max(1, 8))]
         menu3.append("\(menu3.count)")
      }
       var souN: Int = 0
      contentw /= CRecordsTool3.max(3 - descriptM.count, 5)
      linesx &= linesx
   repeat {
       var bufferh: String! = String(cString: [115,101,116,115,0], encoding: .utf8)!
       var register__4_: Double = 1.0
       var desclabelU: Double = 3.0
          var url3: [Any]! = [String(cString: [105,110,116,101,114,115,101,99,116,105,110,103,0], encoding: .utf8)!, String(cString: [108,111,99,107,101,100,0], encoding: .utf8)!, String(cString: [100,121,110,108,111,97,100,0], encoding: .utf8)!]
         bufferh.append("\(Int(desclabelU) << (CRecordsTool3.min(2, labs(Int(register__4_)))))")
         url3 = [url3.count]
      if bufferh.hasPrefix("\(register__4_)") {
         bufferh = "\(Int(register__4_) / (CRecordsTool3.max(1, 1)))"
      }
      repeat {
         desclabelU += Double(1)
         if desclabelU == 2844302.0 {
            break
         }
      } while (desclabelU == 2844302.0) && (3.97 == (3.26 / (CRecordsTool3.max(4, desclabelU))) || 4.4 == (register__4_ - 3.26))
      while (!bufferh.hasPrefix("\(desclabelU)")) {
         desclabelU *= Double(bufferh.count)
         break
      }
      repeat {
         desclabelU *= Double(1 % (CRecordsTool3.max(Int(desclabelU), 2)))
         if 2850395.0 == desclabelU {
            break
         }
      } while (2850395.0 == desclabelU) && ((4 & bufferh.count) > 3)
         desclabelU *= Double(Int(desclabelU) * Int(register__4_))
         bufferh = "\(Int(desclabelU))"
      while (5.33 > register__4_) {
         register__4_ /= CRecordsTool3.max(Double(3 & bufferh.count), 2)
         break
      }
         bufferh.append("\(Int(register__4_))")
      linesx *= contentw
      if linesx == 4887199 {
         break
      }
   } while (5 <= (linesx >> (CRecordsTool3.min(2, labs(contentw))))) && (linesx == 4887199)
      leftj.append("\(2)")

   if (linesx * contentw) < 5 {
      contentw += contentw
   }
       var response6: Bool = false
       var leftt: Double = 1.0
      for _ in 0 ..< 3 {
          var popupD: Double = 2.0
          var while_oU: [Any]! = [255, 528, 128]
          var btnJ: [Any]! = [5032.0]
         leftt *= Double(Int(leftt) >> (CRecordsTool3.min(5, labs(2))))
         popupD /= CRecordsTool3.max(5, Double(Int(popupD) - btnJ.count))
         while_oU = [2]
         btnJ = [while_oU.count]
      }
      if 2.16 == leftt {
         response6 = !response6
      }
      for _ in 0 ..< 1 {
          var modelo: Double = 0.0
          var eventE: String! = String(cString: [104,105,103,104,112,97,115,115,0], encoding: .utf8)!
         response6 = (((response6 ? 76 : eventE.count) % (CRecordsTool3.max(eventE.count, 5))) < 76)
         modelo *= Double(Int(modelo))
      }
          var main_zC: String! = String(cString: [114,95,55,56,0], encoding: .utf8)!
         response6 = !response6
         main_zC.append("\(main_zC.count - main_zC.count)")
      for _ in 0 ..< 2 {
         response6 = !response6
      }
      while (response6) {
          var edith: Int = 3
          var backgroundm: Double = 1.0
          var recognitionC: [String: Any]! = [String(cString: [99,116,105,109,101,0], encoding: .utf8)!:529, String(cString: [111,112,99,111,100,101,115,0], encoding: .utf8)!:117, String(cString: [97,114,116,105,99,108,101,115,0], encoding: .utf8)!:855]
         leftt *= Double(3 * edith)
         edith *= recognitionC.keys.count
         backgroundm *= Double(recognitionC.keys.count)
         break
      }
      linesx /= CRecordsTool3.max(1, contentw)
        return self.graphics.count
    }

    
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
       var bashouB: [Any]! = [946, 512]
    var appQ: Bool = true
    var appF: String! = String(cString: [115,119,102,104,97,115,104,0], encoding: .utf8)!
      appQ = appF.count == 37

   while (bashouB.count >= 3) {
       var nicknamep: String! = String(cString: [97,117,116,104,111,114,105,122,101,100,0], encoding: .utf8)!
       var readw: String! = String(cString: [100,105,114,0], encoding: .utf8)!
      repeat {
          var sublyout6: Bool = true
          var startz: [String: Any]! = [String(cString: [105,110,116,114,101,112,111,108,0], encoding: .utf8)!:String(cString: [116,114,97,99,107,105,110,103,0], encoding: .utf8)!, String(cString: [118,100,101,98,117,103,0], encoding: .utf8)!:String(cString: [102,114,97,109,101,113,117,101,117,101,0], encoding: .utf8)!, String(cString: [102,105,110,103,101,114,112,114,105,110,116,115,0], encoding: .utf8)!:String(cString: [112,114,111,99,101,115,115,111,114,115,0], encoding: .utf8)!]
          var time_8g: Float = 2.0
          var alabelh: String! = String(cString: [102,97,105,108,101,100,0], encoding: .utf8)!
          var iosh: Double = 4.0
         readw = "\(startz.keys.count)"
         sublyout6 = 17.9 > iosh
         startz = ["\(time_8g)": Int(time_8g) + 1]
         alabelh.append("\(((sublyout6 ? 4 : 4) % (CRecordsTool3.max(Int(time_8g), 6))))")
         iosh += (Double(Int(iosh) ^ (sublyout6 ? 4 : 2)))
         if readw.count == 1231907 {
            break
         }
      } while (nicknamep.count >= 3) && (readw.count == 1231907)
      while (readw != String(cString:[90,0], encoding: .utf8)!) {
         nicknamep = "\(readw.count / 3)"
         break
      }
         readw = "\(3 << (CRecordsTool3.min(3, nicknamep.count)))"
       var while_vh: String! = String(cString: [115,101,114,105,97,108,110,111,0], encoding: .utf8)!
       var picW: String! = String(cString: [100,105,103,101,115,116,0], encoding: .utf8)!
          var imgd: String! = String(cString: [115,116,97,99,107,101,100,0], encoding: .utf8)!
          var text_: Double = 0.0
         while_vh.append("\(nicknamep.count << (CRecordsTool3.min(1, labs(Int(text_)))))")
         imgd.append("\(imgd.count % (CRecordsTool3.max(imgd.count, 6)))")
         text_ += Double(imgd.count << (CRecordsTool3.min(imgd.count, 1)))
      while (!readw.contains("\(while_vh.count)")) {
         while_vh.append("\(3 * readw.count)")
         break
      }
      bashouB.append(readw.count)
      break
   }

   while (5 == (1 % (CRecordsTool3.max(1, bashouB.count))) && 3 == (1 % (CRecordsTool3.max(5, appF.count)))) {
      appF.append("\(bashouB.count)")
      break
   }
        let createObject = self.graphics[indexPath.row]
   repeat {
      appQ = appF.count == 24 || !appQ
      if appQ ? !appQ : appQ {
         break
      }
   } while (!appF.contains("\(appQ)")) && (appQ ? !appQ : appQ)
        let s_player = createObject["like"]!
       
        if s_player.elementsEqual("MeQ") {
            let handleCell = tableView.dequeueReusableCell(withIdentifier: "MeQ") as! XZGraphicsCell
      appQ = appF.count >= 45
            handleCell.backgroundColor = .clear
      bashouB.append(((appQ ? 4 : 4) >> (CRecordsTool3.min(labs(3), 4))))
            handleCell.selectionStyle = .none
            handleCell.meQlabel.text = createObject["content"]
            return handleCell
        }
        else {
            let handleCell = tableView.dequeueReusableCell(withIdentifier: "graphics") as! BXFPrefixCell
            handleCell.backgroundColor = .clear
            handleCell.selectionStyle = .none
            handleCell.dataSource = self
            
            handleCell.picImage.image = nil
            if let status = createObject["status"], status.elementsEqual("1") {
                if let url = createObject["content"] {
                    handleCell.picImage.layer.yy_setImage(with: URL(string: url), placeholder: UIImage())
                }
            }
            else
            {
                handleCell.picImage.image = loadingGif()
            }
            
            return handleCell
        }
        
        return UITableViewCell()
    }

@discardableResult
 func preferredResultTaskFromDirectoryLabel(filledUtils: Double, downApp: Bool, wangBrief: Float) -> UILabel! {
    var detailsq: Bool = false
    var urlb: Bool = true
      detailsq = !urlb
   while (!urlb && detailsq) {
       var enabledJ: Float = 5.0
       var rowsV: [String: Any]! = [String(cString: [115,101,114,105,97,108,105,122,97,116,105,111,110,0], encoding: .utf8)!:String(cString: [115,117,98,98,108,111,99,107,0], encoding: .utf8)!, String(cString: [103,111,110,101,0], encoding: .utf8)!:String(cString: [98,95,53,57,0], encoding: .utf8)!, String(cString: [97,117,116,111,99,111,114,114,0], encoding: .utf8)!:String(cString: [109,118,114,101,102,0], encoding: .utf8)!]
         enabledJ += Float(rowsV.count)
         enabledJ /= CRecordsTool3.max(1, Float(Int(enabledJ) * rowsV.keys.count))
      repeat {
          var total2: Double = 5.0
          var share8: Int = 0
          var chat3: String! = String(cString: [109,97,107,101,100,112,107,103,0], encoding: .utf8)!
         enabledJ += Float(chat3.count | 3)
         total2 *= Double(share8)
         share8 %= CRecordsTool3.max(1 & Int(total2), 3)
         chat3 = "\(Int(total2))"
         if enabledJ == 2436203.0 {
            break
         }
      } while ((1.94 - enabledJ) > 1.24 && 2 > (Int(enabledJ) - rowsV.count)) && (enabledJ == 2436203.0)
      while (1 >= (rowsV.count >> (CRecordsTool3.min(labs(5), 2)))) {
         enabledJ *= Float(3 & Int(enabledJ))
         break
      }
         rowsV["\(enabledJ)"] = 3 >> (CRecordsTool3.min(2, rowsV.count))
      repeat {
          var rawing5: String! = String(cString: [112,97,97,100,0], encoding: .utf8)!
          var vipU: String! = String(cString: [109,95,52,53,95,101,120,99,108,117,100,101,0], encoding: .utf8)!
          var speakG: [Any]! = [799, 350]
         rowsV = [vipU: rawing5.count]
         rawing5.append("\(speakG.count >> (CRecordsTool3.min(1, speakG.count)))")
         vipU = "\(speakG.count)"
         if rowsV.count == 4662684 {
            break
         }
      } while (rowsV.keys.contains("\(enabledJ)")) && (rowsV.count == 4662684)
      urlb = (rowsV.count * Int(enabledJ)) < 63
      break
   }
      detailsq = (!detailsq ? !urlb : detailsq)
      urlb = (!detailsq ? urlb : !detailsq)
     var historyImage: [Any]! = [String(cString: [109,97,116,99,104,101,0], encoding: .utf8)!, String(cString: [116,105,109,101,103,109,0], encoding: .utf8)!, String(cString: [114,101,97,115,115,101,109,98,108,121,0], encoding: .utf8)!]
     var editSpeeds: UIButton! = UIButton()
     let resourcesCurrent: Double = 7298.0
    var xformedMultReverse = UILabel(frame:CGRect(x: 239, y: 402, width: 0, height: 0))
    xformedMultReverse.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    xformedMultReverse.alpha = 1.0
    xformedMultReverse.frame = CGRect(x: 170, y: 50, width: 0, height: 0)
    xformedMultReverse.font = UIFont.systemFont(ofSize:12)
    xformedMultReverse.text = ""
    xformedMultReverse.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    xformedMultReverse.textAlignment = .left
    editSpeeds.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    editSpeeds.alpha = 0.0
    editSpeeds.frame = CGRect(x: 95, y: 83, width: 0, height: 0)
    editSpeeds.setBackgroundImage(UIImage(named:String(cString: [97,118,97,116,97,114,115,0], encoding: .utf8)!), for: .normal)
    editSpeeds.titleLabel?.font = UIFont.systemFont(ofSize:10)
    editSpeeds.setImage(UIImage(named:String(cString: [116,105,109,101,114,0], encoding: .utf8)!), for: .normal)
    editSpeeds.setTitle("", for: .normal)
    
    var editSpeedsFrame = editSpeeds.frame
    editSpeedsFrame.size = CGSize(width: 298, height: 91)
    editSpeeds.frame = editSpeedsFrame
    if editSpeeds.alpha > 0.0 {
         editSpeeds.alpha = 0.0
    }
    if editSpeeds.isHidden {
         editSpeeds.isHidden = false
    }
    if !editSpeeds.isUserInteractionEnabled {
         editSpeeds.isUserInteractionEnabled = true
    }


    
    var xformedMultReverseFrame = xformedMultReverse.frame
    xformedMultReverseFrame.size = CGSize(width: 83, height: 115)
    xformedMultReverse.frame = xformedMultReverseFrame
    if xformedMultReverse.alpha > 0.0 {
         xformedMultReverse.alpha = 0.0
    }
    if xformedMultReverse.isHidden {
         xformedMultReverse.isHidden = false
    }
    if !xformedMultReverse.isUserInteractionEnabled {
         xformedMultReverse.isUserInteractionEnabled = true
    }

    return xformedMultReverse

}





    func numberOfSections(in tableView: UITableView) -> Int {

         let dtorDurb: UILabel! = preferredResultTaskFromDirectoryLabel(filledUtils:6809.0, downApp:true, wangBrief:4815.0)

      if dtorDurb != nil {
          let dtorDurb_tag = dtorDurb.tag
          self.view.addSubview(dtorDurb)
      }
      else {
          print("dtorDurb is nil")      }



       var pathh: Double = 4.0
    var applyQ: Float = 5.0
    var selectP: String! = String(cString: [118,105,98,114,97,116,105,111,110,0], encoding: .utf8)!
       var modityb: String! = String(cString: [112,98,107,100,102,0], encoding: .utf8)!
      while (modityb.hasSuffix("\(modityb.count)")) {
         modityb = "\(2)"
         break
      }
         modityb.append("\((modityb == (String(cString:[105,0], encoding: .utf8)!) ? modityb.count : modityb.count))")
          var buttonW: Double = 4.0
          var emptyM: String! = String(cString: [109,111,110,111,0], encoding: .utf8)!
          var qlabelQ: String! = String(cString: [101,120,99,105,116,97,116,105,111,110,0], encoding: .utf8)!
         modityb.append("\(((String(cString:[89,0], encoding: .utf8)!) == modityb ? Int(buttonW) : modityb.count))")
         buttonW += (Double(qlabelQ == (String(cString:[103,0], encoding: .utf8)!) ? qlabelQ.count : emptyM.count))
         emptyM.append("\(((String(cString:[71,0], encoding: .utf8)!) == emptyM ? emptyM.count : qlabelQ.count))")
      pathh *= Double(modityb.count >> (CRecordsTool3.min(4, labs(Int(applyQ)))))
   while (5.67 >= (5.91 - pathh) && (pathh - 5.91) >= 5.8) {
       var rmbX: String! = String(cString: [118,97,114,115,0], encoding: .utf8)!
       var soui: [Any]! = [String(cString: [103,114,111,117,112,101,100,0], encoding: .utf8)!, String(cString: [99,108,111,110,101,0], encoding: .utf8)!]
       var aimage8: Int = 5
       var taskX: Int = 5
         rmbX.append("\(1 << (CRecordsTool3.min(3, labs(taskX))))")
         rmbX.append("\(taskX)")
          var sousuoN: Float = 5.0
         rmbX = "\(Int(sousuoN))"
         soui.append((rmbX == (String(cString:[69,0], encoding: .utf8)!) ? rmbX.count : soui.count))
       var replaces: Double = 1.0
       var lineZ: Double = 4.0
      applyQ *= Float(Int(applyQ) ^ 1)
      break
   }

      pathh /= CRecordsTool3.max(2, Double(Int(pathh) % 1))
        return 1
    }
    
}

extension LHistoryController: BSPXChatResponse {

@discardableResult
 func cleanTransactionWeixinlabelProTableView() -> UITableView! {
    var jsonA: Bool = true
    var interface1: [String: Any]! = [String(cString: [105,110,105,116,104,0], encoding: .utf8)!:881, String(cString: [113,117,105,122,0], encoding: .utf8)!:493]
      interface1["\(jsonA)"] = ((jsonA ? 1 : 5) * interface1.values.count)
      interface1 = ["\(interface1.count)": interface1.count]
   while ((2 | interface1.values.count) <= 2 && !jsonA) {
       var bodyl: String! = String(cString: [118,105,100,101,111,114,101,110,100,101,114,101,114,0], encoding: .utf8)!
       var btnF: Int = 5
      while (!bodyl.contains("\(btnF)")) {
          var linesk: String! = String(cString: [99,108,97,115,115,105,102,115,0], encoding: .utf8)!
          var jiluM: String! = String(cString: [101,108,101,109,101,110,116,0], encoding: .utf8)!
          var flowu: String! = String(cString: [99,97,108,99,119,0], encoding: .utf8)!
          var bottomB: Double = 3.0
         btnF &= 3
         linesk.append("\(2)")
         jiluM.append("\(2 % (CRecordsTool3.max(8, Int(bottomB))))")
         flowu.append("\(jiluM.count)")
         bottomB *= Double(linesk.count / 3)
         break
      }
      for _ in 0 ..< 2 {
         bodyl = "\(1)"
      }
         bodyl.append("\(((String(cString:[89,0], encoding: .utf8)!) == bodyl ? bodyl.count : btnF))")
      if 1 <= btnF {
          var avatarsZ: [String: Any]! = [String(cString: [112,105,99,116,121,112,101,0], encoding: .utf8)!:211, String(cString: [100,99,116,115,117,98,0], encoding: .utf8)!:553]
          var editW: String! = String(cString: [114,101,115,112,111,110,100,0], encoding: .utf8)!
         bodyl.append("\((editW == (String(cString:[108,0], encoding: .utf8)!) ? editW.count : avatarsZ.values.count))")
      }
         bodyl.append("\(2)")
      repeat {
          var presentH: String! = String(cString: [115,104,97,114,112,110,101,115,115,0], encoding: .utf8)!
          var l_centerW: Double = 5.0
         btnF /= CRecordsTool3.max(2, btnF)
         presentH = "\(1 ^ Int(l_centerW))"
         l_centerW /= CRecordsTool3.max(1, (Double(presentH == (String(cString:[73,0], encoding: .utf8)!) ? Int(l_centerW) : presentH.count)))
         if btnF == 3467018 {
            break
         }
      } while ((btnF - bodyl.count) >= 1) && (btnF == 3467018)
      jsonA = 86 >= bodyl.count
      break
   }
   repeat {
      jsonA = (70 <= (interface1.keys.count >> (CRecordsTool3.min(2, labs((!jsonA ? interface1.keys.count : 40))))))
      if jsonA ? !jsonA : jsonA {
         break
      }
   } while (jsonA || 1 < (1 % (CRecordsTool3.max(3, interface1.count)))) && (jsonA ? !jsonA : jsonA)
     let tabbarConfiguration: Double = 2730.0
     var desclabelPrompt: UIView! = UIView()
     let checkIcon: Bool = false
     var time_jChat: String! = String(cString: [119,101,98,112,97,103,101,0], encoding: .utf8)!
    var contentReplacing:UITableView! = UITableView(frame:CGRect.zero)
    desclabelPrompt.alpha = 0.2;
    desclabelPrompt.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    desclabelPrompt.frame = CGRect(x: 312, y: 71, width: 0, height: 0)
    
    var desclabelPromptFrame = desclabelPrompt.frame
    desclabelPromptFrame.size = CGSize(width: 92, height: 285)
    desclabelPrompt.frame = desclabelPromptFrame
    if desclabelPrompt.alpha > 0.0 {
         desclabelPrompt.alpha = 0.0
    }
    if desclabelPrompt.isHidden {
         desclabelPrompt.isHidden = false
    }
    if !desclabelPrompt.isUserInteractionEnabled {
         desclabelPrompt.isUserInteractionEnabled = true
    }

    contentReplacing.delegate = nil
    contentReplacing.dataSource = nil
    contentReplacing.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    contentReplacing.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    contentReplacing.alpha = 0.7
    contentReplacing.frame = CGRect(x: 211, y: 25, width: 0, height: 0)

    
    var contentReplacingFrame = contentReplacing.frame
    contentReplacingFrame.size = CGSize(width: 194, height: 169)
    contentReplacing.frame = contentReplacingFrame
    if contentReplacing.alpha > 0.0 {
         contentReplacing.alpha = 0.0
    }
    if contentReplacing.isHidden {
         contentReplacing.isHidden = false
    }
    if !contentReplacing.isUserInteractionEnabled {
         contentReplacing.isUserInteractionEnabled = true
    }

    return contentReplacing

}





    
    func graphicsHeaderViewContent(QStr: String) {

         let unpackloIdctxdc: UITableView! = cleanTransactionWeixinlabelProTableView()

      if unpackloIdctxdc != nil {
          let unpackloIdctxdc_tag = unpackloIdctxdc.tag
          self.view.addSubview(unpackloIdctxdc)
      }
      else {
          print("unpackloIdctxdc is nil")      }



       var handleU: Double = 3.0
    var likeu: Int = 5
   while (likeu < Int(handleU)) {
      likeu /= CRecordsTool3.max(4, likeu - 1)
      break
   }
   if 5.0 < (Double(handleU * Double(likeu))) {
      likeu |= Int(handleU) << (CRecordsTool3.min(4, labs(likeu)))
   }

   while (2 < likeu) {
      handleU += Double(3 + likeu)
      break
   }
        textTF.text = QStr
    }
}


extension LHistoryController: MQAElevt {

@discardableResult
 func voiceIdentifierFilledTableView(presentPresent: [Any]!, contentsAimage: Float) -> UITableView! {
    var clearA: String! = String(cString: [112,114,111,114,101,115,0], encoding: .utf8)!
    var restoreV: String! = String(cString: [97,108,116,101,114,110,97,116,105,111,110,0], encoding: .utf8)!
   if 1 == clearA.count {
      clearA = "\(restoreV.count)"
   }
    var alamofirez: String! = String(cString: [115,119,97,112,0], encoding: .utf8)!
   if alamofirez.count < 2 {
      alamofirez.append("\(alamofirez.count)")
   }
     var graphicsAction: Bool = false
     let changePress: [String: Any]! = [String(cString: [114,101,115,111,108,118,101,100,0], encoding: .utf8)!:2692.0]
     let chuangEnter: UIView! = UIView()
    var addrExistingFramedata = UITableView()
    chuangEnter.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    chuangEnter.alpha = 0.5
    chuangEnter.frame = CGRect(x: 165, y: 38, width: 0, height: 0)
    
    var chuangEnterFrame = chuangEnter.frame
    chuangEnterFrame.size = CGSize(width: 191, height: 296)
    chuangEnter.frame = chuangEnterFrame
    if chuangEnter.alpha > 0.0 {
         chuangEnter.alpha = 0.0
    }
    if chuangEnter.isHidden {
         chuangEnter.isHidden = false
    }
    if !chuangEnter.isUserInteractionEnabled {
         chuangEnter.isUserInteractionEnabled = true
    }

    addrExistingFramedata.frame = CGRect(x: 170, y: 319, width: 0, height: 0)
    addrExistingFramedata.alpha = 0.7;
    addrExistingFramedata.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    addrExistingFramedata.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    addrExistingFramedata.delegate = nil
    addrExistingFramedata.dataSource = nil

    
    var addrExistingFramedataFrame = addrExistingFramedata.frame
    addrExistingFramedataFrame.size = CGSize(width: 101, height: 242)
    addrExistingFramedata.frame = addrExistingFramedataFrame
    if addrExistingFramedata.isHidden {
         addrExistingFramedata.isHidden = false
    }
    if addrExistingFramedata.alpha > 0.0 {
         addrExistingFramedata.alpha = 0.0
    }
    if !addrExistingFramedata.isUserInteractionEnabled {
         addrExistingFramedata.isUserInteractionEnabled = true
    }

    return addrExistingFramedata

}





    func deleteGraphicsTableViewCell(cell: BXFPrefixCell) {

         let mulresRemovals: UITableView! = voiceIdentifierFilledTableView(presentPresent:[String(cString: [115,101,110,115,105,116,105,118,101,0], encoding: .utf8)!, String(cString: [115,101,97,100,0], encoding: .utf8)!], contentsAimage:6048.0)

      if mulresRemovals != nil {
          self.view.addSubview(mulresRemovals)
          let mulresRemovals_tag = mulresRemovals.tag
      }
      else {
          print("mulresRemovals is nil")      }



       var msg4: Int = 3
    var detectionV: Bool = true
   if detectionV && (3 + msg4) > 2 {
      msg4 >>= CRecordsTool3.min(1, labs(((detectionV ? 5 : 2) | msg4)))
   }

      msg4 &= 1
        let buying = self.tableView.indexPath(for: cell)
   for _ in 0 ..< 2 {
      msg4 *= (msg4 - (detectionV ? 1 : 4))
   }
        if self.graphics.count > 0 {
            if let row = buying?.row {
                let createObject = self.graphics[row]
   while (!detectionV) {
       var activityN: [Any]! = [464, 512]
       var speedsd: Int = 5
       var handingt: Bool = false
       var pickerT: String! = String(cString: [119,105,110,97,114,109,0], encoding: .utf8)!
          var celldataW: Bool = false
         pickerT = "\(1 >> (CRecordsTool3.min(1, activityN.count)))"
      repeat {
          var markF: [String: Any]! = [String(cString: [99,111,109,112,108,101,116,101,0], encoding: .utf8)!:319, String(cString: [118,115,114,99,0], encoding: .utf8)!:850, String(cString: [108,111,116,116,105,101,107,101,121,112,97,116,104,0], encoding: .utf8)!:453]
          var nameB: String! = String(cString: [109,98,102,105,108,116,101,114,0], encoding: .utf8)!
          var chuangzuoQ: Int = 0
          var webV: String! = String(cString: [115,104,111,119,110,0], encoding: .utf8)!
          var urlsx: String! = String(cString: [115,113,108,105,116,101,99,104,97,110,103,101,103,114,111,117,112,0], encoding: .utf8)!
         handingt = activityN.count <= nameB.count
         markF["\(chuangzuoQ)"] = chuangzuoQ ^ 2
         nameB.append("\(1 + chuangzuoQ)")
         webV.append("\(chuangzuoQ)")
         urlsx = "\(urlsx.count)"
         if handingt ? !handingt : handingt {
            break
         }
      } while ((speedsd * 3) > 3 && handingt) && (handingt ? !handingt : handingt)
         activityN.append(1)
      for _ in 0 ..< 2 {
         speedsd >>= CRecordsTool3.min(5, labs((3 >> (CRecordsTool3.min(1, labs((handingt ? 2 : 1)))))))
      }
          var totalp: Bool = true
          var listeningV: String! = String(cString: [116,121,112,101,99,111,100,101,0], encoding: .utf8)!
         speedsd %= CRecordsTool3.max(5, speedsd)
         totalp = !listeningV.hasSuffix("\(totalp)")
         listeningV.append("\(((totalp ? 5 : 5) - listeningV.count))")
         speedsd *= 2
         speedsd &= pickerT.count
         pickerT = "\(((handingt ? 1 : 5) + pickerT.count))"
      while (3 == (speedsd & 2) || (activityN.count & speedsd) == 2) {
          var valuelabelM: Bool = true
          var urlA: Double = 2.0
          var urlAX: Double = 0.0
         speedsd -= 1
         valuelabelM = urlAX < 2.34 || valuelabelM
         urlA += Double(Int(urlAX) % (CRecordsTool3.max(6, Int(urlA))))
         break
      }
         handingt = (String(cString:[67,0], encoding: .utf8)!) == pickerT
         activityN = [activityN.count]
         speedsd ^= 1
      detectionV = pickerT.count == 37
      break
   }
                if let status = createObject["status"], status.elementsEqual("1") {
                    self.graphics.remove(at: row)
                    UserDefaults.standard.set(self.graphics, forKey: "paint")
                    self.tableView.reloadData()
                }
                else{
                    SVProgressHUD.showError(withStatus: "图片生成中，暂不支持删除")
                }
                
                
            }
            
        }
        
    }

    
    func tapImageGraphicsTableViewCell(cell: BXFPrefixCell) {
       var jsons: Float = 0.0
    var firstf: [Any]! = [840, 833]
    var urlsG: [String: Any]! = [String(cString: [115,99,97,108,97,114,115,0], encoding: .utf8)!:String(cString: [115,116,105,99,107,101,114,112,97,99,107,0], encoding: .utf8)!, String(cString: [116,101,120,116,108,101,0], encoding: .utf8)!:String(cString: [109,117,108,116,105,115,116,101,112,0], encoding: .utf8)!, String(cString: [99,111,112,121,104,0], encoding: .utf8)!:String(cString: [109,111,100,97,108,0], encoding: .utf8)!]
      firstf.append(urlsG.values.count)

      firstf.append(firstf.count >> (CRecordsTool3.min(1, urlsG.values.count)))
        var about: [String] = NSArray() as! [String]
   repeat {
      firstf.append(Int(jsons) & 3)
      if 3338431 == firstf.count {
         break
      }
   } while (3338431 == firstf.count) && (firstf.contains { $0 as? Float == jsons })

   if jsons >= 5.59 {
      jsons *= Float(Int(jsons) >> (CRecordsTool3.min(5, labs(1))))
   }
        let buying = self.tableView.indexPath(for: cell)
      firstf = [3]
        if let row = buying?.row {
            let createObject = self.graphics[row]
   if 2 > (firstf.count * 3) || 3 > (firstf.count * urlsG.count) {
      firstf = [urlsG.values.count]
   }
            if let url = createObject["content"], url.count > 0 {
                about = [url]
                XKItemChange.show.action_displayImages(about, index: row, sender: cell.picImage)
            }
            else {
                SVProgressHUD.showError(withStatus: "图片生成中，暂不支持预览")
            }
        }
    }

    
    func saveImageGraphicsTableViewCell(cell: BXFPrefixCell) {
       var purchasedo: Double = 0.0
    var remark1: Double = 0.0
       var playp: String! = String(cString: [101,120,112,97,110,115,105,111,110,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
          var shareF: Bool = true
         playp = "\(1)"
      }
      for _ in 0 ..< 2 {
          var restore2: Float = 2.0
         playp = "\((playp == (String(cString:[121,0], encoding: .utf8)!) ? playp.count : Int(restore2)))"
      }
       var handler2: String! = String(cString: [97,114,116,0], encoding: .utf8)!
       var adeleteF: String! = String(cString: [98,105,110,107,97,117,100,105,111,0], encoding: .utf8)!
      remark1 /= CRecordsTool3.max(4, Double(1))

   if 3.13 <= (Double(remark1 - Double(4))) {
      purchasedo /= CRecordsTool3.max(Double(Int(remark1)), 1)
   }
        let buying = self.tableView.indexPath(for: cell)
   while (5.90 <= remark1) {
       var playF: [String: Any]! = [String(cString: [100,101,115,101,114,105,97,108,105,122,101,100,0], encoding: .utf8)!:155, String(cString: [112,114,101,100,120,0], encoding: .utf8)!:442, String(cString: [99,104,97,114,103,101,0], encoding: .utf8)!:605]
       var infoO: Double = 2.0
       var finishedX: Double = 5.0
       var userO: [Any]! = [927, 778]
       var transactionss: String! = String(cString: [109,115,105,122,101,0], encoding: .utf8)!
      repeat {
         finishedX += Double(2)
         if finishedX == 4586765.0 {
            break
         }
      } while (finishedX == 4586765.0) && ((finishedX + 1.2) <= 4.59 && 2 <= (userO.count % 4))
      if (infoO - Double(playF.values.count)) == 4.18 && 4.63 == (infoO - 4.18) {
          var controllk: [Any]! = [430, 312]
          var nicknameX: Bool = false
          var queryp: Double = 3.0
          var menu4: String! = String(cString: [105,116,101,114,97,116,105,111,110,115,0], encoding: .utf8)!
          var resultR: String! = String(cString: [117,110,119,114,97,112,112,101,100,0], encoding: .utf8)!
         infoO += Double(playF.values.count & Int(queryp))
         controllk.append(2 << (CRecordsTool3.min(4, controllk.count)))
         nicknameX = menu4.count <= 34
         queryp *= Double(controllk.count)
         menu4.append("\(3)")
         resultR.append("\(((String(cString:[105,0], encoding: .utf8)!) == menu4 ? (nicknameX ? 2 : 5) : menu4.count))")
      }
         playF["\(finishedX)"] = Int(finishedX)
      if (2.50 - infoO) <= 1.85 {
         playF[transactionss] = userO.count >> (CRecordsTool3.min(labs(2), 2))
      }
      for _ in 0 ..< 2 {
         transactionss = "\(Int(infoO))"
      }
          var currentU: String! = String(cString: [105,110,115,101,114,116,0], encoding: .utf8)!
         playF = ["\(playF.values.count)": playF.count >> (CRecordsTool3.min(labs(2), 1))]
         currentU = "\(((String(cString:[98,0], encoding: .utf8)!) == currentU ? currentU.count : currentU.count))"
      for _ in 0 ..< 3 {
         infoO += Double(1)
      }
      while (playF.values.count < 1) {
         transactionss.append("\(1)")
         break
      }
      for _ in 0 ..< 2 {
          var verifyg: String! = String(cString: [116,104,97,116,0], encoding: .utf8)!
          var searchx: Bool = false
          var reusableZ: [Any]! = [973, 725]
          var flowA: Int = 3
         userO = [3 & Int(infoO)]
         verifyg = "\((verifyg == (String(cString:[105,0], encoding: .utf8)!) ? (searchx ? 2 : 2) : verifyg.count))"
         searchx = verifyg.contains("\(searchx)")
         reusableZ.append(((searchx ? 2 : 3) - reusableZ.count))
         flowA /= CRecordsTool3.max(4, reusableZ.count)
      }
          var home8: Float = 0.0
          var gundongb: Double = 4.0
         finishedX -= Double(Int(finishedX) ^ userO.count)
         home8 -= Float(Int(home8) - Int(gundongb))
         gundongb *= Double(2)
      repeat {
         finishedX += Double(playF.values.count)
         if 2522287.0 == finishedX {
            break
         }
      } while (2522287.0 == finishedX) && ((userO.count | 5) >= 4)
         infoO += Double(1)
         transactionss.append("\(playF.values.count & transactionss.count)")
       var chuangzuoO: String! = String(cString: [99,111,110,116,97,105,110,101,114,115,0], encoding: .utf8)!
       var toplayoutR: String! = String(cString: [97,99,107,117,112,0], encoding: .utf8)!
      if chuangzuoO.count > 3 {
          var lengthX: Double = 0.0
          var refreshm: String! = String(cString: [109,97,107,101,99,116,0], encoding: .utf8)!
          var long_5j: String! = String(cString: [120,97,118,115,0], encoding: .utf8)!
         transactionss = "\(toplayoutR.count ^ 1)"
         lengthX /= CRecordsTool3.max(Double(refreshm.count % 1), 2)
         refreshm = "\((refreshm == (String(cString:[111,0], encoding: .utf8)!) ? refreshm.count : long_5j.count))"
         long_5j.append("\(Int(lengthX) | long_5j.count)")
      }
      remark1 -= Double(2)
      break
   }
        if let row = buying?.row {
            let createObject = self.graphics[row]
      purchasedo /= CRecordsTool3.max(5, Double(Int(remark1)))
            if let url = createObject["content"], url.count > 0 {
                let coll = URL(string: url)
                self.saveImageToPhotoAlbum(imageURL: coll!)
                SVProgressHUD.show(withStatus: "图片下载中……")
            }
            else{
                SVProgressHUD.showError(withStatus: "图片生成中，请等待……")
            }
        }
    }
}

extension LHistoryController: KKCAnswerTable {

@discardableResult
 func equalManagerGenerateCompleteZhidinges(msgShared: Bool, mainScale: String!, queueWeixin: Float) -> Int {
    var w_animation9: Double = 1.0
    var homed: String! = String(cString: [105,110,115,101,114,116,105,111,110,115,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      homed = "\((homed == (String(cString:[109,0], encoding: .utf8)!) ? homed.count : Int(w_animation9)))"
   }
   repeat {
      w_animation9 *= Double(3 + homed.count)
      if 3579240.0 == w_animation9 {
         break
      }
   } while (3579240.0 == w_animation9) && (2 == homed.count)
   if 2.31 < (w_animation9 - 2.81) {
      w_animation9 /= CRecordsTool3.max(3, Double(homed.count & 3))
   }
     let safeTable: Float = 8247.0
     var dicGundong: String! = String(cString: [121,95,52,53,95,112,103,110,111,0], encoding: .utf8)!
    var lessStickerpackCdft:Int = 0
    lessStickerpackCdft -= Int(safeTable)

    return lessStickerpackCdft

}





    func elevtCardViewPresent() {

         let shortcutAds: Int = equalManagerGenerateCompleteZhidinges(msgShared:false, mainScale:String(cString: [115,117,98,115,116,114,105,110,103,0], encoding: .utf8)!, queueWeixin:9591.0)

      if shortcutAds < 4 {
             print(shortcutAds)
      }



       var showr: Double = 1.0
    var detailsw: [String: Any]! = [String(cString: [97,115,112,101,99,116,0], encoding: .utf8)!:String(cString: [119,97,118,101,102,111,114,109,0], encoding: .utf8)!, String(cString: [97,99,116,105,118,97,116,105,111,110,115,0], encoding: .utf8)!:String(cString: [101,120,112,105,114,97,116,105,111,110,0], encoding: .utf8)!, String(cString: [110,111,116,104,101,108,100,0], encoding: .utf8)!:String(cString: [107,105,110,103,102,105,115,104,101,114,0], encoding: .utf8)!]
    var shus: String! = String(cString: [110,111,110,110,117,108,108,99,111,110,116,101,110,116,115,0], encoding: .utf8)!
    var substring7: Bool = false
       var popup1: String! = String(cString: [97,118,97,115,115,101,114,116,0], encoding: .utf8)!
       var message9: Float = 2.0
      if 5.86 == (5.93 - message9) || (message9 - Float(popup1.count)) == 5.93 {
         message9 *= Float(3 << (CRecordsTool3.min(4, popup1.count)))
      }
       var completedY: Double = 3.0
         completedY /= CRecordsTool3.max(5, Double(2 * Int(message9)))
       var endm: Bool = true
       var containsD: Bool = true
         message9 -= (Float((endm ? 3 : 2) << (CRecordsTool3.min(popup1.count, 2))))
         containsD = endm || completedY > 52.67
      showr -= Double(Int(showr))
   for _ in 0 ..< 1 {
       var instanceC: Bool = true
       var w_titleD: String! = String(cString: [116,105,109,101,115,116,97,109,112,115,0], encoding: .utf8)!
       var adeleteU: String! = String(cString: [115,112,108,105,116,116,105,110,103,0], encoding: .utf8)!
      if 5 < w_titleD.count || 5 < adeleteU.count {
         w_titleD.append("\((w_titleD.count >> (CRecordsTool3.min(4, labs((instanceC ? 3 : 3))))))")
      }
         adeleteU = "\(w_titleD.count ^ 3)"
         adeleteU = "\(w_titleD.count)"
         w_titleD.append("\(adeleteU.count * w_titleD.count)")
         adeleteU.append("\(2)")
      repeat {
         w_titleD.append("\(adeleteU.count)")
         if (String(cString:[114,52,48,48,52,112,98,56,116,0], encoding: .utf8)!) == w_titleD {
            break
         }
      } while (3 > w_titleD.count || instanceC) && ((String(cString:[114,52,48,48,52,112,98,56,116,0], encoding: .utf8)!) == w_titleD)
      for _ in 0 ..< 1 {
          var avatarY: Double = 5.0
          var size_ouS: Double = 2.0
          var substringM: String! = String(cString: [114,101,113,117,105,114,105,110,103,0], encoding: .utf8)!
          var shouM: Bool = true
         instanceC = 31 >= substringM.count
         avatarY -= Double(3 / (CRecordsTool3.max(1, Int(avatarY))))
         size_ouS /= CRecordsTool3.max(3, Double(Int(avatarY) / (CRecordsTool3.max(7, Int(size_ouS)))))
         substringM.append("\((Int(avatarY) >> (CRecordsTool3.min(2, labs((shouM ? 4 : 2))))))")
         shouM = size_ouS > avatarY
      }
      for _ in 0 ..< 1 {
          var notificationa: [Any]! = [592, 714, 592]
          var speed0: String! = String(cString: [114,101,109,111,118,105,110,103,0], encoding: .utf8)!
          var elevtx: Double = 4.0
          var listenH: Bool = false
         w_titleD = "\(2)"
         notificationa = [Int(elevtx)]
         speed0.append("\(speed0.count)")
         elevtx *= Double(notificationa.count + Int(elevtx))
         listenH = (notificationa.contains { $0 as? Double == elevtx })
      }
      repeat {
         adeleteU.append("\(((String(cString:[115,0], encoding: .utf8)!) == adeleteU ? w_titleD.count : adeleteU.count))")
         if 4137790 == adeleteU.count {
            break
         }
      } while (4137790 == adeleteU.count) && (w_titleD == adeleteU)
      detailsw = ["\(showr)": w_titleD.count]
   }

   if 4 > (2 ^ detailsw.values.count) && (detailsw.values.count / 2) > 5 {
      showr += (Double(shus == (String(cString:[97,0], encoding: .utf8)!) ? shus.count : detailsw.count))
   }
      substring7 = 15.68 < showr && !substring7
        let removeController = TEAEditChangeController()
       var displayo: String! = String(cString: [111,98,106,101,99,116,115,0], encoding: .utf8)!
       var photob: String! = String(cString: [120,106,112,101,103,0], encoding: .utf8)!
         displayo.append("\(photob.count)")
      for _ in 0 ..< 1 {
         photob.append("\(3 & photob.count)")
      }
      while (1 > displayo.count || 1 > photob.count) {
         photob = "\(1)"
         break
      }
      while (photob == String(cString:[108,0], encoding: .utf8)!) {
         displayo.append("\(displayo.count & 3)")
         break
      }
      if !displayo.hasPrefix(photob) {
          var timer2: Bool = false
          var audioP: [Any]! = [359, 499, 168]
          var controllA: [Any]! = [9401]
          var toplayoutF: Double = 0.0
         photob = "\(1)"
         timer2 = toplayoutF <= 51.27
         audioP = [1]
         controllA.append(1 << (CRecordsTool3.min(labs(Int(toplayoutF)), 5)))
      }
       var action_: Double = 5.0
      showr += Double(1)
        removeController.modalPresentationStyle = .fullScreen
        self.present(removeController, animated: true)
    }
}

extension LHistoryController: UITextViewDelegate {

@discardableResult
 func collateWidthMemsetDetailsObjFrom(handingBody: Int, regionDismiss: [Any]!, yloadingLeft: Bool) -> String! {
    var playR: String! = String(cString: [109,95,56,48,95,110,101,103,111,116,105,97,116,101,100,0], encoding: .utf8)!
    var result3: String! = String(cString: [115,99,97,108,97,98,108,101,0], encoding: .utf8)!
      playR = "\(result3.count | playR.count)"
      playR = "\(playR.count)"
      playR = "\(result3.count & 3)"
   repeat {
      playR = "\(playR.count)"
      if (String(cString:[101,53,114,122,111,105,119,0], encoding: .utf8)!) == playR {
         break
      }
   } while (result3.count >= 4) && ((String(cString:[101,53,114,122,111,105,119,0], encoding: .utf8)!) == playR)
   return playR

}





    func textViewDidChange(_ textView: UITextView) {

         let encThrough: String! = collateWidthMemsetDetailsObjFrom(handingBody:2580, regionDismiss:[973, 673, 47], yloadingLeft:true)

      let encThrough_len = encThrough?.count ?? 0
      print(encThrough)



       var listf: [String: Any]! = [String(cString: [112,116,114,109,97,112,115,0], encoding: .utf8)!:60, String(cString: [109,101,115,97,103,101,115,0], encoding: .utf8)!:335, String(cString: [98,111,117,110,100,115,0], encoding: .utf8)!:918]
    var interface4: String! = String(cString: [102,108,111,97,116,0], encoding: .utf8)!
   repeat {
       var prefix_enp: String! = String(cString: [114,97,99,101,0], encoding: .utf8)!
       var shouE: Float = 0.0
       var rowe: Float = 4.0
       var holderlabelI: Float = 4.0
      repeat {
          var controllb: String! = String(cString: [99,111,109,112,111,115,101,100,0], encoding: .utf8)!
          var success3: Double = 3.0
         shouE -= Float(2)
         controllb.append("\(controllb.count % (CRecordsTool3.max(3, Int(success3))))")
         success3 -= Double(Int(success3) - 1)
         if 3948892.0 == shouE {
            break
         }
      } while (3948892.0 == shouE) && ((prefix_enp.count - 4) >= 5 || (prefix_enp.count * Int(shouE)) >= 4)
      for _ in 0 ..< 1 {
         shouE -= Float(Int(holderlabelI))
      }
         holderlabelI *= Float(3 ^ Int(rowe))
         holderlabelI += Float(prefix_enp.count)
      while (5 <= prefix_enp.count) {
         prefix_enp.append("\(prefix_enp.count & 1)")
         break
      }
      interface4.append("\(interface4.count * 3)")
      if 1140131 == interface4.count {
         break
      }
   } while (interface4.contains("\(listf.values.count)")) && (1140131 == interface4.count)
   while ((interface4.count * 3) > 1 || (listf.values.count * 3) > 5) {
      listf = ["\(listf.keys.count)": interface4.count - 2]
      break
   }
       var playingf: String! = String(cString: [97,110,121,0], encoding: .utf8)!
      repeat {
          var flow6: Double = 1.0
          var photo6: String! = String(cString: [112,109,107,0], encoding: .utf8)!
          var removep: [Any]! = [428, 255]
          var backgroundS: [String: Any]! = [String(cString: [104,97,110,103,117,112,0], encoding: .utf8)!:93, String(cString: [103,112,109,100,0], encoding: .utf8)!:934]
          var regionW: String! = String(cString: [112,97,115,115,112,104,114,97,115,101,0], encoding: .utf8)!
         playingf = "\(playingf.count << (CRecordsTool3.min(3, backgroundS.keys.count)))"
         flow6 /= CRecordsTool3.max(5, Double(3))
         photo6.append("\(Int(flow6))")
         removep = [removep.count * regionW.count]
         backgroundS = ["\(removep.count)": regionW.count]
         if playingf == (String(cString:[107,119,50,103,119,0], encoding: .utf8)!) {
            break
         }
      } while (1 <= playingf.count) && (playingf == (String(cString:[107,119,50,103,119,0], encoding: .utf8)!))
         playingf = "\(playingf.count)"
      repeat {
         playingf = "\(playingf.count | playingf.count)"
         if (String(cString:[113,98,56,116,0], encoding: .utf8)!) == playingf {
            break
         }
      } while (!playingf.hasSuffix("\(playingf.count)")) && ((String(cString:[113,98,56,116,0], encoding: .utf8)!) == playingf)
      interface4.append("\(2)")

      listf = ["\(listf.count)": interface4.count]
        updateTextViewHeight()
    }
}

 class CRecordsTool3 {
    static func min<C: Comparable>(_ n1: C, _ n2: C) -> C {
        return n1 > n2 ? n2 : n1
    }
    
    static func max<C: Comparable>(_ n1: C, _ n2: C) -> C {
        return n1 > n2 ? n1 : n2
    }
}