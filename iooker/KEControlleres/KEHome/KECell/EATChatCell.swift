
import Foundation

import UIKit

protocol ZFTShou: AnyObject {
    func deleteCollectionCell(cell: EATChatCell)
}

class EATChatCell: UITableViewCell {
private var scale_min: Double = 0.0
var callbackMax: Double = 0.0




    weak var dataSource: ZFTShou?
    @IBOutlet weak var label: UILabel!

@discardableResult
 func historyBeganRingbuffer(btnDescript: String!, while_vCollate: [String: Any]!) -> [String: Any]! {
    var remarkl: String! = String(cString: [100,101,115,99,114,105,112,116,111,114,0], encoding: .utf8)!
    var clean1: String! = String(cString: [116,119,105,100,100,108,101,115,0], encoding: .utf8)!
   while (remarkl.count >= 4) {
      remarkl.append("\(3 * clean1.count)")
      break
   }
    var speedsO: String! = String(cString: [115,117,112,112,114,101,115,115,101,100,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
       var main_zJ: String! = String(cString: [116,111,117,99,104,101,115,0], encoding: .utf8)!
       var pressg: String! = String(cString: [118,100,98,101,0], encoding: .utf8)!
       var selectV: [String: Any]! = [String(cString: [112,105,120,101,108,102,108,111,97,116,0], encoding: .utf8)!:55, String(cString: [105,112,97,100,100,0], encoding: .utf8)!:851]
       var elevtj: String! = String(cString: [97,98,115,108,0], encoding: .utf8)!
       var toplayoutW: String! = String(cString: [109,117,116,97,98,108,101,0], encoding: .utf8)!
      repeat {
          var volumeo: Int = 4
          var window_st4: Float = 5.0
         pressg.append("\(volumeo)")
         volumeo %= RCShowGraphicsTool10.max(2 * Int(window_st4), 3)
         window_st4 /= RCShowGraphicsTool10.max(2, Float(2 % (RCShowGraphicsTool10.max(7, Int(window_st4)))))
         if (String(cString:[105,120,55,110,121,52,101,112,100,0], encoding: .utf8)!) == pressg {
            break
         }
      } while (!pressg.hasSuffix(elevtj)) && ((String(cString:[105,120,55,110,121,52,101,112,100,0], encoding: .utf8)!) == pressg)
         selectV[toplayoutW] = elevtj.count & toplayoutW.count
          var speakings: String! = String(cString: [112,101,114,109,97,110,101,110,116,0], encoding: .utf8)!
         toplayoutW = "\(toplayoutW.count)"
         speakings = "\(speakings.count)"
      for _ in 0 ..< 2 {
         toplayoutW.append("\(main_zJ.count - pressg.count)")
      }
      if pressg.hasPrefix(main_zJ) {
         main_zJ = "\(pressg.count)"
      }
      repeat {
          var pictureG: Float = 3.0
          var itemD: Int = 5
          var emailA: String! = String(cString: [105,99,111,110,115,0], encoding: .utf8)!
          var shuu: Float = 2.0
          var bang4: Int = 4
         main_zJ.append("\(1)")
         pictureG -= Float(bang4 % (RCShowGraphicsTool10.max(emailA.count, 7)))
         itemD -= 1 | bang4
         emailA = "\(Int(shuu) | emailA.count)"
         shuu /= RCShowGraphicsTool10.max(Float(bang4), 4)
         if 871139 == main_zJ.count {
            break
         }
      } while ((main_zJ.count & selectV.count) < 3 || 1 < (3 & main_zJ.count)) && (871139 == main_zJ.count)
      for _ in 0 ..< 1 {
         elevtj.append("\(pressg.count)")
      }
         pressg = "\(3)"
       var regiond: Bool = true
       var successT: Bool = true
      for _ in 0 ..< 1 {
         elevtj = "\(main_zJ.count - pressg.count)"
      }
         successT = toplayoutW.count <= 39
         toplayoutW = "\(main_zJ.count)"
      while (elevtj.count >= toplayoutW.count) {
          var elevtx: Float = 2.0
         elevtj = "\(((successT ? 4 : 5) >> (RCShowGraphicsTool10.min(labs(1), 2))))"
         elevtx *= Float(3)
         break
      }
      repeat {
         elevtj.append("\(((regiond ? 2 : 5)))")
         if elevtj.count == 1043846 {
            break
         }
      } while (elevtj.count == 1043846) && (3 <= toplayoutW.count)
      if !pressg.hasSuffix("\(selectV.keys.count)") {
         pressg.append("\(((String(cString:[118,0], encoding: .utf8)!) == toplayoutW ? elevtj.count : toplayoutW.count))")
      }
      speedsO.append("\(main_zJ.count / 2)")
   }
     var gundElevt: Int = 9546
    var submvYonlyFread :[String: Any]! = [String: Any]()
    gundElevt -= 41
    submvYonlyFread.updateValue(gundElevt, forKey:String(cString: [75,0], encoding: .utf8)!)

    return submvYonlyFread

}






    override func setSelected(_ selected: Bool, animated: Bool) {

         let tencLocalizable: [String: Any]! = historyBeganRingbuffer(btnDescript:String(cString: [99,114,101,97,116,101,101,120,0], encoding: .utf8)!, while_vCollate:[String(cString: [118,101,114,115,105,111,110,115,0], encoding: .utf8)!:237, String(cString: [105,110,115,116,97,110,99,101,115,0], encoding: .utf8)!:536])

      let tencLocalizable_len = tencLocalizable.count
      tencLocalizable.enumerated().forEach({ (index, element) in
          if index  ==  6 {
              print(element.key)
              print(element.value)
          }
      })



       var avatarsb: Double = 5.0
    var modelI: String! = String(cString: [99,111,111,114,100,105,110,97,116,101,0], encoding: .utf8)!
    var selectedy: String! = String(cString: [109,105,115,117,115,101,0], encoding: .utf8)!
    var alabelP: Int = 3
   for _ in 0 ..< 1 {
       var popupN: Bool = false
       var scaleZ: Bool = false
       var sliderr: String! = String(cString: [112,108,97,110,97,114,116,111,117,121,118,121,0], encoding: .utf8)!
       var threshold9: Float = 3.0
         threshold9 += (Float(3 | (scaleZ ? 3 : 3)))
         scaleZ = Int(threshold9) < sliderr.count
      repeat {
         scaleZ = (scaleZ ? popupN : !scaleZ)
         if scaleZ ? !scaleZ : scaleZ {
            break
         }
      } while (scaleZ && popupN) && (scaleZ ? !scaleZ : scaleZ)
         sliderr = "\(3 - sliderr.count)"
      for _ in 0 ..< 3 {
         scaleZ = 84.54 <= threshold9
      }
         threshold9 /= RCShowGraphicsTool10.max(3, (Float((scaleZ ? 1 : 3) ^ Int(threshold9))))
      if !popupN {
         scaleZ = sliderr.count >= 13
      }
         popupN = popupN || scaleZ
      repeat {
         scaleZ = threshold9 >= Float(sliderr.count)
         if scaleZ ? !scaleZ : scaleZ {
            break
         }
      } while (!popupN) && (scaleZ ? !scaleZ : scaleZ)
          var responseO: String! = String(cString: [98,111,100,101,114,0], encoding: .utf8)!
          var verityC: String! = String(cString: [109,100,99,116,0], encoding: .utf8)!
         scaleZ = responseO.contains("\(scaleZ)")
         responseO.append("\(3 - verityC.count)")
         verityC.append("\(2 >> (RCShowGraphicsTool10.min(2, verityC.count)))")
      for _ in 0 ..< 1 {
         sliderr = "\(((popupN ? 2 : 2)))"
      }
         sliderr = "\(sliderr.count)"
      modelI.append("\(3)")
   }
      modelI = "\(selectedy.count)"

   if 5.1 > (Double(alabelP) * avatarsb) || 1.29 > (avatarsb * 5.1) {
      avatarsb /= RCShowGraphicsTool10.max(Double(alabelP % (RCShowGraphicsTool10.max(selectedy.count, 3))), 2)
   }
      alabelP <<= RCShowGraphicsTool10.min(labs(Int(avatarsb) & modelI.count), 4)
        super.setSelected(selected, animated: animated)

        
   repeat {
       var connect3: String! = String(cString: [112,114,111,102,105,108,105,110,103,0], encoding: .utf8)!
       var datah: String! = String(cString: [97,116,114,97,99,100,97,116,97,0], encoding: .utf8)!
       var rawingq: Float = 4.0
       var veritya: Int = 4
       var holderlabelQ: Double = 0.0
      for _ in 0 ..< 2 {
         connect3.append("\(veritya)")
      }
         datah.append("\(datah.count & Int(rawingq))")
         rawingq += Float(1)
       var connect_: String! = String(cString: [112,111,115,116,112,114,111,99,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         rawingq += Float(connect_.count)
      }
      if (datah.count * Int(rawingq)) < 3 && 5.74 < (Float(datah.count) * rawingq) {
         datah.append("\(2 * Int(holderlabelQ))")
      }
      for _ in 0 ..< 3 {
         rawingq -= (Float(connect_ == (String(cString:[49,0], encoding: .utf8)!) ? Int(holderlabelQ) : connect_.count))
      }
      repeat {
          var timelabelW: String! = String(cString: [98,117,102,114,101,102,0], encoding: .utf8)!
          var phoneL: Float = 1.0
          var sou_: Int = 1
          var failedC: Bool = true
         rawingq += Float(Int(rawingq) << (RCShowGraphicsTool10.min(datah.count, 5)))
         timelabelW = "\(((String(cString:[56,0], encoding: .utf8)!) == timelabelW ? timelabelW.count : (failedC ? 4 : 5)))"
         phoneL *= Float(Int(phoneL) | 3)
         sou_ ^= timelabelW.count
         if rawingq == 1908903.0 {
            break
         }
      } while (rawingq == 1908903.0) && ((Double(Double(2) + holderlabelQ)) == 1.37)
      if (Double(Double(veritya) + holderlabelQ)) <= 3.53 {
          var namelabelQ: Double = 3.0
          var connectM: String! = String(cString: [99,100,97,116,97,0], encoding: .utf8)!
          var resumptionJ: Double = 4.0
          var length0: String! = String(cString: [99,111,109,112,97,110,100,0], encoding: .utf8)!
          var detailv: Int = 0
         holderlabelQ -= Double(1 ^ Int(rawingq))
         namelabelQ += Double(length0.count)
         connectM.append("\(Int(resumptionJ))")
         resumptionJ *= Double(Int(resumptionJ) | detailv)
         length0 = "\(Int(resumptionJ))"
         detailv /= RCShowGraphicsTool10.max(1, detailv * Int(namelabelQ))
      }
         rawingq += Float(2 + datah.count)
      repeat {
          var ends: String! = String(cString: [111,110,121,120,0], encoding: .utf8)!
         datah.append("\(datah.count % 2)")
         ends.append("\(ends.count & ends.count)")
         if 272195 == datah.count {
            break
         }
      } while (connect3.count >= datah.count) && (272195 == datah.count)
      while (rawingq <= Float(veritya)) {
          var gundc: String! = String(cString: [109,118,104,100,0], encoding: .utf8)!
          var containsw: [Any]! = [73, 405]
         rawingq /= RCShowGraphicsTool10.max(Float(connect3.count % (RCShowGraphicsTool10.max(3, 6))), 2)
         gundc = "\(containsw.count)"
         containsw.append(2)
         break
      }
      while (4.10 == (holderlabelQ / (RCShowGraphicsTool10.max(5.56, 5)))) {
         holderlabelQ -= Double(Int(rawingq))
         break
      }
         connect3.append("\(veritya)")
      if 4.34 >= (rawingq - 4.28) {
          var aboutM: Bool = true
          var int_fg: [String: Any]! = [String(cString: [109,111,122,106,112,101,103,0], encoding: .utf8)!:3102.0]
          var launch1: String! = String(cString: [115,99,111,112,101,105,100,0], encoding: .utf8)!
          var register_9c: Double = 5.0
          var popupq: String! = String(cString: [115,105,103,110,97,108,105,110,103,0], encoding: .utf8)!
         connect_.append("\(datah.count)")
         aboutM = 89 >= int_fg.count
         int_fg["\(register_9c)"] = Int(register_9c) << (RCShowGraphicsTool10.min(int_fg.values.count, 3))
         launch1 = "\(popupq.count)"
         popupq = "\(Int(register_9c) * launch1.count)"
      }
      alabelP *= connect3.count
      if 252287 == alabelP {
         break
      }
   } while (252287 == alabelP) && ((avatarsb * 4.35) > 4.50 || 2 > (alabelP * Int(avatarsb)))
    }

    
    override func awakeFromNib() {
       var progressc: Float = 5.0
    var gress4: Double = 1.0
      gress4 *= Double(2)
   repeat {
      progressc *= Float(3)
      if progressc == 3131379.0 {
         break
      }
   } while ((Double(Int(gress4) * 5)) >= 2.47) && (progressc == 3131379.0)

       var vip_: Bool = true
       var meal2: String! = String(cString: [97,99,102,105,108,116,101,114,0], encoding: .utf8)!
       var navigationV: Double = 0.0
         vip_ = !vip_
      if (Int(navigationV) / 3) <= 5 && (meal2.count % (RCShowGraphicsTool10.max(3, 5))) <= 2 {
          var aymento: [Any]! = [String(cString: [111,112,101,110,101,100,0], encoding: .utf8)!, String(cString: [97,115,115,105,103,110,109,101,110,116,0], encoding: .utf8)!, String(cString: [112,114,111,120,105,109,105,116,121,0], encoding: .utf8)!]
          var refreshT: Int = 1
          var time_bss: Double = 0.0
         navigationV /= RCShowGraphicsTool10.max((Double(Int(navigationV) >> (RCShowGraphicsTool10.min(1, labs((vip_ ? 1 : 2)))))), 3)
         aymento.append(refreshT + 2)
         refreshT <<= RCShowGraphicsTool10.min(2, labs(aymento.count ^ 2))
         time_bss += Double(refreshT << (RCShowGraphicsTool10.min(1, labs(2))))
      }
      if !vip_ {
         navigationV += Double(meal2.count)
      }
       var main_jB: String! = String(cString: [101,114,114,108,111,103,0], encoding: .utf8)!
       var timelabelR: String! = String(cString: [109,115,118,105,100,101,111,0], encoding: .utf8)!
         main_jB = "\(timelabelR.count)"
          var aboutD: Int = 0
         meal2 = "\(aboutD)"
      repeat {
         timelabelR = "\((Int(navigationV) & (vip_ ? 3 : 3)))"
         if timelabelR.count == 4044284 {
            break
         }
      } while (main_jB.contains(timelabelR)) && (timelabelR.count == 4044284)
         main_jB = "\(((String(cString:[109,0], encoding: .utf8)!) == timelabelR ? Int(navigationV) : timelabelR.count))"
      for _ in 0 ..< 2 {
          var detectn: [String: Any]! = [String(cString: [99,111,110,102,105,103,117,114,97,98,108,101,0], encoding: .utf8)!:267, String(cString: [110,101,116,119,111,114,107,115,0], encoding: .utf8)!:352]
         main_jB.append("\(meal2.count)")
         detectn = ["\(detectn.values.count)": detectn.count * 1]
      }
      gress4 += Double(3)
        super.awakeFromNib()
        
    }

    
    @IBAction func collectionClick(_ sender: Any) {
       var systemh: String! = String(cString: [98,121,116,101,99,111,100,101,0], encoding: .utf8)!
    var briefD: Int = 0
   if briefD == 3 {
      briefD |= briefD
   }
      briefD /= RCShowGraphicsTool10.max(briefD | 2, 5)
   for _ in 0 ..< 1 {
       var namep: Int = 5
       var loginl: String! = String(cString: [114,101,115,116,0], encoding: .utf8)!
      if 4 >= loginl.count {
         namep >>= RCShowGraphicsTool10.min(2, labs(namep))
      }
         namep -= (loginl == (String(cString:[108,0], encoding: .utf8)!) ? loginl.count : namep)
         namep += 3 + namep
         loginl.append("\(loginl.count)")
         loginl = "\(3 << (RCShowGraphicsTool10.min(2, labs(namep))))"
      for _ in 0 ..< 1 {
         loginl = "\(loginl.count - 2)"
      }
      briefD %= RCShowGraphicsTool10.max(3 * systemh.count, 4)
   }
      briefD |= briefD

       var self_kcN: String! = String(cString: [100,101,108,97,0], encoding: .utf8)!
       var navgationH: [String: Any]! = [String(cString: [112,97,100,100,105,110,103,0], encoding: .utf8)!:String(cString: [98,97,99,107,111,102,102,0], encoding: .utf8)!, String(cString: [102,105,108,101,0], encoding: .utf8)!:String(cString: [114,116,112,112,108,97,121,0], encoding: .utf8)!]
       var systemW: String! = String(cString: [104,101,97,114,116,0], encoding: .utf8)!
          var rmblabeli: String! = String(cString: [99,111,110,100,101,110,115,97,98,108,101,0], encoding: .utf8)!
         navgationH[systemW] = 1 % (RCShowGraphicsTool10.max(4, rmblabeli.count))
      if (navgationH.count - systemW.count) <= 1 {
          var queueI: String! = String(cString: [112,97,116,104,109,116,117,0], encoding: .utf8)!
          var zhidingesI: String! = String(cString: [101,116,104,101,114,0], encoding: .utf8)!
          var connectC: Float = 2.0
          var thresholdq: Bool = false
          var startM: Bool = true
         systemW = "\(zhidingesI.count | 2)"
         queueI.append("\(queueI.count)")
         zhidingesI = "\(queueI.count - 2)"
         connectC *= (Float(Int(connectC) + (thresholdq ? 3 : 4)))
         thresholdq = queueI.count == 53
         startM = startM && thresholdq
      }
         navgationH["\(systemW)"] = 3 << (RCShowGraphicsTool10.min(5, navgationH.keys.count))
      repeat {
         systemW = "\(navgationH.values.count | self_kcN.count)"
         if 4753684 == systemW.count {
            break
         }
      } while (!systemW.hasPrefix("\(navgationH.count)")) && (4753684 == systemW.count)
         navgationH = [self_kcN: systemW.count]
       var elevtL: String! = String(cString: [109,111,110,111,115,112,97,99,101,100,0], encoding: .utf8)!
       var verifyF: String! = String(cString: [100,105,118,105,100,101,110,100,0], encoding: .utf8)!
      if systemW.hasPrefix("\(navgationH.values.count)") {
          var sourceY: [String: Any]! = [String(cString: [108,105,115,116,101,100,0], encoding: .utf8)!:411, String(cString: [109,112,101,103,97,117,100,105,111,116,97,98,0], encoding: .utf8)!:290]
          var checku: [Any]! = [9487.0]
          var btn5: [String: Any]! = [String(cString: [115,101,97,108,97,110,116,0], encoding: .utf8)!:true]
          var huihuao: [String: Any]! = [String(cString: [117,115,101,114,115,0], encoding: .utf8)!:String(cString: [98,110,108,101,0], encoding: .utf8)!, String(cString: [104,105,103,104,98,105,116,115,0], encoding: .utf8)!:String(cString: [103,114,97,100,105,101,110,116,0], encoding: .utf8)!, String(cString: [118,98,112,114,105,110,116,102,0], encoding: .utf8)!:String(cString: [115,101,103,109,101,110,116,115,0], encoding: .utf8)!]
          var menuT: Double = 1.0
         systemW.append("\(navgationH.values.count)")
         sourceY = ["\(huihuao.values.count)": huihuao.count]
         checku = [sourceY.values.count >> (RCShowGraphicsTool10.min(checku.count, 3))]
         btn5 = ["\(sourceY.keys.count)": 1 | sourceY.keys.count]
         menuT /= RCShowGraphicsTool10.max(4, Double(huihuao.keys.count))
      }
      while (!verifyF.hasSuffix(elevtL)) {
          var alamofireD: Bool = true
          var objk: Double = 0.0
          var type_0u_: String! = String(cString: [99,104,114,111,109,97,116,105,99,0], encoding: .utf8)!
          var attributesb: Bool = true
         elevtL = "\(((attributesb ? 1 : 5)))"
         alamofireD = !type_0u_.contains("\(alamofireD)")
         objk /= RCShowGraphicsTool10.max(3, Double(2 * Int(objk)))
         type_0u_.append("\(2)")
         attributesb = (9 <= ((!alamofireD ? 9 : type_0u_.count) % (RCShowGraphicsTool10.max(type_0u_.count, 3))))
         break
      }
      for _ in 0 ..< 1 {
         elevtL.append("\(3 | elevtL.count)")
      }
      systemh.append("\(navgationH.values.count)")
   repeat {
      systemh = "\((systemh == (String(cString:[87,0], encoding: .utf8)!) ? systemh.count : systemh.count))"
      if 4413158 == systemh.count {
         break
      }
   } while (4413158 == systemh.count) && (systemh.count <= 4)
        dataSource?.deleteCollectionCell(cell: self)
    }
}

 class RCShowGraphicsTool10 {
    static func min<Q: Comparable>(_ r1: Q, _ r2: Q) -> Q {
        return r1 > r2 ? r2 : r1
    }
    
    static func max<Q: Comparable>(_ r1: Q, _ r2: Q) -> Q {
        return r1 > r2 ? r1 : r2
    }
}