
import Foundation

import UIKit

class DLaunchController: UIViewController {
private var info_margin: Float = 0.0
var strObj_arr: [Any]!
var did_size: Double = 0.0

@discardableResult
 func respondsRingQlabelControllTableView(sublyoutLabeel: [Any]!, toolsStart: [String: Any]!) -> UITableView! {
    var opend: Bool = false
    var processW: Double = 2.0
   if 1.54 > (processW * 2.37) && opend {
       var handlert: Bool = false
      while (handlert) {
         handlert = !handlert
         break
      }
      repeat {
         handlert = !handlert
         if handlert ? !handlert : handlert {
            break
         }
      } while (handlert ? !handlert : handlert) && (handlert)
      repeat {
         handlert = handlert || !handlert
         if handlert ? !handlert : handlert {
            break
         }
      } while (!handlert) && (handlert ? !handlert : handlert)
      processW /= MKIAnswerTool7.max(5, (Double((handlert ? 3 : 2) % (MKIAnswerTool7.max(5, Int(processW))))))
   }
      processW -= (Double(Int(processW) | (opend ? 3 : 5)))
   if 2.82 < processW {
      processW /= MKIAnswerTool7.max((Double((opend ? 2 : 3) & Int(processW))), 5)
   }
   repeat {
      processW /= MKIAnswerTool7.max(Double(2 * Int(processW)), 2)
      if 163115.0 == processW {
         break
      }
   } while (5.70 > processW) && (163115.0 == processW)
     var pickerPasteboard: Bool = true
     let backWhile_m: UIImageView! = UIImageView()
     let bigSize_u: UIView! = UIView(frame:CGRect.zero)
     let unewsQuery: UILabel! = UILabel()
    var impactDeferred:UITableView! = UITableView(frame:CGRect.zero)
    backWhile_m.frame = CGRect(x: 42, y: 147, width: 0, height: 0)
    backWhile_m.alpha = 0.8;
    backWhile_m.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    backWhile_m.image = UIImage(named:String(cString: [114,111,119,115,0], encoding: .utf8)!)
    backWhile_m.contentMode = .scaleAspectFit
    backWhile_m.animationRepeatCount = 5
    
    var backWhile_mFrame = backWhile_m.frame
    backWhile_mFrame.size = CGSize(width: 264, height: 150)
    backWhile_m.frame = backWhile_mFrame
    if backWhile_m.isHidden {
         backWhile_m.isHidden = false
    }
    if backWhile_m.alpha > 0.0 {
         backWhile_m.alpha = 0.0
    }
    if !backWhile_m.isUserInteractionEnabled {
         backWhile_m.isUserInteractionEnabled = true
    }

    bigSize_u.alpha = 0.7;
    bigSize_u.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    bigSize_u.frame = CGRect(x: 52, y: 33, width: 0, height: 0)
    
    var bigSize_uFrame = bigSize_u.frame
    bigSize_uFrame.size = CGSize(width: 103, height: 222)
    bigSize_u.frame = bigSize_uFrame
    if bigSize_u.alpha > 0.0 {
         bigSize_u.alpha = 0.0
    }
    if bigSize_u.isHidden {
         bigSize_u.isHidden = false
    }
    if !bigSize_u.isUserInteractionEnabled {
         bigSize_u.isUserInteractionEnabled = true
    }

    unewsQuery.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    unewsQuery.alpha = 0.1
    unewsQuery.frame = CGRect(x: 201, y: 57, width: 0, height: 0)
    unewsQuery.font = UIFont.systemFont(ofSize:16)
    unewsQuery.text = ""
    unewsQuery.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    unewsQuery.textAlignment = .center
    
    var unewsQueryFrame = unewsQuery.frame
    unewsQueryFrame.size = CGSize(width: 285, height: 58)
    unewsQuery.frame = unewsQueryFrame
    if unewsQuery.alpha > 0.0 {
         unewsQuery.alpha = 0.0
    }
    if unewsQuery.isHidden {
         unewsQuery.isHidden = false
    }
    if !unewsQuery.isUserInteractionEnabled {
         unewsQuery.isUserInteractionEnabled = true
    }

    impactDeferred.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    impactDeferred.alpha = 0.0
    impactDeferred.frame = CGRect(x: 4, y: 114, width: 0, height: 0)
    impactDeferred.delegate = nil
    impactDeferred.dataSource = nil
    impactDeferred.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    var impactDeferredFrame = impactDeferred.frame
    impactDeferredFrame.size = CGSize(width: 98, height: 173)
    impactDeferred.frame = impactDeferredFrame
    if impactDeferred.isHidden {
         impactDeferred.isHidden = false
    }
    if impactDeferred.alpha > 0.0 {
         impactDeferred.alpha = 0.0
    }
    if !impactDeferred.isUserInteractionEnabled {
         impactDeferred.isUserInteractionEnabled = true
    }

    return impactDeferred

}









    @IBAction func back(_ sender: Any) {

         let maxreadersHelp: UITableView! = respondsRingQlabelControllTableView(sublyoutLabeel:[String(cString: [111,118,101,114,119,114,105,116,101,0], encoding: .utf8)!], toolsStart:[String(cString: [100,105,115,116,114,105,99,116,0], encoding: .utf8)!:938, String(cString: [109,97,114,103,105,110,115,0], encoding: .utf8)!:883, String(cString: [114,105,118,97,116,101,0], encoding: .utf8)!:825])

      if maxreadersHelp != nil {
          let maxreadersHelp_tag = maxreadersHelp.tag
          self.view.addSubview(maxreadersHelp)
      }



       var shuJ: String! = String(cString: [115,112,101,97,107,101,114,0], encoding: .utf8)!
    var valuelabelD: Double = 1.0
   while ((3 | shuJ.count) <= 2) {
      valuelabelD += Double(Int(valuelabelD) + 2)
      break
   }
       var rowW: String! = String(cString: [97,118,101,114,97,103,101,115,0], encoding: .utf8)!
       var avatar0: Float = 4.0
       var navigatione: Float = 4.0
         rowW = "\(Int(avatar0) + rowW.count)"
         avatar0 *= Float(Int(navigatione))
          var user1: String! = String(cString: [114,101,109,111,116,101,0], encoding: .utf8)!
          var strH: String! = String(cString: [115,117,98,98,97,110,100,0], encoding: .utf8)!
         rowW = "\(Int(navigatione) / (MKIAnswerTool7.max(9, rowW.count)))"
         user1 = "\(1 - user1.count)"
         strH = "\(user1.count)"
       var stringu: String! = String(cString: [105,110,116,114,110,108,0], encoding: .utf8)!
       var dictd: String! = String(cString: [118,99,111,100,101,99,0], encoding: .utf8)!
          var othert: [String: Any]! = [String(cString: [118,97,108,105,100,0], encoding: .utf8)!:705, String(cString: [118,114,97,115,116,101,114,0], encoding: .utf8)!:50]
          var imgJ: String! = String(cString: [102,114,97,109,101,98,117,102,102,101,114,115,0], encoding: .utf8)!
         navigatione *= Float(Int(avatar0) >> (MKIAnswerTool7.min(rowW.count, 1)))
         othert[imgJ] = imgJ.count
         rowW.append("\(1)")
         navigatione -= Float(rowW.count >> (MKIAnswerTool7.min(labs(1), 5)))
          var total_: Int = 3
          var backgroundR: [String: Any]! = [String(cString: [115,108,102,0], encoding: .utf8)!:20, String(cString: [98,105,110,115,0], encoding: .utf8)!:813]
         rowW = "\(stringu.count << (MKIAnswerTool7.min(5, labs(total_))))"
         total_ &= backgroundR.count / (MKIAnswerTool7.max(2, 9))
         backgroundR["\(backgroundR.keys.count)"] = backgroundR.count
          var user1a: [Any]! = [843, 425]
          var adeleteY: String! = String(cString: [104,111,115,116,110,97,109,101,0], encoding: .utf8)!
         stringu.append("\(((String(cString:[71,0], encoding: .utf8)!) == dictd ? dictd.count : stringu.count))")
         user1a.append(user1a.count * adeleteY.count)
         adeleteY.append("\(3 | adeleteY.count)")
      shuJ.append("\(1)")

   if 5.38 >= (valuelabelD + 1.77) && (1 / (MKIAnswerTool7.max(2, shuJ.count))) >= 4 {
       var findf: String! = String(cString: [115,116,114,101,97,109,97,98,108,101,0], encoding: .utf8)!
       var navM: Float = 4.0
       var aboutv: Double = 5.0
       var response0: [Any]! = [UILabel()]
       var checkK: [String: Any]! = [String(cString: [114,101,100,105,114,101,99,116,105,111,110,0], encoding: .utf8)!:String(cString: [97,118,114,101,115,97,109,112,108,101,114,101,115,0], encoding: .utf8)!, String(cString: [97,114,97,98,105,99,0], encoding: .utf8)!:String(cString: [114,101,99,101,105,118,101,0], encoding: .utf8)!]
       var messagesy: Double = 5.0
       var voiceB: Double = 3.0
          var namelabeld: String! = String(cString: [114,101,115,112,111,110,115,101,115,0], encoding: .utf8)!
          var avatarsq: String! = String(cString: [100,114,101,102,0], encoding: .utf8)!
          var urlZ: String! = String(cString: [116,111,107,101,110,105,122,101,0], encoding: .utf8)!
         voiceB *= Double(findf.count)
         namelabeld.append("\(avatarsq.count << (MKIAnswerTool7.min(labs(2), 3)))")
         avatarsq = "\(avatarsq.count + 2)"
         urlZ = "\(avatarsq.count)"
          var date4: String! = String(cString: [106,115,101,112,0], encoding: .utf8)!
          var itemsQ: String! = String(cString: [101,108,101,109,101,110,116,115,0], encoding: .utf8)!
          var paintu: [String: Any]! = [String(cString: [97,115,99,98,110,0], encoding: .utf8)!:String(cString: [114,119,103,116,0], encoding: .utf8)!, String(cString: [111,118,101,114,108,97,112,0], encoding: .utf8)!:String(cString: [100,113,117,97,110,116,0], encoding: .utf8)!, String(cString: [116,97,117,0], encoding: .utf8)!:String(cString: [113,116,112,97,108,101,116,116,101,0], encoding: .utf8)!]
         voiceB -= Double(Int(navM) ^ 3)
         date4.append("\(date4.count << (MKIAnswerTool7.min(labs(2), 2)))")
         itemsQ = "\(2)"
         paintu = [itemsQ: date4.count + itemsQ.count]
       var drainx: Double = 1.0
         messagesy += Double(response0.count)
      for _ in 0 ..< 1 {
         voiceB += Double(response0.count)
      }
         messagesy += Double(checkK.values.count | findf.count)
      while ((1.19 / (MKIAnswerTool7.max(9, messagesy))) == 4.55 && (voiceB / (MKIAnswerTool7.max(1.19, 7))) == 1.53) {
         messagesy *= Double(3)
         break
      }
      while (Int(aboutv) < findf.count) {
          var socketj: Double = 4.0
          var restoreG: String! = String(cString: [115,119,115,99,97,108,101,0], encoding: .utf8)!
          var speakd: String! = String(cString: [98,117,116,116,111,110,115,0], encoding: .utf8)!
         findf.append("\(Int(drainx))")
         socketj *= Double(restoreG.count % (MKIAnswerTool7.max(3, Int(socketj))))
         restoreG = "\(((String(cString:[84,0], encoding: .utf8)!) == restoreG ? restoreG.count : Int(socketj)))"
         speakd.append("\(speakd.count / 3)")
         break
      }
          var backy: [Any]! = [556, 905, 437]
          var cellr: String! = String(cString: [112,105,112,101,0], encoding: .utf8)!
         navM /= MKIAnswerTool7.max(5, Float(checkK.values.count / (MKIAnswerTool7.max(cellr.count, 4))))
         backy.append(backy.count ^ 1)
         cellr.append("\(backy.count)")
      if checkK["\(aboutv)"] == nil {
         aboutv += Double(2 >> (MKIAnswerTool7.min(5, checkK.count)))
      }
         messagesy -= Double(Int(aboutv) * Int(drainx))
          var elevtK: [Any]! = [false]
         navM *= Float(Int(aboutv) / 1)
         elevtK.append(elevtK.count & 3)
       var showK: String! = String(cString: [112,114,111,100,117,99,101,114,0], encoding: .utf8)!
      if response0.contains { $0 as? Double == drainx } {
          var sharedY: String! = String(cString: [100,105,116,104,101,114,0], encoding: .utf8)!
          var gesture4: Int = 3
          var queryV: String! = String(cString: [115,116,105,99,107,101,114,115,0], encoding: .utf8)!
          var bottomk: Float = 1.0
         response0 = [Int(navM) - queryV.count]
         sharedY = "\(gesture4)"
         gesture4 >>= MKIAnswerTool7.min(sharedY.count, 5)
         queryV.append("\(gesture4)")
         bottomk += (Float((String(cString:[118,0], encoding: .utf8)!) == sharedY ? sharedY.count : gesture4))
      }
      shuJ.append("\(1 / (MKIAnswerTool7.max(8, response0.count)))")
   }
        dismiss(animated: true)
    }

@discardableResult
 func navgationFailedAffineImageView() -> UIImageView! {
    var datas6: Float = 5.0
    var toplayouty: String! = String(cString: [109,95,49,95,97,112,112,101,110,100,0], encoding: .utf8)!
   repeat {
      datas6 /= MKIAnswerTool7.max(2, Float(toplayouty.count))
      if datas6 == 3833977.0 {
         break
      }
   } while (datas6 == 3833977.0) && ((1.2 - datas6) < 2.43 && 3.71 < (datas6 - 1.2))
   for _ in 0 ..< 3 {
      datas6 *= Float(toplayouty.count)
   }
   for _ in 0 ..< 2 {
      toplayouty.append("\(3)")
   }
      datas6 += Float(toplayouty.count)
     let refreshLabel: String! = String(cString: [119,104,101,101,108,0], encoding: .utf8)!
     let executeUpdate_6b: [Any]! = [374, 262]
     var loadingCode: UIButton! = UIButton()
     var imageHolderlabel: Bool = true
    var nonlinearDistributionsUintpow = UIImageView()
    loadingCode.alpha = 0.2;
    loadingCode.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    loadingCode.frame = CGRect(x: 241, y: 121, width: 0, height: 0)
    loadingCode.setBackgroundImage(UIImage(named:String(cString: [115,116,114,105,110,103,0], encoding: .utf8)!), for: .normal)
    loadingCode.titleLabel?.font = UIFont.systemFont(ofSize:16)
    loadingCode.setImage(UIImage(named:String(cString: [114,109,98,108,97,98,101,108,0], encoding: .utf8)!), for: .normal)
    loadingCode.setTitle("", for: .normal)
    
    var loadingCodeFrame = loadingCode.frame
    loadingCodeFrame.size = CGSize(width: 162, height: 74)
    loadingCode.frame = loadingCodeFrame
    if loadingCode.alpha > 0.0 {
         loadingCode.alpha = 0.0
    }
    if loadingCode.isHidden {
         loadingCode.isHidden = false
    }
    if !loadingCode.isUserInteractionEnabled {
         loadingCode.isUserInteractionEnabled = true
    }

    nonlinearDistributionsUintpow.frame = CGRect(x: 128, y: 132, width: 0, height: 0)
    nonlinearDistributionsUintpow.alpha = 0.8;
    nonlinearDistributionsUintpow.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nonlinearDistributionsUintpow.contentMode = .scaleAspectFit
    nonlinearDistributionsUintpow.animationRepeatCount = 5
    nonlinearDistributionsUintpow.image = UIImage(named:String(cString: [99,104,117,97,110,103,122,117,111,0], encoding: .utf8)!)

    
    var nonlinearDistributionsUintpowFrame = nonlinearDistributionsUintpow.frame
    nonlinearDistributionsUintpowFrame.size = CGSize(width: 146, height: 121)
    nonlinearDistributionsUintpow.frame = nonlinearDistributionsUintpowFrame
    if nonlinearDistributionsUintpow.isHidden {
         nonlinearDistributionsUintpow.isHidden = false
    }
    if nonlinearDistributionsUintpow.alpha > 0.0 {
         nonlinearDistributionsUintpow.alpha = 0.0
    }
    if !nonlinearDistributionsUintpow.isUserInteractionEnabled {
         nonlinearDistributionsUintpow.isUserInteractionEnabled = true
    }

    return nonlinearDistributionsUintpow

}






    override func viewDidLoad() {

         let includeLossy: UIImageView! = navgationFailedAffineImageView()

      if includeLossy != nil {
          let includeLossy_tag = includeLossy.tag
          self.view.addSubview(includeLossy)
      }
      else {
          print("includeLossy is nil")      }



       var bottomD: Int = 1
    var rawingM: String! = String(cString: [98,114,111,107,101,110,0], encoding: .utf8)!
    var fullZ: String! = String(cString: [113,95,54,56,0], encoding: .utf8)!
    var descripto: Float = 3.0
      bottomD &= rawingM.count
      fullZ = "\(rawingM.count)"

   while ((Int(descripto) + rawingM.count) <= 5) {
      rawingM.append("\(fullZ.count)")
      break
   }
      bottomD |= 3
        super.viewDidLoad()

        
    }
    

}

 struct MKIAnswerTool7 {
    static func min<K: Comparable>(_ c1: K, _ c2: K) -> K {
        return c1 > c2 ? c2 : c1
    }
    
    static func max<K: Comparable>(_ c1: K, _ c2: K) -> K {
        return c1 > c2 ? c1 : c2
    }
}