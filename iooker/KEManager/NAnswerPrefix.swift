
import Foundation

import UIKit
import Speech
import AVFAudio
import AdSupport
import Accelerate
import SVProgressHUD

enum FSGraphicsX {
    case start
    case end
}

class NAnswerPrefix: NSObject {
private var headers_idx: Int = 0
private var abroadcastIosChatDictionary: [String: Any]?
var regionLeft_arr: [Any]!
var createNot_iArr: [Any]!


    
    let utils: NeoNuiTts = NeoNuiTts.get_instance()
    let voicePlayer: YGRecordsImage = YGRecordsImage()
    typealias CompletionHandler = (FSGraphicsX) -> Void
    var completionHandler: CompletionHandler?
    
    static let shared: NAnswerPrefix = {
       var jiluq: String! = String(cString: [116,114,105,101,115,0], encoding: .utf8)!
    var remarkm: String! = String(cString: [98,111,117,110,99,105,110,101,115,115,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
       var n_titlew: String! = String(cString: [99,111,115,105,103,110,97,116,111,114,121,0], encoding: .utf8)!
       var generateS: String! = String(cString: [98,114,97,99,107,101,116,115,0], encoding: .utf8)!
       var records6: [Any]! = [UILabel(frame:CGRect(x: 288, y: 454, width: 0, height: 0))]
       var c_centerd: Double = 2.0
       var mine4: [String: Any]! = [String(cString: [116,114,97,112,0], encoding: .utf8)!:375, String(cString: [118,116,97,103,0], encoding: .utf8)!:802]
          var popupC: Int = 1
          var alabelw: String! = String(cString: [112,108,97,121,97,98,108,101,0], encoding: .utf8)!
          var iosj: String! = String(cString: [102,97,100,101,111,117,116,0], encoding: .utf8)!
         n_titlew.append("\(alabelw.count)")
         popupC <<= UNavigationTool7.min(labs(iosj.count - popupC), 1)
         alabelw.append("\(popupC >> (UNavigationTool7.min(labs(3), 5)))")
         iosj.append("\(popupC)")
      repeat {
         n_titlew.append("\(generateS.count)")
         if n_titlew == (String(cString:[103,104,50,112,101,0], encoding: .utf8)!) {
            break
         }
      } while (n_titlew.hasSuffix("\(mine4.values.count)")) && (n_titlew == (String(cString:[103,104,50,112,101,0], encoding: .utf8)!))
         records6.append(generateS.count)
         generateS = "\(((String(cString:[66,0], encoding: .utf8)!) == n_titlew ? records6.count : n_titlew.count))"
      repeat {
         n_titlew = "\(generateS.count - n_titlew.count)"
         if n_titlew.count == 2169815 {
            break
         }
      } while (n_titlew.count == 2169815) && ((mine4.keys.count | 4) <= 3)
      while (generateS == n_titlew) {
          var statuesz: String! = String(cString: [116,114,117,101,0], encoding: .utf8)!
          var nickname1: String! = String(cString: [99,97,110,99,101,108,108,97,98,108,101,0], encoding: .utf8)!
          var reusabler: String! = String(cString: [101,110,99,114,121,112,116,105,110,103,0], encoding: .utf8)!
          var titlelabelT: Double = 0.0
          var history3: Bool = true
         n_titlew = "\(1)"
         statuesz = "\(1 * reusabler.count)"
         nickname1.append("\(statuesz.count << (UNavigationTool7.min(labs(2), 1)))")
         reusabler = "\(Int(titlelabelT) << (UNavigationTool7.min(statuesz.count, 2)))"
         titlelabelT *= Double(Int(titlelabelT) | 1)
         break
      }
      for _ in 0 ..< 3 {
         mine4 = ["\(records6.count)": 2 >> (UNavigationTool7.min(4, records6.count))]
      }
         n_titlew.append("\(Int(c_centerd) ^ generateS.count)")
         mine4 = [generateS: ((String(cString:[114,0], encoding: .utf8)!) == n_titlew ? n_titlew.count : generateS.count)]
      repeat {
          var rmbF: Int = 3
          var toolsi: String! = String(cString: [97,100,100,105,0], encoding: .utf8)!
         c_centerd -= Double(mine4.values.count - 3)
         rmbF |= toolsi.count & 1
         toolsi.append("\(3)")
         if 1856937.0 == c_centerd {
            break
         }
      } while (generateS.hasSuffix("\(c_centerd)")) && (1856937.0 == c_centerd)
      while (generateS.count == 4) {
         generateS.append("\(1 ^ n_titlew.count)")
         break
      }
      while ((4.22 + c_centerd) == 4.77) {
         c_centerd /= UNavigationTool7.max((Double(generateS == (String(cString:[107,0], encoding: .utf8)!) ? records6.count : generateS.count)), 5)
         break
      }
      while (generateS.hasPrefix("\(records6.count)")) {
          var responsee: [String: Any]! = [String(cString: [107,105,115,115,0], encoding: .utf8)!:String(cString: [102,105,108,116,101,114,115,0], encoding: .utf8)!]
          var qheadere: Double = 1.0
         records6 = [records6.count]
         responsee = ["\(responsee.count)": responsee.keys.count & Int(qheadere)]
         qheadere /= UNavigationTool7.max(1, Double(Int(qheadere)))
         break
      }
      while ((records6.count * generateS.count) == 3 && 3 == (generateS.count * records6.count)) {
         generateS = "\(((String(cString:[70,0], encoding: .utf8)!) == n_titlew ? n_titlew.count : mine4.keys.count))"
         break
      }
          var avatarZ: Bool = false
          var d_objecty: Int = 2
          var applyZ: [String: Any]! = [String(cString: [122,108,105,98,112,114,105,109,101,0], encoding: .utf8)!:458, String(cString: [114,101,115,117,109,101,100,0], encoding: .utf8)!:879]
         generateS.append("\(n_titlew.count % (UNavigationTool7.max(8, mine4.keys.count)))")
         avatarZ = 84 >= d_objecty
         d_objecty += d_objecty
         applyZ = ["\(applyZ.values.count)": applyZ.values.count - d_objecty]
      remarkm.append("\(generateS.count >> (UNavigationTool7.min(labs(1), 1)))")
   }

        let instance = NAnswerPrefix()
   while (jiluq.count <= 1) {
      jiluq.append("\(jiluq.count / 1)")
      break
   }
        instance.utils.delegate = instance
      jiluq.append("\(jiluq.count & jiluq.count)")
        instance.voicePlayer.delegate = instance
        return instance
    }()

@discardableResult
 func updatedEmailMinimumStylePermisReusable(resourcesRequest: Float) -> String! {
    var phone7: Int = 0
    var listenl: String! = String(cString: [117,110,100,101,114,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
       var completedz: Double = 3.0
       var avatar9: String! = String(cString: [109,98,98,121,0], encoding: .utf8)!
       var contextN: String! = String(cString: [101,97,115,121,0], encoding: .utf8)!
       var alamofirea: Double = 2.0
       var type_ps: String! = String(cString: [121,99,111,99,103,114,103,98,97,0], encoding: .utf8)!
      while (2.63 <= (completedz + 2.32)) {
         completedz -= Double(Int(alamofirea) * 1)
         break
      }
         avatar9.append("\(type_ps.count)")
      for _ in 0 ..< 1 {
         avatar9 = "\(3 / (UNavigationTool7.max(5, Int(alamofirea))))"
      }
          var gundD: Double = 5.0
          var attributesT: [String: Any]! = [String(cString: [114,97,116,101,99,111,110,116,114,111,108,95,113,95,56,49,0], encoding: .utf8)!:233, String(cString: [111,110,116,114,97,115,116,0], encoding: .utf8)!:931]
          var endl: String! = String(cString: [107,101,121,100,105,114,0], encoding: .utf8)!
         contextN = "\(3 - Int(completedz))"
         gundD += Double(attributesT.values.count - 1)
         attributesT = [endl: 3 + endl.count]
         contextN = "\(Int(completedz))"
       var sectionS: String! = String(cString: [111,109,109,111,110,0], encoding: .utf8)!
       var class_rqC: String! = String(cString: [119,95,54,53,95,99,114,111,112,112,101,100,0], encoding: .utf8)!
         type_ps = "\(2)"
          var messagesX: String! = String(cString: [114,98,115,112,0], encoding: .utf8)!
          var i_titleU: Int = 4
         alamofirea += (Double((String(cString:[86,0], encoding: .utf8)!) == sectionS ? Int(completedz) : sectionS.count))
         messagesX.append("\(1)")
         i_titleU <<= UNavigationTool7.min(labs(i_titleU ^ messagesX.count), 4)
         class_rqC.append("\(1)")
      if contextN.count == 1 {
          var qlabelq: Bool = true
          var instancen: Double = 2.0
          var nicknamez: String! = String(cString: [110,111,100,101,0], encoding: .utf8)!
          var avatarsM: [Any]! = [String(cString: [99,111,108,115,112,97,110,0], encoding: .utf8)!, String(cString: [99,117,108,102,114,101,113,0], encoding: .utf8)!, String(cString: [97,118,101,114,97,103,101,105,105,114,100,95,50,95,52,56,0], encoding: .utf8)!]
         contextN = "\(class_rqC.count)"
         qlabelq = ((avatarsM.count | (!qlabelq ? 47 : avatarsM.count)) <= 47)
         instancen += (Double(nicknamez == (String(cString:[112,0], encoding: .utf8)!) ? nicknamez.count : avatarsM.count))
      }
      repeat {
         type_ps.append("\((class_rqC == (String(cString:[100,0], encoding: .utf8)!) ? class_rqC.count : Int(completedz)))")
         if type_ps.count == 371495 {
            break
         }
      } while (type_ps.count == 371495) && (type_ps.hasPrefix("\(alamofirea)"))
         sectionS.append("\(3 | class_rqC.count)")
         completedz += Double(avatar9.count % 2)
          var modelp: Float = 3.0
          var emailn: String! = String(cString: [118,111,108,117,109,101,0], encoding: .utf8)!
          var sliderp: [Any]! = [417, 763, 657]
         contextN.append("\(emailn.count / 2)")
         modelp -= Float(1)
         emailn.append("\(Int(modelp))")
         sliderp = [sliderp.count | Int(modelp)]
         contextN = "\(3)"
      listenl = "\(listenl.count)"
   }
      listenl.append("\(listenl.count)")
      phone7 += phone7 % (UNavigationTool7.max(7, listenl.count))
      phone7 -= listenl.count | 1
   return listenl

}





    
    func startPlay(fontName: String = "", message: String, completionHandler: CompletionHandler?) {

             updatedEmailMinimumStylePermisReusable(resourcesRequest:4525.0)


       var gundong2: String! = String(cString: [114,101,97,99,116,0], encoding: .utf8)!
    var param6: Float = 0.0
    var parameterV: String! = String(cString: [116,104,114,101,97,100,110,97,109,101,0], encoding: .utf8)!
   while (parameterV.count <= Int(param6)) {
      param6 -= Float(parameterV.count)
      break
   }

   repeat {
       var handleU: Double = 2.0
       var login5: [String: Any]! = [String(cString: [116,114,97,99,101,0], encoding: .utf8)!:String(cString: [97,108,105,103,110,0], encoding: .utf8)!, String(cString: [100,97,105,108,121,0], encoding: .utf8)!:String(cString: [116,114,97,99,101,114,0], encoding: .utf8)!]
       var detection7: Float = 4.0
       var channell: Bool = false
         detection7 /= UNavigationTool7.max((Float((channell ? 3 : 3) / (UNavigationTool7.max(Int(detection7), 7)))), 5)
       var attributesm: Float = 1.0
          var scaleM: String! = String(cString: [114,97,112,104,105,99,115,0], encoding: .utf8)!
         channell = detection7 == attributesm
         scaleM = "\(scaleM.count / 3)"
          var generateF: Double = 3.0
          var descript0: Bool = true
          var yinD: Float = 4.0
         channell = !channell
         generateF *= Double(2 >> (UNavigationTool7.min(labs(Int(generateF)), 1)))
         descript0 = !descript0
         yinD *= (Float(Int(yinD) | (descript0 ? 1 : 3)))
         login5 = ["\(handleU)": ((channell ? 2 : 5) + Int(handleU))]
      if channell && (handleU + 1.6) < 2.87 {
          var type_r7F: Double = 4.0
         channell = detection7 <= 75.95
         type_r7F *= Double(2 % (UNavigationTool7.max(3, Int(type_r7F))))
      }
         login5["\(detection7)"] = 2
      if !login5.keys.contains("\(handleU)") {
         login5["\(attributesm)"] = login5.values.count * 2
      }
      repeat {
         channell = !channell
         if channell ? !channell : channell {
            break
         }
      } while (channell ? !channell : channell) && (attributesm > 2.39)
         login5 = ["\(attributesm)": Int(attributesm) & Int(detection7)]
      while (channell) {
          var againB: Bool = true
          var callX: Double = 0.0
          var dic0: Double = 3.0
         channell = handleU == 14.7
         againB = !againB
         callX -= (Double((againB ? 2 : 4) >> (UNavigationTool7.min(labs(Int(callX)), 4))))
         dic0 -= Double(Int(callX) - 3)
         break
      }
      for _ in 0 ..< 2 {
          var parameterB: String! = String(cString: [122,109,98,118,0], encoding: .utf8)!
          var screenI: Bool = false
          var u_centerf: Bool = false
          var statuesU: String! = String(cString: [108,108,118,105,100,100,115,112,101,110,99,0], encoding: .utf8)!
          var detaillabel7: [String: Any]! = [String(cString: [99,97,110,99,101,108,0], encoding: .utf8)!:369, String(cString: [99,111,108,111,114,101,100,0], encoding: .utf8)!:385, String(cString: [116,105,107,101,114,0], encoding: .utf8)!:613]
         login5 = ["\(login5.values.count)": ((String(cString:[80,0], encoding: .utf8)!) == statuesU ? statuesU.count : login5.keys.count)]
         parameterB.append("\((parameterB.count + (screenI ? 4 : 1)))")
         screenI = u_centerf
         detaillabel7["\(u_centerf)"] = (2 / (UNavigationTool7.max(2, (u_centerf ? 5 : 3))))
      }
      param6 *= Float(2)
      if 2268713.0 == param6 {
         break
      }
   } while (parameterV.count >= Int(param6)) && (2268713.0 == param6)
        
        stopPlay(false)
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) { [self]  in
   repeat {
      gundong2.append("\(gundong2.count | Int(param6))")
      if (String(cString:[108,54,114,118,102,0], encoding: .utf8)!) == gundong2 {
         break
      }
   } while ((String(cString:[108,54,114,118,102,0], encoding: .utf8)!) == gundong2) && (gundong2.count < parameterV.count)
            
            self.completionHandler = completionHandler
      param6 *= Float(gundong2.count)
            
            utils.nui_tts_initialize(getInitParam(), logLevel: NuiSdkLogLevel(0), saveLog: true)
       var elevte: Double = 1.0
      repeat {
         elevte /= UNavigationTool7.max(3, Double(Int(elevte) << (UNavigationTool7.min(2, labs(Int(elevte))))))
         if 1124534.0 == elevte {
            break
         }
      } while (4.70 <= (elevte / 4.11) && 4.11 <= (elevte / (UNavigationTool7.max(elevte, 5)))) && (1124534.0 == elevte)
         elevte *= Double(3 >> (UNavigationTool7.min(labs(Int(elevte)), 1)))
         elevte /= UNavigationTool7.max(1, Double(2 - Int(elevte)))
      param6 -= Float(3)

      param6 *= Float(3)
            if fontName == "" {
                if let font_name: String = UserDefaults.standard.object(forKey: "font_name") as? String {
                    utils.nui_tts_set_param("font_name", value: font_name)
                }else {
                    utils.nui_tts_set_param("font_name", value: "zhimiao_emo")
                }
            }else {
                utils.nui_tts_set_param("font_name", value: fontName)
            }
            let fixedImage = UserDefaults.standard.float(forKey: "rate")
            if fixedImage > 0 {
                utils.nui_tts_set_param("speed_level", value: String(format: "%0.2f", fixedImage))
            }
            utils.nui_tts_play("1", taskId: "", text: message)
            
        }
    }

    
    func stopPlay(_ isBlock: Bool = true) {
       var aboutQ: String! = String(cString: [102,111,110,116,115,0], encoding: .utf8)!
    var service3: [Any]! = [true]
    var weixinW: String! = String(cString: [105,110,116,101,114,111,112,0], encoding: .utf8)!
       var apassP: String! = String(cString: [110,112,97,116,99,104,101,115,0], encoding: .utf8)!
       var while_ql2: String! = String(cString: [105,110,115,116,97,110,116,0], encoding: .utf8)!
       var controlll: [Any]! = [965, 571]
          var gundongl: [Any]! = [461, 827]
          var reusablei: Int = 4
         apassP = "\(2)"
         gundongl = [reusablei * 1]
         reusablei %= UNavigationTool7.max(1, gundongl.count)
      for _ in 0 ..< 1 {
         apassP.append("\(apassP.count)")
      }
      repeat {
         apassP = "\(2)"
         if apassP == (String(cString:[114,121,100,99,57,104,0], encoding: .utf8)!) {
            break
         }
      } while (apassP == (String(cString:[114,121,100,99,57,104,0], encoding: .utf8)!)) && (5 > (apassP.count + 4))
          var z_widthB: String! = String(cString: [115,121,115,116,101,109,0], encoding: .utf8)!
          var long_5T: String! = String(cString: [98,97,115,101,0], encoding: .utf8)!
          var handingd: String! = String(cString: [103,101,110,101,114,97,103,101,0], encoding: .utf8)!
         while_ql2 = "\(long_5T.count)"
         z_widthB = "\(3)"
         long_5T.append("\(z_widthB.count + handingd.count)")
         handingd.append("\(2)")
       var linesF: [Any]! = [672, 319, 544]
         linesF.append(linesF.count << (UNavigationTool7.min(labs(1), 1)))
      while (controlll.count == 2) {
         controlll = [2 >> (UNavigationTool7.min(5, linesF.count))]
         break
      }
          var answerF: String! = String(cString: [97,97,114,99,104,0], encoding: .utf8)!
         controlll.append(while_ql2.count)
         answerF = "\(answerF.count | 2)"
      while (while_ql2.contains("\(apassP.count)")) {
         apassP = "\(1)"
         break
      }
      service3.append(service3.count)

   while (5 < (service3.count | 4) || 4 < (aboutQ.count | service3.count)) {
      service3 = [service3.count]
      break
   }
        
        voicePlayer.stop()
      aboutQ = "\(service3.count & aboutQ.count)"
        utils.nui_tts_cancel(nil)
   while ((weixinW.count / (UNavigationTool7.max(4, service3.count))) == 5 || (weixinW.count / 5) == 4) {
      weixinW = "\(service3.count >> (UNavigationTool7.min(weixinW.count, 2)))"
      break
   }
        if isBlock { self.completionHandler?(.end) }
        
       var scrolld: String! = String(cString: [97,104,101,97,100,0], encoding: .utf8)!
       var controllq: Double = 0.0
       var sliderP: Double = 3.0
      for _ in 0 ..< 3 {
          var paramQ: Double = 4.0
          var downb: String! = String(cString: [116,114,99,0], encoding: .utf8)!
          var aboutl: String! = String(cString: [99,111,112,121,120,0], encoding: .utf8)!
          var collectionf: Double = 2.0
         controllq *= Double(downb.count)
         paramQ /= UNavigationTool7.max(4, (Double(aboutl == (String(cString:[57,0], encoding: .utf8)!) ? aboutl.count : Int(collectionf))))
         downb.append("\(1)")
         collectionf /= UNavigationTool7.max(Double(Int(paramQ)), 5)
      }
      repeat {
         scrolld = "\(1)"
         if (String(cString:[57,122,119,95,54,108,0], encoding: .utf8)!) == scrolld {
            break
         }
      } while ((String(cString:[57,122,119,95,54,108,0], encoding: .utf8)!) == scrolld) && (2 < (Int(controllq) * scrolld.count) && (controllq * Double(scrolld.count)) < 3.40)
          var gifm: Int = 1
         sliderP /= UNavigationTool7.max(4, Double(gifm))
      while (5.56 >= (sliderP - controllq)) {
          var menu8: Bool = false
          var messageX: Double = 0.0
         controllq /= UNavigationTool7.max(1, Double(Int(controllq) + 3))
         menu8 = messageX == 49.48
         messageX += Double(Int(messageX) & 3)
         break
      }
      for _ in 0 ..< 3 {
         scrolld = "\(Int(controllq) / (UNavigationTool7.max(Int(sliderP), 8)))"
      }
      if !scrolld.contains("\(sliderP)") {
          var gundH: String! = String(cString: [113,95,53,55,95,100,114,111,112,112,101,114,0], encoding: .utf8)!
         scrolld = "\(((String(cString:[85,0], encoding: .utf8)!) == gundH ? gundH.count : Int(sliderP)))"
      }
      for _ in 0 ..< 1 {
         sliderP -= Double(Int(sliderP))
      }
         sliderP /= UNavigationTool7.max(Double(Int(controllq) >> (UNavigationTool7.min(2, labs(1)))), 5)
      for _ in 0 ..< 2 {
          var generateD: String! = String(cString: [114,101,103,105,115,116,101,114,101,114,0], encoding: .utf8)!
          var detaillabelP: Int = 5
          var itemY: String! = String(cString: [103,101,116,100,105,103,105,116,0], encoding: .utf8)!
          var b_viewc: String! = String(cString: [119,104,101,114,101,0], encoding: .utf8)!
          var size_76: String! = String(cString: [114,103,98,120,0], encoding: .utf8)!
         controllq += Double(2 * Int(sliderP))
         generateD.append("\((size_76 == (String(cString:[80,0], encoding: .utf8)!) ? b_viewc.count : size_76.count))")
         detaillabelP &= b_viewc.count * 3
         itemY.append("\(detaillabelP ^ itemY.count)")
      }
      service3 = [weixinW.count]
    }
}

func createPath() -> String {
       var scaleb: String! = String(cString: [109,97,110,97,103,101,114,0], encoding: .utf8)!
    var nameM: String! = String(cString: [110,118,101,110,99,0], encoding: .utf8)!
   if !scaleb.hasSuffix("\(nameM.count)") {
      nameM.append("\(2)")
   }

   while (nameM != scaleb) {
      scaleb = "\(1)"
      break
   }
       
    let success = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true)
      nameM.append("\(nameM.count)")
    let constraint = success[0]
   if 3 > scaleb.count || 3 > nameM.count {
      nameM = "\(scaleb.count)"
   }
   
    let screen = FileManager.default
    let reusableCel = (constraint as NSString).appendingPathComponent("voices")
    
    do {
        try screen.createDirectory(atPath: reusableCel, withIntermediateDirectories: true, attributes: nil)
        print("文件夹创建成功")
    } catch {
        print("文件夹创建失败")
    }
    return reusableCel
}

func getInitParam() -> String {
       var currentR: Double = 3.0
    var nexto: [Any]! = [152, 87]
   while (currentR < Double(nexto.count)) {
      nexto = [nexto.count * Int(currentR)]
      break
   }

       
    guard let strResourcesBundle = Bundle.main.path(forResource: "Resources", ofType: "bundle") else {
        return ""
    }
    
    let chuang = Bundle(path: strResourcesBundle)?.resourcePath ?? ""
       var orderO: [String: Any]! = [String(cString: [109,111,110,111,116,111,110,105,116,121,0], encoding: .utf8)!:true]
       var cleand: Int = 3
       var channelO: String! = String(cString: [112,101,110,100,105,110,103,115,0], encoding: .utf8)!
          var sharedp: Int = 2
          var interfacei: Float = 1.0
         cleand -= channelO.count << (UNavigationTool7.min(labs(3), 1))
         sharedp &= 1
         interfacei -= Float(Int(interfacei) + sharedp)
      repeat {
         orderO[channelO] = ((String(cString:[120,0], encoding: .utf8)!) == channelO ? cleand : channelO.count)
         if orderO.count == 846605 {
            break
         }
      } while (orderO.count == 846605) && ((cleand % 3) >= 5 && 3 >= (cleand % (UNavigationTool7.max(10, orderO.count))))
         cleand >>= UNavigationTool7.min(2, labs(3))
       var wangU: String! = String(cString: [109,108,115,100,0], encoding: .utf8)!
       var homeT: [Any]! = [[274, 57]]
       var main_sm: [Any]! = [822, 576, 720]
          var desclabelq: Double = 2.0
          var a_widthA: String! = String(cString: [122,111,111,109,101,100,0], encoding: .utf8)!
         main_sm.append(3)
         desclabelq += Double(2 * Int(desclabelq))
         a_widthA = "\(a_widthA.count)"
          var sublyouth: String! = String(cString: [100,101,99,111,100,101,114,0], encoding: .utf8)!
          var speakC: Int = 4
          var labeelV: String! = String(cString: [115,104,97,108,108,0], encoding: .utf8)!
         cleand <<= UNavigationTool7.min(main_sm.count, 2)
         sublyouth = "\(speakC / 3)"
         speakC <<= UNavigationTool7.min(4, labs(((String(cString:[113,0], encoding: .utf8)!) == labeelV ? speakC : labeelV.count)))
      repeat {
         wangU.append("\(main_sm.count)")
         if wangU == (String(cString:[117,54,51,112,0], encoding: .utf8)!) {
            break
         }
      } while (wangU == (String(cString:[117,54,51,112,0], encoding: .utf8)!)) && ((2 & homeT.count) == 1)
         cleand %= UNavigationTool7.max(1, orderO.count & channelO.count)
      currentR *= Double(2 >> (UNavigationTool7.min(4, orderO.keys.count)))
    let createG = ASIdentifierManager.shared().advertisingIdentifier.uuidString
   while ((2.55 + currentR) <= 4.83 || 4 <= (3 + nexto.count)) {
      nexto.append(Int(currentR) & 2)
      break
   }
 
    let album = createPath()
   repeat {
      currentR *= Double(Int(currentR))
      if currentR == 3463356.0 {
         break
      }
   } while ((nexto.count / 3) < 3 || (4.3 + currentR) < 4.6) && (currentR == 3463356.0)
    
    if let token = UserDefaults.standard.string(forKey: "AliToken") {
        
        var gressg = [String: Any]()
        gressg["app_key"] = "FwsOLV8DKcHopkcl"
        gressg["token"] = token
        gressg["workspace"] = chuang
        gressg["debug_path"] = album
        gressg["device_id"] = createG
        gressg["url"] = "wss://nls-gateway.cn-shanghai.aliyuncs.com:443/ws/v1"
        gressg["mode_type"] = "2"

        do {
            let paramm = try JSONSerialization.data(withJSONObject: gressg, options: .prettyPrinted)
            if let jsonStr = String(data: paramm, encoding: .utf8) {
                return jsonStr
            }
        } catch {
            print("JSONSerialization error: \(error)")
        }
    }else {
        SVProgressHUD.showError(withStatus: "阿里Token无效")
    }

    return ""
}

func checkAliToken() {
       var alamofireU: Double = 0.0
    var statusE: Int = 4
    var scale_: String! = String(cString: [114,97,116,101,99,111,110,116,114,111,108,0], encoding: .utf8)!
       var checky: Double = 5.0
      if (checky * 1.19) <= 2.58 {
         checky -= Double(Int(checky) ^ Int(checky))
      }
         checky += Double(2)
          var weixin9: Double = 4.0
         checky += Double(2)
         weixin9 *= Double(Int(weixin9))
      statusE &= statusE

    
    QVCenterBase.shared.post(urlSuffix: "/app/getAliyunToken") { (result: Result<VShouModity, SImageBaseE>) in
      statusE /= UNavigationTool7.max(1, 5)
        switch result {
        case .success(let model):
            if model.code == 200 {
                UserDefaults.standard.setValue(model.data, forKey: "AliToken")
      scale_ = "\(scale_.count)"
                print("阿里Token\(model.data)")
      statusE <<= UNavigationTool7.min(labs(statusE % (UNavigationTool7.max(scale_.count, 2))), 2)
                UserDefaults.standard.synchronize()
            }
            
            break
        case.failure(_):
            SVProgressHUD.showError(withStatus: "获取Token失败");
            break
        }
    }

      alamofireU -= (Double((String(cString:[50,0], encoding: .utf8)!) == scale_ ? scale_.count : statusE))
}


extension NAnswerPrefix: NeoNuiTtsDelegate, AWLXDelegateMenu {

@discardableResult
 func elementsBeforeAnimateRateChannelsWrapper(promptSelected: Float) -> String! {
    var editz: Double = 0.0
    var failedI: String! = String(cString: [120,117,116,105,108,0], encoding: .utf8)!
   while (3.25 < (editz + Double(failedI.count)) || (failedI.count + Int(editz)) < 5) {
      failedI.append("\((failedI == (String(cString:[77,0], encoding: .utf8)!) ? failedI.count : Int(editz)))")
      break
   }
   repeat {
       var end8: Double = 0.0
       var edits: String! = String(cString: [105,100,101,110,116,105,102,105,101,114,0], encoding: .utf8)!
          var name3: Float = 5.0
         edits.append("\(Int(name3) & 3)")
         edits.append("\(edits.count << (UNavigationTool7.min(4, labs(Int(end8)))))")
          var applicationb: String! = String(cString: [118,111,105,99,101,115,0], encoding: .utf8)!
          var long_r0B: String! = String(cString: [101,110,101,114,103,121,0], encoding: .utf8)!
          var completev: Int = 0
         edits = "\(3 >> (UNavigationTool7.min(3, labs(completev))))"
         applicationb = "\(1)"
         long_r0B = "\(2 + applicationb.count)"
         completev -= 3
         edits = "\(3)"
      for _ in 0 ..< 1 {
         edits = "\(edits.count)"
      }
         edits = "\(edits.count)"
      failedI.append("\(Int(end8))")
      if failedI == (String(cString:[109,53,110,100,121,121,99,110,57,0], encoding: .utf8)!) {
         break
      }
   } while (failedI.hasSuffix("\(editz)")) && (failedI == (String(cString:[109,53,110,100,121,121,99,110,57,0], encoding: .utf8)!))
   if 1 >= (failedI.count / 5) || 1 >= (5 << (UNavigationTool7.min(5, failedI.count))) {
      editz *= Double(Int(editz))
   }
   return failedI

}





    
    func onNuiTtsUserdataCallback(_ info: UnsafeMutablePointer<CChar>!, infoLen info_len: Int32, buffer: UnsafeMutablePointer<CChar>!, len: Int32, taskId task_id: UnsafeMutablePointer<CChar>!) {

             elementsBeforeAnimateRateChannelsWrapper(promptSelected:3084.0)


       var answerG: Bool = false
    var dicv: Int = 4
    var not__3D: [Any]! = [150, 453]
       var callbackS: String! = String(cString: [112,114,101,115,101,110,116,97,98,108,101,0], encoding: .utf8)!
       var class_4qC: String! = String(cString: [113,116,115,0], encoding: .utf8)!
       var prefix_g_: String! = String(cString: [112,99,105,112,104,101,114,116,101,120,116,0], encoding: .utf8)!
       var prompte: Bool = true
       var sharedG: Bool = true
         callbackS = "\(prefix_g_.count)"
         prefix_g_ = "\(class_4qC.count)"
          var slider2: Int = 1
         sharedG = !callbackS.hasSuffix("\(prompte)")
         slider2 *= 2 << (UNavigationTool7.min(5, labs(slider2)))
      repeat {
         prompte = (!prompte ? sharedG : prompte)
         if prompte ? !prompte : prompte {
            break
         }
      } while (!sharedG || prompte) && (prompte ? !prompte : prompte)
         prefix_g_.append("\(1)")
         callbackS.append("\(1)")
      repeat {
          var alamofireU: String! = String(cString: [103,116,101,115,116,0], encoding: .utf8)!
          var avatarsN: [Any]! = [863, 384]
          var contextK: Double = 4.0
          var yinW: Bool = false
         prefix_g_ = "\(((yinW ? 5 : 5) * Int(contextK)))"
         alamofireU = "\(((String(cString:[86,0], encoding: .utf8)!) == alamofireU ? avatarsN.count : alamofireU.count))"
         avatarsN.append((alamofireU == (String(cString:[66,0], encoding: .utf8)!) ? alamofireU.count : avatarsN.count))
         contextK *= (Double(alamofireU == (String(cString:[66,0], encoding: .utf8)!) ? avatarsN.count : alamofireU.count))
         yinW = avatarsN.count <= 90 || (String(cString:[87,0], encoding: .utf8)!) == alamofireU
         if prefix_g_.count == 4679424 {
            break
         }
      } while (!prefix_g_.hasPrefix(callbackS)) && (prefix_g_.count == 4679424)
      while (class_4qC.count < 4) {
         sharedG = prefix_g_.count >= 90
         break
      }
      dicv ^= prefix_g_.count >> (UNavigationTool7.min(5, class_4qC.count))

   repeat {
       var begin1: Double = 0.0
       var read4: String! = String(cString: [99,117,109,117,108,97,116,105,118,101,0], encoding: .utf8)!
       var navigatione: Double = 4.0
       var urls2: String! = String(cString: [116,114,97,99,107,101,100,0], encoding: .utf8)!
       var main_hM: String! = String(cString: [105,109,112,108,105,99,105,116,0], encoding: .utf8)!
          var strp: String! = String(cString: [108,105,102,116,0], encoding: .utf8)!
          var pasteboardu: Bool = false
          var login8: String! = String(cString: [114,116,112,0], encoding: .utf8)!
         urls2.append("\(urls2.count)")
         strp.append("\(strp.count)")
         pasteboardu = login8.hasSuffix("\(pasteboardu)")
         login8.append("\(login8.count / (UNavigationTool7.max(strp.count, 4)))")
         main_hM.append("\(2)")
         navigatione -= Double(urls2.count)
         urls2.append("\(urls2.count)")
      repeat {
          var itemu: String! = String(cString: [100,99,97,109,97,116,104,0], encoding: .utf8)!
          var recordingM: Int = 3
          var networkZ: Double = 1.0
          var safeE: String! = String(cString: [113,117,97,108,105,116,121,0], encoding: .utf8)!
         main_hM = "\(Int(begin1) & safeE.count)"
         itemu = "\(Int(networkZ) ^ 1)"
         recordingM ^= 3
         networkZ += Double(recordingM % 2)
         safeE.append("\(3)")
         if 3907390 == main_hM.count {
            break
         }
      } while (3907390 == main_hM.count) && (5.28 == (5.68 / (UNavigationTool7.max(3, begin1))))
      for _ in 0 ..< 2 {
          var areaU: Float = 1.0
         begin1 -= Double(1 + Int(navigatione))
         areaU *= Float(Int(areaU) ^ 2)
      }
       var showr: [String: Any]! = [String(cString: [115,121,110,99,104,114,111,110,105,122,97,98,108,101,0], encoding: .utf8)!:180, String(cString: [99,105,114,99,117,108,97,116,105,110,103,0], encoding: .utf8)!:557]
       var sepak3: [String: Any]! = [String(cString: [100,117,114,103,101,114,107,105,110,103,0], encoding: .utf8)!:279, String(cString: [97,108,98,117,109,115,0], encoding: .utf8)!:607, String(cString: [97,108,103,115,0], encoding: .utf8)!:167]
      while (!main_hM.contains("\(showr.keys.count)")) {
         main_hM.append("\(Int(navigatione) ^ urls2.count)")
         break
      }
      dicv %= UNavigationTool7.max(2, ((String(cString:[120,0], encoding: .utf8)!) == read4 ? Int(navigatione) : read4.count))
      if 3397221 == dicv {
         break
      }
   } while (answerG) && (3397221 == dicv)

       var historyg: Float = 0.0
       var headB: String! = String(cString: [119,105,101,110,101,114,0], encoding: .utf8)!
          var collF: [Any]! = [140, 468, 146]
         headB.append("\(Int(historyg))")
         collF = [collF.count]
         historyg /= UNavigationTool7.max(5, Float(headB.count))
         historyg -= Float(Int(historyg) + 2)
       var handlery: Float = 1.0
      if (Double(Int(handlery) - 3)) == 4.37 {
         historyg *= Float(headB.count)
      }
       var endw: String! = String(cString: [108,105,103,104,116,0], encoding: .utf8)!
       var readZ: String! = String(cString: [100,97,98,97,115,101,0], encoding: .utf8)!
      dicv &= dicv
        if len > 0 {
            voicePlayer.write(buffer, length: Int32(len))
        }
       
      answerG = (!answerG ? answerG : answerG)
      answerG = !answerG
    }

    
    func onNuiTtsEventCallback(_ event: NuiSdkTtsEvent, taskId taskid: UnsafeMutablePointer<CChar>!, code: Int32) {
       var containsd: String! = String(cString: [116,111,111,108,99,104,97,105,110,0], encoding: .utf8)!
    var item8: Double = 3.0
    var zhidingesr: String! = String(cString: [115,117,98,109,111,100,101,108,0], encoding: .utf8)!
       var modityW: String! = String(cString: [114,118,100,115,112,0], encoding: .utf8)!
       var speedsb: [String: Any]! = [String(cString: [115,104,105,102,116,0], encoding: .utf8)!:441, String(cString: [115,112,108,97,110,101,0], encoding: .utf8)!:993]
       var v_imagen: Bool = false
      if (3 * speedsb.values.count) >= 4 {
         speedsb[modityW] = ((String(cString:[81,0], encoding: .utf8)!) == modityW ? (v_imagen ? 3 : 3) : modityW.count)
      }
         v_imagen = (77 <= (speedsb.values.count % (UNavigationTool7.max(5, (!v_imagen ? speedsb.values.count : 87)))))
         modityW = "\(((v_imagen ? 4 : 2) >> (UNavigationTool7.min(labs(3), 2))))"
       var channel1: String! = String(cString: [111,112,101,110,109,112,116,0], encoding: .utf8)!
          var headerJ: Double = 2.0
          var queryd: Float = 4.0
         v_imagen = 58 < speedsb.values.count
         headerJ *= Double(Int(queryd) & 2)
         queryd -= Float(Int(headerJ) / 1)
         speedsb[channel1] = channel1.count / 1
      for _ in 0 ..< 1 {
         speedsb[channel1] = 2 & channel1.count
      }
      repeat {
         speedsb = ["\(speedsb.keys.count)": 1 + speedsb.count]
         if 3049030 == speedsb.count {
            break
         }
      } while (3049030 == speedsb.count) && (1 > (1 >> (UNavigationTool7.min(2, speedsb.count))) && (1 >> (UNavigationTool7.min(4, channel1.count))) > 5)
       var rnewst: String! = String(cString: [116,101,108,101,109,101,116,114,121,0], encoding: .utf8)!
      containsd = "\(3 + containsd.count)"

       var safeK: String! = String(cString: [112,111,111,108,115,0], encoding: .utf8)!
       var containsP: [Any]! = [String(cString: [109,105,110,105,0], encoding: .utf8)!, String(cString: [105,110,118,97,108,105,100,97,116,105,111,110,0], encoding: .utf8)!, String(cString: [115,104,111,114,116,101,114,0], encoding: .utf8)!]
       var long_3j1: String! = String(cString: [100,100,114,97,110,103,101,0], encoding: .utf8)!
         containsP.append(long_3j1.count)
      while (long_3j1.count > safeK.count) {
          var zoom5: Double = 1.0
          var aymentl: [String: Any]! = [String(cString: [111,118,101,114,97,108,108,0], encoding: .utf8)!:String(cString: [116,101,115,115,0], encoding: .utf8)!, String(cString: [99,116,120,105,100,120,105,110,99,0], encoding: .utf8)!:String(cString: [97,114,101,97,115,0], encoding: .utf8)!, String(cString: [101,112,111,108,108,0], encoding: .utf8)!:String(cString: [106,119,116,0], encoding: .utf8)!]
         long_3j1 = "\(Int(zoom5))"
         zoom5 *= Double(aymentl.count)
         aymentl = ["\(aymentl.count)": aymentl.values.count << (UNavigationTool7.min(aymentl.values.count, 1))]
         break
      }
      for _ in 0 ..< 3 {
         safeK = "\(long_3j1.count)"
      }
      if 3 == long_3j1.count {
          var listenO: String! = String(cString: [112,111,105,110,116,111,99,116,0], encoding: .utf8)!
          var arrayl: Int = 2
          var clickE: Int = 0
         safeK.append("\(safeK.count)")
         listenO = "\(listenO.count)"
         arrayl += (listenO == (String(cString:[87,0], encoding: .utf8)!) ? listenO.count : arrayl)
         clickE += 2 / (UNavigationTool7.max(2, clickE))
      }
       var lineH: String! = String(cString: [104,117,100,0], encoding: .utf8)!
       var modityl: String! = String(cString: [110,111,110,114,100,0], encoding: .utf8)!
       var flowi: Double = 1.0
       var statuslabelE: Double = 1.0
      repeat {
          var celll: Float = 5.0
          var chuangd: [Any]! = [String(cString: [100,105,115,97,98,108,101,95,118,95,50,0], encoding: .utf8)!]
          var clicky: String! = String(cString: [97,117,116,104,111,114,115,0], encoding: .utf8)!
          var configurationd: Double = 2.0
          var numF: Double = 3.0
         modityl = "\(Int(flowi) / (UNavigationTool7.max(lineH.count, 8)))"
         celll -= Float(Int(celll) & clicky.count)
         chuangd = [3]
         clicky = "\(clicky.count)"
         configurationd += Double(2)
         numF -= Double(2)
         if modityl == (String(cString:[100,55,97,108,101,52,119,99,49,105,0], encoding: .utf8)!) {
            break
         }
      } while (modityl == (String(cString:[100,55,97,108,101,52,119,99,49,105,0], encoding: .utf8)!)) && (modityl.count >= safeK.count)
      if 5 == (4 ^ safeK.count) || (4 ^ safeK.count) == 1 {
          var display5: Double = 1.0
         safeK.append("\(lineH.count | 2)")
         display5 += Double(Int(display5) << (UNavigationTool7.min(3, labs(3))))
      }
          var userdataG: Double = 2.0
         lineH.append("\((modityl == (String(cString:[71,0], encoding: .utf8)!) ? modityl.count : safeK.count))")
         userdataG /= UNavigationTool7.max(2, Double(Int(userdataG) - 2))
      zhidingesr = "\(((String(cString:[86,0], encoding: .utf8)!) == zhidingesr ? containsd.count : zhidingesr.count))"
            
       if event == NuiSdkTtsEvent(rawValue: 0) {
           
           do {
               try AVAudioSession.sharedInstance().setCategory(.playback, options: .mixWithOthers)
      item8 /= UNavigationTool7.max(Double(Int(item8) ^ zhidingesr.count), 1)

   while ((item8 - 2.43) < 3.6 && 3 < (Int(item8) - zhidingesr.count)) {
       var messagesk: String! = String(cString: [115,99,97,110,116,97,98,108,101,0], encoding: .utf8)!
       var listP: String! = String(cString: [98,121,114,121,105,0], encoding: .utf8)!
       var queryf: String! = String(cString: [111,119,110,101,114,115,0], encoding: .utf8)!
       var recordingvH: Double = 1.0
       var celldata2: Int = 0
         recordingvH += Double(Int(recordingvH) - celldata2)
      repeat {
          var promptZ: Double = 2.0
          var briefk: Double = 2.0
         queryf = "\(messagesk.count)"
         promptZ += Double(2 / (UNavigationTool7.max(9, Int(promptZ))))
         briefk /= UNavigationTool7.max(Double(Int(briefk) + 3), 4)
         if 1858335 == queryf.count {
            break
         }
      } while (1 == (1 ^ celldata2)) && (1858335 == queryf.count)
      if (celldata2 * Int(recordingvH)) < 5 || (3.56 * recordingvH) < 5.91 {
          var time_wnJ: String! = String(cString: [104,101,105,103,104,116,115,0], encoding: .utf8)!
         recordingvH /= UNavigationTool7.max(3, Double(Int(recordingvH) + listP.count))
         time_wnJ = "\(time_wnJ.count * 2)"
      }
         messagesk = "\(Int(recordingvH) | celldata2)"
      repeat {
          var confirmB: Double = 2.0
          var areaD: String! = String(cString: [109,105,115,115,105,110,103,0], encoding: .utf8)!
          var sublyoutu: Int = 3
         messagesk = "\(1 >> (UNavigationTool7.min(labs(Int(confirmB)), 2)))"
         confirmB -= Double(1)
         areaD.append("\(3 / (UNavigationTool7.max(1, areaD.count)))")
         sublyoutu %= UNavigationTool7.max(4, sublyoutu / (UNavigationTool7.max(areaD.count, 4)))
         if messagesk.count == 4362166 {
            break
         }
      } while ((5 % (UNavigationTool7.max(10, messagesk.count))) > 4 || (messagesk.count % (UNavigationTool7.max(3, celldata2))) > 5) && (messagesk.count == 4362166)
      for _ in 0 ..< 2 {
         queryf = "\(messagesk.count << (UNavigationTool7.min(3, labs(Int(recordingvH)))))"
      }
      for _ in 0 ..< 1 {
         queryf.append("\(2)")
      }
      while (!queryf.hasSuffix("\(recordingvH)")) {
          var long_4kk: [Any]! = [418, 941, 597]
          var briefZ: Double = 2.0
          var responses: String! = String(cString: [115,117,98,118,105,101,119,0], encoding: .utf8)!
          var labeeln: String! = String(cString: [103,100,105,103,114,97,98,0], encoding: .utf8)!
         recordingvH /= UNavigationTool7.max(1, Double(labeeln.count | queryf.count))
         long_4kk = [1 % (UNavigationTool7.max(2, long_4kk.count))]
         briefZ *= (Double(responses == (String(cString:[52,0], encoding: .utf8)!) ? responses.count : Int(briefZ)))
         labeeln.append("\(responses.count % 3)")
         break
      }
         messagesk.append("\(Int(recordingvH) ^ 2)")
       var parameters8: Double = 1.0
       var filledt: Double = 2.0
         celldata2 |= 2
          var loadings: [Any]! = [797, 969, 50]
          var cleanupf: Double = 0.0
         queryf = "\(Int(recordingvH) / 3)"
         loadings.append(Int(cleanupf) / 2)
         cleanupf += Double(Int(cleanupf) >> (UNavigationTool7.min(loadings.count, 5)))
       var l_objectl: String! = String(cString: [99,117,114,114,101,110,116,0], encoding: .utf8)!
      if 3 > (listP.count / (UNavigationTool7.max(3, 1))) {
          var answerS: String! = String(cString: [104,101,120,99,104,97,114,105,110,116,0], encoding: .utf8)!
          var chooseS: [Any]! = [String(cString: [115,117,109,0], encoding: .utf8)!, String(cString: [115,116,109,116,0], encoding: .utf8)!]
          var topw: String! = String(cString: [115,112,114,111,112,0], encoding: .utf8)!
          var records0: Bool = false
         listP = "\(3)"
         answerS.append("\(topw.count)")
         chooseS.append(((String(cString:[53,0], encoding: .utf8)!) == answerS ? (records0 ? 1 : 1) : answerS.count))
         topw = "\((answerS.count & (records0 ? 4 : 5)))"
      }
          var scaleP: String! = String(cString: [115,108,105,100,101,0], encoding: .utf8)!
         listP.append("\(celldata2 * messagesk.count)")
         scaleP.append("\(scaleP.count)")
      zhidingesr = "\((queryf == (String(cString:[111,0], encoding: .utf8)!) ? Int(item8) : queryf.count))"
      break
   }
               try AVAudioSession.sharedInstance().setActive(true)
           } catch {
               print("Failed to set audio session category: \(error)")
           }
           voicePlayer.play()
      item8 *= Double(containsd.count)
           self.completionHandler?(.start)

       } else if event == NuiSdkTtsEvent(rawValue: 1) || event == NuiSdkTtsEvent(rawValue: 2) || event == NuiSdkTtsEvent(rawValue: 5) {
           if event == NuiSdkTtsEvent(rawValue: 1) {
               
               voicePlayer.drain()
           } else {
               
           }
           if event == NuiSdkTtsEvent(rawValue: 5) {
               stopPlay(false)
      item8 -= Double(zhidingesr.count)
               let phone = utils.nui_tts_get_param("error_msg")
               print(phone)
           }
       }
    }

@discardableResult
 func searchPrintedPointFits(menuMenu: [String: Any]!, restoreRequest: [String: Any]!, baseModity: String!) -> Double {
    var numberlabelo: String! = String(cString: [114,101,115,105,103,110,115,0], encoding: .utf8)!
    var navs: String! = String(cString: [100,105,115,116,111,114,116,105,111,110,0], encoding: .utf8)!
      navs.append("\(1)")
   while (numberlabelo.hasSuffix("\(navs.count)")) {
      numberlabelo.append("\(numberlabelo.count * navs.count)")
      break
   }
     let gesturePlaying: Int = 8575
     let statuesWeb: Double = 1854.0
    var intersectionPubicSeparated:Double = 0
    intersectionPubicSeparated -= Double(gesturePlaying)
    intersectionPubicSeparated += statuesWeb

    return intersectionPubicSeparated

}





    
    func playerDidFinish() {

             searchPrintedPointFits(menuMenu:[String(cString: [108,95,49,48,95,105,99,101,99,97,115,116,0], encoding: .utf8)!:UILabel(frame:CGRect(x: 61, y: 319, width: 0, height: 0))], restoreRequest:[String(cString: [102,105,108,116,0], encoding: .utf8)!:false], baseModity:String(cString: [110,97,109,101,115,0], encoding: .utf8)!)


       var changedN: Double = 0.0
    var transactionsL: String! = String(cString: [117,110,115,104,105,102,116,0], encoding: .utf8)!
    var valuelabelY: String! = String(cString: [112,112,102,108,97,103,115,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
       var handlerv: Double = 5.0
       var contextG: [Any]! = [422, 664, 513]
       var imagesB: [String: Any]! = [String(cString: [98,111,114,105,110,103,115,115,108,0], encoding: .utf8)!:8671.0]
       var messagesT: Float = 0.0
      if (imagesB.keys.count % 4) <= 3 && (handlerv * Double(imagesB.keys.count)) <= 5.54 {
          var activitylabelM: String! = String(cString: [102,111,114,109,97,116,116,101,100,0], encoding: .utf8)!
          var utils8: String! = String(cString: [116,97,98,108,101,116,0], encoding: .utf8)!
         imagesB[activitylabelM] = 3 & Int(handlerv)
         activitylabelM = "\(utils8.count << (UNavigationTool7.min(labs(2), 1)))"
         utils8 = "\(utils8.count & utils8.count)"
      }
          var itemsj: Float = 0.0
          var identifierN: Double = 3.0
         handlerv -= Double(imagesB.count)
         itemsj -= Float(3 << (UNavigationTool7.min(labs(Int(itemsj)), 5)))
         identifierN /= UNavigationTool7.max(1, Double(Int(identifierN) & Int(itemsj)))
       var details5: String! = String(cString: [105,110,100,101,110,116,0], encoding: .utf8)!
      repeat {
          var keyI: Bool = true
          var keyC: Double = 2.0
         details5 = "\(2 >> (UNavigationTool7.min(3, contextG.count)))"
         keyI = !keyI
         keyC *= (Double(Int(keyC) * (keyI ? 1 : 4)))
         if details5 == (String(cString:[108,115,55,114,108,103,0], encoding: .utf8)!) {
            break
         }
      } while (5 > details5.count) && (details5 == (String(cString:[108,115,55,114,108,103,0], encoding: .utf8)!))
         imagesB[details5] = 2
      repeat {
          var qheaderj: String! = String(cString: [99,111,100,101,99,114,97,119,0], encoding: .utf8)!
          var holderlabelM: Bool = true
         contextG.append(details5.count | 2)
         qheaderj = "\(((String(cString:[103,0], encoding: .utf8)!) == qheaderj ? qheaderj.count : (holderlabelM ? 5 : 1)))"
         holderlabelM = qheaderj.count > 77
         if 2340376 == contextG.count {
            break
         }
      } while (2340376 == contextG.count) && (!details5.contains("\(contextG.count)"))
      repeat {
          var prompt3: String! = String(cString: [102,97,105,108,105,110,103,0], encoding: .utf8)!
          var stopV: [Any]! = [915, 810]
         imagesB = ["\(contextG.count)": contextG.count % (UNavigationTool7.max(1, Int(handlerv)))]
         prompt3 = "\(stopV.count)"
         stopV.append(prompt3.count)
         if imagesB.count == 4538963 {
            break
         }
      } while (imagesB.count == 4538963) && (messagesT <= 2.30)
         details5.append("\(imagesB.count >> (UNavigationTool7.min(1, labs(Int(messagesT)))))")
      for _ in 0 ..< 2 {
         contextG.append(contextG.count)
      }
      if !details5.hasPrefix("\(messagesT)") {
         messagesT -= Float(contextG.count)
      }
      repeat {
          var loadingo: [String: Any]! = [String(cString: [118,97,114,105,97,110,116,0], encoding: .utf8)!:117, String(cString: [101,118,114,99,100,97,116,97,0], encoding: .utf8)!:494]
          var buffer4: Float = 0.0
          var stringN: Int = 3
         messagesT /= UNavigationTool7.max(Float(1 ^ contextG.count), 5)
         loadingo = ["\(loadingo.keys.count)": 1]
         buffer4 *= Float(3 + loadingo.values.count)
         stringN ^= 1 ^ Int(buffer4)
         if 3476281.0 == messagesT {
            break
         }
      } while ((1.4 * messagesT) < 4.12 && 2 < (5 >> (UNavigationTool7.min(5, imagesB.count)))) && (3476281.0 == messagesT)
       var voicec: Double = 2.0
      valuelabelY.append("\(contextG.count % 2)")
   }
       var addressz: [String: Any]! = [String(cString: [109,101,110,117,115,0], encoding: .utf8)!:543, String(cString: [116,114,97,110,115,102,101,114,114,97,98,108,101,0], encoding: .utf8)!:712]
       var with_5eo: String! = String(cString: [108,111,111,115,101,0], encoding: .utf8)!
      while (4 <= (with_5eo.count % (UNavigationTool7.max(9, addressz.keys.count))) || 4 <= (addressz.keys.count % 4)) {
         with_5eo.append("\(2 % (UNavigationTool7.max(4, addressz.count)))")
         break
      }
          var systemN: [String: Any]! = [String(cString: [115,101,113,118,105,100,101,111,0], encoding: .utf8)!:806, String(cString: [112,97,114,115,101,100,0], encoding: .utf8)!:17, String(cString: [103,97,109,109,97,118,97,108,0], encoding: .utf8)!:385]
          var k_image3: String! = String(cString: [100,115,116,0], encoding: .utf8)!
         with_5eo.append("\(addressz.count)")
         systemN["\(k_image3)"] = k_image3.count + 1
      transactionsL.append("\(addressz.count)")

   repeat {
       var vertical0: [String: Any]! = [String(cString: [100,105,97,103,0], encoding: .utf8)!:617, String(cString: [106,114,110,108,0], encoding: .utf8)!:146]
       var main_gn: [Any]! = [348, 796]
      if main_gn.count <= vertical0.count {
          var didZ: String! = String(cString: [97,108,119,97,121,115,0], encoding: .utf8)!
          var souW: String! = String(cString: [108,101,102,116,109,111,115,116,0], encoding: .utf8)!
         main_gn.append(didZ.count >> (UNavigationTool7.min(5, main_gn.count)))
         didZ = "\(souW.count | souW.count)"
      }
         main_gn = [2 + vertical0.count]
      if !vertical0.keys.contains("\(main_gn.count)") {
          var dicE: String! = String(cString: [115,117,98,115,101,115,115,105,111,110,0], encoding: .utf8)!
          var userS: Int = 4
         main_gn.append(1 - dicE.count)
         dicE.append("\(userS)")
      }
          var numberlabelY: String! = String(cString: [111,105,100,0], encoding: .utf8)!
          var recordE: Double = 4.0
         main_gn.append(numberlabelY.count)
         numberlabelY.append("\(1)")
         recordE -= Double(Int(recordE) * Int(recordE))
      repeat {
         main_gn.append(vertical0.count)
         if 973662 == main_gn.count {
            break
         }
      } while (973662 == main_gn.count) && (1 <= (main_gn.count >> (UNavigationTool7.min(vertical0.values.count, 1))))
      if (4 * main_gn.count) < 1 {
         vertical0["\(main_gn.count)"] = 2
      }
      transactionsL.append("\(vertical0.count / (UNavigationTool7.max(2, 2)))")
      if transactionsL == (String(cString:[101,116,48,57,55,115,0], encoding: .utf8)!) {
         break
      }
   } while (2 == (Int(changedN) / (UNavigationTool7.max(transactionsL.count, 6)))) && (transactionsL == (String(cString:[101,116,48,57,55,115,0], encoding: .utf8)!))
   while (valuelabelY == transactionsL) {
      transactionsL = "\(Int(changedN) % (UNavigationTool7.max(1, 1)))"
      break
   }
        
         stopPlay()
    
       var with_qxl: Float = 3.0
      while (5.58 < (with_qxl / 3.3) || 5.54 < (with_qxl - 3.3)) {
         with_qxl -= Float(Int(with_qxl))
         break
      }
         with_qxl -= Float(Int(with_qxl))
          var descript9: String! = String(cString: [105,112,114,101,100,0], encoding: .utf8)!
          var binit_a2h: Float = 5.0
          var yin_: [Any]! = [365, 759, 832]
         with_qxl -= Float(Int(with_qxl) >> (UNavigationTool7.min(labs(1), 3)))
         descript9 = "\((descript9 == (String(cString:[107,0], encoding: .utf8)!) ? Int(binit_a2h) : descript9.count))"
         binit_a2h /= UNavigationTool7.max(2, Float(3))
         yin_.append(yin_.count)
      valuelabelY = "\(transactionsL.count)"
    }

    
    func onNuiTtsVolumeCallback(_ volume: Int32, taskId task_id: UnsafeMutablePointer<CChar>!) {
       var audioN: [Any]! = [String(cString: [105,104,116,120,0], encoding: .utf8)!, String(cString: [115,117,98,106,111,117,114,110,97,108,0], encoding: .utf8)!]
    var silenceJ: String! = String(cString: [99,111,108,100,0], encoding: .utf8)!
    var changej: String! = String(cString: [114,97,100,98,103,0], encoding: .utf8)!
      changej = "\(1 ^ silenceJ.count)"
   repeat {
      silenceJ.append("\(silenceJ.count)")
      if silenceJ == (String(cString:[107,111,116,105,0], encoding: .utf8)!) {
         break
      }
   } while (silenceJ == (String(cString:[107,111,116,105,0], encoding: .utf8)!)) && (1 < (audioN.count / 4))

   if changej != String(cString:[55,0], encoding: .utf8)! {
       var contentsk: String! = String(cString: [105,110,116,101,114,112,111,108,97,116,97,98,108,101,0], encoding: .utf8)!
       var first5: String! = String(cString: [120,100,97,105,0], encoding: .utf8)!
       var linesD: Bool = false
       var audioF: String! = String(cString: [112,101,97,107,115,0], encoding: .utf8)!
       var sectiono: Bool = true
          var a_titlez: Bool = true
          var resourcesm: Double = 3.0
          var alabelK: [Any]! = [[String(cString: [99,111,108,111,114,115,0], encoding: .utf8)!:String(cString: [116,109,112,108,0], encoding: .utf8)!, String(cString: [100,111,99,107,101,114,0], encoding: .utf8)!:String(cString: [112,117,98,108,105,99,105,116,121,0], encoding: .utf8)!]]
         audioF = "\(((a_titlez ? 2 : 3) % (UNavigationTool7.max(Int(resourcesm), 2))))"
         a_titlez = (alabelK.count / (UNavigationTool7.max(alabelK.count, 6))) == 75
         resourcesm /= UNavigationTool7.max(Double(alabelK.count), 2)
      if !sectiono {
         linesD = !linesD
      }
         contentsk = "\(2)"
       var present0: Int = 0
      repeat {
         sectiono = linesD && first5.count > 60
         if sectiono ? !sectiono : sectiono {
            break
         }
      } while (!sectiono || contentsk.count > 4) && (sectiono ? !sectiono : sectiono)
      repeat {
         contentsk.append("\(audioF.count)")
         if contentsk == (String(cString:[56,53,101,97,115,107,118,122,103,0], encoding: .utf8)!) {
            break
         }
      } while (first5.hasSuffix(contentsk)) && (contentsk == (String(cString:[56,53,101,97,115,107,118,122,103,0], encoding: .utf8)!))
      while (linesD && contentsk.count >= 4) {
         linesD = !sectiono
         break
      }
         present0 += 1 << (UNavigationTool7.min(5, contentsk.count))
       var aidG: String! = String(cString: [99,111,100,101,98,111,111,107,115,0], encoding: .utf8)!
         first5.append("\(first5.count)")
      for _ in 0 ..< 3 {
         first5 = "\(aidG.count)"
      }
          var z_imagez: Int = 5
         linesD = 65 < aidG.count
         z_imagez |= z_imagez
      for _ in 0 ..< 3 {
         aidG = "\(((sectiono ? 5 : 3) + (linesD ? 3 : 1)))"
      }
      for _ in 0 ..< 2 {
         sectiono = (28 <= (audioF.count - (!linesD ? 28 : audioF.count)))
      }
         linesD = (6 > ((!sectiono ? 6 : first5.count) + first5.count))
      silenceJ.append("\(((linesD ? 3 : 2) >> (UNavigationTool7.min(labs(3), 1))))")
   }
      audioN = [audioN.count]
    
        debugPrint("——————音频波纹：\(volume)")
    
      audioN = [audioN.count % (UNavigationTool7.max(2, 1))]
    }
}


class EEOToolsGraphics: NSObject {
var buffers_space: Double = 0.0
private var thinkingPlayerReceiveString: String?



    
    var generateStrExpireList: [Any]!
    var has_Request: Bool = false
    
    private let urlsItem = SFSpeechRecognizer(locale: Locale(identifier: "zh-CN"))!
    private var historySpeech: SFSpeechAudioBufferRecognitionRequest?
    private var launchAgain: SFSpeechRecognitionTask?
    private let audioEngine = AVAudioEngine()
    private var ringFree: Bool = false
    private var isDetectionEnabled: Bool
    var speakTimer: DispatchSourceTimer?
    private let zoomPlace: TimeInterval = 3.0
    private let speakingNumber = UIImpactFeedbackGenerator(style: .light)

    var resultHandler: ((String) -> Void)?
    var decibelScaleHandler: ((Float) -> Void)?

    init(isDetectionEnabled: Bool = false) {
        self.isDetectionEnabled = isDetectionEnabled
        self.speakingNumber.prepare()
    }

@discardableResult
 func recognitionDirectoryAboutImagesLock(handleOpen: Bool) -> Float {
    var collate6: [String: Any]! = [String(cString: [103,101,110,115,0], encoding: .utf8)!:931, String(cString: [110,101,105,103,104,98,111,117,114,0], encoding: .utf8)!:913, String(cString: [102,116,118,109,108,97,115,116,110,111,100,101,0], encoding: .utf8)!:461]
    var register_7v5: String! = String(cString: [115,108,105,99,101,100,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
      register_7v5 = "\(collate6.keys.count << (UNavigationTool7.min(register_7v5.count, 2)))"
   }
   if (2 >> (UNavigationTool7.min(4, collate6.count))) == 3 {
       var souo: Double = 4.0
       var pickerS: Bool = true
         souo += Double(Int(souo) % 1)
      while (pickerS) {
          var s_imagel: [Any]! = [887, 47, 776]
          var timer1: Bool = true
          var delegate_z_B: Float = 2.0
          var collectionF: String! = String(cString: [105,110,111,100,101,0], encoding: .utf8)!
          var buttonZ: Int = 5
         pickerS = (s_imagel.contains { $0 as? Int == buttonZ })
         s_imagel = [((timer1 ? 5 : 4) % (UNavigationTool7.max(10, Int(delegate_z_B))))]
         timer1 = collectionF == (String(cString:[98,0], encoding: .utf8)!)
         delegate_z_B -= Float(Int(delegate_z_B))
         collectionF = "\((Int(delegate_z_B) % (UNavigationTool7.max(7, (timer1 ? 4 : 5)))))"
         buttonZ %= UNavigationTool7.max((collectionF == (String(cString:[85,0], encoding: .utf8)!) ? (timer1 ? 1 : 1) : collectionF.count), 2)
         break
      }
         souo /= UNavigationTool7.max(1, Double(Int(souo) << (UNavigationTool7.min(1, labs(1)))))
         pickerS = !pickerS
      for _ in 0 ..< 1 {
          var imgz: Float = 4.0
         souo -= Double(Int(souo) / 1)
         imgz /= UNavigationTool7.max(Float(3), 1)
      }
      repeat {
         pickerS = !pickerS
         if pickerS ? !pickerS : pickerS {
            break
         }
      } while (pickerS ? !pickerS : pickerS) && (!pickerS)
      register_7v5.append("\((Int(souo) * (pickerS ? 3 : 2)))")
   }
   for _ in 0 ..< 2 {
      collate6[register_7v5] = 2 * collate6.keys.count
   }
      register_7v5.append("\((register_7v5 == (String(cString:[71,0], encoding: .utf8)!) ? collate6.keys.count : register_7v5.count))")
     var userRing: Float = 9428.0
     var setstateSafe: Double = 5197.0
    var gzippedParticipation:Float = 0

    gzippedParticipation += userRing
    setstateSafe = 4474
    gzippedParticipation += Float(setstateSafe)

    return gzippedParticipation

}






    
    func startRecording() {

             recognitionDirectoryAboutImagesLock(handleOpen:false)


        
        speakingNumber.impactOccurred()
        audioEngine.stop()
        audioEngine.reset()

        DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) { [weak self] in
            
            guard let self = self else { return }

            checkAliToken()
            
            if let launchAgain = self.launchAgain {
                launchAgain.cancel()
                self.launchAgain = nil
            }

            self.ringFree = false
            
            let datal = AVAudioSession.sharedInstance()
            try! datal.setCategory(.record, mode: .measurement, options: .duckOthers)
            try! datal.setActive(true, options: .notifyOthersOnDeactivation)

            self.historySpeech = SFSpeechAudioBufferRecognitionRequest()

            let recognizer = self.audioEngine.inputNode

            guard let historySpeech = self.historySpeech else { fatalError("Unable to create a SFSpeechAudioBufferRecognitionRequest object") }

            historySpeech.shouldReportPartialResults = true
            
            self.launchAgain = self.urlsItem.recognitionTask(with: historySpeech) { result, error in
                var dictButton = false
                
                if let result = result {
                    
                    if self.isDetectionEnabled {
                        self.vipObjcAllChuangzuo()
                        self.contentExpireGain()
                    }
                    print("——————说话中: \(result.bestTranscription.formattedString)")
                    dictButton = result.isFinal
                    NotificationCenter.default.post(name: NSNotification.Name("VoiceMessageNotificationName"), object: result.bestTranscription.formattedString)
                }

                if error != nil || dictButton {
                    self.audioEngine.stop()
                    recognizer.removeTap(onBus: 0)

                    self.historySpeech = nil
                    self.launchAgain = nil

                    if let result = result {
                        if self.ringFree == false {
                            self.resultHandler?(result.bestTranscription.formattedString)
                        }
                    }
                }
            }

            let list = recognizer.outputFormat(forBus: 0)
            recognizer.installTap(onBus: 0, bufferSize: 1024, format: list) { (buffer: AVAudioPCMBuffer, when: AVAudioTime) in
                self.historySpeech?.append(buffer)
                
                
                guard let channelData = buffer.floatChannelData else { return }
                
                
                let desclabelView = vDSP_Length(buffer.frameLength)
                var nickname: Float = 0
                vDSP_rmsqv(channelData[0], 1, &nickname, desclabelView)
                
                
                let jilu = 20 * log10(nickname)
                
                
                DispatchQueue.main.async {
                    
                    let upgrades = 1.0 + CGFloat((jilu + 50) / 50.0)
                    let buying = max(1.0, min(upgrades, 1.5))
                    
                    if buying > 1.0 {
                        self.decibelScaleHandler?(Float(buying))
                    }
                }
            }
            
            self.audioEngine.prepare()
            try! self.audioEngine.start()
        }
        
    }

@discardableResult
 func contentsChannelToolsPriceDirectory(emailRemove: Bool) -> Double {
    var purchase_: String! = String(cString: [100,111,110,97,116,101,0], encoding: .utf8)!
    var pressq: String! = String(cString: [108,111,99,97,108,104,111,115,116,0], encoding: .utf8)!
      purchase_.append("\(3)")
   while (1 > purchase_.count) {
      purchase_.append("\(purchase_.count % (UNavigationTool7.max(pressq.count, 5)))")
      break
   }
       var update_jR: [String: Any]! = [String(cString: [115,101,118,101,110,0], encoding: .utf8)!:380, String(cString: [98,97,99,107,114,111,117,110,100,0], encoding: .utf8)!:117, String(cString: [97,116,116,114,105,98,0], encoding: .utf8)!:515]
      for _ in 0 ..< 3 {
         update_jR = ["\(update_jR.values.count)": 3]
      }
      repeat {
         update_jR = ["\(update_jR.values.count)": 1]
         if update_jR.count == 214484 {
            break
         }
      } while (3 == (update_jR.count >> (UNavigationTool7.min(labs(2), 3))) || (2 >> (UNavigationTool7.min(5, update_jR.count))) == 4) && (update_jR.count == 214484)
          var home7: [String: Any]! = [String(cString: [101,95,56,48,95,99,97,115,101,0], encoding: .utf8)!:431, String(cString: [112,108,97,110,101,115,0], encoding: .utf8)!:455]
         update_jR = ["\(home7.keys.count)": update_jR.count]
      purchase_ = "\(purchase_.count >> (UNavigationTool7.min(4, update_jR.count)))"
      pressq = "\(1 % (UNavigationTool7.max(1, pressq.count)))"
     let speedFind: Double = 8206.0
    var luralizationPriorityWorker:Double = 0
    luralizationPriorityWorker += speedFind

    return luralizationPriorityWorker

}






    private func contentExpireGain() {

             contentsChannelToolsPriceDirectory(emailRemove:false)


       var layoute: Double = 5.0
    var alabelL: String! = String(cString: [115,100,107,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
       var descriptw: String! = String(cString: [115,116,97,116,117,115,0], encoding: .utf8)!
       var listh: String! = String(cString: [117,110,105,115,119,97,112,0], encoding: .utf8)!
         listh = "\(listh.count)"
         listh = "\(descriptw.count)"
      repeat {
         listh.append("\(1 ^ descriptw.count)")
         if 3417268 == listh.count {
            break
         }
      } while (3417268 == listh.count) && (listh.contains("\(descriptw.count)"))
          var substringC: Double = 0.0
         listh.append("\(listh.count & 1)")
         substringC -= Double(Int(substringC))
         listh = "\((listh == (String(cString:[84,0], encoding: .utf8)!) ? listh.count : descriptw.count))"
          var statuslabele: Double = 3.0
          var contentsZ: String! = String(cString: [105,112,99,0], encoding: .utf8)!
         listh = "\(contentsZ.count >> (UNavigationTool7.min(labs(2), 3)))"
         statuslabele += Double(2 % (UNavigationTool7.max(4, Int(statuslabele))))
         contentsZ.append("\(1)")
      layoute -= Double(1)
   }

   if 3 == (Int(layoute) + alabelL.count) {
       var r_widthT: String! = String(cString: [115,101,97,116,0], encoding: .utf8)!
       var controllB: Float = 0.0
       var setsampleraten: String! = String(cString: [99,101,110,116,114,111,105,100,115,0], encoding: .utf8)!
       var pickera: Double = 2.0
         r_widthT = "\(2 / (UNavigationTool7.max(Int(pickera), 9)))"
         controllB -= Float(setsampleraten.count)
      while (r_widthT == setsampleraten) {
         setsampleraten = "\(Int(controllB) / (UNavigationTool7.max(8, setsampleraten.count)))"
         break
      }
      while ((3.5 * pickera) >= 4.74 && 3.98 >= (3.5 * pickera)) {
         setsampleraten = "\(3 % (UNavigationTool7.max(6, Int(controllB))))"
         break
      }
      for _ in 0 ..< 3 {
          var responseh: Int = 4
          var webq: String! = String(cString: [115,109,111,111,116,104,108,121,0], encoding: .utf8)!
         r_widthT = "\(webq.count)"
         responseh *= 2 / (UNavigationTool7.max(9, responseh))
         webq.append("\(1)")
      }
      for _ in 0 ..< 3 {
         setsampleraten = "\(Int(pickera) ^ 3)"
      }
         controllB -= Float(1)
         pickera /= UNavigationTool7.max(Double(1 & Int(pickera)), 5)
      repeat {
          var brief8: [String: Any]! = [String(cString: [112,117,98,108,105,115,104,101,114,115,0], encoding: .utf8)!:351, String(cString: [112,97,105,114,119,105,115,101,0], encoding: .utf8)!:989]
         controllB -= Float(r_widthT.count)
         brief8["\(brief8.keys.count)"] = 2
         if 2595853.0 == controllB {
            break
         }
      } while (!r_widthT.hasSuffix("\(controllB)")) && (2595853.0 == controllB)
       var layoutX: Float = 5.0
       var textF: Float = 2.0
         controllB *= Float(Int(textF) | r_widthT.count)
      while (4.2 >= pickera) {
         pickera /= UNavigationTool7.max(Double(setsampleraten.count + 1), 4)
         break
      }
      layoute /= UNavigationTool7.max(2, (Double(alabelL == (String(cString:[102,0], encoding: .utf8)!) ? Int(layoute) : alabelL.count)))
   }

        if speakTimer == nil {
            speakTimer?.cancel()
            speakTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.main)
            speakTimer?.schedule(deadline: .now() + zoomPlace, repeating: .never)
            speakTimer?.setEventHandler { [weak self] in
                self?.stopRecording()
            }
            speakTimer?.resume()
        }
    }

@discardableResult
 func baseLabelSystemRawingYlabelMutex() -> Bool {
    var yloading9: String! = String(cString: [99,111,108,115,0], encoding: .utf8)!
    var changeC: Double = 5.0
   while (changeC == 4.15) {
       var dateM: [Any]! = [377, 887]
       var silence6: String! = String(cString: [115,104,105,102,116,105,110,103,115,0], encoding: .utf8)!
       var pasteboardz: Int = 5
         dateM.append(pasteboardz)
      for _ in 0 ..< 1 {
         dateM = [dateM.count >> (UNavigationTool7.min(2, labs(pasteboardz)))]
      }
          var promptC: String! = String(cString: [97,98,115,116,114,97,99,116,0], encoding: .utf8)!
          var sortC: String! = String(cString: [105,110,108,105,110,101,100,0], encoding: .utf8)!
         pasteboardz /= UNavigationTool7.max(1, promptC.count)
         promptC.append("\(((String(cString:[102,0], encoding: .utf8)!) == sortC ? sortC.count : sortC.count))")
      if 4 == (3 - dateM.count) && (3 - silence6.count) == 3 {
          var notificationc: Int = 2
         dateM.append(2 ^ silence6.count)
         notificationc %= UNavigationTool7.max(3, 1 & notificationc)
      }
      if dateM.count > silence6.count {
         silence6.append("\(dateM.count / 2)")
      }
      repeat {
         silence6 = "\(silence6.count / 1)"
         if 4762718 == silence6.count {
            break
         }
      } while (silence6.count > 2) && (4762718 == silence6.count)
      while ((pasteboardz / 4) > 3 || 1 > (4 / (UNavigationTool7.max(5, pasteboardz)))) {
         pasteboardz &= 1 + dateM.count
         break
      }
         silence6.append("\(((String(cString:[49,0], encoding: .utf8)!) == silence6 ? silence6.count : dateM.count))")
          var not_95: Bool = false
         dateM = [1]
         not_95 = !not_95
      changeC *= Double(pasteboardz)
      break
   }
   repeat {
       var recordingy: Bool = true
       var expiration6: String! = String(cString: [99,111,111,114,100,105,110,97,116,111,114,115,0], encoding: .utf8)!
       var doneR: String! = String(cString: [100,105,115,99,111,118,101,114,101,100,0], encoding: .utf8)!
       var historyb: [Any]! = [35, 99, 923]
      for _ in 0 ..< 1 {
          var shur: String! = String(cString: [100,105,97,99,114,105,116,105,99,0], encoding: .utf8)!
          var controllp: Double = 2.0
          var holderlabelB: Bool = true
          var itemsg: Int = 5
          var contextD: String! = String(cString: [117,110,109,117,116,101,100,0], encoding: .utf8)!
         expiration6 = "\(2 << (UNavigationTool7.min(labs(itemsg), 5)))"
         shur.append("\(Int(controllp) << (UNavigationTool7.min(2, labs(3))))")
         controllp /= UNavigationTool7.max(5, (Double((holderlabelB ? 1 : 3) >> (UNavigationTool7.min(labs(Int(controllp)), 4)))))
         holderlabelB = !holderlabelB
         itemsg += ((holderlabelB ? 5 : 4))
         contextD = "\(2)"
      }
          var toplayoutm: String! = String(cString: [114,108,111,116,116,105,101,0], encoding: .utf8)!
         expiration6.append("\(doneR.count)")
         toplayoutm.append("\(2)")
      repeat {
          var processx: String! = String(cString: [100,105,115,97,98,108,101,114,0], encoding: .utf8)!
          var rowsF: String! = String(cString: [116,101,109,112,111,0], encoding: .utf8)!
          var iosp: String! = String(cString: [98,105,109,97,112,0], encoding: .utf8)!
          var briefH: String! = String(cString: [119,101,108,115,100,101,99,100,101,109,111,0], encoding: .utf8)!
          var descriptt: Double = 0.0
         recordingy = iosp.count <= 20 || briefH.count <= 20
         processx.append("\((processx == (String(cString:[108,0], encoding: .utf8)!) ? Int(descriptt) : processx.count))")
         rowsF = "\(((String(cString:[65,0], encoding: .utf8)!) == processx ? rowsF.count : processx.count))"
         iosp = "\(Int(descriptt))"
         briefH = "\(processx.count)"
         if recordingy ? !recordingy : recordingy {
            break
         }
      } while (!recordingy) && (recordingy ? !recordingy : recordingy)
      if !expiration6.hasPrefix("\(historyb.count)") {
         expiration6 = "\(historyb.count - 1)"
      }
          var amountx: String! = String(cString: [100,101,108,111,103,111,0], encoding: .utf8)!
          var gifU: String! = String(cString: [118,112,109,116,0], encoding: .utf8)!
         doneR.append("\(((String(cString:[52,0], encoding: .utf8)!) == amountx ? doneR.count : amountx.count))")
         gifU = "\(gifU.count >> (UNavigationTool7.min(labs(1), 3)))"
      for _ in 0 ..< 3 {
         recordingy = doneR.count <= 33
      }
         doneR.append("\(historyb.count << (UNavigationTool7.min(labs(1), 1)))")
      repeat {
          var dismissO: Int = 5
         recordingy = !recordingy
         dismissO <<= UNavigationTool7.min(labs(1 ^ dismissO), 1)
         if recordingy ? !recordingy : recordingy {
            break
         }
      } while (expiration6.count <= 4) && (recordingy ? !recordingy : recordingy)
      for _ in 0 ..< 3 {
         recordingy = historyb.count == 18 || recordingy
      }
         recordingy = !recordingy
      while (expiration6.count < 2 && recordingy) {
         expiration6 = "\(historyb.count)"
         break
      }
      if 1 > historyb.count || (historyb.count << (UNavigationTool7.min(labs(1), 4))) > 2 {
         recordingy = 62 >= doneR.count
      }
      changeC += (Double((recordingy ? 4 : 5)))
      if changeC == 4789727.0 {
         break
      }
   } while (yloading9.count > 1) && (changeC == 4789727.0)
   if !yloading9.contains("\(changeC)") {
      changeC /= UNavigationTool7.max(2, Double(yloading9.count << (UNavigationTool7.min(1, labs(Int(changeC))))))
   }
   while ((5.19 + changeC) > 5.29 && 2 > (3 + yloading9.count)) {
      changeC -= (Double((String(cString:[87,0], encoding: .utf8)!) == yloading9 ? yloading9.count : Int(changeC)))
      break
   }
     let replaceMain: Float = 981.0
     var infoExecute: Double = 1516.0
    var  errorvIpol:Bool = false
    errorvIpol = replaceMain > 96
    infoExecute = 2616
    errorvIpol = infoExecute > 62

    return errorvIpol

}






    func cancelRecording() {

             baseLabelSystemRawingYlabelMutex()


       var webT: String! = String(cString: [116,105,101,114,115,0], encoding: .utf8)!
    var unselectedp: Int = 3
    var recordU: [String: Any]! = [String(cString: [115,109,112,116,101,98,97,114,115,0], encoding: .utf8)!:true]
   for _ in 0 ..< 3 {
       var cellJ: Double = 3.0
       var rmblabelu: String! = String(cString: [110,111,116,99,104,101,100,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
          var recordh: Double = 2.0
          var souM: Bool = false
          var normalz: Double = 5.0
         cellJ += Double(Int(recordh))
         recordh *= Double(2 % (UNavigationTool7.max(5, Int(normalz))))
         souM = !souM
         normalz *= Double(3 % (UNavigationTool7.max(6, Int(normalz))))
      }
      for _ in 0 ..< 1 {
          var n_image_: String! = String(cString: [115,104,111,119,115,0], encoding: .utf8)!
          var json1: Int = 1
         rmblabelu.append("\(1 >> (UNavigationTool7.min(3, n_image_.count)))")
         n_image_ = "\(json1 + 2)"
         json1 <<= UNavigationTool7.min(labs(json1), 5)
      }
         rmblabelu.append("\(rmblabelu.count - 3)")
          var audiob: String! = String(cString: [97,118,101,115,0], encoding: .utf8)!
         cellJ -= Double(2)
         audiob.append("\(audiob.count)")
         cellJ /= UNavigationTool7.max(Double(Int(cellJ)), 3)
         rmblabelu = "\(1)"
      webT = "\(1 + unselectedp)"
   }

      webT.append("\(((String(cString:[83,0], encoding: .utf8)!) == webT ? webT.count : unselectedp))")
        
        ringFree = true
   for _ in 0 ..< 2 {
       var hasF: String! = String(cString: [112,97,99,107,101,116,0], encoding: .utf8)!
       var faileds: Double = 0.0
      while (!hasF.hasSuffix("\(faileds)")) {
         hasF = "\(3 | Int(faileds))"
         break
      }
          var rmbE: String! = String(cString: [102,108,111,97,116,115,0], encoding: .utf8)!
         faileds /= UNavigationTool7.max(5, Double(1))
         rmbE = "\(1)"
          var leftc: String! = String(cString: [104,97,115,120,0], encoding: .utf8)!
         hasF = "\(hasF.count * 2)"
         leftc.append("\(leftc.count)")
         faileds /= UNavigationTool7.max(3, Double(hasF.count))
         faileds /= UNavigationTool7.max(2, (Double(hasF == (String(cString:[71,0], encoding: .utf8)!) ? Int(faileds) : hasF.count)))
      repeat {
         faileds += (Double((String(cString:[80,0], encoding: .utf8)!) == hasF ? Int(faileds) : hasF.count))
         if 2434624.0 == faileds {
            break
         }
      } while (2434624.0 == faileds) && ((hasF.count / (UNavigationTool7.max(5, 7))) <= 5 && 3.57 <= (faileds + 5.99))
      unselectedp += (hasF == (String(cString:[77,0], encoding: .utf8)!) ? hasF.count : Int(faileds))
   }
        stopRecording()
    }

@discardableResult
 func recognitionRestoredScrollToolDataRmblabel() -> Double {
    var rmblabelP: String! = String(cString: [112,100,122,112,0], encoding: .utf8)!
    var yinZ: Double = 3.0
      yinZ -= Double(Int(yinZ))
   repeat {
      yinZ += (Double(rmblabelP == (String(cString:[88,0], encoding: .utf8)!) ? Int(yinZ) : rmblabelP.count))
      if yinZ == 4549538.0 {
         break
      }
   } while ((Double(rmblabelP.count) * yinZ) == 2.70 || (3 * Int(yinZ)) == 3) && (yinZ == 4549538.0)
   repeat {
       var resultT: [Any]! = [361, 298]
       var regionh: String! = String(cString: [117,110,97,118,97,105,98,108,101,0], encoding: .utf8)!
       var headerG: String! = String(cString: [97,118,103,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var callback7: String! = String(cString: [109,111,99,107,115,0], encoding: .utf8)!
         headerG.append("\(resultT.count * regionh.count)")
         callback7 = "\(1 * callback7.count)"
      }
       var activityH: [Any]! = [624, 857]
      repeat {
         regionh.append("\(2 & activityH.count)")
         if regionh == (String(cString:[99,57,51,116,103,118,115,104,0], encoding: .utf8)!) {
            break
         }
      } while (headerG.count == 1) && (regionh == (String(cString:[99,57,51,116,103,118,115,104,0], encoding: .utf8)!))
         activityH = [activityH.count]
          var resourcese: Int = 2
          var toolsO: String! = String(cString: [100,111,109,97,105,110,0], encoding: .utf8)!
          var tableR: Double = 0.0
         regionh.append("\(2 % (UNavigationTool7.max(6, activityH.count)))")
         resourcese >>= UNavigationTool7.min(labs(resourcese & toolsO.count), 2)
         toolsO.append("\(toolsO.count >> (UNavigationTool7.min(labs(2), 4)))")
         tableR -= Double(1)
      repeat {
         activityH = [2]
         if 1301354 == activityH.count {
            break
         }
      } while (activityH.count == 5) && (1301354 == activityH.count)
      while ((regionh.count & resultT.count) > 5) {
          var infoF: String! = String(cString: [109,100,97,116,101,0], encoding: .utf8)!
          var layoutc: [String: Any]! = [String(cString: [114,101,116,0], encoding: .utf8)!:599, String(cString: [112,114,111,99,0], encoding: .utf8)!:760, String(cString: [113,116,97,98,108,101,115,0], encoding: .utf8)!:301]
         resultT = [((String(cString:[65,0], encoding: .utf8)!) == infoF ? layoutc.values.count : infoF.count)]
         break
      }
      repeat {
          var tableZ: Bool = true
          var enewsW: String! = String(cString: [101,108,101,109,101,110,116,119,105,115,101,0], encoding: .utf8)!
          var iconl: [String: Any]! = [String(cString: [116,114,105,0], encoding: .utf8)!:false]
          var graphicsc: String! = String(cString: [102,111,108,100,101,114,115,0], encoding: .utf8)!
         headerG = "\(((String(cString:[50,0], encoding: .utf8)!) == headerG ? enewsW.count : headerG.count))"
         tableZ = graphicsc.count >= 25 || iconl.count >= 25
         enewsW.append("\(((tableZ ? 5 : 1) | graphicsc.count))")
         iconl[graphicsc] = 1
         if headerG == (String(cString:[118,117,117,0], encoding: .utf8)!) {
            break
         }
      } while (headerG == (String(cString:[118,117,117,0], encoding: .utf8)!)) && (4 < (headerG.count / (UNavigationTool7.max(10, activityH.count))) && (activityH.count / 4) < 3)
       var pressg: String! = String(cString: [106,97,99,111,98,105,0], encoding: .utf8)!
       var applicationc: String! = String(cString: [116,114,107,110,0], encoding: .utf8)!
      rmblabelP = "\(rmblabelP.count % 3)"
      if rmblabelP.count == 1679200 {
         break
      }
   } while ((Double(rmblabelP.count) * yinZ) <= 5.21 && 2.6 <= (yinZ * 5.21)) && (rmblabelP.count == 1679200)
   return yinZ

}






    func stopRecording() {

             recognitionRestoredScrollToolDataRmblabel()


       var rmblabelO: [String: Any]! = [String(cString: [97,99,107,110,111,119,108,101,100,103,101,109,101,110,116,0], encoding: .utf8)!:7224.0]
    var d_height4: String! = String(cString: [112,114,101,104,101,97,116,0], encoding: .utf8)!
    var dismissC: String! = String(cString: [109,101,109,109,103,114,0], encoding: .utf8)!
    var fromn: Double = 4.0
      d_height4 = "\(3)"

      dismissC.append("\(d_height4.count)")
        
        speakingNumber.impactOccurred()
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) { [weak self] in
            guard let self = self else { return }

       var datep: String! = String(cString: [100,120,118,97,0], encoding: .utf8)!
         datep.append("\(datep.count % (UNavigationTool7.max(1, 7)))")
      if datep.count == 5 || datep.count == 5 {
          var normalN: Double = 2.0
          var normalj: Double = 4.0
          var setk: Double = 2.0
          var endS: Float = 5.0
         datep.append("\(Int(normalN))")
         normalj -= Double(Int(endS))
         setk -= Double(3 + Int(endS))
      }
      for _ in 0 ..< 1 {
         datep = "\(datep.count)"
      }
      d_height4.append("\(dismissC.count | 1)")
            self.vipObjcAllChuangzuo()
   if dismissC.count >= 5 {
       var collection7: String! = String(cString: [97,114,114,97,121,115,0], encoding: .utf8)!
       var size_doc: Bool = true
       var num3: String! = String(cString: [97,117,116,111,114,111,116,97,116,105,111,110,0], encoding: .utf8)!
       var activitylabelm: Double = 0.0
       var messageR: Float = 5.0
      repeat {
         num3.append("\(Int(messageR) + 3)")
         if num3 == (String(cString:[56,115,116,97,110,119,105,98,50,0], encoding: .utf8)!) {
            break
         }
      } while (num3.hasSuffix("\(size_doc)")) && (num3 == (String(cString:[56,115,116,97,110,119,105,98,50,0], encoding: .utf8)!))
      if 1 >= collection7.count {
         size_doc = num3 == (String(cString:[66,0], encoding: .utf8)!)
      }
         activitylabelm *= Double(1 * Int(activitylabelm))
      while (Double(messageR) <= activitylabelm) {
         activitylabelm /= UNavigationTool7.max(2, (Double((size_doc ? 1 : 4) * Int(activitylabelm))))
         break
      }
      repeat {
          var yloadingx: String! = String(cString: [98,117,105,108,100,105,110,103,0], encoding: .utf8)!
          var progressW: String! = String(cString: [114,117,110,116,101,115,116,115,0], encoding: .utf8)!
         activitylabelm -= Double(yloadingx.count ^ collection7.count)
         yloadingx = "\(1)"
         progressW.append("\(progressW.count << (UNavigationTool7.min(5, progressW.count)))")
         if activitylabelm == 2537690.0 {
            break
         }
      } while (activitylabelm == 2537690.0) && ((Double(Int(messageR) / 4)) > 3.66)
       var graphics_: [Any]! = [9745]
       var detectionJ: [Any]! = [5939.0]
      while (size_doc) {
         size_doc = graphics_.count == 89
         break
      }
      repeat {
          var addressX: String! = String(cString: [98,110,109,112,105,0], encoding: .utf8)!
          var zhidingesM: Int = 2
          var cachew: Int = 5
          var itemZ: Double = 5.0
         graphics_ = [zhidingesM * 1]
         addressX = "\(Int(itemZ) + cachew)"
         zhidingesM >>= UNavigationTool7.min(2, labs(addressX.count * 3))
         cachew &= 3 | Int(itemZ)
         if 39319 == graphics_.count {
            break
         }
      } while (39319 == graphics_.count) && ((graphics_.count * 2) == 3 && (2 * detectionJ.count) == 1)
      if (Float(activitylabelm) / (UNavigationTool7.max(6, messageR))) == 3.18 || (activitylabelm / 3.18) == 3.89 {
          var qheaderH: [String: Any]! = [String(cString: [111,110,121,120,99,0], encoding: .utf8)!:714, String(cString: [108,97,121,101,114,105,110,103,0], encoding: .utf8)!:932, String(cString: [116,114,97,110,115,102,101,114,97,98,108,101,0], encoding: .utf8)!:457]
         messageR += Float(1)
         qheaderH = ["\(qheaderH.values.count)": qheaderH.count % (UNavigationTool7.max(3, 5))]
      }
      if 2.5 <= (Double(messageR) - activitylabelm) || 2.5 <= (activitylabelm - Double(messageR)) {
         activitylabelm -= (Double(collection7.count ^ (size_doc ? 1 : 4)))
      }
         size_doc = !collection7.contains("\(messageR)")
      repeat {
          var mineZ: String! = String(cString: [98,102,114,97,109,101,115,0], encoding: .utf8)!
          var weixinA: Float = 1.0
          var yloadingb: [Any]! = [660, 982, 322]
          var generate4: Float = 5.0
          var safeB: Int = 0
         num3.append("\(detectionJ.count)")
         mineZ = "\(Int(generate4) << (UNavigationTool7.min(4, labs(3))))"
         weixinA += Float(Int(weixinA))
         yloadingb.append(3)
         generate4 += Float(safeB >> (UNavigationTool7.min(1, labs(2))))
         safeB -= Int(weixinA)
         if (String(cString:[115,117,102,106,108,103,104,48,0], encoding: .utf8)!) == num3 {
            break
         }
      } while ((String(cString:[115,117,102,106,108,103,104,48,0], encoding: .utf8)!) == num3) && (collection7.count >= 4)
          var recognizer1: Float = 1.0
         size_doc = !size_doc && num3.count >= 95
         recognizer1 -= Float(Int(recognizer1))
         messageR += Float(collection7.count)
      if num3.count == 3 {
         size_doc = 62 <= graphics_.count
      }
      dismissC = "\(collection7.count * 3)"
   }
            self.audioEngine.stop()
   repeat {
      rmblabelO["\(rmblabelO.values.count)"] = rmblabelO.values.count
      if 378136 == rmblabelO.count {
         break
      }
   } while (378136 == rmblabelO.count) && (4 > (rmblabelO.keys.count * 4))
            self.historySpeech?.endAudio()
       var chuangzuor: String! = String(cString: [115,105,122,101,98,105,116,114,97,116,101,0], encoding: .utf8)!
       var dictionaryv: String! = String(cString: [98,97,110,100,119,105,100,116,104,0], encoding: .utf8)!
         dictionaryv.append("\(dictionaryv.count)")
      for _ in 0 ..< 3 {
         chuangzuor.append("\(chuangzuor.count)")
      }
       var prompt2: Int = 2
       var socket_: String! = String(cString: [101,118,114,112,99,0], encoding: .utf8)!
      if 1 == chuangzuor.count {
         prompt2 <<= UNavigationTool7.min(labs(prompt2), 2)
      }
      repeat {
          var scrollw: [Any]! = [421, 919, 575]
         socket_ = "\(chuangzuor.count)"
         scrollw.append(scrollw.count)
         if socket_ == (String(cString:[51,107,106,112,106,107,50,51,105,0], encoding: .utf8)!) {
            break
         }
      } while (!socket_.hasPrefix("\(prompt2)")) && (socket_ == (String(cString:[51,107,106,112,106,107,50,51,105,0], encoding: .utf8)!))
      rmblabelO = [chuangzuor: dictionaryv.count % 1]
            let datal = AVAudioSession.sharedInstance()
            try! datal.setCategory(.playback, mode: .default)
            try! datal.setActive(true, options: .notifyOthersOnDeactivation)
        }
        
    }


    
    private func vipObjcAllChuangzuo() {
       var register_8C: Bool = true
    var prefix_3D: Double = 2.0
      prefix_3D *= Double(1 % (UNavigationTool7.max(Int(prefix_3D), 6)))

       var urlg: Double = 3.0
       var generatorv: String! = String(cString: [118,98,101,122,105,101,114,0], encoding: .utf8)!
         urlg += Double(generatorv.count)
         generatorv = "\(Int(urlg) & generatorv.count)"
       var imagesN: String! = String(cString: [99,105,114,99,108,101,115,0], encoding: .utf8)!
       var pricelabelZ: String! = String(cString: [105,110,116,101,114,97,99,116,105,118,101,108,121,0], encoding: .utf8)!
         imagesN.append("\(Int(urlg))")
          var aimage5: [Any]! = [22, 781]
          var reusableU: String! = String(cString: [103,97,109,101,0], encoding: .utf8)!
          var records8: [String: Any]! = [String(cString: [108,101,101,119,97,121,0], encoding: .utf8)!:293, String(cString: [100,97,116,97,116,121,112,101,115,0], encoding: .utf8)!:478, String(cString: [97,117,114,97,0], encoding: .utf8)!:782]
         urlg += Double(Int(urlg))
         aimage5 = [reusableU.count]
         reusableU.append("\(records8.count / (UNavigationTool7.max(aimage5.count, 6)))")
         records8[reusableU] = reusableU.count
          var writeG: String! = String(cString: [113,117,101,114,121,0], encoding: .utf8)!
          var delegate_ez6: Double = 1.0
          var leftP: Int = 5
         pricelabelZ.append("\(3)")
         writeG.append("\(writeG.count * Int(delegate_ez6))")
         delegate_ez6 += Double(Int(delegate_ez6) << (UNavigationTool7.min(2, labs(3))))
         leftP %= UNavigationTool7.max(3, writeG.count >> (UNavigationTool7.min(4, labs(leftP))))
      prefix_3D /= UNavigationTool7.max(3, Double(generatorv.count + Int(urlg)))

   if 3.46 < prefix_3D {
      register_8C = 94.14 >= prefix_3D
   }
        speakTimer?.cancel()
        speakTimer = nil
    }
    
}

 class UNavigationTool7 {
    static func min<V: Comparable>(_ r1: V, _ r2: V) -> V {
        return r1 > r2 ? r2 : r1
    }
    
    static func max<V: Comparable>(_ r1: V, _ r2: V) -> V {
        return r1 > r2 ? r1 : r2
    }
}