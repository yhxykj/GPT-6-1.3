
import Foundation

import UIKit
import SocketRocket

class SMClassPlay: NSObject {
var qheader_padding: Double = 0.0
var saveOffset: Double = 0.0



    static let shared = SMClassPlay()
    
    var webSocket: SRWebSocket?
    var connectFailedCallBlock: ((Error) -> Void)?
    var connectSuccessCallBlock: (() -> Void)?
    var didReceiveMessageCallBlock: ((String) -> Void)?

@discardableResult
 func restoreRectNeoAccelerateResponderWang(imgQuery: Double) -> Int {
    var open3: String! = String(cString: [99,108,97,115,115,0], encoding: .utf8)!
    var answerb: String! = String(cString: [99,111,108,114,0], encoding: .utf8)!
      open3.append("\(open3.count ^ 3)")
       var not_p7V: Float = 4.0
         not_p7V /= NWModityTool18.max(3, Float(Int(not_p7V)))
      repeat {
         not_p7V -= Float(Int(not_p7V) << (NWModityTool18.min(1, labs(2))))
         if 1449062.0 == not_p7V {
            break
         }
      } while (4.69 == not_p7V) && (1449062.0 == not_p7V)
      if not_p7V < not_p7V {
          var detailsN: Int = 0
          var phoneK: [Any]! = [844, 529]
         not_p7V /= NWModityTool18.max(Float(phoneK.count), 4)
         detailsN %= NWModityTool18.max(detailsN >> (NWModityTool18.min(5, labs(detailsN))), 5)
         phoneK.append(detailsN)
      }
      open3 = "\(Int(not_p7V))"
      answerb = "\((answerb == (String(cString:[88,0], encoding: .utf8)!) ? open3.count : answerb.count))"
      open3.append("\(((String(cString:[84,0], encoding: .utf8)!) == open3 ? open3.count : answerb.count))")
     let chuangzuoWindow_rx: String! = String(cString: [98,111,100,121,0], encoding: .utf8)!
    var starredCompactCodecctl:Int = 0

    return starredCompactCodecctl

}





    
    func connect(scoketlink urlStr: String) {

             restoreRectNeoAccelerateResponderWang(imgQuery:8008.0)


       var pricelabelL: Bool = true
    var answero: String! = String(cString: [117,112,116,105,109,101,0], encoding: .utf8)!
      pricelabelL = answero.hasSuffix("\(pricelabelL)")

      pricelabelL = !pricelabelL
        webSocket = SRWebSocket(url: NSURL(string: urlStr)! as URL)
   while (pricelabelL) {
       var resumptionE: String! = String(cString: [109,111,115,97,105,99,0], encoding: .utf8)!
       var avatarsg: Double = 1.0
       var numberlabelJ: String! = String(cString: [103,101,116,112,97,103,101,115,105,122,101,0], encoding: .utf8)!
         numberlabelJ = "\(resumptionE.count >> (NWModityTool18.min(1, labs(Int(avatarsg)))))"
      for _ in 0 ..< 2 {
          var recognition5: Bool = true
          var parametersz: String! = String(cString: [100,101,116,101,114,109,105,110,97,98,108,101,0], encoding: .utf8)!
         avatarsg += (Double((recognition5 ? 4 : 3) & parametersz.count))
      }
         avatarsg -= Double(numberlabelJ.count ^ Int(avatarsg))
      for _ in 0 ..< 3 {
         resumptionE.append("\(3)")
      }
      repeat {
         resumptionE.append("\(2)")
         if 4460193 == resumptionE.count {
            break
         }
      } while (numberlabelJ.count <= resumptionE.count) && (4460193 == resumptionE.count)
         avatarsg += Double(numberlabelJ.count)
       var menuX: String! = String(cString: [108,105,110,107,0], encoding: .utf8)!
      if 1 <= (menuX.count + 2) {
          var press2: Double = 0.0
          var webC: Bool = false
          var recordingvS: [Any]! = [[String(cString: [97,98,115,100,105,102,102,0], encoding: .utf8)!:316, String(cString: [100,102,108,97,0], encoding: .utf8)!:820]]
         avatarsg += (Double(menuX.count ^ (webC ? 5 : 5)))
         press2 /= NWModityTool18.max(Double(2 >> (NWModityTool18.min(labs(Int(press2)), 1))), 1)
         webC = (recordingvS.contains { $0 as? Double == press2 })
         recordingvS = [recordingvS.count / (NWModityTool18.max(2, 4))]
      }
      while (resumptionE.hasPrefix("\(avatarsg)")) {
          var systemH: Double = 3.0
          var regionw: Float = 4.0
          var queryU: Int = 3
          var sharedj: Double = 2.0
         avatarsg -= Double(2 >> (NWModityTool18.min(labs(Int(avatarsg)), 3)))
         systemH *= Double(2)
         regionw += Float(Int(sharedj) * queryU)
         queryU *= Int(regionw) / (NWModityTool18.max(8, queryU))
         sharedj -= Double(2 << (NWModityTool18.min(labs(queryU), 3)))
         break
      }
      answero.append("\(resumptionE.count - Int(avatarsg))")
      break
   }
        webSocket?.delegate = self
   repeat {
       var searchb: Int = 3
       var relationC: Bool = true
       var itemsb: String! = String(cString: [112,114,101,118,118,101,99,0], encoding: .utf8)!
       var historyq: Double = 0.0
       var statuesO: Double = 1.0
         searchb |= searchb % 3
      if (itemsb.count | 1) < 2 {
          var detectionJ: [String: Any]! = [String(cString: [99,102,102,116,102,0], encoding: .utf8)!:String(cString: [100,101,97,100,105,110,101,0], encoding: .utf8)!, String(cString: [111,102,102,105,99,105,97,108,0], encoding: .utf8)!:String(cString: [99,111,117,112,108,105,110,103,0], encoding: .utf8)!, String(cString: [102,97,100,115,116,0], encoding: .utf8)!:String(cString: [97,100,106,97,99,101,110,116,0], encoding: .utf8)!]
          var zhidingesq: Bool = false
          var verticalf: Int = 4
          var r_imageB: Double = 5.0
          var collt: String! = String(cString: [97,118,120,115,121,110,116,104,0], encoding: .utf8)!
         searchb -= searchb
         detectionJ["\(verticalf)"] = detectionJ.count & verticalf
         zhidingesq = verticalf < 75
         r_imageB -= Double(Int(r_imageB))
         collt = "\(verticalf ^ detectionJ.count)"
      }
         searchb ^= 2 * itemsb.count
         historyq -= (Double(itemsb == (String(cString:[80,0], encoding: .utf8)!) ? searchb : itemsb.count))
      repeat {
         relationC = (String(cString:[65,0], encoding: .utf8)!) == itemsb
         if relationC ? !relationC : relationC {
            break
         }
      } while (relationC ? !relationC : relationC) && ((searchb << (NWModityTool18.min(labs(4), 5))) >= 2 || 4 >= searchb)
         relationC = searchb <= 96
         searchb += Int(historyq)
         searchb *= ((relationC ? 5 : 5) << (NWModityTool18.min(labs(2), 4)))
      for _ in 0 ..< 1 {
          var aimageJ: Bool = true
          var heady: String! = String(cString: [101,118,98,117,102,102,101,114,0], encoding: .utf8)!
          var main_bY: Double = 3.0
          var modityk: String! = String(cString: [117,116,105,108,115,0], encoding: .utf8)!
          var btnJ: String! = String(cString: [102,95,50,53,0], encoding: .utf8)!
         relationC = (statuesO - Double(heady.count)) == 50.45
         aimageJ = main_bY == 90.8
         heady = "\(((aimageJ ? 2 : 5) / (NWModityTool18.max(Int(main_bY), 3))))"
         modityk = "\(2)"
         btnJ.append("\(((aimageJ ? 3 : 1) | Int(main_bY)))")
      }
      while (relationC) {
         relationC = searchb >= 3 && !relationC
         break
      }
      for _ in 0 ..< 1 {
         statuesO += (Double((String(cString:[52,0], encoding: .utf8)!) == itemsb ? (relationC ? 3 : 4) : itemsb.count))
      }
      repeat {
         statuesO /= NWModityTool18.max(2, Double(Int(statuesO)))
         if 3998497.0 == statuesO {
            break
         }
      } while (3998497.0 == statuesO) && (3.83 == (5.91 + statuesO))
          var rowg: String! = String(cString: [114,101,103,101,120,0], encoding: .utf8)!
          var recordsK: Bool = true
         searchb <<= NWModityTool18.min(2, labs((itemsb.count | (relationC ? 5 : 3))))
         rowg.append("\(((recordsK ? 3 : 4) / 1))")
         recordsK = !recordsK
      repeat {
          var time_vno: String! = String(cString: [115,112,101,99,105,102,105,101,100,0], encoding: .utf8)!
         statuesO /= NWModityTool18.max(3, (Double(Int(statuesO) * (relationC ? 1 : 3))))
         time_vno.append("\(time_vno.count & 3)")
         if 4440218.0 == statuesO {
            break
         }
      } while ((4 | searchb) >= 4) && (4440218.0 == statuesO)
         itemsb.append("\((2 + (relationC ? 5 : 3)))")
      pricelabelL = 93 == answero.count
      if pricelabelL ? !pricelabelL : pricelabelL {
         break
      }
   } while (pricelabelL ? !pricelabelL : pricelabelL) && (answero.count >= 4)
        webSocket?.open()
        
    }

@discardableResult
 func performObjcExpirationCapacity(sendObject: Bool, gundResume: String!) -> String! {
    var popupJ: String! = String(cString: [117,112,108,111,97,100,97,98,108,101,0], encoding: .utf8)!
    var instance6: String! = String(cString: [115,117,98,114,97,110,103,101,115,0], encoding: .utf8)!
      popupJ.append("\(popupJ.count)")
       var activitylabelQ: Double = 1.0
          var base3: Double = 2.0
          var verifyv: String! = String(cString: [98,97,99,107,101,100,0], encoding: .utf8)!
         activitylabelQ += Double(3 - Int(base3))
         base3 /= NWModityTool18.max(4, Double(verifyv.count - verifyv.count))
         activitylabelQ -= Double(Int(activitylabelQ) << (NWModityTool18.min(2, labs(2))))
         activitylabelQ /= NWModityTool18.max(5, Double(1))
      popupJ.append("\(popupJ.count * instance6.count)")
      popupJ = "\(1)"
   return popupJ

}





    
    func disconnect() {

             performObjcExpirationCapacity(sendObject:true, gundResume:String(cString: [100,105,114,101,99,116,105,111,110,0], encoding: .utf8)!)


       var popupw: Double = 5.0
    var ringbufferb: Double = 1.0
    var recordingv1: String! = String(cString: [105,110,99,108,117,100,105,110,103,0], encoding: .utf8)!
      ringbufferb *= Double(1)
   if 1.51 == (3.55 / (NWModityTool18.max(2, ringbufferb))) {
      ringbufferb *= Double(recordingv1.count | Int(popupw))
   }
      recordingv1 = "\(2 << (NWModityTool18.min(2, labs(Int(ringbufferb)))))"
   repeat {
       var searcht: String! = String(cString: [105,115,109,101,109,115,101,116,0], encoding: .utf8)!
       var e_productsN: Double = 0.0
       var headc: Int = 0
       var datai: String! = String(cString: [102,114,97,109,101,104,97,115,104,0], encoding: .utf8)!
         headc *= datai.count
          var selectedF: String! = String(cString: [104,97,114,100,101,110,101,100,0], encoding: .utf8)!
          var rateP: String! = String(cString: [105,100,115,117,98,116,121,112,101,0], encoding: .utf8)!
          var disconnect3: String! = String(cString: [118,105,110,116,101,114,112,111,108,97,116,111,114,0], encoding: .utf8)!
         headc /= NWModityTool18.max(4, datai.count)
         selectedF = "\(((String(cString:[73,0], encoding: .utf8)!) == disconnect3 ? rateP.count : disconnect3.count))"
         rateP.append("\(1 | selectedF.count)")
          var aimaged: Int = 1
         e_productsN -= Double(aimaged)
      if (datai.count | 1) <= 4 || (datai.count | 1) <= 5 {
          var successP: String! = String(cString: [101,118,114,99,0], encoding: .utf8)!
          var activityf: Bool = false
         datai.append("\(2)")
         successP.append("\(2)")
         activityf = !successP.hasSuffix("\(activityf)")
      }
       var handingh: Int = 2
       var c_widtht: Int = 2
      while (Int(e_productsN) <= headc) {
         headc /= NWModityTool18.max(2, 2 / (NWModityTool18.max(10, handingh)))
         break
      }
          var pathk: [String: Any]! = [String(cString: [102,114,101,111,112,101,110,0], encoding: .utf8)!:0, String(cString: [99,102,102,116,98,0], encoding: .utf8)!:112]
          var showT: [Any]! = [UILabel()]
         headc |= 3 << (NWModityTool18.min(1, labs(c_widtht)))
         pathk = ["\(pathk.values.count)": pathk.count >> (NWModityTool18.min(showT.count, 3))]
         showT.append(1 << (NWModityTool18.min(5, pathk.values.count)))
       var henga: String! = String(cString: [111,102,102,101,114,0], encoding: .utf8)!
         henga.append("\(searcht.count * 1)")
      for _ in 0 ..< 1 {
          var thresholdJ: String! = String(cString: [109,97,115,107,115,0], encoding: .utf8)!
          var findJ: Int = 2
         c_widtht %= NWModityTool18.max(2, 5)
         thresholdJ = "\((thresholdJ == (String(cString:[48,0], encoding: .utf8)!) ? thresholdJ.count : findJ))"
         findJ >>= NWModityTool18.min(3, labs(findJ))
      }
      repeat {
          var elevtT: Bool = true
          var alabelz: String! = String(cString: [112,117,114,101,0], encoding: .utf8)!
          var rowS: String! = String(cString: [115,101,110,100,105,110,103,0], encoding: .utf8)!
          var codee: String! = String(cString: [115,99,97,108,101,102,97,99,116,111,114,115,0], encoding: .utf8)!
          var collK: String! = String(cString: [103,101,116,116,101,114,0], encoding: .utf8)!
         handingh *= 1
         elevtT = codee == collK
         alabelz.append("\(((String(cString:[80,0], encoding: .utf8)!) == collK ? collK.count : (elevtT ? 5 : 5)))")
         rowS = "\(collK.count ^ 3)"
         codee = "\(collK.count + codee.count)"
         if 4279466 == handingh {
            break
         }
      } while (headc >= 5) && (4279466 == handingh)
         henga = "\(1)"
      ringbufferb -= Double(3 | Int(ringbufferb))
      if 4836269.0 == ringbufferb {
         break
      }
   } while (popupw > 2.4) && (4836269.0 == ringbufferb)

      popupw += Double(recordingv1.count - 3)
       var utilsn: String! = String(cString: [118,114,101,103,105,111,110,0], encoding: .utf8)!
       var headers6: String! = String(cString: [111,110,101,115,0], encoding: .utf8)!
       var imagesj: Float = 1.0
          var nameA: Float = 4.0
          var rmbT: String! = String(cString: [112,114,111,0], encoding: .utf8)!
         headers6.append("\(Int(nameA))")
         rmbT.append("\(rmbT.count - rmbT.count)")
         utilsn = "\(utilsn.count * Int(imagesj))"
          var pricelabels: String! = String(cString: [99,111,110,102,105,103,117,114,97,116,105,111,110,115,0], encoding: .utf8)!
          var playingP: String! = String(cString: [114,101,99,104,117,110,107,0], encoding: .utf8)!
          var tableF: String! = String(cString: [114,101,108,97,116,105,118,101,0], encoding: .utf8)!
         headers6.append("\(tableF.count)")
         pricelabels = "\(pricelabels.count >> (NWModityTool18.min(labs(2), 2)))"
         playingP.append("\(pricelabels.count % (NWModityTool18.max(10, playingP.count)))")
         tableF.append("\(((String(cString:[73,0], encoding: .utf8)!) == pricelabels ? pricelabels.count : playingP.count))")
      repeat {
         imagesj -= (Float(headers6 == (String(cString:[48,0], encoding: .utf8)!) ? headers6.count : Int(imagesj)))
         if 1315954.0 == imagesj {
            break
         }
      } while (5 >= (headers6.count * 2)) && (1315954.0 == imagesj)
         utilsn.append("\(3)")
      repeat {
          var sumC: Double = 3.0
          var dataU: Double = 0.0
          var heng0: String! = String(cString: [98,117,102,102,101,114,0], encoding: .utf8)!
          var successj: String! = String(cString: [121,109,111,100,101,0], encoding: .utf8)!
          var successx: String! = String(cString: [115,116,114,116,97,103,0], encoding: .utf8)!
         utilsn = "\(((String(cString:[66,0], encoding: .utf8)!) == successj ? heng0.count : successj.count))"
         sumC += Double(Int(sumC))
         dataU *= Double(Int(sumC) * Int(dataU))
         heng0.append("\(Int(dataU) + successx.count)")
         successx = "\(Int(dataU))"
         if utilsn.count == 4736322 {
            break
         }
      } while (utilsn.count == 4736322) && (utilsn.count == 3)
      while (4.99 < imagesj) {
          var gundD: String! = String(cString: [113,112,113,115,99,97,108,101,0], encoding: .utf8)!
          var aboutD: Int = 1
         utilsn.append("\(utilsn.count << (NWModityTool18.min(labs(1), 1)))")
         gundD.append("\(aboutD)")
         aboutD &= gundD.count
         break
      }
         headers6 = "\(Int(imagesj) >> (NWModityTool18.min(labs(3), 1)))"
         imagesj *= Float(1)
      recordingv1.append("\(recordingv1.count + 1)")
        webSocket?.close()
    }
    
}

extension SMClassPlay: SRWebSocketDelegate {

@discardableResult
 func returnedFormatSpeeds(aidaGenerate: Bool) -> [String: Any]! {
    var gifo: String! = String(cString: [104,119,116,105,109,101,0], encoding: .utf8)!
    var type_0a: [String: Any]! = [String(cString: [104,111,108,101,0], encoding: .utf8)!:[String(cString: [102,105,101,108,100,0], encoding: .utf8)!:String(cString: [105,110,116,108,105,115,116,0], encoding: .utf8)!, String(cString: [109,117,108,116,105,112,108,121,120,0], encoding: .utf8)!:String(cString: [100,101,99,108,97,114,101,0], encoding: .utf8)!]]
   while ((1 >> (NWModityTool18.min(3, gifo.count))) <= 3) {
       var pathP: String! = String(cString: [98,117,108,108,101,116,115,0], encoding: .utf8)!
       var networkT: Float = 1.0
       var transactions2: Bool = false
       var successN: Int = 1
       var numberu: Float = 1.0
      if (successN / 5) >= 1 && transactions2 {
         transactions2 = pathP.hasSuffix("\(networkT)")
      }
       var contentsc: Int = 0
       var pauseL: [String: Any]! = [String(cString: [119,97,116,101,114,109,97,114,107,115,0], encoding: .utf8)!:true]
          var toplayout5: String! = String(cString: [99,104,97,110,103,101,0], encoding: .utf8)!
         contentsc <<= NWModityTool18.min(labs((Int(networkT) ^ (transactions2 ? 3 : 2))), 5)
         toplayout5 = "\(toplayout5.count ^ 3)"
         networkT *= Float(1)
      if (1.85 / (NWModityTool18.max(3, numberu))) >= 3.63 && 1.85 >= numberu {
         transactions2 = contentsc >= pauseL.keys.count
      }
         numberu -= Float(3)
         pauseL = ["\(contentsc)": 2]
       var enteru: Bool = true
       var successj: Bool = false
          var ringv: String! = String(cString: [115,116,114,105,110,103,0], encoding: .utf8)!
          var aboutJ: Float = 3.0
          var enginee: String! = String(cString: [104,97,97,114,0], encoding: .utf8)!
         pauseL = ["\(aboutJ)": ringv.count * Int(aboutJ)]
         ringv = "\(enginee.count >> (NWModityTool18.min(labs(2), 4)))"
      if 3 == (successN >> (NWModityTool18.min(5, labs(contentsc)))) {
         successN >>= NWModityTool18.min(labs(((enteru ? 2 : 4) ^ contentsc)), 5)
      }
         successN &= successN
         pathP.append("\(1 | contentsc)")
      while (4 >= (contentsc + 1) || !successj) {
         successj = enteru
         break
      }
      if networkT < Float(pathP.count) {
          var action1: Bool = true
          var reusableT: Int = 0
          var checkf: String! = String(cString: [97,100,100,101,114,0], encoding: .utf8)!
          var speechc: String! = String(cString: [111,112,101,114,97,116,111,114,115,0], encoding: .utf8)!
          var closew: Int = 2
         pathP.append("\(2)")
         action1 = !action1 && 62 >= reusableT
         reusableT += 2 ^ checkf.count
         checkf = "\(2 / (NWModityTool18.max(10, closew)))"
         speechc = "\(2)"
         closew += (checkf.count | (action1 ? 5 : 1))
      }
      gifo.append("\(Int(numberu))")
      break
   }
   for _ in 0 ..< 3 {
      gifo = "\(type_0a.values.count / 2)"
   }
       var purchase9: String! = String(cString: [119,111,114,107,108,111,97,100,0], encoding: .utf8)!
       var pickerT: String! = String(cString: [119,101,108,108,0], encoding: .utf8)!
          var pasteboard3: [Any]! = [String(cString: [115,99,101,110,101,0], encoding: .utf8)!, String(cString: [99,108,101,97,110,115,101,0], encoding: .utf8)!, String(cString: [110,101,120,116,0], encoding: .utf8)!]
          var gundongA: String! = String(cString: [109,97,103,110,105,102,105,101,114,0], encoding: .utf8)!
         purchase9.append("\(2 / (NWModityTool18.max(7, pickerT.count)))")
         pasteboard3 = [gundongA.count / 2]
         gundongA.append("\(1 - pasteboard3.count)")
      if !pickerT.contains("\(purchase9.count)") {
          var firstY: Bool = false
          var appW: [Any]! = [968, 917]
          var bundlee: [String: Any]! = [String(cString: [106,95,51,48,0], encoding: .utf8)!:false]
          var completedb: String! = String(cString: [119,101,98,112,0], encoding: .utf8)!
         purchase9.append("\(purchase9.count / (NWModityTool18.max(pickerT.count, 10)))")
         firstY = bundlee.count > 25
         appW = [appW.count << (NWModityTool18.min(5, bundlee.keys.count))]
         completedb.append("\(appW.count << (NWModityTool18.min(completedb.count, 2)))")
      }
          var recognizera: String! = String(cString: [112,95,49,57,95,115,101,114,118,101,114,115,0], encoding: .utf8)!
          var sourceq: Int = 4
          var closeY: [Any]! = [698, 109, 221]
         purchase9.append("\(3)")
         recognizera = "\(sourceq >> (NWModityTool18.min(recognizera.count, 5)))"
         sourceq <<= NWModityTool18.min(labs(closeY.count & sourceq), 4)
         closeY = [(recognizera == (String(cString:[97,0], encoding: .utf8)!) ? recognizera.count : sourceq)]
         pickerT = "\(1 ^ pickerT.count)"
      while (pickerT != String(cString:[100,0], encoding: .utf8)!) {
          var titlelabel1: [Any]! = [508, 572, 889]
          var dictM: Float = 1.0
          var chatM: [String: Any]! = [String(cString: [109,97,100,100,0], encoding: .utf8)!:725, String(cString: [118,101,114,105,102,105,99,97,116,105,111,110,0], encoding: .utf8)!:925, String(cString: [116,117,114,98,111,106,112,101,103,0], encoding: .utf8)!:468]
          var infoM: String! = String(cString: [109,97,121,0], encoding: .utf8)!
         purchase9.append("\(titlelabel1.count)")
         titlelabel1.append(chatM.keys.count & 3)
         dictM *= Float(infoM.count >> (NWModityTool18.min(1, chatM.keys.count)))
         infoM.append("\(3 << (NWModityTool18.min(3, labs(Int(dictM)))))")
         break
      }
         purchase9.append("\((pickerT == (String(cString:[105,0], encoding: .utf8)!) ? pickerT.count : purchase9.count))")
      gifo.append("\(gifo.count ^ type_0a.count)")
   repeat {
       var decibela: Float = 4.0
       var failedJ: String! = String(cString: [116,111,100,97,121,115,0], encoding: .utf8)!
       var flowM: Double = 2.0
       var addressM: Double = 0.0
         decibela += Float(Int(addressM) >> (NWModityTool18.min(labs(Int(decibela)), 1)))
         flowM -= Double(3)
         flowM /= NWModityTool18.max(3, Double(Int(decibela) & 1))
      for _ in 0 ..< 1 {
         failedJ = "\(3 << (NWModityTool18.min(labs(Int(flowM)), 3)))"
      }
      if (failedJ.count + Int(flowM)) < 3 || (3.5 + flowM) < 2.59 {
          var receiveL: Double = 4.0
          var qlabelJ: Float = 2.0
          var picy: Int = 5
          var itemV: String! = String(cString: [109,111,116,105,111,110,112,105,120,101,108,115,0], encoding: .utf8)!
         flowM += Double(2 * Int(receiveL))
         receiveL *= Double(picy + itemV.count)
         qlabelJ /= NWModityTool18.max(Float(itemV.count), 5)
         picy += picy
      }
         flowM += Double(Int(flowM) & 3)
       var m_widthj: Double = 5.0
       var resultk: Double = 2.0
         failedJ = "\(Int(flowM) / (NWModityTool18.max(failedJ.count, 6)))"
         failedJ = "\(Int(decibela) | 3)"
         resultk -= Double(Int(resultk) & 3)
          var about4: String! = String(cString: [116,114,97,118,101,108,0], encoding: .utf8)!
          var statuesA: String! = String(cString: [111,103,103,100,101,99,0], encoding: .utf8)!
         resultk *= Double(Int(flowM))
         about4.append("\(1 - statuesA.count)")
         statuesA = "\((statuesA == (String(cString:[79,0], encoding: .utf8)!) ? about4.count : statuesA.count))"
      repeat {
         decibela *= Float(Int(flowM))
         if decibela == 2004774.0 {
            break
         }
      } while ((Double(Int(resultk) - 5)) == 3.78) && (decibela == 2004774.0)
      gifo.append("\(Int(flowM) + Int(addressM))")
      if (String(cString:[122,108,115,51,107,99,99,106,113,0], encoding: .utf8)!) == gifo {
         break
      }
   } while ((String(cString:[122,108,115,51,107,99,99,106,113,0], encoding: .utf8)!) == gifo) && (1 == (gifo.count ^ type_0a.values.count) || 1 == (type_0a.values.count ^ gifo.count))
   return type_0a

}





    
    func webSocketDidOpen(_ webSocket: SRWebSocket) {

             returnedFormatSpeeds(aidaGenerate:true)


       var slider1: String! = String(cString: [99,104,97,105,110,115,0], encoding: .utf8)!
    var anew__r_: String! = String(cString: [104,109,97,99,0], encoding: .utf8)!
   while (1 <= slider1.count) {
      slider1.append("\(slider1.count)")
      break
   }
   while (slider1.count <= anew__r_.count) {
      slider1.append("\(slider1.count)")
      break
   }

   if anew__r_ == slider1 {
      slider1 = "\(slider1.count)"
   }
   repeat {
      slider1.append("\(anew__r_.count)")
      if 125996 == slider1.count {
         break
      }
   } while (4 == anew__r_.count && 4 == slider1.count) && (125996 == slider1.count)
        print("WebSocket 连接成功")
        connectSuccessCallBlock?()
    }

@discardableResult
 func postRestoredSpeechFoundationNotificationPartial(activityNumberlabel: [String: Any]!, editBody: String!, listDelete_s: Int) -> [String: Any]! {
    var done7: String! = String(cString: [103,101,116,120,118,97,114,0], encoding: .utf8)!
    var ringE: String! = String(cString: [102,97,115,116,116,101,115,116,0], encoding: .utf8)!
       var avatar5: Float = 5.0
       var gnewsf: String! = String(cString: [109,101,116,97,100,97,116,97,115,0], encoding: .utf8)!
          var itemso: String! = String(cString: [114,101,109,97,105,110,95,101,95,56,57,0], encoding: .utf8)!
          var userc: [String: Any]! = [String(cString: [113,117,97,110,116,115,0], encoding: .utf8)!:true]
          var chuan8: String! = String(cString: [97,101,118,97,108,0], encoding: .utf8)!
         gnewsf.append("\((gnewsf == (String(cString:[122,0], encoding: .utf8)!) ? gnewsf.count : chuan8.count))")
         itemso = "\(2 * itemso.count)"
         userc = ["\(userc.keys.count)": userc.values.count ^ itemso.count]
         chuan8 = "\(2)"
      while ((5 - gnewsf.count) >= 2 && 5 >= (Int(avatar5) - gnewsf.count)) {
          var share4: [String: Any]! = [String(cString: [115,116,114,117,99,116,117,114,101,95,112,95,53,52,0], encoding: .utf8)!:28, String(cString: [116,114,97,110,115,109,105,116,116,101,100,95,108,95,50,49,0], encoding: .utf8)!:247, String(cString: [99,108,116,111,115,116,114,0], encoding: .utf8)!:116]
         gnewsf = "\(((String(cString:[103,0], encoding: .utf8)!) == gnewsf ? gnewsf.count : share4.keys.count))"
         break
      }
         gnewsf = "\(1 ^ Int(avatar5))"
         avatar5 /= NWModityTool18.max(4, Float(Int(avatar5) & 3))
      while ((gnewsf.count - Int(avatar5)) >= 3 || (3 & gnewsf.count) >= 1) {
          var prefix_6d: [String: Any]! = [String(cString: [102,114,97,110,100,0], encoding: .utf8)!:403, String(cString: [117,110,105,100,101,110,116,105,102,105,101,100,95,55,95,54,48,0], encoding: .utf8)!:400]
         avatar5 *= Float(gnewsf.count * 3)
         prefix_6d = ["\(prefix_6d.values.count)": prefix_6d.count - prefix_6d.values.count]
         break
      }
      repeat {
         avatar5 /= NWModityTool18.max(3, Float(Int(avatar5)))
         if avatar5 == 3166492.0 {
            break
         }
      } while (avatar5 == 3166492.0) && (!gnewsf.contains("\(avatar5)"))
      ringE = "\(1 >> (NWModityTool18.min(3, ringE.count)))"
   if done7.contains(ringE) {
       var enginel: Bool = true
       var numberp: Bool = true
       var register_veS: Double = 5.0
       var confirmh: String! = String(cString: [109,105,99,114,111,100,118,100,0], encoding: .utf8)!
      if register_veS < 3.97 {
         register_veS /= NWModityTool18.max(Double(1), 3)
      }
      for _ in 0 ..< 3 {
          var audioW: [Any]! = [51, 828, 395]
          var liste: Double = 5.0
          var strG: Float = 5.0
          var tap8: Int = 0
         confirmh.append("\((Int(strG) + (enginel ? 3 : 5)))")
         audioW = [Int(liste)]
         liste += Double(audioW.count)
         strG *= Float(tap8 % 2)
         tap8 *= 3 | tap8
      }
      while (!numberp) {
          var obj1: Int = 2
          var activitylabelg: Double = 0.0
          var sourceq: Float = 1.0
         enginel = !enginel
         obj1 >>= NWModityTool18.min(labs(2), 2)
         activitylabelg -= Double(1 * obj1)
         sourceq /= NWModityTool18.max(1, Float(Int(sourceq) << (NWModityTool18.min(labs(Int(activitylabelg)), 2))))
         break
      }
      while (!numberp) {
         enginel = confirmh.count == 7
         break
      }
         register_veS /= NWModityTool18.max((Double((numberp ? 4 : 4) << (NWModityTool18.min(labs(Int(register_veS)), 1)))), 3)
         numberp = 45.17 == register_veS && enginel
      for _ in 0 ..< 2 {
         confirmh = "\(1 & confirmh.count)"
      }
          var save0: [String: Any]! = [String(cString: [99,95,56,51,95,98,105,116,120,0], encoding: .utf8)!:4389.0]
          var numberlabelx: Int = 5
         numberp = enginel
         save0["\(numberlabelx)"] = 2
         numberlabelx %= NWModityTool18.max(4, save0.values.count)
          var navP: Int = 5
          var controllb: Double = 1.0
         numberp = enginel || 25 > navP
         navP ^= Int(controllb) % 2
         controllb *= Double(Int(controllb) << (NWModityTool18.min(labs(Int(controllb)), 3)))
         register_veS *= (Double((numberp ? 4 : 3)))
      repeat {
         numberp = register_veS >= 37.42
         if numberp ? !numberp : numberp {
            break
         }
      } while (numberp) && (numberp ? !numberp : numberp)
      repeat {
         enginel = !numberp
         if enginel ? !enginel : enginel {
            break
         }
      } while (enginel ? !enginel : enginel) && (!numberp)
      ringE.append("\(3 ^ ringE.count)")
   }
   repeat {
       var upgrades2: Bool = true
       var relationa: Float = 1.0
      repeat {
         relationa -= (Float(Int(relationa) % (NWModityTool18.max(8, (upgrades2 ? 5 : 3)))))
         if relationa == 4882375.0 {
            break
         }
      } while (!upgrades2) && (relationa == 4882375.0)
         relationa *= Float(Int(relationa) ^ 2)
      while ((3.59 / (NWModityTool18.max(2, relationa))) <= 1.78 && upgrades2) {
         relationa *= Float(Int(relationa) + 3)
         break
      }
         upgrades2 = !upgrades2
         upgrades2 = !upgrades2
          var collectionC: String! = String(cString: [122,101,114,111,0], encoding: .utf8)!
          var selected0: Bool = true
          var recognition0: String! = String(cString: [101,120,99,101,112,116,105,111,110,0], encoding: .utf8)!
         relationa -= (Float((selected0 ? 4 : 4)))
         collectionC.append("\(1)")
         selected0 = (String(cString:[106,0], encoding: .utf8)!) == collectionC
         recognition0.append("\(recognition0.count >> (NWModityTool18.min(5, collectionC.count)))")
      ringE.append("\(((String(cString:[90,0], encoding: .utf8)!) == ringE ? done7.count : ringE.count))")
      if 1324695 == ringE.count {
         break
      }
   } while (1324695 == ringE.count) && (done7 != String(cString:[112,0], encoding: .utf8)!)
     var playerPhoto: Bool = false
    var armonySiprSubband :[String: Any]! = [String: Any]()
    playerPhoto = false
    armonySiprSubband.updateValue(playerPhoto, forKey:String(cString: [118,0], encoding: .utf8)!)

    return armonySiprSubband

}





    
    func webSocket(_ webSocket: SRWebSocket, didFailWithError error: Error) {

             postRestoredSpeechFoundationNotificationPartial(activityNumberlabel:[String(cString: [101,114,114,109,115,103,0], encoding: .utf8)!:292, String(cString: [99,95,54,55,95,99,111,110,116,105,110,117,97,108,0], encoding: .utf8)!:968, String(cString: [98,105,111,109,101,116,114,105,99,0], encoding: .utf8)!:607], editBody:String(cString: [114,105,110,100,101,120,0], encoding: .utf8)!, listDelete_s:1622)


       var config0: String! = String(cString: [105,116,117,110,101,115,0], encoding: .utf8)!
    var dictionaryi: Double = 0.0
   while ((4.83 + dictionaryi) == 1.95 || (5 << (NWModityTool18.min(5, config0.count))) == 3) {
      dictionaryi *= Double(config0.count | Int(dictionaryi))
      break
   }
       var scaleZ: [Any]! = [60, 472]
       var screenm: Double = 1.0
       var normal2: Double = 1.0
       var y_heightP: Double = 0.0
      for _ in 0 ..< 3 {
          var loginJ: String! = String(cString: [111,108,111,114,0], encoding: .utf8)!
          var head4: Double = 4.0
         screenm /= NWModityTool18.max(Double(3), 4)
         loginJ.append("\((loginJ == (String(cString:[54,0], encoding: .utf8)!) ? Int(head4) : loginJ.count))")
         head4 -= Double(1 & Int(head4))
      }
      if 2 < (scaleZ.count + Int(y_heightP)) && 5.83 < (1.51 + y_heightP) {
         y_heightP -= Double(2 - Int(normal2))
      }
      if 4 > (scaleZ.count / (NWModityTool18.max(7, Int(screenm)))) || (1.5 / (NWModityTool18.max(6, screenm))) > 3.32 {
          var silenceK: String! = String(cString: [99,97,105,112,0], encoding: .utf8)!
          var serviceZ: [String: Any]! = [String(cString: [101,105,112,118,0], encoding: .utf8)!:String(cString: [114,103,116,99,0], encoding: .utf8)!, String(cString: [114,101,99,111,109,109,101,110,100,0], encoding: .utf8)!:String(cString: [97,100,100,114,105,110,102,111,0], encoding: .utf8)!]
         scaleZ = [1 & Int(y_heightP)]
         silenceK.append("\(3 >> (NWModityTool18.min(3, serviceZ.values.count)))")
         serviceZ = ["\(serviceZ.count)": 1]
      }
       var chuangzuoF: String! = String(cString: [112,97,114,116,105,99,105,112,97,110,116,0], encoding: .utf8)!
      repeat {
         y_heightP *= Double(Int(normal2))
         if y_heightP == 1742507.0 {
            break
         }
      } while (y_heightP == 1742507.0) && (!scaleZ.contains { $0 as? Double == y_heightP })
      config0 = "\(2 % (NWModityTool18.max(8, Int(screenm))))"

       var ylabelJ: String! = String(cString: [116,104,105,99,107,0], encoding: .utf8)!
       var collateM: String! = String(cString: [114,116,112,101,110,99,0], encoding: .utf8)!
       var first4: String! = String(cString: [100,101,116,101,114,109,105,110,105,115,116,105,99,0], encoding: .utf8)!
         collateM.append("\(1)")
      for _ in 0 ..< 2 {
         first4.append("\(collateM.count % (NWModityTool18.max(2, first4.count)))")
      }
      repeat {
          var picg: Int = 5
          var alabelT: String! = String(cString: [109,98,103,114,97,112,104,0], encoding: .utf8)!
         collateM.append("\(1 * alabelT.count)")
         picg -= picg
         alabelT = "\(picg * picg)"
         if 1260358 == collateM.count {
            break
         }
      } while (first4 != String(cString:[87,0], encoding: .utf8)!) && (1260358 == collateM.count)
          var delegate_m0: [String: Any]! = [String(cString: [105,100,101,110,116,105,116,105,101,115,0], encoding: .utf8)!:28, String(cString: [121,95,49,48,95,115,112,100,105,102,0], encoding: .utf8)!:362]
          var y_heightD: Bool = true
          var messageR: Float = 1.0
         ylabelJ = "\(first4.count)"
         delegate_m0 = ["\(delegate_m0.values.count)": Int(messageR)]
         y_heightD = 57 == delegate_m0.keys.count || 37.87 == messageR
       var loadingk: Bool = true
      if loadingk {
         loadingk = ylabelJ.count >= 45
      }
      while (collateM.contains(first4)) {
         first4 = "\(((String(cString:[79,0], encoding: .utf8)!) == first4 ? collateM.count : first4.count))"
         break
      }
          var bottom1: String! = String(cString: [109,105,110,115,0], encoding: .utf8)!
         loadingk = first4.count < 15
         bottom1 = "\((bottom1 == (String(cString:[81,0], encoding: .utf8)!) ? bottom1.count : bottom1.count))"
      while (ylabelJ == String(cString:[82,0], encoding: .utf8)!) {
          var tabler: Double = 0.0
          var navU: Double = 3.0
          var cleanupF: String! = String(cString: [100,101,99,114,121,112,116,0], encoding: .utf8)!
         first4 = "\(ylabelJ.count)"
         tabler += Double(cleanupF.count)
         navU *= Double(Int(navU) ^ 3)
         cleanupF = "\(cleanupF.count)"
         break
      }
      dictionaryi -= Double(ylabelJ.count << (NWModityTool18.min(config0.count, 5)))
   while ((dictionaryi + 1.69) > 5.79) {
       var recognitiona: Double = 2.0
       var toolsD: String! = String(cString: [111,108,100,0], encoding: .utf8)!
       var buffer9: String! = String(cString: [116,105,100,121,0], encoding: .utf8)!
         toolsD = "\(2 << (NWModityTool18.min(4, buffer9.count)))"
      for _ in 0 ..< 1 {
          var time_wN: [String: Any]! = [String(cString: [110,116,104,0], encoding: .utf8)!:134, String(cString: [118,95,49,51,0], encoding: .utf8)!:654, String(cString: [116,111,98,105,116,0], encoding: .utf8)!:335]
          var tablec: Double = 0.0
         buffer9 = "\(buffer9.count + 1)"
         time_wN = ["\(time_wN.keys.count)": time_wN.count]
         tablec /= NWModityTool18.max(Double(Int(tablec)), 5)
      }
         toolsD.append("\(buffer9.count << (NWModityTool18.min(labs(2), 5)))")
      for _ in 0 ..< 3 {
         toolsD.append("\(Int(recognitiona))")
      }
         recognitiona -= Double(3 << (NWModityTool18.min(2, labs(Int(recognitiona)))))
      repeat {
          var sousuoP: Bool = false
         toolsD.append("\(toolsD.count % 3)")
         sousuoP = !sousuoP
         if toolsD == (String(cString:[57,52,56,112,49,50,115,0], encoding: .utf8)!) {
            break
         }
      } while (toolsD.count < 2) && (toolsD == (String(cString:[57,52,56,112,49,50,115,0], encoding: .utf8)!))
         buffer9 = "\(2 << (NWModityTool18.min(3, toolsD.count)))"
       var contentw: Double = 5.0
         buffer9.append("\(3)")
      dictionaryi /= NWModityTool18.max(Double(buffer9.count | 1), 5)
      break
   }
        print("WebSocket 连接失败")
        connectFailedCallBlock?(error)
    }

    
    func webSocket(_ webSocket: SRWebSocket, didReceiveMessage message: Any) {
       var modityM: Bool = false
    var descripth: String! = String(cString: [116,104,114,101,97,100,103,114,111,117,112,0], encoding: .utf8)!
    var resultM: Float = 3.0
       var findG: Double = 5.0
      for _ in 0 ..< 3 {
         findG += Double(Int(findG) - Int(findG))
      }
      if (Double(2 * Int(findG))) >= 5.19 {
          var ynews8: Double = 0.0
          var saveY: String! = String(cString: [115,99,97,110,115,0], encoding: .utf8)!
          var rmbz: Bool = true
         findG -= Double(saveY.count)
         ynews8 /= NWModityTool18.max(1, Double(1 - Int(ynews8)))
         saveY.append("\(1)")
      }
         findG *= Double(Int(findG))
      resultM -= Float(2)
   for _ in 0 ..< 1 {
       var configT: [String: Any]! = [String(cString: [117,110,115,112,101,99,105,102,105,101,100,0], encoding: .utf8)!:542, String(cString: [105,119,97,108,115,104,0], encoding: .utf8)!:558, String(cString: [100,105,115,112,111,115,97,98,108,101,115,0], encoding: .utf8)!:348]
      while (5 <= (1 / (NWModityTool18.max(9, configT.keys.count))) || 2 <= (configT.keys.count / (NWModityTool18.max(1, 7)))) {
         configT["\(configT.values.count)"] = configT.count
         break
      }
         configT = ["\(configT.keys.count)": configT.count & 2]
          var navigationw: String! = String(cString: [110,119,105,115,101,0], encoding: .utf8)!
         configT = ["\(configT.keys.count)": 1]
         navigationw.append("\(((String(cString:[115,0], encoding: .utf8)!) == navigationw ? navigationw.count : navigationw.count))")
      descripth = "\(Int(resultM) + descripth.count)"
   }

   repeat {
       var numberN: String! = String(cString: [115,116,101,108,108,97,114,0], encoding: .utf8)!
         numberN.append("\(numberN.count)")
         numberN = "\(((String(cString:[51,0], encoding: .utf8)!) == numberN ? numberN.count : numberN.count))"
      if numberN == String(cString:[57,0], encoding: .utf8)! {
          var resumeQ: Double = 3.0
          var user6: Bool = true
         numberN = "\((Int(resumeQ) >> (NWModityTool18.min(1, labs((user6 ? 2 : 2))))))"
      }
      descripth = "\(descripth.count)"
      if 2640346 == descripth.count {
         break
      }
   } while (!descripth.hasPrefix("\(modityM)")) && (2640346 == descripth.count)
   for _ in 0 ..< 3 {
      descripth.append("\(1 | Int(resultM))")
   }
        if let text = message as? String {
            didReceiveMessageCallBlock?(text)
            print("WebSocket消息：\(text)")
        }
      modityM = 66 >= descripth.count
      modityM = 56.51 >= resultM
    }
}

 class NWModityTool18 {
    static func min<A: Comparable>(_ a1: A, _ a2: A) -> A {
        return a1 > a2 ? a2 : a1
    }
    
    static func max<A: Comparable>(_ a1: A, _ a2: A) -> A {
        return a1 > a2 ? a1 : a2
    }
}